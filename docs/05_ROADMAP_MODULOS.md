# Roadmap de Módulos
## Forma 3D Studio + Assistente Virtual Sky

**Última atualização:** 2026-08-27
**Como usar este documento:** atualizar a tabela macro e o detalhamento do módulo em
desenvolvimento ao final de cada sessão de trabalho. Não é necessário reescrever
`04_PLANO_IMPLEMENTACAO.md` — ele contém o escopo fixo de cada módulo; este arquivo contém
apenas o **estado atual**.

Fases técnicas (iguais para todos os módulos): **1** Definição funcional · **2** Modelo e regras
· **3** Backend · **4** Frontend · **5** Integração E2E · **6** Piloto real · **7** Estabilização
/ release.

**Status OPERACIONAL** = o módulo pode ser usado normalmente pelo frontend, sem PowerShell nem
chamadas manuais de API.

---

# 1. Regra de cálculo do percentual macro

Cada módulo tem 7 fases de peso igual: **1/7 ≈ 14,29% cada**.

- **Percentual macro do módulo = média simples do peso das 7 fases.**
- Fase concluída (100%) recebe seu peso integral (≈14,29%).
- Fase parcialmente concluída recebe proporcionalmente seu percentual (ex.: fase 50% concluída
  contribui com ≈7,14%).
- Fase não iniciada contribui com 0%.

**"Especificado/documentado" não é o mesmo que "implementado/validado".** Isso vale sobretudo
para a Fase 2 (Modelo e regras):

- Fase 2 só recebe **100%** quando o modelo estiver **implementado** (migrations aplicadas) **e**
  as regras de negócio associadas **validadas** (funções/constraints funcionando).
- Se o modelo existir **apenas como especificação** em `03_MODELO_BANCO_DADOS.md`, sem
  migrations criadas, a Fase 2 é considerada **parcial (50%)** — o trabalho de modelagem foi
  feito e conta como progresso real, mas não como implementação.
- Fase marcada **"não aplicável"** (ex.: módulos de Sky, que não modelam tabelas próprias)
  contribui com **0%** no cálculo macro, por padrão conservador — é sinalizada separadamente
  como N/A para não ser confundida com uma pendência esquecida.
- A Fase 1 (Definição funcional) é sobre **definir**, não implementar: por isso ela recebe 100%
  quando a especificação funcional do módulo está completa em `01_ESPECIFICACAO_FUNCIONAL.md`,
  mesmo sem nenhuma linha de código.

Essa mesma regra vale para as Fases 3 a 7: só recebem 100% quando existir artefato real
(Edge Function deployada e testada, tela funcionando, teste E2E executado, piloto em uso,
release estabilizado). Documentação de escopo/planejamento nessas fases não conta como
progresso — só como intenção registrada em `04_PLANO_IMPLEMENTACAO.md`.

---

# 2. Visão macro

| Nº | Módulo | Fases com peso (100% / parcial) | % macro | Status OPERACIONAL |
| --- | --- | --- | --- | --- |
| 0 | Fundação e Segurança | 5×100% (F1–F5) + 2×50% (F6–F7) | ~86% | 🟡 Quase operacional — validar formalmente (Fases 6–7 pendentes) |
| 1 | Clientes, Produtos e Pedidos | 5×100% (F1–F5) + 2×0% (F6–F7) | ~71% | 🟡 Fluxo E2E completo do MVP (Clientes/Empresas/Produtos/Pedidos, incl. bloqueio de pagamento excedente) validado manualmente em 2026-08-26, com a proteção de backend aplicada e confirmada no Supabase remoto — Fase 6 (Piloto real) ainda não iniciada |
| 2 | Produção | 1×100% (F1) + 1×50% (F2) + 5×0% | ~21% | ❌ Não iniciado |
| 3 | Estoque e Inventário | F1 100% + F2 70% + F3 70% + F4 65% + F5 50% + F6 0% + F7 0% = 355/7 | **~51%** (recalculado 2026-09-01, ver §9c) | 🟡 Inventário manual **operacional e validado**: saldo/entrada/saída/ajuste/histórico de Acessórios e Embalagens (`stock_movements` + `register_stock_movement`, aplicados ao remoto; Edge Function `stock-movements` v1), tipos/rolos/movimentações/pesagem de filamento (`filament_types`/`filament_spools`/`filament_movements` + RPCs, aplicados; 3 Edge Functions v1) e Compras das 3 categorias (`inventory_purchases` + `register_inventory_purchase`, aplicados; Edge Function `inventory-purchases` v1) — todos validados manualmente pelo usuário (2026-08-27/28/29). **Ausentes**: reserva/consumo/liberação automática por Pedido, inventário periódico, tela de histórico de compras, custo médio por item. Frontend não publicado. Módulo 3 **não concluído** — ver §9c |
| 4 | Precificação e Rentabilidade | 1×100% (F1) + 1×50% (F2) + 5×0% | ~21% | ❌ Não iniciado |
| 5 | Manutenção e Equipamentos | 1×100% (F1) + 1×50% (F2) + 5×0% | ~21% | ❌ Não iniciado |
| 6 | Onboarding, Alertas e Gestão | 1×100% (F1) + 1×50% (F2) + 5×0% | ~21% | ❌ Não iniciado |
| 7 | Sky Assistente em Texto | 1×100% (F1) + 1×0% (F2, N/A) + 5×0% | ~14% | ❌ Não iniciado |
| 8 | Sky Assistente por Voz | 1×100% (F1) + 1×0% (F2, N/A) + 5×0% | ~14% | ❌ Não iniciado |
| 9 | Divulgação e Marketing | 1×100% (F1) + 1×50% (F2) + 5×0% | ~21% | ❌ Não iniciado |
| 10 | Integrações Externas | 1×100% (F1) + 6×0% (F2 sem modelo definido por integração) | ~14% | ❌ Não iniciado |

> Cálculo exato do Módulo 1: 5 fases 100% + 2 fases 0% = 5/7 ≈ 71%.
> Módulo 3: percentual macro **recalculado formalmente em 2026-09-01** pela metodologia oficial das
> 7 fases (auditoria somente-leitura do estado real: 45/45 migrations sincronizadas, 7 Edge
> Functions de Estoque publicadas, 4 blocos de validação manual aprovados). Resultado: **~51%**
> (era herdado como "~21%", número que estava obsoleto desde os Incrementos 1–5). O detalhamento
> por fase, as evidências e o gate para o próximo avanço estão em §9c.

---

# 3. Módulo em desenvolvimento atual: Módulo 1 — Clientes, Produtos e Pedidos

| Fase | Status | Peso | Observação |
| --- | --- | --- | --- |
| 1. Definição funcional | ✅ 100% | 14,29% | Doc `01` §5–11, §68 |
| 2. Modelo e regras | ✅ 100% | 14,29% | 19 migrations **aplicadas**: `companies`, `customers`, `lead_sources`, `orders`, `order_items`, `custom_item_details`, `custom_versions`, `approvals`, `model_sources`, `spot_item_details`, `products`, `product_price_history`, `payments`, `order_status_history`, `payment_status_history`, `accessories`, `packaging`, `product_accessories`, `product_packaging` + funções de negócio (incl. `set_product_composition`) + views. **2026-08-29**: mais 4 migrations aplicadas — `customers.is_protected` (proteção absoluta e incondicional contra exclusão física, checada antes de qualquer vínculo, coluna nunca editável por `authenticated` via grant restrito) + `delete_customer`; `delete_order` (revisão só de comentário); `orders.payment_condition`/`orders.idempotency_key` + `create_order_with_payment` (cria pedido + pagamento inicial numa única transação, com idempotência de retry); `update_product` (whitelist explícita de campos descritivos/produção, nunca `default_price`/`is_active`/`product_type`) — ver §10. **2026-08-29 (rodada seguinte)**: mais 1 migration aplicada — `20260829150000_add_order_initial_status_classification.sql` (status inicial automático de Pedidos por composição de itens + remoção do gate de tempo de pesquisa/modelagem do SPOT em `change_order_status()`) — **42/42 migrations agora sincronizadas** — ver §10 |
| 3. Backend | ✅ 100% | 14,29% | 6 Edge Functions **deployadas e validadas em runtime**: `products`, `orders`, `order-items`, `order-status`, `payments`, `order-approvals`. Nova rota `PATCH /products/:id/composition` **deployada e validada em runtime** — smoke test real ponta a ponta (frontend → Edge Function → RPC `set_product_composition` → banco → leitura de volta) concluído com sucesso em 2026-08-16. **2026-08-29**: `products` (v8→v9, rota `PATCH /products/:id`) e `orders` (v5→v6, rotas `POST /orders/with-payment` e `DELETE /orders/:id`) redeployadas; `customers` publicada pela primeira vez (v1, rota `DELETE /customers/:id`) — as 11 demais Edge Functions preservaram versão/`updated_at`. Smoke tests HTTP não autenticados aprovados nas 3 (OPTIONS→204, sem auth→401, rota desconhecida→404); smoke tests autenticados não realizados (sem sessão de teste disponível neste ambiente) — ver §10. **2026-08-29 (rodada seguinte)**: `orders` republicada (v6→v7, remove o gate de tempo do SPOT em `change_order_status`) — as 13 demais Edge Functions preservaram versão/`updated_at`; smoke tests não autenticados aprovados (OPTIONS→204, `POST /orders`/`POST /orders/with-payment` sem auth→401, rota desconhecida→404) — ver §10 |
| 4. Frontend | ✅ 100% | 14,29% | Clientes, Produtos (listar/criar/alterar preço/composição padrão), Empresas (listar/criar/editar/ativar-desativar) e Pedidos (listar/criar/editar via diálogo, equivalente funcional a "Novo Pedido"/"Detalhe do Pedido") **implementados e cobertos por teste automatizado** (`CustomersPage`, `CompaniesPage`, `ProductsPage`, `ProductDetailPage`, `OrdersPage` + respectivos formulários). Auditoria de 2026-08-26 confirmou por leitura de código que Empresas/Pedidos já estavam implementados antes desta rodada — a linha anterior deste documento ("Empresas e Pedidos ainda sem tela") estava desatualizada. Bug corrigido nesta rodada: `CustomerForm` listava empresas inativas no campo "Empresa" (só `OrderForm` filtrava corretamente); agora `CustomersPage.tsx` calcula a lista disponível (empresas ativas + a empresa já vinculada ao cliente em edição, mesmo se desativada depois) e repassa a `CustomerForm`, sem alterar `listCompanies()` — `frontend/src/pages/customers/CustomersPage.tsx`, testes novos em `CustomersPage.test.tsx`. **Extensão substancial em 2026-08-26 (mesmo dia, rodada separada)**: fluxo de gerenciamento de Pedidos implementado — diálogo "Gerenciar pedido" (`OrderManagementPanel.tsx`) com resumo (total, saldo devedor, situação financeira), avanço de status (`change_order_status`, respeitando a máquina de estados linear QUOTE→WAITING_APPROVAL→APPROVED→IN_PRODUCTION_QUEUE→IN_PRODUCTION→WAITING_DELIVERY→DELIVERED, sem pular etapas), cancelamento (`CANCELLED`, só antes de IN_PRODUCTION, com confirmação explícita), registro de pagamento (`register_payment` — Sinal/Final/Integral/Ajuste, validação de valor/soma-nunca-negativa espelhando o backend, `RegisterPaymentForm.tsx`), e histórico combinado (`order_status_history` + `payment_status_history` + `payments`, `frontend/src/lib/api/orderHistory.ts`) — tudo via `useOrderManagement.ts` reaproveitando só Edge Functions/RPCs já existentes, nenhuma nova migration/Edge Function criada. **Aprovação manual (`register_approval`) NÃO tem botão na interface** — decisão deliberada, não lacuna: a RPC rejeita itens CATALOG ("Item CATALOG não exige aprovação") e só CATALOG é criável pelo frontend hoje (Personalizado/Spot seguem "Em breve"), então nenhum item hoje jamais precisaria dessa chamada; avançar para "Aguardando aprovação" já auto-promove a "Aprovado" via `try_auto_approve_order()` para qualquer pedido só-CATALOG, dentro da própria chamada. Filtros rápidos por status adicionados à listagem (`Todos`/8 status, com contagem, locais, combináveis com a busca). Vínculos desativados corrigidos em `OrderForm.tsx`: cliente/empresa já vinculados a um pedido continuam visíveis/selecionados na edição mesmo se desativados depois (mesmo padrão de `CustomerForm`); produto de um item existente, se desativado, aparece rotulado "(inativo)" e continua bloqueando o salvamento, agora com mensagem explicando reativar/substituir (antes: "Produto inválido ou inativo — selecione outro."). Novos arquivos: `OrderManagementPanel.tsx`/`.test.tsx`, `RegisterPaymentForm.tsx`/`.test.tsx`, `useOrderManagement.ts`/`.test.ts`, `lib/api/orderHistory.ts`, `lib/orders/orderStatusMachine.ts`/`.test.ts`; alterados: `OrdersPage.tsx`/`.test.tsx`, `OrderForm.tsx`/`.test.tsx`, `types/domain.ts` (`OrderStatusHistory`/`PaymentStatusHistory`). Suíte completa 1005 → 1092 testes, lint e build sem erros. **Tudo isto é implementação nova, coberta só por teste automatizado (mocks) — nenhuma validação manual do usuário ainda ocorreu**, por isso não altera a Fase 5 nesta entrada (ver validação registrada na entrada seguinte). **Refinamento visual em 2026-08-26 (rodada seguinte, mesmo dia)**: `RegisterPaymentForm.tsx` — "Tipo de pagamento" e "Método de pagamento" passam de `Select` para action buttons/cards com ícone (mesmo padrão de `OrderForm.tsx`: radiogroup, ícone `size-8`, borda/fundo por estado; `PAYMENT_METHOD_ITEMS`/`IconComponent` agora exportados de `OrderForm.tsx` e reaproveitados, não duplicados); campo "Valor" passa a usar o mesmo campo monetário "bancário" de `ProductPriceForm.tsx` (`lib/forms/currencyField.ts`, formatação R$ em tempo real, duas casas, conversão para `number` antes do envio) — `currencyField.ts` **não foi alterado** (zero risco para `ProductPriceForm`), o sinal negativo do Ajuste é um `Switch` adicional só visível quando Tipo = Ajuste; resumo financeiro (Total/Já pago/Saldo devedor) destacado no topo do formulário. `OrderForm.tsx` reorganizado em 3 seções visuais (`FormSection`, mesmo padrão de borda/raio do bloco de Itens já existente) — Seção 1 "Dados do cliente" (Tipo de venda, Empresa se B2B, Cliente/Contato, Entrou em contato por), Seção 2 "Dados do pedido" (Tipo de item, Itens, **Método de pagamento passa a morar aqui**, antes ficava em Entrega), Seção 3 "Entrega" (Forma de entrega, Frete se aplicável, Prazo de entrega, Observações) — numeração de campo reinicia em 1 por seção; nenhum payload, validação, regra de negócio, vínculo inativo corrigido ou modo somente-leitura foi alterado, só a organização visual. Status agora também alterável direto da listagem de Pedidos (`OrderStatusControl.tsx`, coluna Status vira badge clicável exceto em DELIVERED/CANCELLED, que continuam só texto) — reaproveita só `orderStatusMachine.ts` + `changeOrderStatus()` (a mesma função já usada por `useOrderManagement`), nunca uma segunda máquina de estados; exige confirmação, bloqueia duplo clique, mostra erro real do backend, atualiza listagem/contagens via o mesmo `refetch()`; botão "Gerenciar pedido" preservado ao lado para pagamentos/histórico/detalhes. Novo arquivo: `OrderStatusControl.tsx`/`.test.tsx`. Suíte completa 1092 → 1130 testes, lint e build sem erros. **Estes ajustes visuais ainda não foram validados manualmente pelo usuário nesta forma nova** (só teste automatizado) — ver ressalva na Fase 5. **As seis alterações deste refinamento visual (commit `8ea17c0`) foram validadas manualmente pelo usuário em 2026-08-26** — ver Fase 5. **Nova extensão em 2026-08-26 (mesma data, rodada seguinte)**: status financeiro também alterável direto da listagem (`OrderPaymentStatusControl.tsx`) — a coluna "Status financeiro" vira um badge clicável (mesmo padrão visual/acessível de `OrderStatusControl.tsx`, nome acessível `"Registrar pagamento — status financeiro: <label atual>"`, `title` de dica) que abre o mesmíssimo `RegisterPaymentForm.tsx` já usado em "Gerenciar pedido", com Total do pedido/Já pago/Saldo devedor vindos diretamente de `OrderSummary` (`vw_order_summary`, já carregado por `useOrders` — nenhuma nova busca). Reaproveita só `registerPayment()` (a mesma função de `useOrderManagement`), nunca uma segunda lógica de pagamento; `orders.payment_status` continua **exclusivamente derivado** por `recalculate_order_financials()` no backend — este controle nunca escreve nem estima esse campo, só reabre o formulário que já provoca o recálculo real. Auditado no código de `register_payment()`: a função **não verifica `orders.order_status` em nenhum momento** (só existência do pedido via lock e a soma dos pagamentos nunca ficar negativa) — logo não há regra real de backend que bloqueie um pagamento em pedido `CANCELLED`, `PAID` ou com excedente; por isso o controle nunca vira badge sem ação, em nenhum desses casos (comportamento confirmado pelo contrato, não inventado). Novos arquivos: `OrderPaymentStatusControl.tsx`/`.test.tsx`. Suíte completa 1130 → 1155 testes, lint e build sem erros. **Esta extensão ainda não foi validada manualmente pelo usuário** — ver ressalva na Fase 5. **REGRA DE NEGÓCIO REVERTIDA em 2026-08-26 (rodada seguinte, mesmo dia) — bloqueio de pagamento excedente**: até esta entrada, `register_payment()` permitia deliberadamente pagamento acima do saldo devedor ("nenhum estorno automático: pagamento excedente é permitido", rastreado por `vw_order_summary.has_overpayment`/`overpayment_amount`). O usuário confirmou explicitamente a regra oposta: **o total acumulado dos pagamentos nunca pode ultrapassar o total do pedido** (`total_receivable` = `total_value + shipping_cost`, a mesma fórmula/valor já exibido como "Total do pedido"). Backend: nova migration `supabase/migrations/20260826120000_add_payment_overpayment_guard.sql`, `create or replace function public.register_payment(uuid, text, numeric, text, timestamptz, uuid, text)` — mesma assinatura de tipos da função original (Migration `20260814030351`), portanto substitui a mesma entrada de catálogo/OID em vez de criar uma sobrecarga nova; adiciona `raise exception` quando `v_new_total > v_total_receivable`, reaproveitando o mesmo lock `for update` sobre `orders` já usado pela checagem de soma-nunca-negativa (nenhuma nova estratégia de concorrência: a mesma linha travada agora também é lida para `total_value`/`shipping_cost`, serializando pagamentos concorrentes do mesmo pedido contra os dois limites). **Esta migration existe só localmente — NÃO foi aplicada ao projeto Supabase remoto nesta rodada** (restrição explícita do usuário; aplicar requer autorização separada). Frontend: `RegisterPaymentForm.tsx` ganha validação-espelho (bloqueia localmente com "O valor não pode ultrapassar o saldo devedor. Valor máximo permitido: R$ X,XX." antes mesmo de chamar o backend) e um novo estado "totalmente pago" (`balanceDue <= 0`) que substitui o formulário por um aviso e desabilita o registro de novo pagamento — aplicado sem exceção para AJUSTE/estorno negativo também, decisão conservadora por a instrução do usuário não abrir exceção; resumo financeiro (Total/Já pago/Saldo devedor) continua visível mesmo nesse estado. `OrderPaymentStatusControl.tsx` (badge "Status financeiro" da listagem) e `OrderManagementPanel.tsx` herdam o mesmo bloqueio automaticamente, por reaproveitarem o mesmíssimo `RegisterPaymentForm.tsx` — nenhuma lógica duplicada; o badge da listagem continua mostrando o texto "Pago" normalmente, só o formulário de registro fica bloqueado. `vw_order_summary.has_overpayment`/`overpayment_amount` permanecem no schema sem alteração, agora só como proteção histórica para dados legados anteriores a esta regra (nunca deveriam ficar diferentes de `false`/`0` para pagamentos novos). Testes novos cobrindo os 8 cenários exigidos (inferior ao saldo, exatamente igual, um centavo acima, pedido já totalmente pago, valores zero/negativos — já cobertos pela regra pré-existente de soma-nunca-negativa, duas parcelas somando exatamente o total, tentativa que ultrapassa o total, tratamento da mensagem de erro real do backend no frontend) em `RegisterPaymentForm.test.tsx` (31 testes) e `OrderPaymentStatusControl.test.tsx` (18 testes, incluindo reescrita do teste que antes validava o excedente sendo permitido — invalidado pela reversão da regra). Suíte completa 1155 → 1167 testes, lint 0 erros, build sem erros. Ambiente desta sessão não tem Deno/Supabase CLI/Docker instalados — a migration não pôde ser executada/validada contra um Postgres local, só revisada por leitura. **Migration aplicada ao Supabase remoto em 2026-08-26 (rodada seguinte)**: `npx supabase db push --linked` no projeto `tjhacqreupfqefntjevf`, confirmando 29/29 migrations sincronizadas local/remoto (antes 28/29, só `20260826120000` pendente); verificação somente leitura via `db query --linked` confirmou a definição ao vivo de `register_payment()` idêntica ao arquivo local, sem sobrecarga duplicada; nenhuma Edge Function, seed ou dado alterado. **Validação manual do usuário aprovada em seguida, com registros de teste**, cobrindo os 10 cenários do bloqueio de excedente pela listagem — ver detalhamento na Fase 5 |
| 5. Integração E2E | ✅ 100% | 14,29% | **Cinco blocos de validação manual do usuário aprovados em 2026-08-26** (Blocos 1–4 já registrados nas entradas anteriores: Clientes/Empresas, Produtos, fluxo completo de Pedidos, refinamento visual do commit `8ea17c0`). **Bloco 5 (nesta entrada, com registros de teste)** — bloqueio de pagamento excedente validado ponta a ponta: (1) abertura do pagamento pelo status financeiro da listagem; (2) exibição correta de Total/Já pago/Saldo; (3) registro de pagamento parcial; (4) atualização do status financeiro e saldo; (5) bloqueio de pagamento um centavo acima do saldo; (6) mensagem com o valor máximo permitido; (7) confirmação de que nenhum pagamento excedente foi registrado; (8) pagamento exatamente igual ao saldo aceito; (9) atualização para situação "Pago"; (10) bloqueio de novo pagamento com saldo zerado. A proteção de backend (`register_payment()`, migration `20260826120000_add_payment_overpayment_guard.sql`) está **aplicada e confirmada no Supabase remoto** (`tjhacqreupfqefntjevf`, 29/29 migrations sincronizadas) — a validação manual exercitou a proteção real de produção, não só o espelho client-side. Isso completa os critérios de aceite de Pedidos do Módulo 1 (`04_PLANO_IMPLEMENTACAO.md` §6) alcançáveis no escopo MVP atual (CATALOG-only; "classificar tipos" permanece N/A enquanto Personalizado/Spot não forem implementados). **Fase 5 concluída para o escopo atualmente implementado** — Piloto real (Fase 6) pode avançar |
| 6. Piloto real | ❌ 0% | 0% | Depende da Fase 5 completa — **não iniciado, não marcado como concluído**. **Decisão do usuário em 2026-08-27**: antes de finalizar as Fases 6 e 7 do Módulo 1, será concluída a continuação do Módulo 3 (Estoque e Inventário — saldo, movimentações, filamentos, consumo automático pelo pedido, integração completa com Pedidos/Produtos). Módulo 1 permanece tecnicamente pronto para o Piloto real no escopo atual (MVP CATALOG-only), mas aguarda essa integração antes de avançar — ver §9b abaixo |
| 7. Estabilização / release | ❌ 0% | 0% | Depende da Fase 6 — **não iniciado, não marcado como concluído**. Mesma decisão acima: aguarda a conclusão da integração de Estoque |

**Percentual macro: (100+100+100+100+100+0+0) / 7 = 500/7 ≈ 71%.**

**Status OPERACIONAL: PARCIAL** — backend pronto e testado via API, incluindo a composição padrão
de produtos (Migrations 18–19 aplicadas no remoto, Edge Function `products` deployada, rota
`PATCH /products/:id/composition` validada por smoke test real ponta a ponta); frontend de
Clientes, Empresas, Produtos e Pedidos funciona localmente contra o Supabase remoto real
(1167/1167 testes automatizados passando, lint e build sem erros). **Cinco blocos de validação
manual do usuário aprovados em 2026-08-26**: (1) correção do bug de empresas inativas em Clientes;
(2) composição de produto com acessório/embalagem sem custo; (3) **fluxo completo de Pedidos**
(criar pedido, avançar status, registrar pagamento, avançar até entrega, cancelar, filtrar por
status); (4) **as seis alterações visuais do commit `8ea17c0`** (action buttons de Tipo/Método de
pagamento, campo de valor no padrão bancário, `OrderForm.tsx` em 3 seções, status do pedido
alterável direto da listagem); (5) **bloqueio de pagamento excedente**, com registros de teste —
status financeiro pela listagem, pagamento parcial, pagamento integral/exato, bloqueio de valor
acima do saldo com mensagem do máximo permitido, e pedido totalmente pago bloqueando novo
pagamento. A proteção de backend correspondente (migration
`20260826120000_add_payment_overpayment_guard.sql`) está **aplicada e confirmada no projeto
Supabase remoto `tjhacqreupfqefntjevf`** (29/29 migrations sincronizadas) — a validação exercitou a
proteção real de produção. **Fluxo E2E do Módulo 1 concluído para o escopo MVP atualmente
implementado** (CATALOG-only; Personalizado/Spot seguem fora de escopo). Hospedagem pública ainda
inativa (Vercel configurada em código desde `ecfc510`, mas publicação **deliberadamente adiada**
até o início da Fase 6 — ver §9). **Piloto real (Fase 6) e Estabilização/release (Fase 7)
permanecem não iniciados** — próxima etapa recomendada é a Fase 6. **Módulo 1 permanece não
concluído** (Fases 6–7 pendentes).

**Atualização de 2026-08-29** (ver §10 para o relato completo): auditoria final seguida de correção
de proteção da Petlink, aplicação de 4 migrations e publicação de 3 Edge Functions. **Proteção
absoluta e incondicional contra exclusão física de registros permanentes** implementada
(`customers.is_protected`, checada antes de qualquer vínculo, nunca editável por `authenticated`) —
a Petlink em si continua **não cadastrada** no banco (confirmado por auditoria somente leitura antes
e depois das migrations); o mecanismo está pronto para protegê-la assim que o cadastro oficial for
autorizado. Idempotência de retry adicionada a `create_order_with_payment` (mesmo padrão já usado em
Compras/Estoque). **41/41 migrations agora sincronizadas** local/remoto. **`products` (v9), `orders`
(v6) e `customers` (v1, primeira publicação)** publicadas — as demais 11 Edge Functions preservaram
versão. Teste de integração SQL transacional das 4 novas RPCs: **30 PASS, 0 FAIL**, `ROLLBACK`
confirmado, zero resíduo, Petlink não tocada. Smoke tests HTTP não autenticados aprovados nas 3
funções; smoke tests autenticados **não realizados** (sem sessão de teste disponível neste
ambiente). **Validação manual do usuário pela interface aprovada integralmente** (correção
documental de 2026-08-29, rodada seguinte — ver checklist completo logo abaixo e detalhamento em
§10): os 8 passos do checklist desta entrada (Editar produto e Histórico de preços; exclusão
protegida de Cliente; cores nos status operacional/financeiro e indicador "Atrasado"; Forma de
pagamento "Na entrega"; "Sinal"; "Adiantado"; Registrar pagamento; exclusão protegida de Pedido)
foram todos aprovados pelo usuário — nenhum item deste checklist permanece pendente. Reserva/consumo
automático de estoque **continuam sem nenhuma linha de código**; Módulo 3 **continua não
concluído**. Nenhum push, nenhum deploy de frontend.

**VALIDAÇÃO MANUAL — checklist aprovado (correção documental de 2026-08-29):**
1. Produtos — Editar produto e Histórico de preços: **aprovado**.
2. Clientes — exclusão protegida: **aprovada**.
3. Cores nos status (operacional/financeiro) e indicador "Atrasado": **aprovados**.
4. Forma de pagamento "Na entrega": **aprovado**.
5. Forma de pagamento "Sinal": **aprovado**.
6. Forma de pagamento "Adiantado": **aprovado**.
7. Registrar pagamento: **aprovado**.
8. Excluir pedido: **aprovado**.

**Atualização de 2026-08-29** (regra de status inicial automático, aplicada e publicada nesta
mesma data — ver §10): **validação manual PARCIAL aprovada pelo usuário**, restrita aos cenários
de Catálogo, os únicos hoje possíveis pela interface (Personalizado/Spot continuam "Em breve"):
(1) Pedido só Catálogo com composição de filamentos — **aprovado**, confirmado nascendo
diretamente em Fila de produção; (2) Pedido Catálogo sem composição — **aprovado**, criação
bloqueada com a mensagem correta, nenhum Pedido residual criado. **Catálogo + Personalizado NÃO
pôde ser testado manualmente** (item Personalizado indisponível na interface) — esse cenário
misto permanece validado só pelo teste SQL (`order_initial_status_test.sql`, testes 1.6/1.7).
**Spot (sozinho ou combinado) também NÃO tem validação manual** (item Spot indisponível na
interface) — permanece validado só pelo teste SQL (testes 1.2/1.3/6.1–6.6). **Nenhuma validação
manual integral desta regra deve ser declarada enquanto Personalizado/Spot continuarem fora da
interface** — só os dois cenários de Catálogo puro estão de fato confirmados na prática.
Registros `TESTE` usados nessa validação manual foram **removidos pelo próprio usuário** — limpeza
**aprovada**, nenhum resíduo. A pendência de `bloco1_integration_test.sql` (abortando por uma
fixture sem composição, não relacionada ao SPOT) **permanece em aberto**, sem alteração. Reserva e
consumo automático de estoque **continuam sem nenhuma linha de código** — entrar em Fila de
produção **ainda não reserva nem baixa estoque** de nenhum tipo. Módulo 3 **continua não
concluído**.

## Próximo marco

Fase 4 (Frontend) do Módulo 1 concluída, incluindo o fluxo completo de gerenciamento de Pedidos
(status/cancelamento/pagamento/filtros/histórico), o refinamento visual dessas mesmas telas
(`RegisterPaymentForm.tsx` com action buttons e campo monetário bancário; `OrderForm.tsx` em 3
seções; status do pedido alterável direto da listagem via `OrderStatusControl.tsx`) e, na
sequência da mesma data, o status financeiro também alterável direto da listagem
(`OrderPaymentStatusControl.tsx`, reaproveitando `RegisterPaymentForm.tsx`/`registerPayment()`).
O fluxo funcional principal de Pedidos e as seis alterações visuais do commit `8ea17c0` já foram
**validados manualmente pelo usuário em 2026-08-26**, elevando a Fase 5 para ~60% — mas essa
validação **não cobre** a extensão mais recente (status financeiro na listagem), implementada
depois. Próximo passo: validação manual do usuário especificamente do checklist desta rodada
(badge de status financeiro clicável, abertura do `RegisterPaymentForm` com os totais corretos,
registro de pagamento parcial/integral/excedente/ajuste a partir da listagem) antes de considerar
a Fase 5 concluída e iniciar a Fase 6 (piloto real). Publicação no Vercel continua fora de escopo
até a conclusão da roadmap (ver §9).

---

# 4. Módulo 0 — Fundação e Segurança (referência — já avançado)

| Fase | Status | Peso | Observação |
| --- | --- | --- | --- |
| 1. Definição funcional | ✅ 100% | 14,29% | |
| 2. Modelo e regras | ✅ 100% | 14,29% | Tabela `users` + triggers de sincronização com Auth **implementados** |
| 3. Backend | ✅ 100% | 14,29% | Supabase Auth (e-mail/senha) **configurado e em uso** |
| 4. Frontend | ✅ 100% | 14,29% | Concluído para autenticação: login, contexto de auth, rota protegida |
| 5. Integração E2E | ✅ 100% | 14,29% | Login validado ponta a ponta pelo navegador |
| 6. Piloto real | 🟡 ~50% | 7,14% | Em uso no dia a dia de desenvolvimento, mas sem validação formal |
| 7. Estabilização / release | 🟡 ~50% | 7,14% | Falta validação formal de segurança/RLS |

**Percentual macro: (100+100+100+100+100+50+50) / 7 = 600/7 = 85,71% ≈ 86%.**

**Status OPERACIONAL: Quase operacional / validar formalmente.**

---

# 5. Módulos 2–10 — visão macro (não iniciados na prática; parte já especificada)

Todos têm a Fase 1 concluída (documentada nos docs `01`/`03`) e parte da Fase 2 já especificada
no modelo de banco de dados (`03`). A especificação conta como progresso real (ver §1), mas não
deve ser lida como implementação. Exceção: o Módulo 3 (Estoque) já tem Edge Functions e tela
próprias — mas só para o cadastro mestre de `accessories`/`packaging`, antecipado pelo Módulo 1
(Migration 18); nenhum outro módulo desta seção (2, 4–10) tem Edge Function ou tela criada; e o
próprio Módulo 3 segue sem nenhuma implementação de inventário físico — ver nota na linha do
Módulo 3.

| Módulo | Fase 1 — Definição funcional | Fase 2 — Modelo e regras | Fases 3–7 |
| --- | --- | --- | --- |
| 2 — Produção | ✅ 100% (doc 01 §12–14) | 🟡 50% — especificado (doc 03 §10–11), sem migrations | ❌ 0% |
| 3 — Estoque e Inventário | ✅ 100% (doc 01 §15–21) | 🟡 **70%** (2026-09-01) — **aplicados ao remoto** (45 migrations, todas sincronizadas): `accessories`/`packaging` + write functions (doc 03 §13); `stock_movements` + `register_stock_movement` (§15.1); `filament_types`/`filament_spools`/`filament_movements` + `register_filament_movement`/`register_filament_weighing` + `vw_filament_type_summary` (§12.1–12.3); `inventory_purchases` + `register_inventory_purchase` (compras). Tara passou a ser `filament_spools.empty_spool_weight_grams` por rolo (a tabela `spool_tares` foi **descartada por arquitetura**, doc 03 §12.5). `product_filaments`/`product_plate_filaments` existem mas são **LEGADAS** (composição de filamento saiu do Produto e foi para o Pedido — `order_item_unit_plate_filaments`). **Ausentes** (sem migration): tipos de movimento `RESERVATION`/`RELEASE`/`CONSUMPTION`, integração com `change_order_status`, snapshot de Acessórios/Embalagens por Pedido, `inventories`/`inventory_items` (inventário periódico), `suppliers` (fora de escopo), custo médio por item | 🟡 Inventário manual **operacional e validado pelo usuário** (2026-08-27/28/29): saldo/entrada/saída/ajuste/histórico de Acessórios e Embalagens; tipos/rolos/movimentações/pesagem/arquivamento de filamento; Compras das 3 categorias em produção real. 7 Edge Functions de Estoque publicadas (`accessories`, `packaging`, `stock-movements`, `filament-types`, `filament-spools`, `filament-movements`, `inventory-purchases`, todas v1). Rotas `/estoque`, `/estoque/acessorios`, `/estoque/embalagens`, `/estoque/filamentos`. **Não é inventário completo**: entrar em Fila **não reserva** e iniciar Produção **não baixa** estoque — motor de reserva/consumo/liberação por Pedido ainda sem nenhuma linha de código (contrato arquitetural congelado em §9c). Cadastro oficial (incl. Petlink) segue bloqueado por autorização — ver §6 |
| 4 — Precificação e Rentabilidade | ✅ 100% (doc 01 §22–30) | 🟡 50% — especificado (doc 03 §17–18), sem migrations | ❌ 0% |
| 5 — Manutenção e Equipamentos | ✅ 100% (doc 01 §65) | 🟡 50% — especificado (doc 03 §24), sem migrations | ❌ 0% |
| 6 — Onboarding, Alertas e Gestão | ✅ 100% (doc 01 §3, §59–66) | 🟡 50% — `alerts`/`notifications` especificados (doc 03 §22); escala (§25) especificada; reaproveita `parameters` do Módulo 4 (também não implementado) | ❌ 0% |
| 7 — Sky Assistente em Texto | ✅ 100% (doc 01 §52–60, §73) | ⚪ N/A (0% no cálculo) — não modela tabelas próprias, reaproveita módulos já modelados | ❌ 0% |
| 8 — Sky Assistente por Voz | ✅ 100% (doc 01 §54–55, §69) | ⚪ N/A (0% no cálculo) — reaproveita Módulo 7 | ❌ 0% |
| 9 — Divulgação e Marketing | ✅ 100% (doc 01 §31–43) | 🟡 50% — especificado (doc 03 §21), sem migrations | ❌ 0% |
| 10 — Integrações Externas | ✅ 100% (doc 01 §44–51, §69) | 🔴 0% — sem modelo de dados definido por integração; depende de prova técnica | ❌ 0% |

---

# 6. Gatilhos de retorno pendentes

Pendências que devem **pausar o roadmap corrente** quando sua condição de disparo for
satisfeita, para retomar um trabalho já entregue parcialmente em outro módulo. Nenhum item
desta seção conta como concluído nem altera os percentuais das seções 2/3/5 — é só um lembrete
formal de retomada.

| Gatilho | Condição de disparo | Ação ao disparar | Status |
| --- | --- | --- | --- |
| Ficha Técnica do Produto — detalhamento completo de custos | Estrutura oficial de custos de material, energia, máquina, perdas, MDO e margem estiver implementada no banco (Módulo 4 — Precificação e Rentabilidade, Fase 2/3 — hoje só especificada em `03_MODELO_BANCO_DADOS.md` §18.1 `pricing_calculations` e §10.1 `printers`/§12.1 `filament_types`, sem nenhuma migration) | Pausar o roadmap corrente e retornar à Ficha Técnica do Produto (Módulo 1, `frontend/src/pages/products/ProductDetailPage.tsx`) para implementar o detalhamento completo de custos, custo total e margem — hoje a ficha já cobre Identificação, Produção, Acessórios, Embalagens e Subtotal de componentes (Incrementos 1–2); seguem pendentes material, energia, máquina, perdas, MDO, custo total e margem | ⏳ Pendente — não disparado |
| Cadastro oficial de acessórios/embalagens mestres e composição de produtos (incl. Petlink) | Módulo 3 — Estoque e Inventário possuir uma **interface funcional e validada** para cadastrar acessórios e embalagens mestres | Avisar o usuário de que a interface de Estoque está disponível, para que ele faça os cadastros oficiais de acessórios/embalagens e complete a composição dos produtos existentes — incluindo o cadastro oficial da Petlink já presente no ambiente, hoje sem composição associada | ✅ **Condição satisfeita desde 2026-08-24** (corrige texto anterior desatualizado, que ainda afirmava "interface de Estoque ainda não existe" — a interface de cadastro mestre foi implementada, publicada no Supabase remoto e validada manualmente pelo usuário nessa data, ver §9/§10). **A ação ainda NÃO foi executada**: o cadastro oficial (incl. Petlink) continua bloqueado até autorização explícita separada do usuário para tocar dados oficiais — a existência da interface não libera isso automaticamente. Aguardando essa autorização. |
| Filtros rápidos por status na listagem de Pedidos | Os fluxos de status operacional (`order_status`) e status financeiro (`payment_status`) de Pedidos estarem **funcionais e validados de ponta a ponta** (hoje esses campos existem só como dados exibidos/traduzidos na listagem — `OrdersPage.tsx` — e podem ser ordenados/pesquisados por texto exibido, mas não há nenhuma transição de status operacional/financeiro implementada nem validada nesta rodada) | Retornar à listagem de Pedidos e criar botões de filtro rápido por status operacional e financeiro, incluindo "Aguardando pagamento" e os demais estados disponíveis (`ORDER_STATUS_LABELS`/`PAYMENT_STATUS_LABELS` já existentes em `OrdersPage.tsx`) | ⏳ Pendente — não disparado (fluxos de status ainda não são funcionais/validados) |

---

# 8. Regra permanente — padrão de novas listagens e módulos

Regra permanente do Forma Sky, registrada em 2026-08-22 durante o planejamento do Módulo 3
(Estoque): **"Toda nova listagem ou módulo deverá nascer seguindo o padrão visual e funcional
consolidado em Clientes, Produtos, Empresas e Pedidos."**

O padrão inclui, no mínimo:

- `AppLayout` e navegação global existentes;
- identidade visual e tokens oficiais da Forma 3D Studio;
- margens, espaçamentos e densidade já aprovados nas páginas existentes;
- tabela no padrão existente (`table-fixed`, distribuição de colunas consistente);
- linhas ímpares em roxo suave, linhas pares em branco;
- hover no padrão Forma;
- truncamento de textos longos com `title`;
- coluna de ações compacta;
- `Switch` de Ativo padronizado;
- busca rápida;
- filtros pertinentes ao módulo;
- ordenação por coluna;
- estado de carregamento (loading);
- estado de erro;
- estado de lista vazia;
- estado específico de busca/filtro sem resultado;
- acessibilidade, foco visível e operação por teclado;
- responsividade;
- testes correspondentes.

Nenhum módulo novo deve criar uma identidade visual paralela; componentes, utilitários e tokens já
aprovados (`AppLayout`, `SearchAutocomplete`, `SortableColumnHeader`, `sorting.ts`,
`textSearch.ts`) devem ser reutilizados quando tecnicamente adequados, sem refatoração ampla dos
módulos já existentes.

---

# 9. Módulo 3 — Estoque e Inventário: plano de cadastro mestre (CONCLUÍDO)

> **Correção de 2026-09-01 (o corpo datado abaixo é histórico, preservado como foi escrito):**
> o plano de 8 incrementos de cadastro mestre de Acessórios/Embalagens está **concluído** —
> backend e Edge Functions publicados, telas implementadas, validação manual aprovada. As
> passagens abaixo que dizem "em implementação", "migration criada, não aplicada remotamente"
> ou "não deployado" refletem o estado da data de cada entrada, não o estado atual. O plano
> vigente para o Módulo 3 é o de **operações de estoque** (§9b) e sua continuação (§9c).

Planejamento aprovado em 2026-08-22 para a interface de cadastro mestre de `accessories`/
`packaging` (Bloco 1, Migration 18) — cobre definição funcional e regras de negócio do cadastro
mestre. Plano dividido em 8 incrementos (Incremento 1 = esta documentação). Cadastro oficial
(incluindo a composição da Petlink) segue não liberado até a interface estar completa (leitura +
escrita) e validada em produção — ver gatilho pendente em §6.

**Progresso registrado em 2026-08-23** (implementado e testado localmente, nada deployado/liberado
ainda):

- **Incremento 2** — backend protegido de Acessórios (`create_accessory`/`update_accessory`/
  `delete_accessory`, Edge Function `accessories`) implementado e testado localmente (Deno +
  Vitest); migration criada, **não aplicada remotamente**.
- **Incremento 3** — mesmo backend espelhado para Embalagens (`create_packaging`/
  `update_packaging`/`delete_packaging`, Edge Function `packaging`); mesma situação (local,
  testado, não aplicado remotamente).
- **Incremento 4** — listagem de Estoque no frontend (`/estoque`, `/estoque/acessorios`,
  `/estoque/embalagens`, `InventoryPage.tsx`): consulta, busca, filtro por status e ordenação para
  as duas áreas (Acessórios e Embalagens), reaproveitando o padrão visual já aprovado em
  Clientes/Produtos/Empresas/Pedidos. **Somente leitura nesta etapa** — nenhuma criação, edição,
  exclusão ou ativação/desativação está disponível na tela. A coluna "Ativo" desta listagem é
  **só um indicador informativo** (badge de texto "Ativo"/"Inativo"), não um controle interativo;
  não existe coluna de ações (editar/excluir) nesta etapa. Validação manual da área
  `/estoque/acessorios` aprovada pelo usuário em 2026-08-23.
- Criação/edição/ativação/desativação/exclusão pela interface (conectando as RPCs dos Incrementos
  2/3 às telas) continuam **pendentes** — cadastro oficial de acessórios/embalagens (e a
  composição da Petlink) segue bloqueado até essas operações existirem, serem testadas e o backend
  ser deployado com autorização explícita.

**Progresso registrado em 2026-08-24** (backend publicado e sincronizado; criação, edição,
ativação/desativação e exclusão segura completas na interface, validadas manualmente contra o
backend remoto):

- **Incrementos 2/3** — migrations `20260822120000`/`20260823120000` (RPCs `create_accessory`/
  `update_accessory`/`delete_accessory` e equivalentes de `packaging`) **aplicadas no projeto
  Supabase remoto** (`tjhacqreupfqefntjevf`); Edge Functions `accessories`/`packaging`
  **publicadas e ativas** (`supabase functions deploy`). 28/28 migrations do projeto sincronizadas,
  nenhuma pendente.
- **Incremento 4 e seguintes** — a interface de Estoque deixou de ser somente leitura: criação,
  edição, ativação/desativação (com confirmação explícita) e exclusão física segura (bloqueada
  quando há vínculo em `product_accessories`/`product_packaging`, orientando desativação em vez de
  excluir) estão implementadas para Acessórios e Embalagens, com testes automatizados (frontend +
  Deno) e **validação manual no navegador aprovada pelo usuário contra o backend remoto**.
- Cadastro oficial (incluindo a composição da Petlink) **continua bloqueado** — a interface estar
  completa e validada não libera automaticamente o cadastro oficial; isso depende de autorização
  explícita separada, conforme §6.

Regras de campos/tamanho/custo/exclusão aprovadas: ver `03_MODELO_BANCO_DADOS.md` §13.3.

### Padrão visual e funcional

Segue a regra permanente registrada em §8 (Clientes/Produtos/Empresas/Pedidos como referência) —
nenhuma identidade paralela.

### Busca, filtros e ordenação

Acessórios e Embalagens terão estados de busca/filtro/ordenação **independentes** entre si (nunca
compartilhados).

- Busca por: Nome, Tamanho, Variante.
- Filtros: Status (Todos/Ativos/Inativos); Tamanho (Todos/Não se aplica/PP/P/M/G/GG); Custo
  (Todos/Custo disponível/Custo não informado); ação "Limpar filtros"; indicação visual de filtros
  ativos.
- Ordenação: Nome, Tamanho (ordem semântica PP < P < M < G < GG, não alfabética), Variante, Custo,
  Estoque mínimo, Ativo — valores vazios sempre em posição previsível (ao final, independente da
  direção).
- Busca e filtros serão **locais** (sobre a lista já carregada), conforme o padrão já usado em
  Clientes/Produtos/Empresas/Pedidos, e **não serão persistidos na URL** nesta etapa.

### Rotas planejadas (decisão técnica, não implementada)

`/estoque`, `/estoque/acessorios`, `/estoque/embalagens` — a entrada "Estoque" da navegação deverá
permanecer marcada como ativa nas duas sub-rotas. Esta é só uma decisão registrada; `App.tsx` e
`AppLayout.tsx` não foram alterados por esta entrada.

### Contratos de escrita — decisão pendente antes do Incremento 2

Ainda **não está decidido definitivamente** se criação/edição/ativação/desativação usarão acesso
autenticado direto ao Supabase ou uma Edge Function dedicada. Requisitos já fixados,
independentemente de qual opção for escolhida:

- toda escrita exige autenticação, validação e tratamento padronizado de erro;
- exclusão obrigatoriamente passa por function/RPC protegida via Edge Function (nunca acesso
  direto) — ver `03_MODELO_BANCO_DADOS.md` §13.3;
- a decisão entre acesso direto e Edge Function para criar/editar/ativar/desativar será tomada
  explicitamente antes do Incremento 2 — a recomendação preferencial registrada é Edge Function,
  por consistência, validação centralizada e auditoria futura;
- nenhuma operação remota (migration, deploy, escrita em produção) será executada sem autorização
  explícita separada.

### Status

Definição funcional: ✅ aprovada. Regras do cadastro mestre: ✅ aprovadas (ver doc `03` §13.3).
Backend protegido (Acessórios e Embalagens, Incrementos 2–3): ✅ implementado, testado e
**publicado no projeto Supabase remoto** (Edge Functions `accessories`/`packaging` ativas,
migrations aplicadas). Interface de Estoque (listagem, busca, filtros, ordenação, criação, edição,
ativação/desativação, exclusão segura): ✅ implementada, testada e **validada manualmente pelo
usuário contra o backend remoto** para Acessórios e Embalagens. Hospedagem pública do frontend:
⏳ configurada no código (Vercel, `frontend/vercel.json` — procedimento completo no `README.md`
da raiz), mas a **publicação foi deliberadamente adiada até a conclusão desta roadmap** — decisão
confirmada em 2026-08-26, não um bloqueio técnico; nenhum deploy foi executado, URL não existe.
Cadastro oficial: 🔒 ainda não liberado — depende de autorização
explícita separada (§6), independente do estado técnico acima. Plano: 8 incrementos definidos,
backend e interface tecnicamente completos e publicados. O percentual macro (seções 2/5) deste
roadmap não foi recalculado nesta entrada — fica registrado como pendência para uma próxima
atualização dedicada do documento.

---

# 9b. Módulo 3 — Estoque e Inventário: plano de operações de estoque (novo, 2026-08-27)

**Não confundir com o plano de 8 incrementos de §9** (esse era só sobre a interface de cadastro
mestre de `accessories`/`packaging`, já concluído). Este é um plano **novo e separado**, aprovado
pelo usuário em 2026-08-27, para o motor real de saldo/movimentação/consumo de estoque — 9
incrementos, dos quais só o primeiro foi implementado nesta rodada.

## Regras operacionais aprovadas (22 regras, 2026-08-27)

**Regras operacionais do MVP — versão inicial para validação, sujeitas a revisão após o teste
prático do usuário.** A aprovação do usuário em 2026-08-27 autoriza **construir o MVP** com estas
22 regras — **não as torna definitivas**. Tipos de movimentação poderão ser ampliados ou
revisados; reserva, consumo, cancelamento, pesagem, escolha de rolo e perdas continuam **só
intenção aprovada**, aguardando validação prática antes de qualquer implementação. O Incremento 1
(único implementado até agora) cobre **somente o núcleo seguro de saldo e movimentações manuais**
de Acessórios/Embalagens — nenhuma das regras futuras abaixo tem código ainda, e nenhuma delas
deve ser lida como validada. O ledger e o histórico de `stock_movements` são imutáveis desde já e
permanecem imutáveis mesmo quando essas regras forem revisadas: correções futuras acontecem por
**novas movimentações e novas migrations**, nunca reescrevendo uma linha já gravada. O Módulo 3 só
avançará para validação operacional real quando existir **interface utilizável** (Incremento 2 em
diante, ainda não iniciado).

Documentadas com detalhe funcional em `01_ESPECIFICACAO_FUNCIONAL.md` §16/§18/§19/§20 e com
detalhe de schema em `03_MODELO_BANCO_DADOS.md` §15.1. Resumo: acessórios/embalagens controlados
em unidades inteiras; estoque inicial registrado como movimentação "Saldo inicial" (nunca
`UPDATE` direto); entradas (Saldo inicial/Compra/Devolução/Ajuste positivo) e saídas manuais
(Perda-Avaria/Amostra-Doação/Uso interno/Ajuste negativo) definidas; saldo negativo proibido;
reserva na Fila de produção, consumo de acessórios/filamento ao iniciar produção, consumo de
embalagens em Aguardando entrega (todos ainda não implementados); cancelamento antes do consumo
libera reserva automaticamente, depois do consumo não devolve automaticamente (devolução sempre
manual); falha/reimpressão lançada manualmente até existir o Módulo de Produção; escolha de rolo
de filamento (aberto primeiro, depois mais antigo, consumo divisível entre rolos), pesagem gera
ajuste pela diferença, perdas sempre exigem motivo, tipo de filamento = material+fabricante+
linha+cor, peso nominal livre por rolo (1.000 g só sugestão), múltiplos rolos por tipo, ficha do
produto deverá futuramente aceitar múltiplos filamentos/cores — tudo isto **especificado**;
proteção contra duplicidade por pedido/item/evento em movimentações automáticas — **implementada**
já neste Incremento 1 (via `idempotency_key`), mesmo sem nenhuma movimentação automática existir
ainda.

## Incremento 1 — saldo e motor de movimentações (Acessórios/Embalagens) — IMPLEMENTADO LOCALMENTE

Implementado em 2026-08-27, na branch `feature/inventory-operations` (partindo de `45dac99`):

- `public.stock_movements` — ledger imutável (migration
  `20260827090000_create_stock_movements_table.sql`), suporta `item_type`
  `ACCESSORY`/`PACKAGING` (extensível a `FILAMENT_SPOOL` sem remodelagem completa) e 8
  `movement_type` manuais (`INITIAL_BALANCE`/`PURCHASE`/`RETURN`/`POSITIVE_ADJUSTMENT`/`LOSS`/
  `SAMPLE_DONATION`/`INTERNAL_USE`/`NEGATIVE_ADJUSTMENT`). RLS habilitada, `SELECT` para
  `authenticated`, nenhum `INSERT`/`UPDATE`/`DELETE` concedido a nenhuma role de sessão.
- `public.register_stock_movement(...)` — única função que escreve em `stock_movements` e em
  `accessories.current_stock`/`packaging.current_stock`, na mesma transação (`FOR UPDATE` no item,
  bloqueio de saldo negativo, motivo obrigatório por tipo, regras de `INITIAL_BALANCE`,
  idempotência via `idempotency_key`). `security definer`, `EXECUTE` só para `service_role`.
- `delete_accessory()`/`delete_packaging()` atualizadas (migration
  `20260827093000_update_accessory_packaging_delete_guards.sql`, mesma assinatura, sem sobrecarga
  nova) — agora também bloqueiam exclusão física quando o item tem qualquer movimentação
  registrada (`ACCESSORY_HAS_STOCK_HISTORY:`/`PACKAGING_HAS_STOCK_HISTORY:`), além do bloqueio já
  existente por vínculo em `product_accessories`/`product_packaging`.
- Teste de integração SQL completo (`supabase/tests/inventory_movements_test.sql`, mesmo padrão de
  `bloco1_integration_test.sql`) cobrindo saldo inicial, entradas, saídas com motivo obrigatório,
  bloqueio de saldo negativo, saldo exatamente zerado, item/tipo inválido, quantidade fracionada
  rejeitada, idempotência (mesmo payload vs. payload diferente), imutabilidade (UPDATE/DELETE
  bloqueados para `authenticated`), exclusão segura com histórico, item desativado preservando
  saldo/histórico, usuário inválido/inexistente, grants.

**Fora de escopo desta rodada** (Incrementos 2–9, todos pendentes): interface de movimentações,
histórico no frontend, filamentos, rolos, pesagens, `RESERVATION`/`RELEASE`/`CONSUMPTION`,
integração com `change_order_status`, produção.

**Correção adicional em 2026-08-27 (mesmo dia, rodada seguinte, antes da aplicação remota)**:
revisão corretiva do Incremento 1 encontrou e corrigiu uma lacuna real de idempotência
concorrente em `register_stock_movement()` — duas chamadas com a mesma `idempotency_key` para
itens DIFERENTES, sob concorrência real (duas conexões), podiam terminar com um `unique_violation`
bruto em vez da mensagem estável `IDEMPOTENCY_KEY_CONFLICT:`; corrigido com um bloco
`BEGIN/EXCEPTION WHEN unique_violation` em volta do `INSERT`, mais normalização de
`reference_type` consistente com `reason`. As 22 regras aprovadas foram também explicitamente
rotuladas nos 4 documentos como **"Regras operacionais do MVP — versão inicial para validação,
sujeitas a revisão após o teste prático do usuário"** — aprovação autoriza construir o MVP, não
torna as regras definitivas; nenhuma regra futura (reserva/consumo/cancelamento/pesagem/escolha de
rolo/perdas) aparece como validada. Checkpoint local: commit `fix: harden inventory ledger
validation`, sem push, sem migration aplicada.

**Aplicação e validação remota em 2026-08-27 (mesmo dia, rodada seguinte)**: as duas migrations
foram **aplicadas ao projeto Supabase remoto `tjhacqreupfqefntjevf`** via `npx supabase db push
--linked` — 31/31 migrations agora sincronizadas local/remoto (antes 29/31). Verificação somente
leitura confirmou que a definição ao vivo de `stock_movements` (colunas, constraints, índices,
incl. o índice único parcial de `idempotency_key`), RLS/policies/grants e as três funções
(`register_stock_movement`, `delete_accessory`, `delete_packaging`, cada uma com assinatura única,
sem sobrecarga duplicada) correspondem exatamente aos arquivos locais. Em seguida, o teste de
integração SQL (`supabase/tests/inventory_movements_test.sql`) foi executado contra o remoto
dentro de uma transação `BEGIN...ROLLBACK` — **40 PASS, 1 SKIP (ambiente sem usuário inativo
disponível, não é falha), 0 FAIL**; consultas somente leitura pós-teste confirmaram **zero
resíduo** (nenhum acessório/embalagem/movimentação de teste persistido, `stock_movements` com 0
linhas, nenhum `current_stock` oficial alterado, nenhum usuário oficial alterado). Smoke test do
CRUD existente: testes frontend de `InventoryPage`/`InventoryItemForm` (108/108, sem regressão);
testes Deno de `accessories`/`packaging` **não executados** — ambiente sem Deno, mesma limitação já
registrada em rodadas anteriores. **Backend do Incremento 1 está agora aplicado e validado por
teste automatizado real contra o banco remoto** — mas isso ainda não substitui a validação manual
do usuário nem a existência de uma interface (Incremento 2, ainda não implementado). Checkpoint
local: commit `docs: record inventory ledger deployment`, sem push.

## Incrementos 2/3 — interface de movimentações e histórico (MVP) — IMPLEMENTADOS LOCALMENTE

Implementados em 2026-08-27, na branch `feature/inventory-operations`, reunindo os dois
incrementos numa só rodada (interface de saldo/entrada/saída/ajuste + histórico/rastreabilidade —
o pedido do usuário explicitamente os agrupou, por serem uma única superfície de UI coesa). As 22
regras aprovadas continuam **provisórias do MVP** (disclaimer completo em
`01_ESPECIFICACAO_FUNCIONAL.md` §20) — esta interface existe para o usuário **validar** as regras
na prática, não para confirmá-las como definitivas.

**Backend (nenhuma migration nova — só código de aplicação, conforme o escopo desta rodada)**:

- `supabase/functions/stock-movements/` — nova Edge Function (`index.ts`/`handler.ts`/
  `handler.test.ts`), única rota `POST /stock-movements`, chamando exclusivamente
  `register_stock_movement()` (RPC já aplicada e testada na rodada anterior). Validação estrutural
  do payload (item_type/movement_type/quantity inteiro positivo/motivo obrigatório por tipo) vive
  na Edge Function; regras que dependem do banco (saldo insuficiente, INITIAL_BALANCE repetido/
  exige saldo zero, item existe, idempotência) continuam exclusivas da RPC — mesmo critério já
  usado por `accessories`/`packaging`. Nenhuma escrita direta em `stock_movements` nem em
  `current_stock` a partir desta função. **Ainda NÃO publicada** — só criada localmente.
- `supabase/functions/_shared/errors.ts` — 8 novos padrões estáveis mapeados
  (`STOCK_INSUFFICIENT_BALANCE:`, `INITIAL_BALANCE_ALREADY_EXISTS:`, `INITIAL_BALANCE_REQUIRES_ZERO:`,
  `IDEMPOTENCY_KEY_CONFLICT:`, `ACCESSORY_HAS_STOCK_HISTORY:`, `PACKAGING_HAS_STOCK_HISTORY:`, mais
  as mensagens de validação estrutural de `register_stock_movement`), preparando o mapeamento de
  erro compreensível pedido para quando a Edge Function for publicada.
- `supabase/config.toml` — `[functions.stock-movements]` registrada (`verify_jwt = false`, mesmo
  padrão das demais — a Edge Function resolve o operador via `resolveOperator()`, não pelo gateway).

**Frontend**:

- Tipos novos em `types/domain.ts`: `StockItemType`, `StockMovementType` (8 valores manuais desta
  etapa), `StockMovement` (ledger completo, incl. `balance_before`/`balance_after`).
  `Accessory`/`Packaging.current_stock` já existiam no tipo desde antes — só passam a ser
  exibidos/usados agora.
- `lib/api/stockMovements.ts` — `listStockMovements` (leitura direta via supabase-js, RLS já
  aplicada, `authenticated` só SELECT) e `registerStockMovement` (via Edge Function). Histórico
  limitado a 50 registros mais recentes por item (`occurred_at`/`created_at` decrescente) — **sem
  paginação real neste MVP**, documentado aqui como pendência futura caso um item acumule mais
  movimentações do que isso (nenhuma é apagada do banco, só não aparece nesta consulta).
- `hooks/useStockMovements.ts` — histórico + registro por item, sem refetch após `register`
  (a RPC já devolve a movimentação completa, incl. `balance_after`).
- `hooks/useAccessories.ts`/`usePackaging.ts` — novo `setLocalStock(id, currentStock)`: atualização
  local pura (sem rede), usada pelo painel depois de uma movimentação bem-sucedida.
- `components/inventory/StockMovementForm.tsx` — action buttons com ícones Lucide (padrão de
  `RegisterPaymentForm.tsx`/`OrderForm.tsx`): 1º nível Entrada/Saída/Ajuste, 2º nível conforme a
  categoria (Saldo inicial/Compra/Devolução; Ajuste positivo/negativo; Perda-Avaria/
  Amostra-Doação/Uso interno). Quantidade inteira positiva, projeção "Saldo após movimentação",
  bloqueio de saída acima do saldo também no cliente (backend continua a proteção definitiva),
  motivo obrigatório por tipo, idempotência (ver abaixo).
- `components/inventory/StockMovementHistory.tsx` — histórico imutável (sem editar/excluir),
  estados de carregando/vazio/erro-com-retry, fallback legível para `movement_type` desconhecido
  (nunca quebra a tela), nenhum UUID exibido.
- `components/inventory/StockMovementPanel.tsx` — painel único (nome/categoria/saldo/estoque
  mínimo/situação + formulário + histórico), com `getStockLevel()`/`StockLevelBadge`
  (Sem estoque/Estoque baixo/Estoque normal — baixo só quando `minimum_stock > 0` e
  `current_stock <= minimum_stock`).
- `pages/InventoryPage.tsx` — nova coluna "Saldo atual" (ordenável), badge de situação, botão único
  "Movimentar estoque" por linha (Editar/Movimentar estoque/Excluir — sem poluição visual), abrindo
  o painel num `Dialog`. `delete_accessory`/`delete_packaging` (já bloqueados por histórico desde a
  rodada anterior) mapeados no frontend com a mesma mensagem real do backend.

**Decisões de UX sem regra documentada explícita (sinalizadas conforme pedido)**:

- Item inativo: Saldo inicial e Compra ficam indisponíveis; **Devolução permanece disponível**
  (decisão desta rodada) — `register_stock_movement()` não verifica `is_active` em nenhum
  `movement_type`, então bloquear Devolução na interface seria uma restrição só de UI sem
  correspondência no contrato real do backend.
- O painel fecha automaticamente após uma movimentação bem-sucedida (mesmo padrão de
  `RegisterPaymentForm`/`InventoryItemForm` — nenhum diálogo deste projeto permanece aberto após
  sucesso); para registrar uma segunda movimentação em seguida, o usuário reabre "Movimentar
  estoque" (a coluna "Saldo atual" já reflete o novo valor imediatamente, sem precisar reabrir).

**Idempotência no frontend**: `idempotency_key` gerada com `crypto.randomUUID()` só dentro de
`handleSubmit`, nunca a cada render; a mesma chave é reenviada enquanto
movement_type/quantidade/motivo/data não mudam (retry seguro de duplo clique/falha de rede);
qualquer mudança nesses campos gera uma chave nova; descartada implicitamente no sucesso (o painel
fecha, desmontando o formulário — a próxima abertura sempre começa do zero).

**Testes**: Deno (`stock-movements/handler.test.ts`, `_shared/errors.test.ts`) escritos mas **não
executados nesta sessão** — ambiente sem Deno instalado, mesma limitação já registrada em todas as
rodadas anteriores; revisados por leitura, seguindo a mesma disciplina de `accessories`/
`packaging`. Frontend: suíte completa 1167 → **1261 testes, todos passando**; lint 0 erros (1 aviso
novo, mesma categoria já tolerada em outros arquivos do projeto — export de `getStockLevel`/
`StockLevelBadge` junto do componente); build sem erros; `git diff --check` limpo; scan de segredos
sem ocorrências reais.

**Publicação em 2026-08-27 (mesmo dia, rodada seguinte)**: revisão estática integral do handler,
dos módulos `_shared` reaproveitados e da RPC (nenhuma inconsistência encontrada — payload
corresponde exatamente à assinatura de `register_stock_movement`, `quantity` sempre número,
`occurred_at` em formato ISO 8601 compatível com `timestamptz`, `idempotency_key` preservada sem
transformação, nenhuma escrita direta em `stock_movements`/`current_stock`, nenhuma service role
exposta, CORS/`verify_jwt=false` no mesmo padrão das demais funções). Deno continua indisponível
nesta sessão — os testes Deno já escritos não puderam ser executados, cobertura complementar via
suíte de frontend/API já aprovada (1261/1261). Edge Function `stock-movements` **publicada e
ativa** no projeto `tjhacqreupfqefntjevf` (`npx supabase functions deploy stock-movements`,
exclusivamente essa função — as 8 já existentes permaneceram com a mesma versão/`updated_at`,
confirmado por leitura antes/depois). Verificação HTTP real pós-publicação: `OPTIONS` sem
autenticação → **204**; `POST` sem autenticação (payload fictício, `item_id`
`00000000-0000-0000-0000-000000000000`, não real) → **401** (`Header Authorization ausente.`) —
rejeitado antes de qualquer acesso ao banco. Consultas somente leitura pós-teste confirmaram
**zero movimentação criada**: `stock_movements` continua com 0 linhas, nenhum `current_stock`
alterado em `accessories`/`packaging`. Frontend local confirmado (sem deploy): `.env.local` aponta
para o projeto correto e continua ignorado pelo git; `edgeFunctionClient.ts` monta a URL da função
dinamicamente a partir do nome `'stock-movements'`, nenhuma URL hardcoded. **Nenhum dado oficial ou
Petlink foi cadastrado ou alterado.**

**Percentual macro**: **não alterado por esta entrada**, deliberadamente — mesmo critério já
aplicado a Módulo 3 em rodadas anteriores. O backend do Incremento 1 e a Edge Function dos
Incrementos 2/3 agora estão **ambos deployados e testados automaticamente/via HTTP real**, mas
**nenhuma validação manual do usuário ainda ocorreu** — o padrão já estabelecido neste módulo só
reconhece uma fase como completa quando backend **e** interface **e** validação manual do usuário
existem juntos. A fórmula oficial (`# 1`) só concede peso de fase quando há artefato real
*deployado e validado*, não só deployado — recalcular o percentual fica para quando a validação
manual do usuário ocorrer.

**Validação manual do usuário em 2026-08-27 (mesmo dia, rodada seguinte)**: quantidade
disponível, entradas, saídas e histórico de Acessórios e Embalagens foram **validados
manualmente pelo usuário** — todos os 15 passos de um checklist manual foram aprovados. Nenhuma
alteração em Petlink ou dados oficiais durante a validação. Esta é a primeira parte do Módulo 3
(Incrementos 1-3, Acessórios/Embalagens) a reunir backend deployado **e** interface **e**
validação manual do usuário ao mesmo tempo — os três juntos, critério já estabelecido acima.
**Módulo 3 como um todo continua NÃO concluído**: falta a validação manual de Filamentos
(Incremento 4 abaixo, implementado só localmente) e todos os incrementos futuros (5 em diante —
consumo automático por pedido, reserva, inventário periódico). Percentual macro **ainda não
recalculado** — fica para quando o Módulo 3 estiver concluído por inteiro ou quando houver
critério explícito de recálculo parcial por incremento (não decidido nesta rodada).

## Incremento 4 — filamentos (tipos, rolos, movimentações, pesagem) — MVP IMPLEMENTADO LOCALMENTE

Implementado em 2026-08-27, na branch `feature/inventory-operations` (partindo de `41ab62c`,
depois da validação manual de Acessórios/Embalagens acima). Cobre os requisitos 1-8 do pedido
desta rodada (tipo de filamento, rolo físico, movimentações, pesagem, interface, histórico) e
parcialmente o requisito 9 (preparação de composição de produto, sem ativar consumo automático).
**Regras operacionais do MVP — versão inicial para validação, sujeitas a revisão após o teste
prático do usuário** (mesmo disclaimer das 22 regras de Acessórios/Embalagens — ver
`01_ESPECIFICACAO_FUNCIONAL.md` §16/§17/§20). **Nada desta seção foi aplicado ao Supabase remoto
nem publicado nem validado manualmente pelo usuário** — só criado e testado localmente.

**Decisão de arquitetura (diagnóstico técnico pedido explicitamente pelo requisito 5)**:
filamentos **não** reaproveitam `public.stock_movements` — ganharam um ledger dedicado,
`public.filament_movements`. Dois motivos concretos: (1) unidade de medida — peso de filamento é
fracionário (gramas), enquanto `stock_movements.quantity_delta` é `integer` (adequado só para
unidades discretas de acessórios/embalagens, uma tabela já em produção e validada manualmente,
que esta rodada não altera); (2) rastreabilidade por rolo — cada movimentação de filamento
precisa referenciar tanto o **tipo** quanto o **rolo físico**, e `stock_movements` só tem um
único item polimórfico. Detalhe completo em `03_MODELO_BANCO_DADOS.md` §12.3.

**Backend (4 migrations novas, nenhuma aplicada ao remoto)**:

- `20260827100000_create_filament_types_table.sql` — `public.filament_types` (material fechado
  `PLA`/`PETG`/`TPU`, **ABS explicitamente fora do MVP**; `line` livre, sem enum de banco;
  `color_code`/`notes` acrescentados frente à especificação original) +
  `create_filament_type`/`update_filament_type`/`delete_filament_type` (`security definer`,
  `service_role`, mesmo padrão de `accessories`/`packaging`).
- `20260827103000_create_filament_spools_table.sql` — `filament_spool_number_counters` +
  `next_filament_spool_code()` (gera `RL-XX-YYY`, mesmo padrão de `next_order_number()`) +
  `public.filament_spools` (peso nominal livre, sem valor fixo obrigatório; tara como campo do
  próprio rolo — `empty_spool_weight_grams` — em vez da tabela `spool_tares` da especificação
  original; status `LACRADO`/`ABERTO`/`ESGOTADO`/`DESCARTADO`, 4 valores, sem "em uso"; `is_active`
  como eixo independente de `status`) + `create_filament_spool`/`update_filament_spool`
  (bloqueia sair de `DESCARTADO` e reduzir o nominal abaixo do saldo atual)/`delete_filament_spool`.
- `20260827110000_create_filament_movements_table.sql` — `public.filament_movements` (9
  `movement_type`: `INITIAL_BALANCE`/`PURCHASE`/`RETURN`/`POSITIVE_ADJUSTMENT` entradas;
  `MANUAL_CONSUMPTION`/`LOSS`/`SAMPLE_TEST`/`NEGATIVE_ADJUSTMENT` saídas; `WEIGHING_ADJUSTMENT`
  pesagem, só via `register_filament_weighing`) + `register_filament_movement()` (mesmo padrão de
  lock/idempotência/concorrência de `register_stock_movement`; teto do peso nominal só para
  entradas de rotina, isento para a família de ajuste; bloqueia rolo `DESCARTADO`; aplica
  `ESGOTADO` automaticamente ao saldo chegar a zero) + `register_filament_weighing()` (peso
  disponível = peso bruto medido − tara quando conhecida, ou peso líquido direto quando não é;
  delta zero não grava nada; motivo sempre obrigatório, nenhuma tolerância percentual inventada) +
  `vw_filament_type_summary` (soma do peso disponível dos rolos ativos e utilizáveis por tipo,
  `security_invoker=true`).
- `20260827113000_create_product_filaments_table.sql` — `public.product_filaments` (peso teórico
  fracionário por tipo/produto, requisito 9) + `set_product_filaments()` (substitui a composição
  atomicamente, mesmo idioma de `set_product_composition`). **Preparação de modelo apenas**:
  nenhuma automação lê esta tabela, nenhum trigger em `orders`/`order_items` foi criado ou
  alterado, sem interface de edição nesta rodada (fora do escopo pedido — "prepare o modelo",
  não "ative o consumo").
- `supabase/functions/filament-types/`, `filament-spools/`, `filament-movements/` — 3 novas Edge
  Functions (`index.ts`/`handler.ts`/`handler.test.ts` cada), mesmo padrão de
  `accessories`/`stock-movements`: validação estrutural na função, regras dependentes do banco
  exclusivas das RPCs. `filament-movements` tem uma segunda rota, `POST
  /filament-movements/weighing`, para a pesagem. **Nenhuma das 3 foi publicada.**
- `supabase/functions/_shared/errors.ts` — ~30 novos padrões estáveis mapeados (validação
  estrutural das 6 novas RPCs + marcadores de regra de negócio:
  `FILAMENT_TYPE_HAS_SPOOLS:`/`FILAMENT_TYPE_HAS_COMPOSITION:`/`FILAMENT_SPOOL_HAS_MOVEMENTS:`/
  `FILAMENT_SPOOL_DISCARD_IS_FINAL:`/`FILAMENT_SPOOL_NOMINAL_BELOW_BALANCE:`/
  `FILAMENT_SPOOL_DISCARDED:`/`FILAMENT_INSUFFICIENT_BALANCE:`/`FILAMENT_EXCEEDS_NOMINAL:`/
  `FILAMENT_TARE_UNKNOWN:`).
- `supabase/config.toml` — `[functions.filament-types/filament-spools/filament-movements]`
  registradas (`verify_jwt = false`, mesmo padrão das demais).
- `supabase/tests/filament_inventory_test.sql` — teste de integração SQL (mesmo padrão de
  `inventory_movements_test.sql`), cobrindo: criação de tipo (aceita, rejeita ABS, rejeita
  duplicata, aceita linha livre não sugerida); múltiplos rolos com pesos nominais diferentes,
  saldo independente começando em zero; saldo inicial (único, exige zero), compra bloqueada acima
  do nominal, ajuste positivo isento do teto, consumo manual exige motivo, saldo negativo
  bloqueado, idempotência (mesmo payload e payload diferente), rejeição de `WEIGHING_ADJUSTMENT`
  pela rota errada; `ESGOTADO` automático ao zerar (e continua aceitando movimentação depois);
  descarte e sua natureza terminal (bloqueia movimentação e bloqueia reverter status); exclusão
  física bloqueada por histórico (rolo) e por rolo vinculado (tipo), permitida sem vínculo;
  desativação seguindo eixo independente de status; `vw_filament_type_summary` somando só rolos
  ativos e utilizáveis; pesagem com tara conhecida, sem tara conhecida, peso bruto abaixo da tara
  rejeitado, peso líquido negativo rejeitado, os dois pesos informados rejeitado, diferença zero
  não grava, motivo sempre obrigatório; `product_filaments`/`set_product_filaments` (grava,
  substitui atomicamente, rejeita tipo inexistente/inativo, rejeita peso não positivo, bloqueia
  exclusão do tipo por composição vinculada); privilégios (SELECT sim, INSERT/UPDATE/DELETE não,
  para `authenticated`; nada para `anon`).

**Frontend**:

- Tipos novos em `types/domain.ts`: `FilamentMaterial`, `FilamentType`, `FilamentSpoolStatus`,
  `FilamentSpool`, `FilamentMovementType`, `FilamentMovement`, `FilamentTypeSummary`,
  `ProductFilament`.
- `lib/api/filamentTypes.ts`/`filamentSpools.ts`/`filamentMovements.ts` — leitura direta via
  supabase-js (RLS já aplicada), escrita via as 3 Edge Functions. Listagem principal de tipos usa
  `vw_filament_type_summary` (já traz os totais agregados, sem exigir um segundo fetch/merge).
- `hooks/useFilamentTypes.ts`/`useFilamentSpools.ts`/`useFilamentMovements.ts` — mesmo idioma de
  `useAccessories`/`useStockMovements` (CRUD local sem refetch, `setLocalSpoolState` depois de uma
  movimentação/pesagem bem-sucedida, `register`/`weigh` separados — `weigh` pode devolver `null`
  quando a diferença calculada é zero).
- `components/inventory/FilamentTypeForm.tsx`/`FilamentSpoolForm.tsx` — cadastro/edição, mesmo
  padrão visual de `InventoryItemForm.tsx`; linha com 5 sugestões clicáveis (texto livre, nunca
  travado); peso nominal com 4 sugestões (250/500/750/1000 g, texto livre).
- `components/inventory/FilamentMovementForm.tsx` — adaptado de `StockMovementForm.tsx` para
  gramas fracionárias e os 8 tipos manuais de filamento (nomes próprios: Consumo manual,
  Amostra/Teste); bloqueia no cliente saída acima do saldo e entrada de rotina acima do nominal
  (backend permanece a proteção definitiva).
- `components/inventory/FilamentWeighingForm.tsx` — novo: alterna entre peso bruto medido (exige
  tara conhecida) e peso líquido direto (quando não é); mostra o peso líquido calculado e a
  diferença em relação ao saldo atual antes de enviar; motivo sempre obrigatório.
- `components/inventory/FilamentMovementHistory.tsx` — adaptado de `StockMovementHistory.tsx`
  para os 9 tipos e gramas.
- `components/inventory/FilamentSpoolPanel.tsx` — painel único por rolo (resumo + alternância
  Movimentar/Registrar pesagem + histórico); ao contrário de `StockMovementPanel`, **não fecha
  sozinho** após uma ação bem-sucedida (o usuário pode encadear mais de uma ação antes de fechar).
- `components/inventory/FilamentTypeDrawer.tsx` — drill-down de um tipo: lista de rolos com
  identificador/pesos/% restante/status/abertura/ações (Editar, Movimentar/Pesar, Ativar-desativar,
  Descartar, Excluir, Novo rolo).
- `components/inventory/InventoryPageShell.tsx` — extraído de `InventoryPage.tsx` (shell + nav de
  3 abas) para permitir a terceira área (Filamentos) sem import circular entre
  `InventoryPage.tsx` e `FilamentsInventoryPage.tsx`.
- `pages/FilamentsInventoryPage.tsx` — nova página (listagem de tipos via `vw_filament_type_summary`
  + badge de situação de estoque reaproveitando `getStockLevel`/`StockLevelBadge` + drill-down para
  rolos), roteada em `/estoque/filamentos` (`App.tsx`), terceira aba ao lado de Acessórios/
  Embalagens.

**Testes**: Deno (3 `handler.test.ts` novos) escritos mas **não executados nesta sessão** —
ambiente sem Deno, mesma limitação de todas as rodadas anteriores; revisados por leitura, seguindo
a mesma disciplina de `accessories`/`stock-movements`. SQL (`filament_inventory_test.sql`) escrito
mas **não executado** — ambiente sem Docker/Postgres local nem migrations aplicadas no remoto;
revisado linha a linha contra as 4 migrations. Frontend: suíte completa 1261 → **1332 testes,
todos passando** (71 novos: API layer, hooks, `FilamentMovementForm`, `FilamentWeighingForm`,
`FilamentsInventoryPage`; mais 1 asserção existente de `InventoryPage.test.tsx` ajustada ao novo
texto "acessórios, embalagens e filamentos"); lint 0 erros (mesmos 6 avisos pré-existentes, nenhum
novo); `tsc -b` sem erros; `vite build` sem erros; `git diff --check` limpo (só avisos de
LF→CRLF do Git no Windows); scan de segredos sem ocorrências reais (só o texto literal
`service_role`, nome de role do Postgres, em comentários/`GRANT`); `.env.local` confirmado
ignorado pelo git.

**Fora de escopo desta rodada** (incrementos futuros, nenhum com código): consumo automático de
filamento por pedido/produção, reserva, inventário periódico (pesagem em lote), tolerância
percentual de pesagem (nenhuma foi definida nem inventada), interface de edição de composição de
filamento por produto, publicação das 3 novas Edge Functions, aplicação das 4 migrations ao
Supabase remoto, qualquer validação manual do usuário sobre filamentos.

**Percentual macro**: **não alterado por esta entrada** — mesmo critério já aplicado. Backend e
interface de Filamentos existem agora **só localmente** (nem deployados, nem validados
manualmente) — dois dos três critérios (backend **e** interface **e** validação manual) ainda
faltam por completo para esta parte do Módulo 3.

## Incremento 4 — aplicação remota e publicação (2026-08-27, rodada seguinte)

Antes de aplicar qualquer coisa, **auditoria pré-deploy** confirmou um problema técnico real:
duas das quatro migrations desta rodada (`20260827106000_create_filament_movements_table.sql` e
`20260827109000_create_product_filaments_table.sql`) tinham identificadores de versão
`YYYYMMDDHHMMSS` **inválidos** — `106000` decodifica como `10:60:00` (minuto 60, fora do intervalo
0–59) e `109000` como `10:90:00` (minuto 90) — confirmado tanto por decodificação manual quanto
pela própria saída de `npx supabase migration list --linked`, que exibia o campo `time` cru
(`"20260827106000"`) em vez de uma data formatada para essas duas entradas, ao contrário das
outras 33. **Renomeadas antes de qualquer aplicação** (nunca uma migration já aplicada foi
editada — nenhuma das quatro tinha sido aplicada ainda neste ponto):
`20260827106000_create_filament_movements_table.sql` →
`20260827110000_create_filament_movements_table.sql` (11:00:00, válido) e
`20260827109000_create_product_filaments_table.sql` →
`20260827113000_create_product_filaments_table.sql` (11:30:00, válido), preservando a ordem
cronológica relativa às outras duas (100000/103000). Todas as referências cruzadas às duas
migrations renomeadas foram atualizadas em 9 arquivos (as próprias migrations
100000/103000, a Edge Function `filament-movements`, `_shared/errors.ts`,
`supabase/tests/filament_inventory_test.sql`, e 5 arquivos de frontend — `domain.ts`,
`FilamentMovementForm.tsx`, `FilamentMovementHistory.tsx`, `FilamentWeighingForm.tsx`,
`filamentMovements.ts`, `filamentTypes.ts`) — confirmado por busca exaustiva sem nenhuma
ocorrência residual dos identificadores inválidos em todo o repositório. Resto da auditoria
(ordem de dependência entre as 4 migrations, RLS, políticas, `SECURITY DEFINER`/`search_path`
seguro, `EXECUTE` restrito a `service_role`, ausência de permissão para `anon`, geração
concorrente/única de código de rolo, idempotência, bloqueio de saldo negativo, imutabilidade do
histórico, vínculo tipo+rolo correto, imutabilidade de `filament_type_id` do rolo, bloqueio de
exclusão com vínculo, cálculo de pesagem, `vw_filament_type_summary`, `ESGOTADO` automático,
`DESCARTADO` terminal, `product_filaments` sem consumo automático, ausência de gatilho em
pedidos/produção) **aprovada sem outra ressalva** — ver detalhamento completo em cada migration.

**Dry run** (`npx supabase db push --linked --dry-run`) apresentou exclusivamente as 4 migrations
de filamentos (agora com os nomes corrigidos), na ordem correta, nenhuma outra alteração.
**Aplicação real** (`npx supabase db push --linked`) bem-sucedida — as 4 migrations foram
aplicadas ao projeto remoto `tjhacqreupfqefntjevf`. `npx supabase migration list --linked`
confirmou **35/35 migrations sincronizadas local/remoto** (31 anteriores + as 4 novas), zero
pendências.

**Teste de integração SQL executado contra o remoto** (`supabase/tests/filament_inventory_test.sql`,
dentro de `BEGIN...ROLLBACK`, sem nenhum registro oficial) — resultado da primeira execução: **50
PASS, 1 FAIL** (Seção 6.1, `vw_filament_type_summary`). Investigação confirmou que o FAIL era um
**bug da própria asserção do teste**, não do banco: a Seção 6 original reutilizava
`spool_250_id`/`spool_1000_id` já mutados pelas Seções 4/5 (um ficou `ESGOTADO` — status que
nunca reverte automaticamente ao saldo voltar a ficar positivo, comportamento documentado e
correto de `register_filament_movement` — e o outro foi desativado em 5.4), então o resultado real
(`total=0`/`usable_count=0`) já estava correto; a asserção esperava `total=50`/`usable_count=1`,
um valor que nunca poderia ocorrer dado o estado real dos fixtures naquele ponto. Corrigida a
Seção 6 com um tipo e dois rolos dedicados, nunca mutados por seções anteriores (um ativo com
saldo, um desativado), testando de forma inequívoca tanto a inclusão quanto a exclusão na mesma
consulta — **nenhuma migration foi alterada**, só o arquivo de teste. Reexecutado: **51 PASS, 0
FAIL, 0 SKIP**. Consultas somente leitura pós-teste (após o `ROLLBACK` automático do próprio
script) confirmaram **zero resíduo**: `filament_types`/`filament_spools`/`filament_movements`/
`product_filaments`/`filament_spool_number_counters` com 0 linhas cada, nenhum produto
`TESTE%` persistido.

**Publicação individual das 3 novas Edge Functions** (`npx supabase functions deploy
<nome>`, uma chamada por função, nenhuma outra tocada) — `filament-types`, `filament-spools` e
`filament-movements` agora **ACTIVE, versão 1**, no projeto `tjhacqreupfqefntjevf`.
`npx supabase functions list` confirmou as 3 novas ativas e as 9 já existentes preservadas com o
mesmo `updated_at` de antes (nenhuma redeployada). Smoke tests HTTP reais: `OPTIONS` nas 3 rotas
→ **204**, cabeçalhos CORS corretos (`Access-Control-Allow-Origin` ecoando a origem permitida,
`Access-Control-Allow-Methods`/`Access-Control-Allow-Headers` no padrão do projeto); `POST` sem
`Authorization` nas 3 rotas → **401** (`Header Authorization ausente.`), rejeitado antes de tocar
o banco — confirmado por consulta pós-teste (0 linhas em todas as 4 tabelas novas). Teste CRUD
autenticado **não realizado nesta rodada** (sem sessão de usuário de teste disponível neste
ambiente de terminal) — fica para a validação manual pela interface.

**Frontend**: nenhuma alteração funcional — só os comentários que citavam os dois nomes de
migration renomeados. Suíte completa reexecutada por precaução: **1332/1332 passando**, lint 0
erros (mesmos 6 avisos pré-existentes), `tsc -b` sem erros, `vite build` sem erros, `git diff
--check` limpo.

**Nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink ou dado oficial, nenhum
`migration repair`, nenhum `db reset`, nenhuma migration já aplicada foi editada.** Interface de
`/estoque/filamentos` continua **aguardando validação manual do usuário** — backend e Edge
Functions agora deployados e testados automaticamente contra produção, mas isso ainda não
substitui essa validação. Regras operacionais do MVP continuam **sujeitas a revisão**; consumo
automático de filamento por pedido continua **pendente** (nenhum código). **Percentual macro não
alterado** — mesmo critério já aplicado: falta a validação manual para este trecho do Módulo 3
reunir os três critérios (backend + interface + validação manual) juntos.

## Incremento 4 — correção de UX pós-validação manual (2026-08-28)

**Primeira validação manual real do usuário na interface de Filamentos.** Resultado: passos 1-10,
12-17 e 19-22 do checklist **aprovados**; passo 11 (saldo consolidado de 1.300 g esperado após os
saldos iniciais/entradas dos dois rolos de teste) e passo 18 (saldo consolidado de 950 g esperado
após consumo/perda/pesagens) **pendentes** — o usuário não encontrou o saldo consolidado nem na
listagem principal nem em "Rolos do tipo". Observações adicionais: janela "Ver rolos"
desproporcional; usuário precisa conseguir excluir rolos com confirmação; janela "Movimentar/
Pesar/Histórico" desproporcional; coluna Data invadindo a coluna Tipo no histórico.

**Diagnóstico do saldo consolidado** — reconstrução completa via consultas **somente leitura**
contra o remoto `tjhacqreupfqefntjevf`, usando exclusivamente os próprios registros `TESTE%` já
criados pelo usuário durante a validação (nenhum dado criado por esta auditoria):
histórico completo de `filament_movements` dos dois rolos de teste confirmou **cada saldo exatamente
igual ao documentado pelo usuário** — Rolo 1: 0→800 (saldo inicial)→1.000 (entrada 200g)→900
(consumo 100g)→850 (perda 50g)→750 (pesagem bruto 1000g/tara 250g); Rolo 2: 0→300 (saldo
inicial)→200 (pesagem líquida direta). Consulta direta a `vw_filament_type_summary` para o tipo de
teste confirmou o valor agregado correto no estado atual (750g, só o rolo 1 — o rolo 2 havia sido
consumido a 0 e descartado em passos posteriores do checklist do usuário, fora da janela 750+200).
**Conclusão: zero erro no banco, na view ou nas RPCs — todas as 6 movimentações/pesagens
calcularam exatamente certo, no valor certo, na hora certa.** O problema era inteiramente de
frontend, com duas causas concretas: (1) `FilamentTypeDrawer` ("Rolos do tipo") nunca exibia
nenhum resumo consolidado — só a tabela de rolos individuais; (2) a listagem principal
(`FilamentsInventoryPage`) buscava `vw_filament_type_summary` **uma única vez** no carregamento da
página e nunca era informada quando uma movimentação/pesagem acontecia dentro do painel aninhado
(`FilamentSpoolPanel`) — o `total_available_grams` exibido ficava permanentemente preso ao valor do
carregamento inicial (tipicamente 0g, antes de qualquer rolo existir). Nenhuma correção foi feita
diretamente no banco — o saldo já estava certo o tempo todo.

**Correções de frontend** (nenhuma migration, nenhuma Edge Function, nenhum objeto remoto tocado):

- `FilamentsInventoryPage.tsx` — `openType` deixou de ser um snapshot congelado no momento do
  clique em "Ver rolos": agora é derivado (`types.find(...)`) do array `types` a cada render, e um
  novo prop `onSummaryChanged={refetch}` é repassado ao drawer. Qualquer ação que possa afetar o
  saldo do tipo agora aciona um refetch de `vw_filament_type_summary` — a listagem e o drawer
  passam a refletir o valor real imediatamente após a ação (requisito explícito: nunca recalcular
  o saldo em JS, sempre usar o resumo devolvido pelo backend).
- `FilamentTypeDrawer.tsx` — novo bloco de resumo no topo (Disponível, Rolos, Abertos, Esgotados,
  Estoque mínimo, Situação): Disponível/Estoque mínimo/Situação vêm sempre de `filamentType`
  (prop, nunca recalculado); Abertos/Esgotados são uma contagem de exibição dos rolos já
  carregados para a própria tabela (não uma segunda fonte de saldo). `onSummaryChanged` é chamado
  após criar/editar/ativar-desativar/descartar/excluir/arquivar um rolo, e repassado ao
  `FilamentSpoolPanel` para ser chamado também após uma movimentação/pesagem bem-sucedida.
- Fluxo de exclusão revisado: um único botão "Excluir rolo" por linha, sempre com confirmação
  explícita ("Tem certeza..."). Tenta a exclusão física primeiro (via `delete_filament_spool`, já
  existente, que já sabe com certeza se há histórico vinculado — nunca duplicada no frontend); se o
  backend bloquear por histórico (`business_rule`/409), o **mesmo diálogo pivota** para oferecer
  "Arquivar rolo" em vez de deixar um erro sem próximo passo — arquivar reaproveita o contrato já
  existente `update_filament_spool({ is_active: false })` (o mesmo do Switch "Ativo" da tabela),
  nunca um hard delete de rolo com histórico. Novo filtro "Mostrar arquivados" (oculto por padrão)
  na tabela de rolos. Botão "Movimentar / Pesar" desabilitado para rolo arquivado — **restrição
  aplicada só no frontend nesta rodada**: `register_filament_movement`/`register_filament_weighing`
  ainda não verificam `is_active` no banco (só bloqueiam `status=DESCARTADO`) — registrado aqui como
  lacuna real para um incremento futuro, não escondida.
- `FilamentMovementHistory.tsx` — corrigida definitivamente a sobreposição Data/Tipo: causa raiz
  era `TableCell` herdar `whitespace-nowrap` por padrão (`ui/table.tsx`) sem nenhuma proteção contra
  transbordo na célula de Data — `table-fixed` reserva a largura da coluna mas não corta conteúdo
  que a ultrapassa. Aplicada a mesma classe `truncate` já usada em Tipo/Motivo, larguras de coluna
  reequilibradas (Data 16%→18%) e `min-w-[720px]` adicionado à tabela (mesmo padrão de rolagem
  horizontal controlada já usado nas demais tabelas do projeto).
- Dimensionamento das duas janelas: "Ver rolos" (`sm:max-w-4xl max-h-[90vh] overflow-y-auto`) e
  "Movimentar/Pesar/Histórico" (`sm:max-w-3xl max-h-[90vh] overflow-y-auto`, alargada de
  `max-w-2xl` para dar mais espaço ao histórico) — reaproveitando **exatamente** o padrão já
  estabelecido em `OrdersPage.tsx` ("Gerenciar pedido"), nenhum valor arbitrário novo introduzido.

**Testes**: 18 testes novos em `FilamentsInventoryPage.test.tsx` (saldo consolidado de 1.300g/950g
na listagem, resumo consolidado completo no drawer, prova de que o resumo nunca é recalculado em
JS, refetch do resumo após movimentação/pesagem, exclusão física com sucesso, pivô para
arquivamento, erro inesperado não pivota, bloqueio de duplo envio, filtro "Mostrar arquivados",
botão desabilitado em rolo arquivado, classes de dimensionamento das duas janelas) + novo arquivo
`FilamentMovementHistory.test.tsx` (5 testes, incl. a proteção `truncate` contra o transbordo
Data→Tipo). Suíte completa 1332 → **1350 testes, todos passando**; lint 0 erros (mesmos 6 avisos
pré-existentes); `tsc -b`/`vite build` sem erros; `git diff --check` limpo; scan de segredos sem
ocorrências reais; `.env.local` confirmado ignorado; nenhum resíduo `TESTE` novo criado por esta
auditoria (todas as consultas de diagnóstico foram somente leitura, contra registros que o próprio
usuário já havia criado e mantido durante a validação manual).

**Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum objeto remoto alterado, nenhum
push, nenhum deploy de frontend, nenhuma alteração em Petlink ou dado oficial.** Validação final da
interface de Filamentos continua **pendente** — o usuário precisa reexecutar especificamente os
passos 11 e 18 (agora com o resumo consolidado visível) e as 4 observações de proporção/exclusão
antes de o Módulo 3 avançar para este critério. Consumo automático de filamento por pedido continua
**sem nenhuma linha de código** (fora de escopo desta rodada). Percentual macro **não alterado**.
Checkpoint local: commit `fix: refine filament inventory validation flow`, sem push.

## Incremento 4 — segundo reteste e correção do fluxo de arquivamento (2026-08-28)

**Segundo reteste manual do usuário.** Resultado: passos 1-6 e 9-10 aprovados; **passo 7** (janela
"Rolos do tipo" ainda com rolagem horizontal em resolução normal de notebook/desktop — o ajuste da
rodada anterior não foi suficiente); **passo 8** (na janela "Movimentar/Pesar/Histórico", o texto
de Tipo no histórico não aparecia completo); **passos 11 e 12** não aprovados (arquivamento de rolo
com histórico e consulta por "Mostrar arquivados" não funcionaram como esperado).

**Diagnóstico do arquivamento** — leitura integral de `FilamentsInventoryPage.tsx`,
`FilamentTypeDrawer.tsx`, `FilamentSpoolPanel.tsx`, API/hooks de `filamentSpools`, Edge Function
`filament-spools`, RPCs de atualização/exclusão de rolo, e auditoria somente leitura dos rolos
`TESTE%` do reteste anterior no remoto (sem alterar nenhum registro — confirmado por `updated_at`
inalterado antes/depois da auditoria). Causa raiz encontrada: o fluxo da rodada anterior **tentava
a exclusão física primeiro** e só decidia arquivar depois de capturar um erro de negócio
(`business_rule`/409) — funcionalmente correto no papel, mas não é a experiência que o usuário
esperava, e a sequência tentar→capturar→pivotar não se comportou de forma confiável na prática
(nenhum dos rolos com histórico testados terminou arquivado — todos permaneceram `is_active=true`).
A view/RPCs continuam corretas (reconfirmado: `is_active=false` já é excluído de
`vw_filament_type_summary.total_available_grams`, sem necessidade de qualquer migration) — o
problema era inteiramente a estratégia de decisão do frontend.

**Correção do fluxo de exclusão/arquivamento**: a decisão entre Excluir e Arquivar passa a ser
tomada **antes** de qualquer confirmação, nunca a partir de texto de erro. Novo campo derivado
`FilamentSpool.has_movement_history` (nunca uma coluna do banco): `listFilamentSpools` agora faz
uma segunda consulta de leitura (`filament_movements.spool_id` para o mesmo tipo, já concedida a
`authenticated`) e marca cada rolo — `delete_filament_spool` bloqueia exclusivamente por
`FILAMENT_SPOOL_HAS_MOVEMENTS:`, então "tem ao menos uma movimentação" é uma predição exata de "a
exclusão vai falhar". Nenhuma migration nova: as duas consultas usam SELECT já concedido. O menu
"Mais ações" de cada rolo já mostra "Excluir rolo" ou "Arquivar rolo" — nunca o primeiro seguido de
um pivô — e o diálogo de confirmação usa o texto pedido ("Este rolo possui histórico e não pode ser
apagado definitivamente. Deseja arquivá-lo?"). `delete_filament_spool` continua sendo chamada
normalmente (defesa em profundidade contra uma corrida real), mas só quando o rolo já é sabido sem
histórico.

**Filtro "Mostrar arquivados"**: já existia desde a rodada anterior e continuou funcionando
(confirmado pelos testes) — o problema relatado nos passos 11/12 era mais provavelmente uma
consequência do arquivamento em si nunca ter persistido de fato (rolo nunca ficava `is_active=false`
para o filtro revelar). Reforçado com indicador visual explícito "Arquivado" (distinto de
Esgotado/Descartado) e persistência do estado do filtro entre atualizações do drawer (nunca reseta
sozinho). "Movimentar"/"Registrar pesagem" continuam bloqueados para um rolo arquivado, mas
`FilamentSpoolPanel` agora abre normalmente para consulta de histórico (rodada anterior desabilitava
o próprio botão, escondendo também o histórico — corrigido: histórico sempre acessível).

**Rolagem horizontal em "Rolos do tipo" (passo 7)**: causa raiz era um `min-w-[900px]` fixo na
tabela, maior que a largura útil do próprio diálogo (`sm:max-w-4xl` ≈ 864px de conteúdo) — forçava
rolagem mesmo em notebook/desktop normal. Corrigido reduzindo de 8 para 7 colunas (peso
nominal/disponível/% restante mesclados numa única célula "750g / 1000g · 75%"), movendo
Editar/Descartar/Excluir-Arquivar para um menu compacto "Mais ações" (`DropdownMenu`, componente já
usado em `SortableColumnHeader.tsx`), e removendo o `min-width` fixo — a tabela agora usa só
`table-fixed` com larguras percentuais, cabendo sem rolagem. Adicionada também uma apresentação em
cartões (`Card`, componente já usado em `OrderManagementPanel.tsx`) para telas realmente pequenas
(`sm:hidden`/`hidden sm:block`), em vez de depender só da tabela larga.

**Texto de Tipo truncado no histórico (passo 8)**: o `truncate` aplicado à coluna Tipo na rodada
anterior (fix correto para a invasão de Data→Tipo) cortava rótulos mais longos do próprio Tipo (ex.
"Ajuste por pesagem"). Corrigido: Tipo nunca mais usa `truncate` — `whitespace-normal break-words`
permite quebra de linha controlada dentro da coluna (table-fixed já impede invadir a coluna
seguinte), mostrando sempre o texto completo. A coluna Data manteve `truncate` (não relatada como
problema neste reteste, continua correta).

**Testes**: suíte completa 1350 → **1358 testes, todos passando** (8 novos + reescrita de ~15 para
o novo fluxo de exclusão/arquivamento e a marcação dupla tabela/cartão). Novos casos: rolo sem
histórico oferece "Excluir rolo" direto no menu; rolo com histórico já oferece "Arquivar rolo" sem
tentar exclusão física primeiro; erro inesperado em cada um dos dois fluxos mantém o diálogo aberto
com a mensagem real; bloqueio de duplo envio nos dois fluxos; indicador visual "Arquivado"; filtro
persiste entre atualizações do drawer; saldo nunca volta a somar o rolo arquivado só por marcar
"Mostrar arquivados"; rolo arquivado mantém histórico acessível; ausência de `min-width` forçado na
tabela; coluna Tipo sem `truncate`, com `whitespace-normal`/`break-words`; apresentação em cartões
existe. Lint 0 erros (mesmos 6 avisos pré-existentes), `tsc -b`/`vite build` sem erros, `git diff
--check` limpo, scan de segredos sem ocorrências reais, `.env.local` confirmado ignorado.

**Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum objeto remoto alterado
(auditoria só leu registros `TESTE%` já existentes — `updated_at` confirmado inalterado), nenhum
push, nenhum deploy de frontend, nenhuma alteração em Petlink ou dado oficial.** Validação final
continua **pendente** — o usuário precisa reexecutar especificamente os passos 7, 8, 11 e 12.
Consumo automático de filamento por pedido continua **sem nenhuma linha de código**. Percentual
macro **não alterado**. Checkpoint local: commit `fix: complete filament inventory usability`, sem
push.

## Incremento 4 — terceira rodada de layout e MVP manual de filamentos aprovado (2026-08-28)

**Terceiro reteste manual do usuário**: o passo 1 (rolagem horizontal + botão "Movimentar / Pesar"
sobreposto ao botão "Mais ações" em "Rolos do tipo", num diálogo de ~720px de largura, resolução
normal de notebook) ainda falhava. Causa raiz: a tabela de 7 colunas da rodada anterior ainda tinha
DUAS células de ação separadas ("Movimentar / Pesar" + "Mais ações"), e um `<button>` não trunca
como texto — quando a largura alocada à célula era menor que a largura natural do botão, ele vazava
visualmente sobre a célula vizinha. **Corrigido**: tabela reduzida para 5 colunas semânticas
(Identificador/Peso/Status/Abertura/Ações) — a coluna "Ativo" foi eliminada, o status
ativo/arquivado passou a aparecer como badge junto ao Status; as duas ações secundárias viraram
**Gerenciar** (botão compacto, abre o mesmo `FilamentSpoolPanel`) + **Mais ações** (só ícone de três
pontos, nome acessível "Mais ações para o rolo [código]"), num único flex com `gap`, nunca
sobrepostos; Ativar/Desativar passou a ser um item do menu (funcionalidade preservada, só deixou de
ser uma coluna própria). `overflow-x-auto` removido do wrapper da tabela — a correção não esconde a
rolagem, elimina a necessidade dela. Suíte 1358 → **1363 testes, todos passando**. Checkpoint local:
commit `fix: make filament spool list responsive`, sem push.

Em seguida, o usuário **aprovou integralmente o MVP manual de filamentos** — os quatro pontos em
aberto desde as rodadas de reteste anteriores (saldo consolidado 1.300g/950g, exclusão/arquivamento
de rolo com histórico, filtro "Mostrar arquivados", e agora o layout da relação de rolos em
resolução de notebook) ficam todos marcados como validados na prática pelo usuário. As regras
operacionais do MVP (22 regras aprovadas em 2026-08-27 + os ajustes desta série de retestes)
permanecem, mesmo assim, **hipóteses documentadas sujeitas a revisão** — a aprovação manual valida o
comportamento implementado, não congela a regra como definitiva para sempre.

## Incremento 5 — Compras (Filamentos/Acessórios/Embalagens) — IMPLEMENTADO LOCALMENTE (2026-08-28)

Com o MVP de filamentos aprovado, pedido do usuário para implementar um fluxo **centralizado de
Compras** dentro do módulo Estoque, cobrindo as três áreas (Filamentos/Acessórios/Embalagens) a
partir de um único botão/diálogo compartilhado — toda compra concluída gera a entrada
correspondente no estoque e atualiza a listagem imediatamente. Junto, uma segunda decisão: remover
da interface a exigência de informar manualmente um código de cor/filamento (o identificador
interno automático do rolo, `RL-YY-NNN`, é outra coisa — gerado pelo backend, nunca afetado).

**Remoção do código informado manualmente**: `filament_types.color_code` já era opcional em todas
as camadas (coluna nullable, RPCs sem exigência, Edge Function com `optionalTrimmedString`) — a
única mudança necessária foi na interface. Campo "Código da cor (opcional)" removido dos diálogos
"Novo tipo de filamento"/"Editar tipo de filamento" (`FilamentTypeForm.tsx`); o formulário nunca
mais envia essa chave (create grava `null` quando omitida, PATCH sem a chave preserva o valor
existente — semântica já documentada em `update_filament_type`) — nenhuma migration destrutiva,
nenhum backfill, compatibilidade total com registros antigos que já tenham um valor preenchido.

**Modelo de dados**: nova tabela `public.inventory_purchases` (ledger financeiro imutável, uma
linha por operação de compra — uma categoria, um item, N unidades; `category`+`item_id` polimórfico,
mesmo padrão sem FK de `stock_movements.item_type/item_id`; `total_value` é uma coluna **gerada**
pelo próprio Postgres como `item_value + freight_value`, nunca inserível diretamente — a invariante
"total = itens + frete" é garantida por construção). Migration complementar (nunca editando as já
aplicadas) acrescenta a `filament_spools`: `initial_gross_weight_grams` (peso bruto informado na
compra) e `purchase_id` (vínculo à compra que originou o rolo) — ambas nullable, `null` para todo
rolo criado manualmente fora do fluxo de Compras.

**RPC `register_inventory_purchase`**: única função que grava `inventory_purchases` e, conforme a
categoria, reaproveita integralmente as RPCs já existentes e validadas — nunca duplica lógica de
saldo. Para Acessório/Embalagem: valida o item ativo e chama `register_stock_movement` (PURCHASE).
Para Filamento: localiza um `filament_type` ativo pela combinação normalizada
material+fabricante+acabamento+cor (case-insensitive, trim — nunca duplicata por
maiúsculas/espaços), cria o tipo quando não existe (`create_filament_type`, sempre com
`color_code = null`), nunca reativa silenciosamente uma correspondência inativa (levanta
`FILAMENT_TYPE_INACTIVE_MATCH:` para o usuário decidir), cria N `filament_spools` (status
`LACRADO`, `is_active=true`, `empty_spool_weight_grams` = tara estimada = peso bruto informado −
peso líquido nominal) e registra, para cada rolo, uma entrada `PURCHASE` via
`register_filament_movement` (saldo inicial = peso líquido nominal) — `reference_type='PURCHASE'`/
`reference_id=<compra>` deixa o vínculo rastreável no histórico de cada rolo. Todo o corpo da
função é uma única transação Postgres: qualquer exceção não capturada desfaz tudo que já tinha sido
feito até aquele ponto (nenhum rolo órfão, nenhuma movimentação sem compra, nenhuma compra sem
efeito completo) — atomicidade por construção, não por controle manual. Idempotência verificada
antes de qualquer trabalho pesado (find-or-create de tipo, criação de rolos).

**Edge Function `inventory-purchases`** (não publicada): valida estruturalmente o payload (categoria,
quantidade inteira positiva, valores não-negativos, campos específicos por categoria — `item_id`
para Acessório/Embalagem; material/marca/acabamento/cor/peso nominal/lista de pesos brutos para
Filamento, com comprimento exigido igual à quantidade e cada peso bruto maior que o nominal) antes
de chamar a RPC — 9 novos padrões estáveis mapeados em `_shared/errors.ts`.

**Interface**: botão "Compras" único, reaproveitando `InventoryPageShell.tsx` (nunca duplicado por
página) — abre o diálogo "Registrar compra" (`PurchaseDialog.tsx`, `sm:max-w-2xl max-h-[90vh]
overflow-y-auto`, sem rolagem horizontal). Seleção de categoria via action buttons ("Item":
Filamento/Acessório/Embalagem, nunca um `<select>`), mesmo padrão de ícone+radiogroup já usado em
Pedidos/Pagamentos. Filamento: Material (PLA/PETG/TPU, nunca ABS) → Peso líquido (250g/500g/1000g,
action buttons) → Quantidade → um campo "Peso bruto do rolo N" por unidade, dinâmico conforme a
quantidade (exige valor maior que o líquido nominal) → Marca/Cor (texto) → Acabamento (Sólido/
Velvet/Silk/DuoColor/TriColor/Transparente, rótulo deliberadamente diferente de "Tipo") → Valor dos
itens/Frete (máscara monetária brasileira, mesmo campo "bancário" de `ProductPriceForm.tsx`) →
resumo (total, custo médio por rolo, custo estimado por kg). Acessório/Embalagem: seleção por busca
entre itens **ativos** (reaproveita `SearchAutocomplete.tsx`; item inativo nunca aparece; nenhum
cadastro automático — orienta cadastrar primeiro na aba correspondente quando não há nenhum ativo)
→ Quantidade → Valor dos itens/Frete. Após sucesso: `onPurchaseCompleted(categoria)` — cada página
de área só refaz sua própria busca quando a categoria comprada é a da área atualmente aberta
(comprar um acessório com a aba Filamentos aberta não dispara um refetch de filamentos ali).

**Testes**: 2 migrations SQL, Edge Function (`handler.ts`/`handler.test.ts`, 34 testes de validação
estrutural — Deno não instalado, não executados, mesma limitação de todas as rodadas), teste de
integração SQL completo (`supabase/tests/inventory_purchases_test.sql`, 10 seções cobrindo compra de
acessório/embalagem/filamento, reaproveitamento de tipo existente, bloqueio de tipo só-inativo,
validações estruturais, idempotência, **atomicidade real** — uma falha forçada no segundo rolo de
uma compra de 2 desfaz também o primeiro rolo e o próprio registro da compra —, regressão do fluxo
manual de rolo, e privilégios/RLS; não executado nesta sessão, sem Postgres local/aplicação remota).
Frontend: novo arquivo `PurchaseDialog.test.tsx` (21 testes) + adições em `InventoryPage.test.tsx`/
`FilamentsInventoryPage.test.tsx` (botão "Compras" nas três áreas, ausência do campo "Código da
cor"). Suíte completa do frontend 1363 → **1390 testes** (1388 passando + 2 falhas pré-existentes,
não relacionadas a esta rodada — `RegisterPaymentForm.test.tsx`, teste sensível à data real do
sistema, que virou o dia durante a sessão; arquivo não tocado nesta rodada). Lint 0 erros, `tsc -b`/
`vite build` sem erros, `git diff --check` limpo, scan de segredos sem ocorrências reais, `.env.local`
confirmado ignorado.

**Nenhuma migration aplicada ao Supabase remoto, nenhuma Edge Function publicada, nenhum deploy de
frontend, nenhum dado oficial/Petlink alterado, nenhum push.** Composição de Produtos e consumo
automático de filamento continuam **sem nenhuma linha de código** — fora de escopo desta rodada
(pedido do usuário explicitamente adiou o assunto de composição de filamentos em Produtos).
Histórico de compras (tela de consulta dedicada) **não implementado** — só o registro em
`inventory_purchases`, já com índice por item, preparado para uma tela futura. **Módulo 3 continua
NÃO concluído** — regras operacionais do MVP continuam sujeitas a validação manual específica desta
nova funcionalidade, ainda não realizada. Percentual macro **não alterado**. Checkpoint local:
commit `feat: add inventory purchase workflow`, sem push.

## Incremento 5 — auditoria, correção de falhas de suíte e aplicação remota (2026-08-28/29)

Auditoria integral solicitada pelo usuário sobre os 20 arquivos do commit `feat: add inventory
purchase workflow`, seguida — só se tudo aprovado — de aplicação das migrations de Compras e
publicação da Edge Function correspondente.

**Duas falhas da suíte, causa raiz confirmada**: não eram regressão do commit de Compras (nenhum dos
dois arquivos afetados — `RegisterPaymentForm.tsx`/`.test.tsx` — foi tocado por aquele commit). Causa
real: os dois testes calculavam "hoje" via `new Date().toISOString().slice(0,10)` (data em **UTC**)
para comparar contra `todayIsoDate()` do componente, que usa `getFullYear/getMonth/getDate` (data
**local**) — correto para um formulário de pagamento brasileiro. Como a máquina desta sessão roda em
`America/Sao_Paulo` (UTC-3) e a suíte foi executada às 21h44 (hora local) — já 00h44 em UTC, ou seja,
já virou o dia em UTC mas ainda não localmente —, a comparação divergia. Não é uma condição rara de
todo: acontece em qualquer execução real entre ~21h e 24h nesse fuso. **Corrigido tornando os dois
testes determinísticos**: `vi.useFakeTimers({ toFake: ['Date'] })` + `vi.setSystemTime(...)` fixam só
`Date` (nunca os timers reais, para não interferir em `userEvent`) num instante em que data local e
UTC caem em dias diferentes — mesma regra funcional preservada, nenhuma expectativa enfraquecida,
nenhum teste ignorado, nenhuma mudança em produção (o componente já estava correto).

**Auditoria do backend**: aprovada, com uma inconsistência real encontrada e corrigida antes de
qualquer aplicação remota — a coluna `filament_spools.initial_gross_weight_grams` só tinha `CHECK (>=
0)`, faltando a defesa em profundidade "> peso líquido nominal" já pretendida na migration original
(a RPC já validava isso antes de inserir, mas a tabela em si não). Adicionado
`filament_spools_initial_gross_weight_exceeds_nominal` (CHECK entre colunas) à migration
`20260828121000` (ainda não aplicada em nenhum ambiente neste ponto — reforço, não edição de
migration já aplicada) + um teste novo (6.4) confirmando que a CHECK bloqueia mesmo um INSERT direto
que contorne a RPC. Restante da auditoria (RLS/grants — `authenticated` só tem SELECT em
`inventory_purchases`, EXECUTE de `register_inventory_purchase` só para `service_role`; atomicidade —
uma única transação PL/pgSQL; idempotência — checada antes de qualquer trabalho pesado; find-or-create
de tipo normalizado; nunca reativa tipo inativo; ABS rejeitado; frontend nunca escreve saldo
diretamente; nenhum segredo no diff) **aprovado sem outra ressalva**.

**Teste de integração SQL executado pela primeira vez contra o remoto** (dentro de
`BEGIN...ROLLBACK`) revelou um segundo problema real, desta vez só no próprio script de teste: 10 dos
26 casos usavam a palavra-chave `DEFAULT` como argumento posicional numa chamada de função — sintaxe
que o Postgres não aceita fora de `INSERT ... VALUES` (não é um recurso de chamada de função). **Não
era um bug da RPC** — confirmado porque os casos que já usavam notação nomeada (`p_x => valor`)
passaram desde a primeira execução. Corrigido convertendo as 10 chamadas para notação nomeada,
omitindo os parâmetros que devem usar o próprio default da function (nunca `DEFAULT` literal).
Reexecutado: **26 PASS, 0 FAIL, 0 SKIP** — incluindo o teste de atomicidade real (falha forçada no
segundo rolo de uma compra de 2 desfaz também o primeiro rolo e o próprio registro da compra, contagens
idênticas antes/depois), idempotência (mesma chave+payload devolve a mesma compra sem duplicar; chave
reusada com payload diferente é rejeitada), bloqueio de tipo só-inativo, e a nova CHECK de defesa em
profundidade. `ROLLBACK` confirmado — zero resíduo (`TESTE COMPRAS%`, tipo de filamento de teste,
rolo `RL-TESTE-CHECK`: todos 0 depois; contagens oficiais idênticas antes/depois: 3 acessórios, 3
embalagens, 5 tipos de filamento, 9 rolos, 0 compras).

**Migrations aplicadas ao projeto Supabase remoto `tjhacqreupfqefntjevf`** via `npx supabase db push
--linked` — exclusivamente `20260828120000_create_inventory_purchases_table.sql` e
`20260828121000_create_register_inventory_purchase_function.sql`; as 35 migrations anteriores
permaneceram alinhadas, dry-run prévio mostrou exatamente essas duas (nenhum DROP/DELETE/TRUNCATE em
nenhuma delas), 37/37 sincronizadas depois. Verificação somente leitura pós-aplicação confirmou:
colunas/tipos de `inventory_purchases` e das duas novas colunas de `filament_spools` corretos;
`authenticated` com só SELECT em `inventory_purchases` (sem INSERT/UPDATE/DELETE); `EXECUTE` de
`register_inventory_purchase` só para `service_role` (e o owner); a nova CHECK
`filament_spools_initial_gross_weight_exceeds_nominal` presente; nenhum dado existente alterado
(mesmas contagens de antes). **Edge Function `inventory-purchases` publicada** (única — as 11 já
existentes preservaram `updated_at` idêntico, nenhuma redeployada) — `OPTIONS` → 204 com CORS
correto; `POST` sem autenticação → 401 (`Header Authorization ausente.`), confirmado sem criar
nenhuma compra real (contagem de `inventory_purchases` continuou 0 depois do smoke test).

Suíte completa do frontend reexecutada após as correções: **1390/1390, 100% passando** (as duas
falhas anteriores agora corrigidas e determinísticas). Lint 0 erros, `tsc -b`/`vite build` sem erros,
`git diff --check` limpo, scan de segredos sem ocorrências reais. Testes Deno **não executados** —
ambiente sem Deno instalado nesta sessão (não instalado, por instrução explícita de não trazer
ferramentas de origem desconhecida).

**Nenhum push realizado. Nenhum deploy de frontend. Nenhuma alteração em Petlink ou dado oficial —
todas as contagens de dados reais permaneceram idênticas antes/depois de toda a rodada.** O backend
de Compras (migrations + RPC + Edge Function) está agora aplicado e validado por teste automatizado
real contra produção, mas **nenhuma validação manual do usuário ainda ocorreu pela interface** —
Módulo 3 continua **NÃO concluído**; composição de Produtos e consumo automático de filamento
continuam sem nenhuma linha de código. Percentual macro **não alterado** — critério já estabelecido
exige backend + interface + validação manual do usuário juntos.

## Incremento 5 — validação manual do usuário aprovada (2026-08-29)

**O usuário validou manualmente, no ambiente real (produção), as três compras do MVP de Compras**,
usando o backend aplicado e a Edge Function publicada na entrada anterior:

- **Compra de Filamento**: criação de dois rolos confirmada; identificadores automáticos (`RL-YY-
  NNN`) gerados corretamente; "Disponível" do tipo atualizado; quantidade de "Rolos" atualizada;
  pesos líquidos e brutos individuais de cada rolo corretos.
- **Compra de Acessório**: entrada registrada; saldo (`current_stock`) atualizado; histórico da
  movimentação correto.
- **Compra de Embalagem**: entrada registrada; saldo atualizado; histórico correto.

Isso **completa a validação manual do fluxo de Compras** (Incremento 5) — as três categorias
(Filamento/Acessório/Embalagem) foram exercitadas e aprovadas de ponta a ponta pelo usuário, contra o
backend real, não só por teste automatizado. **O fluxo de Compras está concluído neste MVP.**

Ressalvas explícitas que continuam valendo:
- **Histórico central de compras (tela de consulta dedicada) continua ausente** — só o registro em
  `inventory_purchases`, com índice por item, preparado para uma tela futura; nenhuma tela de
  consulta foi construída nesta rodada nem em nenhuma anterior.
- **Composição de produtos** (ficha técnica/BOM ligando um Produto de catálogo a filamentos/
  acessórios/embalagens necessários) é o **próximo incremento**, ainda sem nenhuma linha de código.
- **Consumo automático de estoque** (reserva ao entrar em produção, baixa automática por transição
  de status do pedido) **depende da composição existir primeiro** — também sem nenhuma linha de
  código, e não pode ser implementado antes dela.
- **Módulo 3 (Estoque e Inventário) ainda NÃO está concluído** — falta composição de produtos,
  consumo automático, e qualquer forma de reserva/inventário periódico; o percentual macro do módulo
  **não é alterado** por esta entrada (critério já estabelecido: só conta com composição+consumo
  também implementados e validados).

Nenhum código alterado por esta entrada — só documentação, registrando um fato já confirmado pelo
usuário no ambiente real. Nenhuma migration, Edge Function, dado oficial ou Petlink tocados. Nenhum
push, nenhum deploy. Checkpoint local: commit `docs: record inventory purchase validation`, sem push.

---

# 9c. Módulo 3 — recálculo do progresso e contrato do motor de reserva/consumo/liberação por Pedido (congelado 2026-09-01)

Auditoria somente-leitura do estado real (Git, `supabase migration list --linked`,
`supabase functions list`, leitura integral das migrations e das telas/APIs/hooks de Estoque e
Pedido). Nenhum arquivo de código, migration, RPC, Edge Function, interface ou dado remoto foi
alterado nesta rodada. Este é o **congelamento arquitetural** do próximo incremento — a
implementação é de rodada futura, com autorização separada.

## 9c.1 Estado real auditado (correção das declarações obsoletas)

- **45/45 migrations sincronizadas** local/remoto (não "27"; não "só local").
- `stock_movements` + `register_stock_movement` **aplicados ao remoto** desde 2026-08-27 (os
  comentários de cabeçalho das migrations `20260827090000` e `20260829180000` dizendo "ainda NÃO
  foi aplicada" são **texto histórico** do momento da criação — não editados, por regra; a fonte
  autoritativa é `migration list --linked`).
- Filamentos (`filament_types`, `filament_spools`, `filament_movements`), pesagem
  (`register_filament_weighing`), `vw_filament_type_summary` e Compras (`inventory_purchases`,
  `register_inventory_purchase`) **aplicados ao remoto**.
- **7 Edge Functions de Estoque publicadas** (todas v1, ACTIVE): `accessories`, `packaging`,
  `stock-movements`, `filament-types`, `filament-spools`, `filament-movements`,
  `inventory-purchases`.
- **Validação manual do usuário aprovada** para: cadastro mestre (2026-08-24); saldo/entrada/
  saída/histórico de Acessórios e Embalagens — 15/15 passos (2026-08-27); MVP de Filamentos —
  tipos/rolos/movimentações/pesagem/arquivamento (2026-08-28); Compras das 3 categorias em
  produção real (2026-08-29).
- **`spool_tares` foi descartada por decisão de arquitetura** — a tara passou a ser
  `filament_spools.empty_spool_weight_grams` (nullable, por rolo); não é pendência.
- **`product_filaments` e `product_plate_filaments` são LEGADAS** desde `20260829180000` —
  nenhuma tela ou fluxo de Produto lê/escreve nelas; a escolha de filamento/cor migrou para o
  Pedido (`order_item_unit_plate_filaments`), por unidade e por plate. A composição do Produto
  (por plates, peso direto) **já está concluída** — não é "o próximo incremento".
- **Ausentes** (nenhuma linha de código): reserva, consumo automático, liberação/devolução por
  cancelamento, inventário periódico (`inventories`/`inventory_items`), tela de histórico de
  compras, custo médio por item, `suppliers` (fora de escopo declarado).

## 9c.2 Recálculo do Módulo 3 pela metodologia das 7 fases

| Fase | % | Evidência |
| --- | --- | --- |
| 1. Definição funcional | **100%** | doc 01 §15–21; 22 regras operacionais aprovadas 2026-08-27 + ajustes dos retestes |
| 2. Modelo e regras | **70%** | modelo manual aplicado ao remoto (ledgers, RPCs, view, compras, idempotência, atomicidade); **ausentes**: tipos `RESERVATION`/`RELEASE`/`CONSUMPTION`, integração com status do Pedido, snapshot de Acessórios/Embalagens por Pedido, `stock_reservations`/`inventories`/`inventory_items`, `suppliers`, custo médio |
| 3. Backend | **70%** | 7 Edge Functions de Estoque publicadas + RPCs; `inventory_purchases_test.sql` 26/26 PASS no remoto (BEGIN/ROLLBACK); `product_plates`/categorias/composição-inativa PASS no remoto; **ausentes**: motor de reserva/consumo, RPC de inventário periódico, valoração de consumo; testes Deno nunca executados (Deno não instalado) |
| 4. Frontend | **65%** | telas de saldo/movimentação/histórico/pesagem/arquivamento + diálogo de Compras (3 categorias); 338 testes de Estoque passando; **ausentes**: tela de histórico de compras, inventário periódico, exibição de reservado/consumido no Pedido/Produção, visão de custo |
| 5. Integração E2E | **50%** | 4 blocos de validação manual aprovados para o escopo implementado; **ausente**: o eixo Pedido ↔ Produção ↔ Estoque (reserva/consumo/liberação) não construído, logo não validável; inventário periódico idem |
| 6. Piloto real | **0%** | não iniciado |
| 7. Estabilização / release | **0%** | não iniciado; frontend não publicado |

**Macro = (100 + 70 + 70 + 65 + 50 + 0 + 0) / 7 = 355 / 7 ≈ 50,7% → ~51%.**

O Módulo 3 **continua NÃO concluído**. O próximo avanço macro depende do motor deste incremento
estar aplicado e validado.

## 9c.3 Regras aprovadas pelo usuário — decisões DEFINITIVAS do motor de reserva/consumo

1. Acessórios e embalagens são **reservados quando o Pedido entra em `IN_PRODUCTION_QUEUE`**.
2. Filamentos só são reservados **depois que todas as escolhas por unidade e plate estiverem
   completas** (nunca antes).
3. Saldo insuficiente **não impede** o Pedido de permanecer na Fila — **gera alerta**.
4. A produção **não pode começar** (`IN_PRODUCTION`) até **todos os insumos estarem
   integralmente reservados**.
5. Ao entrar em `IN_PRODUCTION`, a **reserva é convertida em consumo físico**.
6. Cancelamento **anterior ao consumo libera integralmente** as reservas.
7. **Depois do consumo não existe devolução automática** — qualquer retorno é uma movimentação
   **manual e auditável**.
8. Em plate multicor, devem ser informados os **gramas de cada filamento**; a **soma deve ser
   exatamente igual ao peso congelado daquele plate, por unidade** (`order_item_plates.weight_grams`).

Restrições preservadas (já vigentes, reafirmadas como definitivas):

- Pedido pode entrar na Fila **sem filamentos definidos**.
- Cores/filamentos são escolhidos **no Pedido**, por unidade e por plate.
- O Produto **não tem mais vínculo ativo** com filamento/cor.
- Ausência de filamentos bloqueia **somente o início da produção**, não a entrada na Fila.
- Reserva e consumo devem ser **atômicos e idempotentes** (uma chave por `(pedido, fase)`,
  toda a operação numa única transação; retry de `change_order_status` nunca duplica).
- **Nenhum saldo negativo** será permitido para iniciar produção.
- Entrar na Fila **não consome** estoque; o consumo ocorre **somente ao iniciar produção**.

## 9c.4 Auditoria: snapshot de Acessórios e Embalagens no Pedido — REQUISITO OBRIGATÓRIO

Leitura de `product_accessories`/`product_packaging` (`quantity integer > 0` por par
produto+item, `unique`), das RPCs de escrita de Pedido (`create_order` e a sobrecarga de 11
parâmetros, `update_order_full`, `create_order_with_payment`) e das Edge Functions `orders`/
`products` + `OrderForm`/`ProductForm`.

**Resultado: NÃO existe snapshot de Acessórios/Embalagens por Pedido.** As RPCs de criação/
edição do Pedido congelam **apenas** `order_item_plates` (peso/tempo por plate) e, opcionalmente,
`order_item_unit_plate_filaments` (cores). A necessidade de acessórios/embalagens de um Pedido só
pode ser lida hoje de `product_accessories`/`product_packaging` — o **estado atual do Produto**.

Requisito obrigatório do próximo incremento (não implementado nesta rodada):

- Criar estrutura **order-scoped** para acessórios e embalagens (ex.: `order_item_accessories`,
  `order_item_packaging`, com `quantity` já escalado por `order_items.quantity`).
- Copiar a composição do Produto **atomicamente, na criação do item** do Pedido (e no
  re-snapshot de `update_order_full`, mesmo momento em que `order_item_plates` é regravado).
- Alterações futuras no Produto **nunca** alteram a necessidade de Pedidos já criados.
- Mistura de itens e quantidades deve **escalar corretamente por unidade** (quantidade do BOM ×
  quantidade do item).
- Um Pedido **sem snapshot** (criado antes desta migration) **não pode** receber composição
  histórica "inventada" a partir do Produto atual — ver §9c.8.

## 9c.5 Auditoria: gramas por filamento por unidade/plate — REQUISITO OBRIGATÓRIO

Leitura de `order_item_unit_plate_filaments` (colunas: `order_item_id`, `order_item_plate_id`,
`unit_number`, `filament_type_id`, `position`; `unique (order_item_plate_id, unit_number,
filament_type_id)`), `order_item_plates` (`weight_grams` congelado), do gate de `IN_PRODUCTION`
em `change_order_status`, `update_order_item_production_colors` e das telas/hook de cores.

Confirmado:

- Uma unidade/plate **aceita mais de um filamento** (várias cores no mesmo plate são
  permitidas; `position` só ordena a exibição).
- Os filamentos são persistidos hoje **por `filament_type_id`** (tipo/cor), com `unit_number` e
  `order_item_plate_id`.
- **Não existe campo de gramas por filamento** — o comentário da tabela diz explicitamente "Não
  pede peso por cor (responsabilidade futura do módulo Produção)".
- **Vínculo com `filament_type` sim; com `filament_spools` (rolo) NÃO.**
- **Nenhuma escolha de rolo** — nem automática nem manual — existe em qualquer camada.
- A seleção **pode ser alterada** enquanto o Pedido está em QUOTE/WAITING_APPROVAL/APPROVED/
  `IN_PRODUCTION_QUEUE` (allow-list de `update_order_item_production_colors`); congela em
  `IN_PRODUCTION` e adiante.

Requisito aprovado (regra 8), a modelar no próximo incremento:

- Cada combinação **unidade + plate + filamento** recebe uma **quantidade em gramas**.
- A **soma por unidade + plate** deve ser **exatamente igual** ao peso congelado do plate
  (`order_item_plates.weight_grams`) — validação na RPC de escrita, com tolerância a definir
  pelo usuário (ver §9c.9).
- **Não** usar divisão igual implícita; **não** usar o peso integral para cada cor; **não**
  inferir quantidade em silêncio — o gramas de cada cor é sempre informado.

## 9c.6 Auditoria: alocação entre rolos — DECISÃO DO USUÁRIO PENDENTE

O Pedido escolhe **tipo/cor**; o estoque físico de filamento está em **rolos** (`filament_spools`,
saldo por rolo em `current_net_weight_grams`, status `LACRADO`/`ABERTO`/`ESGOTADO`/`DESCARTADO`,
`is_active`). `register_filament_movement` **exige `p_spool_id`** — não existe movimentação "por
tipo" sem um rolo concreto. `vw_filament_type_summary.total_available_grams` soma só rolos ativos
e utilizáveis.

Perguntas em aberto (o motor precisa de resposta antes de modelar):

- **Reserva por tipo ou por rolo?** Reservar por tipo adia a escolha do rolo para a Produção;
  reservar por rolo fixa a rastreabilidade cedo, mas trava rolos.
- **Múltiplos rolos do mesmo tipo** — como participam de uma reserva/consumo único?
- **Consumo maior que o saldo de um rolo** — dividir entre rolos? bloquear?
- **Rolo arquivado / esgotado / descartado** — excluído da alocação? e se uma reserva já
  apontava para ele?
- **Rastreabilidade** — registrar qual rolo foi de fato consumido (obrigatório para custo e
  auditoria).
- **Seleção manual de rolo na Produção** — haverá uma tela para o operador escolher o rolo, ou
  a alocação é 100% automática?
- **Política de alocação automática** — FIFO (rolo mais antigo primeiro), FEFO (validade — não
  há campo de validade hoje), "rolo já ABERTO primeiro, depois LACRADO", maior saldo primeiro?
- **Substituição de rolo entre a reserva e a produção** (rolo reservado ficou indisponível).

Opções, com recomendação técnica (a regra de negócio é do usuário):

| Opção | Descrição | Vantagens | Riscos |
| --- | --- | --- | --- |
| **R1 — reserva por tipo, alocação de rolo só no consumo** | A reserva de Fila registra "N gramas do tipo X reservados para o Pedido P"; ao iniciar a Produção, o motor escolhe rolo(s) por política e grava `CONSUMPTION` em `filament_movements(spool_id=…)` | seleção de rolo com informação mais fresca; não trava rolos cedo; casa com a regra 2 (reserva só depois das cores completas) | precisa de um acumulador "reservado por tipo" fora de `filament_movements` (que é por rolo); disponibilidade por tipo = Σ saldo dos rolos − Σ reservado por tipo |
| **R2 — reserva já aloca rolo(s)** | Na entrada da Fila o motor escolhe rolo(s) e grava `RESERVATION` por rolo | rastreabilidade desde a Fila; disponibilidade por rolo direta | trava rolos por muito tempo; rolo pode ficar indisponível antes da produção → precisa de re-alocação; conflita com "reserva só após cores completas" se as cores mudarem |
| **R3 — híbrido** | reserva por tipo (R1) + sugestão de rolo não vinculante, confirmada/ajustada na Produção | equilíbrio; operador tem controle final | mais telas e estado; maior complexidade de teste |

**Recomendação técnica preliminar: R1** — combina com as regras 2 e 5 (reserva só após cores
completas; conversão em consumo ao iniciar produção), evita travar rolos e concentra a
rastreabilidade no momento do consumo real. A **política de escolha de rolo** (FIFO / "ABERTO
antes de LACRADO" / maior saldo) e a **existência ou não de escolha manual na Produção**
permanecem **decisões do usuário**.

## 9c.7 Semântica da reserva — três arquiteturas comparadas

Auditoria dos ledgers: `stock_movements.quantity_delta` é **`integer` e `<> 0`**;
`balance_before`/`balance_after` **`>= 0`** e gravados em toda linha como o saldo físico corrente;
`accessories.current_stock`/`packaging.current_stock` é o **saldo materializado** de leitura.
`filament_movements.quantity_delta` é **`numeric(10,2)` e `<> 0`**, cada linha referencia **tipo E
rolo**, e `filament_spools.current_net_weight_grams` é o saldo materializado.

**Perigo central:** se uma linha `RESERVATION` com delta negativo entrar no ledger físico e
reduzir `balance_after`/`current_stock`, o `CONSUMPTION` posterior **reduziria de novo** → baixa
dupla. Além disso, `filament_movements` exige `spool_id NOT NULL` — uma reserva **por tipo** nem
se expressa nesse ledger sem antes alocar um rolo. E o `CHECK (quantity_delta <> 0)` impede uma
linha "neutra".

O modelo futuro deve distinguir explicitamente: **saldo físico**; **quantidade reservada**;
**disponível = físico − reservado**; **liberação de reserva**; **conversão de reserva em consumo
físico**.

| | **A — eventos de reserva dentro dos ledgers atuais** | **B — ledger/tabela de reservas separado** | **C — coluna materializada `reserved` + ledger de reservas auditável** |
| --- | --- | --- | --- |
| Fonte de verdade | um único ledger, com `movement_type` distinguindo físico vs reserva | reservas num ledger próprio; físico continua onde está | `reserved_qty`/`reserved_grams` materializado (leitura rápida) + ledger de reservas (auditoria/reconciliação) — mesmo padrão de `current_stock` + `stock_movements` |
| Atomicidade | boa (mesma tabela, mesma transação) | boa (transação abrange as duas tabelas) | boa (transação escreve coluna + ledger juntas) |
| Concorrência | `FOR UPDATE` no item já usado por `register_stock_movement` | precisa travar item **e** somar o ledger de reservas | `FOR UPDATE` no item; a coluna materializada evita recomputar a soma sob lock |
| Idempotência | `idempotency_key` já existe | `idempotency_key` no novo ledger | `idempotency_key` no ledger de reservas |
| Consulta de disponibilidade | `Σ` do ledger filtrando tipo de movimento — **cara** e sujeita a interpretação | `saldo_fisico − Σ(reservas abertas)` — join/agregação a cada leitura | `current_stock − reserved_qty` — **O(1)**, direto |
| Reconciliação | difícil (mistura semânticas no mesmo `balance_after`) | média (duas fontes a cruzar) | boa (recomputar `reserved` do ledger, comparar com a coluna — igual ao que já se faz com `current_stock`) |
| Impacto Acessórios/Embalagens | quebra o invariante "`balance_after` = físico corrente"; `CHECK` de `integer`/`<>0` atrapalha | limpo | limpo, e simétrico ao que já existe |
| Impacto Filamentos (tipo × rolo) | inviável para reserva **por tipo** (`spool_id NOT NULL`) | um ledger de reservas **por tipo** resolve; consumo continua por rolo no ledger físico | `reserved_grams` **por tipo** em `filament_types` (ou tabela `filament_type_reservations`) + ledger de reservas por tipo; consumo por rolo em `filament_movements` como hoje |

**Recomendação técnica: opção C.** É a única que preserva o invariante já testado dos ledgers
físicos (`stock_movements`/`filament_movements` continuam sendo **só** movimentação física, sem
`RESERVATION`/`RELEASE`), dá disponibilidade O(1) (`físico − reservado`), reconcilia pelo mesmo
método já validado de `current_stock`, e resolve a reserva de filamento **por tipo** (regra 2 +
opção R1) sem violar `spool_id NOT NULL`. Na prática:

- Acessórios/Embalagens: coluna `reserved_qty integer` (materializada) + tabela
  `stock_reservations` (ledger: `item_type`, `item_id`, `order_id`, `qty`, `status`
  aberta/liberada/consumida, `idempotency_key`).
- Filamentos: `reserved_grams numeric` **por `filament_type`** (materializada) + ledger de
  reservas por tipo, com o mesmo shape. O `CONSUMPTION` real continua em `filament_movements`
  por rolo (regra 5), e ao consumir, a reserva do tipo é baixada na mesma transação.
- Os tipos `RESERVATION`/`RELEASE`/`CONSUMPTION` **não** entram no `CHECK` de `movement_type`
  dos ledgers físicos; o consumo físico usa os tipos já existentes (ex.: um novo
  `ORDER_CONSUMPTION` a ser aprovado, ou reaproveitar `INTERNAL_USE` com `reference_type='ORDER'`
  — decisão do usuário).

Nenhuma migration foi criada. A escolha final entre A/B/C e a forma dos objetos é da rodada de
implementação, com autorização.

## 9c.8 Pedidos anteriores ao novo motor — tratamento do legado

Quando a migration do motor for aplicada, Pedidos já existentes (especialmente os em
`IN_PRODUCTION_QUEUE` ou `IN_PRODUCTION`) **não podem** ser tratados como se sempre tivessem tido
snapshot/gramas/reservas. Recomendações:

- **Não modificar automaticamente nenhum Pedido existente** — nenhuma reserva retroativa, nenhum
  consumo retroativo, nenhuma reconstrução de composição a partir do Produto atual.
- A migration deve **identificar** e listar (consulta somente-leitura, no relatório de
  aplicação) os Pedidos em `IN_PRODUCTION_QUEUE` e `IN_PRODUCTION` no momento da aplicação.
- Para esses Pedidos legados, o motor entra em modo **"sem reserva/consumo rastreado"**: as
  transições de status continuam funcionando, mas nenhuma baixa automática é feita; o operador é
  avisado de que aquele Pedido é anterior ao motor e qualquer baixa é manual.
- Um Pedido legado **sem snapshot de acessórios/embalagens** nunca recebe composição "inventada"
  — se o usuário quiser rastrear o consumo dele, faz uma movimentação manual auditável.
- Decisões do usuário necessárias: (a) reprocessar manualmente os Pedidos legados em produção,
  ou deixá-los fora do rastreamento até serem entregues? (b) exigir que a Fila esteja vazia de
  Pedidos legados antes de aplicar a migration, ou conviver com os dois modos?

## 9c.9 Fronteira Módulo 3 × Módulo 4 e quando o Módulo 4 pode começar

**Módulo 3 fornece:** custo da compra (`inventory_purchases.item_value`), frete
(`freight_value`), custo total (`total_value`, gerado), e — **depois deste incremento** —
quantidade reservada, quantidade consumida por Pedido, e a valoração desse consumo (política de
custo a definir: snapshot do custo unitário no momento do consumo, ou custo médio mantido a
partir de `inventory_purchases` — hoje **não existe** rollup de custo médio em nenhum item).

**Módulo 4 (Precificação e Rentabilidade)** consome do Módulo 3 e adiciona: material consumido
**valorizado**, acessórios/embalagens valorizados, energia, tempo/custo de máquina, depreciação,
perdas/reimpressões, custo total, preço, margem, rentabilidade.

**O Módulo 4 só pode começar com dados confiáveis depois de:**

1. o motor de **reserva/consumo/liberação** deste incremento estar **aplicado e validado**
   manualmente — para que "quantidade consumida" por Pedido seja um fato real, não estimativa;
2. o **consumo estar valorado** com uma política de custo **definida pelo usuário** (snapshot ou
   custo médio) — subincremento pequeno, imediatamente após o motor.

Antes disso, qualquer custo de material no Módulo 4 seria estimativa manual não rastreável ao
estoque real.

## 9c.10 Decisões do usuário ainda pendentes (bloqueiam a modelagem)

1. **Reserva de filamento: por tipo (R1) ou por rolo (R2/R3)?** (§9c.6)
2. **Política de alocação automática de rolo** (FIFO / "ABERTO antes de LACRADO" / maior saldo /
   outra) e **se haverá escolha manual de rolo na Produção**. (§9c.6)
3. **Arquitetura da reserva: A, B ou C?** (recomendação: C). (§9c.7)
4. **Tipo de movimento do consumo físico por Pedido** — novo `ORDER_CONSUMPTION` ou reuso de um
   tipo existente com `reference_type='ORDER'`. (§9c.7)
5. **Tolerância da soma de gramas por unidade/plate** vs. peso congelado do plate (exato? ±X g?
   ±X%?). (§9c.5)
6. **Legado**: reprocessar manualmente Pedidos em produção, ou deixá-los fora do rastreamento?
   Exigir Fila sem Pedidos legados antes de aplicar a migration? (§9c.8)
7. **Política de valoração do consumo** para o Módulo 4 (snapshot de custo unitário no consumo,
   ou custo médio a partir das compras). (§9c.9)
8. **Alerta de saldo insuficiente na Fila (regra 3)**: só visual, ou também um registro/pendência
   consultável?

Nenhuma dessas foi decidida nesta rodada — todas seguem marcadas como pendência do usuário.
Checkpoint local: commit `docs: define order stock integration contract`, sem push, sem deploy,
sem nenhuma alteração de código/banco/dados.

---

# 10. Histórico de atualizações deste roadmap

| Data | Alteração |
| --- | --- |
| 2026-08-16 | Criação do roadmap. Auditoria confirma Módulo 0 quase operacional (~86%) e Módulo 1 com backend 100% pronto porém não operacional (~43%, frontend 0%). Módulos 2–10 recalculados distinguindo "especificado" de "implementado" (Fase 2 parcial = 50% quando só há especificação em doc `03` sem migrations); nenhum está operacional. |
| 2026-08-16 | Módulo 1 Fase 4: telas de Clientes e Produtos (listar/criar/alterar preço) implementadas. Composição padrão de produtos adicionada: Migrations 18–19 antecipam `accessories`/`packaging` (cadastro mestre completo, sem automação de estoque) e criam `product_accessories`/`product_packaging` + `set_product_composition`; `PATCH /products/:id/composition` implementado em `products`, **deployado e validado por smoke test real** (frontend → Edge Function → RPC → banco → leitura de volta). Módulo 1 macro sobe para ~50% (Fase 4 parcial). Módulo 3 Fase 2 ganha nota: 2 de suas tabelas já existem, mas nenhuma regra de estoque foi implementada. |
| 2026-08-20 | Módulo 1 Fase 4: Ficha Técnica do Produto — Incrementos 1 e 2 concluídos. Incremento 1: rota `/produtos/:productId`, busca individual de produto, Identificação e Produção (peso/tempo do plate + estimativa por unidade). Incremento 2: seções Acessórios e Embalagens (nome, situação, quantidade, custo unitário, subtotal por linha, com itens inativos/indisponíveis preservados) e card Subtotal de componentes (estados completo/parcial/não calculável/vazio, custo ausente nunca tratado como zero) — ainda sem custo de material, energia, máquina, perdas, MDO, custo total ou margem. Adicionada seção 6 "Gatilhos de retorno pendentes" registrando a pausa condicionada do roadmap para retomar a Ficha Técnica com custo total/margem quando a estrutura oficial de custos (doc `03` §18.1 `pricing_calculations`) for implementada — nenhum percentual macro alterado por esta entrada. |
| 2026-08-22 | Módulo 1: ajuste visual no formulário de Produto (criação e edição, mesmo componente `ProductForm.tsx`) — campo "Peso total (g)" passa a aparecer antes de "Tempo de Produção" (antes: Tempo de impressão antes do Peso); títulos das colunas correspondentes na listagem (`ProductsPage.tsx`) atualizados para os mesmos textos. Nenhuma lógica de validação, conversão de duração ou contrato de API/banco alterada. Adicionado à seção 6 novo gatilho pendente: quando o Módulo 3 — Estoque tiver interface funcional e validada para cadastrar acessórios/embalagens mestres, avisar o usuário para fazer os cadastros oficiais e completar a composição dos produtos, incluindo o cadastro oficial da Petlink já existente no ambiente — não implementado agora, só registrado como pendência; nenhum percentual macro alterado por esta entrada. |
| 2026-08-22 | Módulo 1: padrão de busca rápida + autocomplete/typeahead + ordenação por coluna (menu estilo filtro de tabela), já validado em Clientes/Produtos/Empresas, estendido para a listagem de Pedidos (`OrdersPage.tsx`) — busca por número do pedido, cliente/empresa exibido ou nome de qualquer produto do pedido; ordenação em todas as 11 colunas de dados (Nº pedido com comparação numérica natural, Total/Saldo devedor numéricos, Prazo pela data real). Componente compartilhado `SearchAutocomplete.tsx` ganhou um campo opcional `description` (badge de tipo — "Pedido"/"Cliente"/"Produto") para diferenciar sugestões heterogêneas; Clientes/Produtos/Empresas não passam esse campo e continuam com a mesma renderização de antes (suítes de teste dos 3 módulos reexecutadas, sem regressão). Nenhuma alteração na criação/edição de pedidos, nos fluxos de status operacional/financeiro, nem no contrato de `vw_order_summary`. Adicionado à seção 6 novo gatilho pendente: quando os fluxos de status operacional e financeiro de Pedidos estiverem funcionais e validados de ponta a ponta, retornar à listagem para criar botões de filtro rápido por status (incluindo "Aguardando pagamento") — não implementado nesta rodada, só registrado como pendência; nenhum percentual macro alterado por esta entrada. |
| 2026-08-22 | Módulo 3 (Estoque): auditoria somente leitura seguida de planejamento aprovado para a interface de cadastro mestre de acessórios/embalagens (Incremento 1 do plano de 8 incrementos) — nenhum código, migration, Edge Function ou dado remoto alterado por esta entrada, só documentação. Registradas em `03_MODELO_BANCO_DADOS.md` §13.3: campos da interface (Nome/Tamanho/Variante/Estoque mínimo/Ativo/Custo somente leitura), remoção de Material e Fornecedor da UI (colunas preservadas/inexistentes no banco), Tamanho opcional com 5 opções oficiais (PP/P/M/G/GG) sem CHECK constraint e sem conversão automática de valores legados, `unit_cost` somente leitura com lembrete de retomada quando compras/entradas de estoque existirem, e regras de exclusão física guardada (exigirá migration própria, ainda não criada). Adicionadas a este roadmap: §8 (regra permanente de padrão visual/funcional para novos módulos) e §9 (plano de Módulo 3 — busca/filtros/ordenação independentes por aba, rotas planejadas `/estoque/*` não implementadas, contrato de escrita ainda em aberto entre acesso direto e Edge Function, recomendação por Edge Function). Nenhum percentual macro alterado por esta entrada — plano aprovado não é implementação. |
| 2026-08-22/23 | Módulo 3 (Estoque), Incrementos 2–4 implementados e testados localmente (nada deployado/liberado): Incremento 2 — backend protegido de Acessórios (`create_accessory`/`update_accessory`/`delete_accessory`, Edge Function `accessories`, migration local). Incremento 3 — mesmo backend espelhado para Embalagens (`create_packaging`/`update_packaging`/`delete_packaging`, Edge Function `packaging`). Incremento 4 — listagem de Estoque no frontend (`/estoque`, `/estoque/acessorios`, `/estoque/embalagens`, `InventoryPage.tsx`): busca, filtro por status e ordenação para as duas áreas, reaproveitando o padrão visual já aprovado nos demais módulos; somente leitura nesta etapa (sem criação/edição/exclusão/ativação — a coluna "Ativo" é um indicador informativo, não um controle, e não há coluna de ações). Validação manual da área `/estoque/acessorios` aprovada pelo usuário em 2026-08-23. Cadastro oficial (incl. composição da Petlink) segue bloqueado até as operações de escrita existirem na interface e o backend ser deployado com autorização explícita. Nenhum percentual macro alterado — trabalho local/testado ainda não atende ao critério "deployado e validado em produção" exigido pela regra de §1. |
| 2026-08-24 | Módulo 3 (Estoque): criação, edição, ativação/desativação (com confirmação explícita) e exclusão física segura completas na interface para Acessórios e Embalagens, com testes automatizados (frontend + Deno) e validação manual no navegador aprovada pelo usuário. Backend publicado no projeto Supabase remoto: migrations `20260822120000`/`20260823120000` aplicadas (28/28 sincronizadas, nenhuma pendente), Edge Functions `accessories`/`packaging` deployadas e ativas — corrige a informação desatualizada das entradas anteriores de que este backend seguia "não deployado". Uma falha de conexão nas escritas (Edge Functions/migrations ainda não publicadas no momento da primeira validação manual) foi diagnosticada e corrigida na mesma janela de trabalho. Preparado (não executado) o deploy do frontend: `frontend/vercel.json` (Root Directory `frontend`, build `npm run build`, output `dist`, rewrite de SPA para `index.html`) e `"engines"` em `frontend/package.json`; hospedagem pública ainda **sem URL verificada**. Cadastro oficial (incl. composição da Petlink) continua bloqueado — depende de autorização explícita separada (§6), não alterada por esta entrada. Nenhum percentual macro recalculado. |
| 2026-08-26 | Auditoria de código (somente leitura) seguida de correção funcional. **Auditoria**: confirmou que Empresas (`CompaniesPage.tsx`) e Pedidos (`OrdersPage.tsx`/`OrderForm.tsx`/`OrderEditForm.tsx`) já tinham frontend completo (CRUD/listar+criar+editar), contradizendo a Fase 4 do Módulo 1 que este documento ainda registrava como "sem tela" — corrigido nesta entrada (Fase 4 do Módulo 1 passa a 100%, macro do módulo para ~57,14%). Confirmado também, por leitura de todas as 27 migrations, que o cadastro mestre de Acessórios/Embalagens (Módulo 3) está implementado/testado/validado, mas **nenhuma tabela ou tela de inventário físico existe** (sem saldo, entrada, saída ou ajuste de estoque) — distinção agora explícita em §5. **Bug corrigido**: `CustomerForm` listava empresas inativas no campo "Empresa" ao cadastrar/editar cliente (só `OrderForm` já filtrava corretamente empresas inativas nesse mesmo cenário). Correção em `frontend/src/pages/customers/CustomersPage.tsx`: nova lista `availableCompanies` (empresas ativas + a empresa já vinculada ao cliente em edição, mesmo se desativada depois, para nunca apagar/substituir automaticamente um vínculo existente) repassada a `CustomerForm`; `listCompanies()` **não foi alterada** (a tela de Empresas continua exibindo ativas e inativas). 6 testes novos adicionados em `CustomersPage.test.tsx` cobrindo os cenários de empresa ativa/inativa/vinculada-desativada na criação e edição. Suíte completa (999 → 1005 testes) e lint/build/`tsc` verificados sem regressão. `frontend/package-lock.json` mantém alteração local pré-existente (só sincroniza o campo `engines` já presente em `package.json` desde `ecfc510` — nenhuma dependência/versão alterada), preservada sem commit nesta rodada. Hospedagem pública (Vercel) permanece configurada em código, mas **publicação segue deliberadamente adiada até a conclusão desta roadmap** — nenhum deploy executado. Nenhuma Fase 5/6/7 marcada como concluída — depende de validação manual do usuário (checklist entregue ao final desta rodada), ainda não realizada. |
| 2026-08-26 | **Validação manual do usuário aprovada**, exatamente no escopo do checklist entregue na entrada anterior: (1) empresa inativa não aparece ao criar novo cliente; (2) empresa inativa já vinculada permanece visível/selecionada ao editar o respectivo cliente; (3) o vínculo permanece após salvar sem alterar o campo; (4) criação de cliente sem empresa continua funcionando; (5) nenhuma regressão observada. Isso inicia a Fase 5 (Integração E2E) do Módulo 1, marcada como 🟡 ~10% — só este trecho estrito de Clientes/Empresas foi validado ponta a ponta pelo usuário; os demais critérios de aceite do módulo (fluxo completo de Pedidos: criar, itens, tipos, status, pagamento, prazo, entrega, persistência — `04_PLANO_IMPLEMENTACAO.md` §6) **continuam sem validação manual**. Macro do Módulo 1 recalculado para ~58,57%. Fases 6 (Piloto real) e 7 (Estabilização/release) **permanecem em 0%, não iniciadas, não marcadas como concluídas**. Checkpoint local criado nesta mesma entrada: commit `fix: filter inactive companies in customer form`, sem push, sem deploy, sem alteração de banco/migrations. |
| 2026-08-26 | Auditoria somente leitura das inconsistências levantadas no teste E2E anterior (cliente aparentemente sem vínculo; ausência de campo de custo em Acessórios/Embalagens), seguida de **segundo bloco de validação manual do usuário aprovado** no mesmo dia, com a ordem de operações corrigida: (1) empresa de teste criada e mantida **ativa** para estabelecer o vínculo; (2) cliente de teste vinculado corretamente à empresa enquanto ela ainda estava ativa; (3) empresa desativada **depois** de o vínculo já existir; (4) empresa inativa vinculada permaneceu visível ao editar o cliente; (5) vínculo permaneceu preservado após salvar; (6) acessório e embalagem **sem custo cadastrado** puderam ser usados normalmente na composição do produto; (7) composição foi salva e a Ficha Técnica apresentou corretamente custo "não informado" e subtotal "não calculável"/"parcial". A auditoria confirmou, por leitura de migrations/RPCs/Edge Functions/frontend (nenhum dado remoto consultado ou alterado), que a causa do "vínculo ausente" observado na rodada anterior era o próprio fix já corrigido funcionando como esperado (empresa inativa não fica mais selecionável ao **criar** um cliente novo — só permanece visível ao **editar** um vínculo pré-existente) combinado com uma ordem de operações do roteiro anterior que tentava vincular a uma empresa já inativa; e que a ausência de campo editável de custo unitário em `InventoryItemForm.tsx` é **comportamento intencional e documentado em todas as camadas** (`accessories`/`packaging.unit_cost` é somente leitura no cadastro mestre desde a Migration 18; `create_accessory`/`update_accessory`/`create_packaging`/`update_packaging` e as Edge Functions correspondentes rejeitam esse campo na escrita; `03_MODELO_BANCO_DADOS.md` §13.3 documenta isso explicitamente) — o custo virá futuramente do fluxo de compras/entradas de estoque (Módulo 3 físico, ainda não implementado), e a composição de produtos (`ProductCompositionForm.tsx`) nunca exige nem bloqueia por custo ausente, por design (`lib/products/productCosts.ts`). Fase 5 (Integração E2E) do Módulo 1 avança de ~10% para 🟡 **~20%** — Clientes/Empresas e a composição de Produtos (com item sem custo) validados ponta a ponta; **Pedidos continua sem nenhuma validação E2E manual**. Macro do Módulo 1 recalculado para 60%. Fases 6 (Piloto real) e 7 (Estabilização/release) **seguem em 0%, não iniciadas**. **Módulo 1 não está concluído.** Nenhum arquivo de código alterado por esta entrada — só documentação; checkpoint local: commit `docs: record module 1 e2e validation`, sem push, sem deploy, sem alteração de banco/migrations. |
| 2026-08-26 | Auditoria somente leitura dos contratos de status/pagamento/aprovação de Pedidos (`change_order_status`, `register_payment`, `register_approval`, migrations `20260814030351`/`20260814023834`, Edge Functions `order-status`/`payments`/`order-approvals`), seguida de **implementação completa do fluxo de gerenciamento de Pedidos no frontend** — ver detalhamento em Fase 4 acima. Achado central da auditoria, confirmado no código da RPC: `register_approval` **rejeita item CATALOG** ("Item CATALOG não exige aprovação") e o frontend só cria itens CATALOG (Personalizado/Spot seguem desabilitados como "Em breve") — logo nenhum botão de "registrar aprovação" manual foi construído (seria morto/sempre-rejeitado); a transição QUOTE→WAITING_APPROVAL já auto-promove a APPROVED via `try_auto_approve_order()` para qualquer pedido só-CATALOG, dentro da mesma chamada — decisão deliberada, documentada no código e aqui, não uma lacuna esquecida. Implementado: diálogo "Gerenciar pedido" (resumo financeiro, avanço de status respeitando a máquina de estados linear sem pular etapas, cancelamento só antes de IN_PRODUCTION, registro de pagamento com validação espelhando as CHECK constraints de `payments`, histórico combinado de status/financeiro/pagamentos); filtros rápidos por status na listagem (`Todos` + 8 status, com contagem, locais, combináveis com a busca); correção de vínculos desativados em `OrderForm.tsx` (cliente/empresa já vinculados continuam visíveis/selecionados na edição mesmo desativados depois, mesmo padrão de `CustomerForm`; produto de item existente inativo aparece rotulado "(inativo)" e continua bloqueando o salvamento, agora com mensagem específica orientando reativar/substituir). Nenhuma migration, Edge Function ou dado remoto alterado — reaproveita só contratos já deployados. Suíte completa 1005 → 1092 testes (87 novos, incluindo 6 arquivos de teste novos), lint 0 erros, build sem erros. Um erro de lint real foi corrigido durante a rodada (`useOrderManagement.ts` chamava `setState` síncrono dentro de `useEffect` — refeito para o padrão de `isLoading` derivado já usado em `useOrders`/`useCustomers`/`useCompanies`, sem nenhuma chamada de `setState` síncrona no corpo do efeito). **Nenhuma validação manual do usuário ainda ocorreu para nada desta rodada** — Fase 5 permanece em ~20%, não alterada; Fases 6/7 seguem em 0%; **Módulo 1 não está concluído**. Checkpoint local: commit `feat: complete order management workflow`, sem push, sem deploy, sem alteração de banco/migrations/dados oficiais. |
| 2026-08-26 | **Validação manual do usuário aprovada** para o fluxo completo de Pedidos (criação, avanço de status, registro de pagamento, avanço até entrega, cancelamento e filtros rápidos por status), na interface como implementada na entrada anterior. Fase 5 (Integração E2E) do Módulo 1 avança de ~20% para 🟡 **~55%** — junto com os blocos de Clientes/Empresas e Produtos já validados, isso completa os critérios de aceite de Pedidos alcançáveis no escopo atual (`04_PLANO_IMPLEMENTACAO.md` §6; "classificar tipos" permanece N/A, Personalizado/Spot ainda não implementados). Macro do Módulo 1 recalculado para ~65%. Em seguida, **na mesma rodada**, refinamento visual aplicado às telas recém-validadas — `RegisterPaymentForm.tsx`: "Tipo de pagamento"/"Método de pagamento" viram action buttons com ícone (reaproveitando `PAYMENT_METHOD_ITEMS`/`IconComponent`, agora exportados de `OrderForm.tsx`, em vez de duplicar); campo "Valor" passa a usar o mesmo campo monetário bancário de `ProductPriceForm.tsx` (`lib/forms/currencyField.ts`, **não alterado** — zero risco de regressão ali), com um `Switch` adicional só para o sinal negativo de Ajuste; resumo financeiro destacado no topo. `OrderForm.tsx` reorganizado em 3 seções visuais ("Dados do cliente"/"Dados do pedido"/"Entrega", numeração reiniciando por seção) — Empresa passa a ficar logo após Tipo de venda, Método de pagamento passa a ficar em "Dados do pedido" (antes ficava em Entrega); nenhum payload, validação, vínculo inativo corrigido ou modo somente-leitura foi alterado, só a organização visual. `OrdersPage.tsx`: a coluna Status agora é um controle clicável (`OrderStatusControl.tsx`) que abre confirmação com a única transição válida — DELIVERED/CANCELLED continuam só texto — reaproveitando só `orderStatusMachine.ts` e `changeOrderStatus()` (a mesma função de `useOrderManagement`), nunca uma segunda máquina de estados; botão "Gerenciar pedido" preservado ao lado. Durante a rodada, um bug real de UI foi encontrado e corrigido: recalcular as opções do seletor de Produto a partir do valor corrente da linha (em vez de um valor estável capturado na montagem) revertia a seleção para vazio no exato momento da troca — mesmo problema já documentado em `ProductCompositionForm.tsx`, corrigido com a mesma técnica (`stableInactiveProductIdByKey`). Suíte completa 1092 → 1130 testes, lint 0 erros (2 erros reais de `no-useless-assignment` corrigidos durante a rodada), build sem erros. **Esta superfície visual nova ainda não foi validada manualmente pelo usuário** — Fase 5 permanece em ~55%, não avança mais até essa validação específica ocorrer; Fases 6/7 seguem em 0%; **Módulo 1 não está concluído**. Checkpoint local: commit `feat: refine order forms and status controls`, sem push, sem deploy, sem alteração de banco/migrations/dados oficiais. |
| 2026-08-26 | **Validação manual do usuário aprovada** para as seis alterações visuais do commit `8ea17c0` (action buttons de Tipo/Método de pagamento, campo de valor no padrão bancário, resumo financeiro destacado, `OrderForm.tsx` em 3 seções, status do pedido alterável direto da listagem). Isso resolve a ressalva registrada na entrada anterior — Fase 5 (Integração E2E) do Módulo 1 avança de ~55% para 🟡 **~60%**; macro do Módulo 1 recalculado para ~66%. Em seguida, **na mesma rodada**, nova extensão implementada: status financeiro também alterável direto da listagem (`OrderPaymentStatusControl.tsx`) — auditoria confirmou que `orders.payment_status` é **sempre derivado** por `recalculate_order_financials()` (nunca editável diretamente), então o novo controle nunca escreve esse campo: clicar no badge "Status financeiro" abre o mesmíssimo `RegisterPaymentForm.tsx` já usado em "Gerenciar pedido" (mesma função `registerPayment()`, nenhuma lógica de pagamento duplicada), com Total do pedido/Já pago/Saldo devedor vindos direto de `OrderSummary` (`vw_order_summary`, já carregado por `useOrders`, sem nova busca). Auditoria do código de `register_payment()` confirmou que a função **não verifica `orders.order_status`** em nenhum momento (só existência do pedido e soma dos pagamentos nunca negativa) — por isso o novo controle nunca vira badge sem ação: permanece clicável em pedidos `CANCELLED`, `PAID` ou com pagamento excedente, comportamento auditado no contrato, não inventado. Botão "Gerenciar pedido" e o controle de Status do pedido (`OrderStatusControl.tsx`) preservados e testados sem regressão; filtros/busca continuam funcionando. Novo arquivo: `OrderPaymentStatusControl.tsx`/`.test.tsx`. Suíte completa 1130 → 1155 testes, lint 0 erros, build sem erros. Nenhuma migration, Edge Function ou dado oficial/Petlink alterado. **Esta extensão ainda não foi validada manualmente pelo usuário** — Fase 5 permanece em ~60%, não avança mais até essa validação específica ocorrer; Fases 6/7 seguem em 0%; **Módulo 1 não está concluído**. Checkpoint local: commit `feat: add financial status payment control`, sem push, sem deploy, sem alteração de banco/migrations/dados oficiais. |
| 2026-08-26 | Regra de negócio **revertida deliberadamente** pelo usuário: **bloqueio de pagamento excedente** — o total acumulado dos pagamentos de um pedido nunca pode ultrapassar `total_receivable` (`total_value + shipping_cost`); a decisão anterior desta mesma função ("nenhum estorno automático: pagamento excedente é permitido") deixa de valer para pagamentos novos. Detalhamento completo em Fase 4 acima. Resumo: nova migration local `supabase/migrations/20260826120000_add_payment_overpayment_guard.sql` substitui `register_payment()` (mesma assinatura de tipos da Migration `20260814030351`, mesmo OID/grants) adicionando o teto, reaproveitando o `for update` já existente sobre `orders` para serializar pagamentos concorrentes do mesmo pedido contra os dois limites (soma nunca negativa e agora também nunca acima do total); `RegisterPaymentForm.tsx` ganha validação-espelho no cliente (mensagem com o valor máximo permitido) e um estado "pedido totalmente pago" que desabilita o registro de novo pagamento (herdado automaticamente por `OrderPaymentStatusControl.tsx` e `OrderManagementPanel.tsx`, que reaproveitam o mesmo formulário); a listagem continua mostrando o badge "Pago" normalmente. Testes novos cobrindo os 8 cenários pedidos pelo usuário (inferior/igual/um-centavo-acima do saldo, pedido já pago, zero/negativo, duas parcelas somando exatamente o total, tentativa que ultrapassa, mensagem de erro no frontend) — suíte completa 1155 → 1167 testes, lint 0 erros, build sem erros, `git diff --check` limpo. **Etapa pendente explícita**: a migration **não foi aplicada ao Supabase remoto** nesta rodada (restrição do usuário) — a proteção de backend só existe localmente até uma aplicação futura com autorização explícita separada; o ambiente desta sessão também não tem Deno/Supabase CLI/Docker, então a migration só foi revisada por leitura, nunca executada contra um Postgres real. Nenhum dado oficial/Petlink alterado. **Nenhuma validação manual do usuário ainda ocorreu para esta regra** — Fase 5 permanece em ~60%, não alterada; Fases 6/7 seguem em 0%; **Módulo 1 não está concluído**. Checkpoint local: commit `feat: block excess payments beyond order total`, sem push, sem deploy, sem alteração de banco/migrations remotas/dados oficiais. |
| 2026-08-26 | Migration `20260826120000_add_payment_overpayment_guard.sql` **aplicada ao projeto Supabase remoto `tjhacqreupfqefntjevf`** via `npx supabase db push --linked` (só essa migration, sem seed, sem alteração de role) — 29/29 migrations agora sincronizadas local/remoto (antes 28/29). Verificação somente leitura via `db query --linked` confirmou a definição ao vivo de `register_payment()` idêntica ao arquivo local, função única (sem sobrecarga duplicada). Nenhuma Edge Function alterada, nenhum dado criado/editado/excluído. Em seguida, **validação manual do usuário aprovada com registros de teste**, cobrindo os 10 cenários do bloqueio de pagamento excedente pela listagem: abertura do pagamento pelo status financeiro, exibição de Total/Já pago/Saldo, pagamento parcial, atualização de status/saldo, bloqueio de um centavo acima do saldo com mensagem do valor máximo permitido, confirmação de que nenhum excedente foi registrado, pagamento exatamente igual ao saldo, atualização para "Pago", e bloqueio de novo pagamento com saldo zerado — exercitando a proteção real de produção, não só o espelho client-side. Fase 5 (Integração E2E) do Módulo 1 avança de ~60% para ✅ **100%** — completa o fluxo E2E do MVP atualmente implementado (Clientes/Empresas/Produtos/Pedidos, incl. bloqueio de excedente). Macro do Módulo 1 recalculado para ~71% (5 fases 100% + 2 fases 0%). Fases 6 (Piloto real) e 7 (Estabilização/release) **seguem em 0%, não iniciadas** — próxima etapa recomendada é a Fase 6. **Módulo 1 permanece não concluído.** Checkpoint documental: commit `docs: record order workflow validation`, seguido de push para `origin/feature/customers-orders` (autorizado nesta rodada) — sem deploy de frontend, sem nova alteração ao Supabase, sem dado oficial/Petlink alterado. |
| 2026-08-27 | **Decisão do usuário**: antes de finalizar as Fases 6 (Piloto real) e 7 (Estabilização/release) do Módulo 1, será concluída a continuação do Módulo 3 (Estoque e Inventário) — quantidade disponível, entradas/saídas, histórico de movimentações, filamentos por rolo/peso, consumo automático de estoque pelo pedido, integração completa entre Pedidos/Produtos/Estoque, nessa ordem. Nova branch `feature/inventory-operations` criada a partir de `45dac99` (tip de `feature/customers-orders`, que continua a base — `main` só tem o commit inicial de documentação). Auditoria completa (documentação, banco, backend, frontend) executada antes da implementação, seguida da aprovação de 22 regras operacionais de estoque pelo usuário — ver detalhamento completo em §9b. **Incremento 1 do novo plano (saldo e motor de movimentações para Acessórios/Embalagens) implementado localmente nesta rodada**: `public.stock_movements` (ledger imutável, extensível a filamentos) + `public.register_stock_movement()` (única função que atualiza saldo, com lock/atomicidade, bloqueio de saldo negativo, motivo obrigatório por tipo, idempotência) — migrations `20260827090000_create_stock_movements_table.sql` e `20260827093000_update_accessory_packaging_delete_guards.sql` (esta última atualiza `delete_accessory`/`delete_packaging` para também bloquear exclusão quando há histórico de movimentação). Teste de integração SQL completo criado (`supabase/tests/inventory_movements_test.sql`), mas **não executado nesta sessão** — sem Docker/Postgres local e migration não aplicada ao remoto (restrição desta rodada); revisado por leitura linha a linha. Suíte completa do frontend reexecutada (1167/1167, sem regressão — nenhum arquivo TypeScript alterado), lint 0 erros, build sem erros, `git diff --check` limpo. Documentação atualizada em `01_ESPECIFICACAO_FUNCIONAL.md` §16/§18/§19/§20, `02_ESPECIFICACAO_TECNICA.md` §6.9 e `03_MODELO_BANCO_DADOS.md` §13/§15.1 — incluindo correção de informações desatualizadas: cadastro mestre de Acessórios/Embalagens confirmado implementado/publicado/validado (não mais "ainda não iniciado"), gatilho de §6 corrigido (a interface de Estoque existe desde 2026-08-24; só a autorização para cadastro oficial, incl. Petlink, continua pendente), e o schema real de `stock_movements` documentado em substituição ao esboço especulativo anterior. **Percentual do Módulo 3 não foi elevado por esta entrada** — a fórmula oficial (§1) só concede peso quando há artefato deployado e validado, e a migration deste Incremento 1 continua só local, mesmo critério já aplicado a este módulo em rodadas anteriores. **Nenhuma migration aplicada ao Supabase remoto, nenhuma Edge Function publicada, nenhum deploy, nenhum dado oficial/Petlink alterado.** Checkpoint local: commit `feat: add inventory movement ledger`, sem push. |
| 2026-08-27 | Revisão corretiva do Incremento 1 (Módulo 3), antes de qualquer aplicação remota. Corrigida uma lacuna real de idempotência concorrente em `register_stock_movement()`: duas chamadas com a mesma `idempotency_key` para itens DIFERENTES, sob concorrência real (duas conexões), podiam terminar com `unique_violation` bruto em vez da mensagem estável `IDEMPOTENCY_KEY_CONFLICT:` — corrigido com bloco `BEGIN/EXCEPTION WHEN unique_violation` em volta do `INSERT` (usando `CONSTRAINT_NAME` via `GET STACKED DIAGNOSTICS` para confirmar a causa exata), mais normalização de `reference_type` consistente com `reason`. Novo teste 5.3 adicionado ao script SQL cobrindo o caminho antecipado dessa checagem (a corrida real entre duas conexões continua não-testável num script de transação única, documentado como tal). As 22 regras aprovadas foram explicitamente rotuladas nos 4 documentos (`01`/`02`/`03`/`05`) como **"Regras operacionais do MVP — versão inicial para validação, sujeitas a revisão após o teste prático do usuário"** — aprovação autoriza construir o MVP, não torna as regras definitivas; tipos de movimentação poderão ser ampliados/revisados; reserva/consumo/cancelamento/pesagem/escolha de rolo/perdas continuam só intenção aprovada (zero código); ledger permanece imutável mesmo sob revisão futura; correções futuras via novas movimentações/migrations, nunca reescrevendo histórico; Módulo 3 só avança para validação operacional com interface utilizável. Auditoria confirmou ausência de menções incorretas de contagem de migrations em qualquer arquivo. Suíte completa do frontend reexecutada (1167/1167), lint 0 erros, build sem erros, `git diff --check` limpo. Checkpoint local: commit `fix: harden inventory ledger validation`, sem push, sem migration aplicada. |
| 2026-08-27 | **Migrations `20260827090000_create_stock_movements_table.sql` e `20260827093000_update_accessory_packaging_delete_guards.sql` aplicadas ao projeto Supabase remoto `tjhacqreupfqefntjevf`** via `npx supabase db push --linked` — 31/31 migrations agora sincronizadas local/remoto (antes 29/31), sem seed, sem alteração de role. Verificação somente leitura confirmou que tabela `stock_movements` (colunas/constraints/índices, incl. índice único parcial de `idempotency_key`), RLS/policies/grants e as três funções (`register_stock_movement`, `delete_accessory`, `delete_packaging`, cada uma com assinatura única) correspondem exatamente aos arquivos locais — nenhuma sobrecarga duplicada. Teste de integração SQL (`inventory_movements_test.sql`) executado contra o remoto dentro de `BEGIN...ROLLBACK` — **40 PASS, 1 SKIP, 0 FAIL**; consultas pós-teste confirmaram **zero resíduo** (nenhum dado de teste persistido, `stock_movements` com 0 linhas, nenhum `current_stock` oficial alterado, nenhum usuário oficial alterado). Smoke test do CRUD existente: `InventoryPage`/`InventoryItemForm` (108/108, sem regressão); testes Deno de `accessories`/`packaging` não executados (ambiente sem Deno). **Backend do Incremento 1 (Módulo 3) está agora aplicado e validado por teste automatizado real contra produção** — regras operacionais continuam provisórias do MVP (ver entrada anterior); interface (Incremento 2) e validação manual do usuário continuam pendentes; Incrementos 2–9 continuam pendentes. Módulo 1 continua aguardando a conclusão da integração de Estoque antes das Fases 6/7 (decisão registrada em 2026-08-27, entrada anterior). Percentual macro do Módulo 3 **não alterado** — critério já estabelecido exige backend + interface + validação manual juntos, não só backend. Nenhuma Edge Function publicada, nenhum deploy de frontend, nenhum dado oficial/Petlink alterado, nenhum push Git. Checkpoint local: commit `docs: record inventory ledger deployment`, sem push. |
| 2026-08-27 | **Incrementos 2/3 do Módulo 3 (interface de saldo/entrada/saída/ajuste + histórico) implementados localmente**: nova Edge Function `stock-movements` (não publicada) chamando exclusivamente `register_stock_movement()`; 8 novos padrões estáveis mapeados em `_shared/errors.ts`; `StockMovementForm.tsx`/`StockMovementHistory.tsx`/`StockMovementPanel.tsx` (ação "Movimentar estoque" por linha na listagem, action buttons com ícones, projeção de saldo, motivo obrigatório por tipo, idempotência via `crypto.randomUUID()` estável entre retries do mesmo payload); coluna "Saldo atual" ordenável e badge de situação (Sem estoque/Estoque baixo/Estoque normal) na listagem. As 22 regras aprovadas permanecem **provisórias do MVP** — esta interface existe para o usuário validar as regras na prática, não para confirmá-las. Decisões de UX sem regra documentada explícita, sinalizadas: Devolução permanece disponível em item inativo (backend não verifica `is_active`); painel fecha automaticamente após sucesso. Suíte completa 1167 → 1261 testes, lint 0 erros, build sem erros, `git diff --check` limpo. Testes Deno (`stock-movements/handler.test.ts`, `_shared/errors.test.ts`) escritos, **não executados** — ambiente sem Deno, mesma limitação já registrada. **Edge Function ainda não publicada, nenhuma validação manual do usuário ainda ocorreu, nenhum dado oficial/Petlink cadastrado ou alterado.** Fase 5/6/7 do Módulo 1 e percentual macro do Módulo 3 **não alterados** por esta entrada — Incrementos 4–9 do plano de estoque continuam pendentes (filamentos, rolos, pesagens, reservas, consumo automático, produção, integração com `change_order_status`). Checkpoint local: commit `feat: add inventory movement interface`, sem push, sem migration criada/aplicada, sem Edge Function publicada, sem deploy. |
| 2026-08-27 | **Edge Function `stock-movements` publicada e ativa** no projeto Supabase remoto `tjhacqreupfqefntjevf` (versão 1) — revisão estática integral antes da publicação não encontrou inconsistências (payload/RPC/CORS/auth/logs todos conferidos); Deno seguiu indisponível nesta sessão, testes Deno já escritos não executados, cobertura complementar via suíte de frontend/API (1261/1261). Publicação exclusiva de `stock-movements` via `npx supabase functions deploy` — as 8 Edge Functions já existentes permaneceram com a mesma versão/`updated_at`. Verificação HTTP real: `OPTIONS` sem autenticação → 204; `POST` sem autenticação (payload fictício, item não real) → 401 (`Header Authorization ausente.`), rejeitado antes de tocar o banco. Consultas pós-teste confirmaram zero movimentação criada e nenhum `current_stock` alterado. Frontend local confirmado apontando para o projeto correto, `.env.local` continua ignorado, nenhuma URL hardcoded — **nenhum deploy de frontend realizado**. **A interface completa (backend + Edge Function publicada) está tecnicamente pronta, mas nenhuma validação manual do usuário ainda ocorreu** — as 22 regras aprovadas continuam provisórias do MVP; Fase 5/6/7 do Módulo 1 e percentual macro do Módulo 3 **não alterados** por esta entrada. Incrementos 4–9 do plano de estoque continuam pendentes. Nenhum dado oficial/Petlink alterado, nenhuma outra migration/Edge Function tocada. Checkpoint local: commit `docs: record stock movement function deployment`, sem push. |
| 2026-08-27 | **Validação manual do usuário aprovada** para quantidade disponível, entradas, saídas e histórico de Acessórios e Embalagens — 15/15 passos de um checklist manual aprovados; nenhuma alteração em Petlink/dados oficiais durante a validação. Reconciliação confirmou HEAD `41ab62c`, árvore limpa, 31/31 migrations sincronizadas, 5 commits à frente de `feature/customers-orders`. Em seguida, **Incremento 4 do plano de estoque (filamentos — tipos, rolos, movimentações, pesagem) implementado localmente**, cobrindo os requisitos 1-8 do pedido e parcialmente o 9 (preparação de composição, sem ativar consumo automático) — ver detalhamento técnico completo em §9b. Resumo: 4 migrations novas (`filament_types`/`filament_spools`/`filament_movements`+`vw_filament_type_summary`/`product_filaments`), decisão de arquitetura de **não** reaproveitar `stock_movements` (ledger próprio, `filament_movements`, por unidade de medida fracionária e necessidade de referenciar tipo+rolo); 3 Edge Functions novas (`filament-types`/`filament-spools`/`filament-movements`, nenhuma publicada); ~30 novos padrões de erro estáveis; página `/estoque/filamentos` (terceira aba, listagem de tipos com drill-down para rolos, ações de cadastro/ativação/descarte/exclusão/movimentar/pesar/histórico). Materiais fechados em PLA/PETG/TPU (**ABS explicitamente fora do MVP**); status do rolo `LACRADO`/`ABERTO`/`ESGOTADO`/`DESCARTADO` (4 valores, corrigindo a proposta de 5 valores nunca implementada de `03_MODELO_BANCO_DADOS.md` §12); tara como campo do próprio rolo, não mais um cadastro `spool_tares` separado. Teste de integração SQL (`filament_inventory_test.sql`) e 3 `handler.test.ts` Deno escritos, **não executados** (mesma limitação de ambiente de todas as rodadas anteriores — sem Docker/Postgres local/Deon, migrations não aplicadas ao remoto). Suíte completa do frontend 1261 → **1332 testes, todos passando**; lint 0 erros; `tsc -b`/`vite build` sem erros; `git diff --check` limpo; scan de segredos sem ocorrências reais; `.env.local` confirmado ignorado. **Nada desta entrada foi aplicado ao Supabase remoto, publicado ou validado manualmente pelo usuário** — regras operacionais do MVP, versão inicial para validação, sujeitas a revisão. **Módulo 3 continua NÃO concluído** (falta validação manual de Filamentos e todos os incrementos 5+ — consumo automático, reserva, inventário periódico); **integração Pedidos↔Estoque continua NÃO concluída**. Percentual macro **não alterado**. Checkpoint local: commit `feat: add filament roll inventory`, sem push. |
| 2026-08-27 | Auditoria pré-deploy do backend de filamentos encontrou um problema técnico real antes de qualquer aplicação: 2 das 4 migrations (`...106000`/`...109000`) tinham identificadores `YYYYMMDDHHMMSS` inválidos (minuto 60 e minuto 90, fora de 0–59) — confirmado por decodificação manual e pela própria saída de `migration list` (campo `time` não formatado para essas duas). Renomeadas para `...110000`/`...113000` (válidas, ordem cronológica preservada) antes de qualquer aplicação remota — nenhuma migration já aplicada foi editada; todas as referências cruzadas atualizadas em 9 arquivos. Resto da auditoria (RLS, políticas, `SECURITY DEFINER`/`search_path`, `EXECUTE` restrito, ausência de permissão a `anon`, idempotência, saldo negativo, imutabilidade, vínculos, `vw_filament_type_summary`, `ESGOTADO`/`DESCARTADO`, `product_filaments` sem consumo automático) aprovada sem ressalva. **As 4 migrations aplicadas ao projeto Supabase remoto `tjhacqreupfqefntjevf`** via `npx supabase db push --linked` (dry run prévio mostrou exclusivamente as 4 migrations) — 35/35 migrations agora sincronizadas local/remoto. Teste de integração SQL executado contra o remoto dentro de `BEGIN...ROLLBACK`: primeira execução **50 PASS/1 FAIL** — o FAIL era um bug da própria asserção do teste (Seção 6.1 reutilizava rolos já mutados por seções anteriores, um `ESGOTADO` que nunca reverte automaticamente ao saldo voltar a ficar positivo, comportamento correto e documentado — não um bug de banco); corrigida a asserção com fixtures dedicados, reexecutado: **51 PASS, 0 FAIL, 0 SKIP**. Consultas pós-teste confirmaram **zero resíduo** (todas as 4 tabelas novas com 0 linhas, nenhum produto `TESTE%`). **As 3 novas Edge Functions publicadas individualmente** (`filament-types`/`filament-spools`/`filament-movements`, todas ACTIVE versão 1) — as 9 já existentes preservaram `updated_at`, nenhuma redeployada. Smoke tests HTTP reais: `OPTIONS` → 204 com CORS correto; `POST` sem autenticação → 401 (`Header Authorization ausente.`) nas 3 rotas, confirmado sem tocar o banco. Teste CRUD autenticado não realizado (sem sessão de teste disponível neste ambiente) — fica para a validação manual pela interface. Frontend: nenhuma alteração funcional (só comentários citando os nomes de migration renomeados); suíte completa reexecutada por precaução, **1332/1332 passando**, lint 0 erros, `tsc -b`/`vite build` sem erros, `git diff --check` limpo. **Nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink/dado oficial, nenhum `migration repair`, nenhum `db reset`, nenhuma migration já aplicada foi editada.** Interface de Filamentos continua aguardando validação manual do usuário; regras do MVP continuam sujeitas a revisão; consumo automático por pedido continua pendente. Percentual macro **não alterado** — falta a validação manual para reunir os três critérios. Checkpoint local: commit `fix: harden filament inventory operations`, sem push. |
| 2026-08-28 | Primeira validação manual real do usuário na interface de Filamentos: passos 1-10/12-17/19-22 aprovados; passos 11 (saldo consolidado de 1.300g) e 18 (950g) pendentes — saldo não encontrado nem na listagem nem em "Rolos do tipo". Mais 4 observações: janelas "Ver rolos" e "Movimentar/Pesar/Histórico" desproporcionais, exclusão de rolo sem confirmação clara, coluna Data invadindo Tipo no histórico. Diagnóstico por consultas somente leitura contra o remoto (registros `TESTE%` já criados pelo usuário) reconstruiu as 6 movimentações/pesagens reais e confirmou **cada saldo exatamente igual ao documentado pelo usuário em todos os passos** — `vw_filament_type_summary` também conferida correta no estado atual. **Zero erro de banco, view ou RPC** — causa 100% frontend: `FilamentTypeDrawer` nunca exibia nenhum resumo consolidado, e a listagem de tipos buscava o resumo só uma vez no carregamento da página, nunca sendo informada de movimentações feitas no painel aninhado. Corrigido sem tocar nenhum objeto remoto: `openType` passou a ser derivado ao vivo do array `types` (não mais um snapshot); novo callback `onSummaryChanged` aciona `refetch()` do resumo após qualquer ação que afete o saldo (criar/editar/ativar-desativar/descartar/excluir/arquivar rolo, movimentar, pesar); novo bloco de resumo no topo do drawer (Disponível/Rolos/Abertos/Esgotados/Estoque mínimo/Situação — sempre do resumo do backend, nunca recalculado em JS). Fluxo de exclusão revisado: botão único "Excluir rolo" com confirmação explícita, tenta exclusão física e — se bloqueada por histórico (business_rule/409) — pivota o mesmo diálogo para oferecer "Arquivar rolo" (reaproveita `update_filament_spool({is_active:false})`, já existente); novo filtro "Mostrar arquivados"; "Movimentar/Pesar" desabilitado em rolo arquivado (restrição só de frontend nesta rodada — a RPC ainda não verifica `is_active`, lacuna registrada para incremento futuro). Sobreposição Data/Tipo corrigida (causa raiz: `TableCell` herda `whitespace-nowrap` por padrão sem proteção contra transbordo — aplicada a mesma classe `truncate` já usada em Tipo/Motivo). Dimensionamento das duas janelas ajustado para `max-h-[90vh] overflow-y-auto` + `sm:max-w-4xl`/`sm:max-w-3xl`, reaproveitando exatamente o padrão já usado em `OrdersPage.tsx`. 18 testes novos em `FilamentsInventoryPage.test.tsx` + novo arquivo `FilamentMovementHistory.test.tsx` (5 testes). Suíte completa 1332 → **1350 testes, todos passando**; lint 0 erros; `tsc -b`/`vite build` sem erros; `git diff --check` limpo; scan de segredos sem ocorrências reais; nenhum resíduo `TESTE` novo criado (auditoria só leu dados já existentes). **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum objeto remoto alterado, nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink/dado oficial.** Validação final (passos 11/18 + as 4 observações) continua pendente. Percentual macro **não alterado**. Checkpoint local: commit `fix: refine filament inventory validation flow`, sem push. |
| 2026-08-28 | Segundo reteste manual do usuário: passos 1-6/9-10 aprovados; passo 7 (rolagem horizontal em "Rolos do tipo" mesmo em resolução normal de notebook) e passo 8 (texto de Tipo truncado no histórico) ainda pendentes; passos 11/12 (arquivamento de rolo com histórico e filtro "Mostrar arquivados") não aprovados. Diagnóstico (leitura integral do código + auditoria somente leitura dos rolos `TESTE%` do reteste anterior, `updated_at` confirmado inalterado antes/depois) encontrou a causa real: o fluxo da rodada anterior tentava a exclusão física primeiro e só decidia arquivar depois de capturar um erro de negócio — nenhum dos rolos com histórico testados terminou de fato arquivado. View/RPCs reconfirmadas corretas (`is_active=false` já excluído de `vw_filament_type_summary`) — nenhuma migration necessária. **Corrigido**: novo campo derivado `FilamentSpool.has_movement_history` (calculado por uma segunda consulta de leitura em `filament_movements`, já concedida a `authenticated` — nenhuma migration nova) decide Excluir vs. Arquivar **antes** de qualquer confirmação, nunca por texto de erro; menu "Mais ações" de cada rolo já mostra o rótulo certo diretamente; `delete_filament_spool` continua como defesa em profundidade, só chamada quando já se sabe que não há histórico. Indicador visual "Arquivado" adicionado; filtro "Mostrar arquivados" persiste entre atualizações do drawer; rolo arquivado mantém histórico acessível (painel abre normalmente, só oculta os formulários). Rolagem horizontal (passo 7) corrigida: tabela reduzida de 8 para 7 colunas (peso nominal/disponível/% restante mesclados), ações secundárias movidas para um menu compacto (`DropdownMenu`, já usado em `SortableColumnHeader.tsx`), `min-width` fixo removido, apresentação em cartões (`Card`, já usado em `OrderManagementPanel.tsx`) adicionada para telas pequenas. Texto de Tipo truncado (passo 8) corrigido: coluna Tipo do histórico não usa mais `truncate`, permite quebra de linha (`whitespace-normal break-words`) — Data manteve `truncate`, não relatada como problema. Suíte completa 1350 → **1358 testes, todos passando**; lint 0 erros; `tsc -b`/`vite build` sem erros; `git diff --check` limpo; scan de segredos sem ocorrências reais. **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum objeto remoto alterado, nenhum registro `TESTE` modificado pela auditoria, nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink/dado oficial.** Validação final dos passos 7, 8, 11 e 12 continua pendente. Consumo automático de filamento por pedido continua sem nenhuma linha de código. Percentual macro **não alterado**. Checkpoint local: commit `fix: complete filament inventory usability`, sem push. |
| 2026-08-28 | Terceiro reteste manual do usuário: passo 1 ainda falhava — rolagem horizontal e botão "Movimentar / Pesar" sobreposto ao botão "Mais ações" em "Rolos do tipo", num diálogo de ~720px de largura (resolução normal de notebook). Causa raiz: a tabela de 7 colunas da rodada anterior ainda tinha duas células de ação separadas, e um `<button>` não trunca como texto — quando a largura da célula ficava menor que a largura natural do botão, ele vazava sobre a célula vizinha. **Corrigido**: tabela reduzida para 5 colunas (Identificador/Peso/Status/Abertura/Ações) — coluna "Ativo" eliminada (status ativo/arquivado passa a ser um badge junto ao Status); as duas ações secundárias viraram "Gerenciar" (botão compacto) + "Mais ações" (só ícone, nome acessível "Mais ações para o rolo [código]") num único flex com `gap`, nunca sobrepostos; Ativar/Desativar virou item do menu (funcionalidade preservada, só deixou de ser coluna própria); `overflow-x-auto` removido do wrapper da tabela. Suíte 1358 → **1363 testes, todos passando**; lint 0 erros; `tsc -b`/`vite build` sem erros; `git diff --check` limpo. Checkpoint local: commit `fix: make filament spool list responsive`, sem push. Em seguida, **o usuário aprovou integralmente o MVP manual de filamentos** — os quatro pontos em aberto desde os retestes anteriores (saldo consolidado, exclusão/arquivamento com histórico, filtro "Mostrar arquivados", e agora o layout da relação de rolos) ficam todos validados na prática. As regras operacionais do MVP continuam, mesmo assim, hipóteses documentadas sujeitas a revisão. Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum push, nenhum deploy, nenhuma alteração em Petlink/dado oficial. Percentual macro **não alterado**. |
| 2026-08-28 | Com o MVP de filamentos aprovado, **Incremento 5 do Módulo 3 implementado localmente**: fluxo centralizado de Compras (Filamentos/Acessórios/Embalagens) — ver detalhamento completo em §9b. Resumo: campo "Código da cor" removido dos diálogos de tipo de filamento (já era opcional em toda a pilha; só a interface exigia/exibia — nenhuma migration, nenhum backfill, compatibilidade total com registros antigos); nova tabela `inventory_purchases` (ledger financeiro imutável, `total_value` gerado pelo Postgres como `item_value + freight_value`); migration complementar acrescenta `initial_gross_weight_grams`/`purchase_id` a `filament_spools` (nullable, `null` para rolo criado manualmente); RPC `register_inventory_purchase` (única escrita, transação única — qualquer falha desfaz tudo, inclusive em N rolos de uma mesma compra de filamento; reaproveita `register_stock_movement`/`register_filament_movement`/`create_filament_type` já existentes e validados, nunca duplica lógica de saldo; localiza ou cria o tipo de filamento por combinação normalizada material+marca+acabamento+cor, nunca reativa um tipo inativo em silêncio); Edge Function `inventory-purchases` (não publicada); diálogo "Registrar compra" (`PurchaseDialog.tsx`) com botão único reaproveitando `InventoryPageShell.tsx`, seleção de categoria por action buttons (Filamento/Acessório/Embalagem), campos por categoria na ordem pedida (Filamento: Material/Peso líquido/Quantidade/Peso bruto por rolo/Marca/Cor/Acabamento/Valor/Frete; Acessório/Embalagem: item ativo via busca/Quantidade/Valor/Frete), resumo com total/custo médio/custo por kg, e refetch seletivo por categoria após sucesso. 2 migrations SQL, Edge Function com 34 testes de validação estrutural (Deno não instalado, não executados), teste de integração SQL completo (`inventory_purchases_test.sql`, 10 seções incluindo atomicidade real de uma falha forçada no meio de uma compra de filamento — não executado, sem Postgres local/aplicação remota), e 21 testes novos de frontend (`PurchaseDialog.test.tsx`) + adições em `InventoryPage.test.tsx`/`FilamentsInventoryPage.test.tsx`. Suíte completa do frontend 1363 → **1390 testes** (1388 passando; 2 falhas pré-existentes e não relacionadas a esta rodada em `RegisterPaymentForm.test.tsx`, sensíveis à data real do sistema — arquivo não tocado aqui); lint 0 erros; `tsc -b`/`vite build` sem erros; `git diff --check` limpo; scan de segredos sem ocorrências reais; `.env.local` confirmado ignorado. **Nenhuma migration aplicada ao Supabase remoto, nenhuma Edge Function publicada, nenhum deploy de frontend, nenhum dado oficial/Petlink alterado, nenhum push.** Composição de Produtos e consumo automático de filamento continuam sem nenhuma linha de código (fora de escopo, adiado explicitamente pelo usuário); histórico de compras (tela dedicada) não implementado. Módulo 3 continua **NÃO concluído** — validação manual desta nova funcionalidade ainda não realizada. Percentual macro **não alterado**. Checkpoint local: commit `feat: add inventory purchase workflow`, sem push. |
| 2026-08-28/29 | Auditoria integral do commit `feat: add inventory purchase workflow` (20 arquivos), seguida de aplicação remota — ver detalhamento completo em §9b. Resumo: as 2 falhas de `RegisterPaymentForm.test.tsx` reportadas na entrada anterior **não eram regressão** desse commit (arquivo não tocado por ele) — causa real era comparação de data local (componente) contra `toISOString()` UTC (teste), divergente sempre que a execução acontece entre ~21h e 24h em `America/Sao_Paulo`; corrigido com relógio fixo determinístico (`vi.useFakeTimers({toFake:['Date']})`), regra funcional preservada. Auditoria do backend encontrou 1 inconsistência real (CHECK entre colunas faltando em `filament_spools.initial_gross_weight_grams`) — corrigida na migration ainda não aplicada, com teste novo confirmando o bloqueio mesmo via INSERT direto. Primeira execução real do teste de integração SQL contra o remoto (dentro de `BEGIN...ROLLBACK`) revelou 10 casos usando sintaxe inválida (`DEFAULT` como argumento posicional de função, não suportado pelo Postgres fora de `INSERT`) — bug do próprio script de teste, não da RPC (confirmado pelos casos com notação nomeada, que já passavam); corrigido, reexecutado: **26 PASS, 0 FAIL, 0 SKIP**, `ROLLBACK` confirmado, zero resíduo, contagens oficiais idênticas antes/depois. Suíte completa do frontend **1390/1390, 100% verde**; lint/`tsc`/build sem erros; `git diff --check` limpo. **Migrations `20260828120000`/`20260828121000` aplicadas ao Supabase remoto `tjhacqreupfqefntjevf`** (37/37 sincronizadas, dry-run prévio sem operação inesperada, nenhuma migration antiga reaplicada) — verificação somente leitura confirmou schema/grants/RLS exatamente como pretendido. **Edge Function `inventory-purchases` publicada** (única — as 11 já existentes preservaram `updated_at`); smoke tests reais: `OPTIONS` → 204 com CORS correto, `POST` sem autenticação → 401, nenhuma compra real criada. Testes Deno não executados (Deno não instalado, não instalado por instrução explícita). **Nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink/dado oficial** — todas as contagens de dados reais permaneceram idênticas do início ao fim da rodada. Backend de Compras agora aplicado e validado por teste automatizado real contra produção; validação manual do usuário pela interface **ainda não ocorreu** — Módulo 3 continua **NÃO concluído**; composição de Produtos e consumo automático de filamento continuam sem nenhuma linha de código. Percentual macro **não alterado**. Checkpoint local: commit de correções (ver mensagem no histórico Git), sem push. |
| 2026-08-29 | **Validação manual do usuário aprovada** para as três categorias do fluxo de Compras, no ambiente real: Filamento (dois rolos criados, identificadores automáticos corretos, "Disponível" e "Rolos" atualizados, pesos líquido/bruto individuais corretos), Acessório (entrada registrada, saldo atualizado, histórico correto) e Embalagem (idem). **Isso completa a validação manual do Incremento 5 — o fluxo de Compras está concluído neste MVP.** Ressalvas que continuam valendo: histórico central de compras (tela dedicada) continua ausente; composição de produtos é o próximo incremento, ainda sem nenhuma linha de código; consumo automático de estoque depende da composição existir primeiro, também sem nenhuma linha de código; Módulo 3 continua **NÃO concluído** (falta composição+consumo). Nenhum código alterado — só documentação. Nenhuma migration, Edge Function, dado oficial ou Petlink tocados. Percentual macro **não alterado**. Checkpoint local: commit `docs: record inventory purchase validation`, sem push. |
| 2026-08-29 | **Incremento 6A do Módulo 3 (composição de Filamentos no Produto) implementado localmente**. A tabela `product_filaments` e a RPC `set_product_filaths` já existiam no backend desde a migration `20260827113000` (aplicada ao remoto junto com o Incremento 4) — esta rodada não criou nem aplicou nenhuma migration nova, só expôs o que já existia: nova rota `PATCH /products/:id/filaments` (Edge Function `products`, chamando exclusivamente `set_product_filaments`, salvamento atômico por DELETE+INSERT do conjunto inteiro dentro da própria RPC). `supabase/functions/products/index.ts` — até então a única Edge Function do projeto ainda com `Deno.serve` inline — foi dividida no padrão `index.ts`/`handler.ts` já usado nas demais 11 funções, preservando 100% do comportamento existente (criação de produto, atualização de preço, composição de Acessórios/Embalagens), só para viabilizar teste automatizado da rota nova sem abrir servidor real. Frontend: `productFilaments.ts` (API), `useProductFilaments.ts` (hook, save/retry independentes), `productFilamentComposition.ts` (linhas de formulário, filtro anti-duplicidade, rótulo com "(inativo)", parser de peso decimal brasileiro reaproveitando `parseNumberField` já existente), `FilamentCompositionForm.tsx` (nova seção "Filamentos" dentro do mesmo diálogo de composição do Produto, com formulário e botão "Salvar filamentos" totalmente independentes de Acessórios/Embalagens — nunca compartilha submit, estado ou rota), `productFilamentLines.ts` (resolução para a Ficha Técnica, sem custo/subtotal — `filament_types` não tem `unit_cost`). Regras aplicadas: múltiplos tipos de filamento por produto, peso teórico por unidade em gramas (aceita vírgula ou ponto), duplicidade de tipo bloqueada tanto na UI (opção removida do `Select`) quanto no backend (defesa em profundidade), zero/negativo rejeitados (`> 0`), nenhuma escolha de rolo em nenhum momento (só tipo), tipo inativo já vinculado nunca desaparece da tela (rótulo "(inativo)", mas bloqueia Salvar até ser removido — mesmo padrão já usado em Acessórios/Embalagens). Ficha Técnica (`ProductDetailPage.tsx`) ganhou um novo card "Filamentos" (Tipo/Situação/Peso teórico), calculado e carregado de forma independente do card "Acessórios e embalagens" — teste dedicado confirma que o Subtotal de componentes não é afetado pela presença de filamentos. Testes novos: `handler.test.ts` (Deno, estrutural, não executado — mesma limitação de ambiente de todas as rodadas anteriores), `productFilaments.test.ts`, `useProductFilaments.test.ts`, `productFilamentComposition.test.ts`, `productFilamentLines.test.ts`, `FilamentCompositionForm.test.tsx` (13 testes, render direto por props) + adições em `ProductsPage.test.tsx`/`ProductDetailPage.test.tsx`. Suíte completa do frontend 1390 → **1449 testes, todos passando**; lint 0 erros (6 avisos pré-existentes de `react-refresh`, não relacionados); `tsc -b`/`vite build` sem erros; `git diff --check` limpo. **Nenhuma migration criada ou aplicada nesta rodada, nenhuma Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink/dado oficial ou TESTE.** Reserva de estoque e consumo automático de filamento pelo pedido continuam sem nenhuma linha de código (fora de escopo desta rodada, por instrução explícita); `change_order_status` não foi tocado. Módulo 3 continua **NÃO concluído** (falta validação manual desta composição, reserva e consumo automático). Percentual macro **não alterado**. Checkpoint local: commit `feat: add filament product composition`, sem push. |
| 2026-08-29 | **Auditoria integral do commit `feat: add filament product composition` (c1b566d), seguida de publicação exclusiva da Edge Function `products`.** Reconciliação confirmou HEAD `c1b566d`, árvore limpa, 37/37 migrations sincronizadas, `products` remoto em ACTIVE v7 antes da rodada. Auditoria de regressão (`diff` linha a linha entre o `index.ts` anterior ao commit e o `handler.ts` novo) confirmou que `handleCreateProduct`, `handleUpdateProductPrice`, `handleUpdateProductComposition` e `validateCompositionItems` são **byte-idênticos** ao código anterior — as únicas mudanças de comportamento são a extração de `Deno.serve` para `export async function handleRequest` (mesmo padrão dos outros 11 handlers) e a rota nova `PATCH /:id/filaments`; `validateCompositionItems` ganhou só a palavra-chave `export` (visibilidade, sem efeito em runtime). Nenhuma regressão encontrada, nenhuma correção necessária. Observação sem correção necessária: o comentário de cabeçalho sobre `Access-Control-Allow-Methods`, que no `index.ts` anterior estava desatualizado (dizia "sem PUT", enquanto `_shared/cors.ts` já incluía PUT desde rodada anterior), já veio corrigido no próprio `handler.ts` do commit `c1b566d` — mudança de comentário, não de comportamento, e não algo desta rodada de auditoria. Auditoria da rota nova confirmou: UUID do produto validado, corpo restrito ao campo `filaments` (nenhum outro é lido), `set_product_filaments` é a única escrita (nenhum INSERT/UPDATE/DELETE direto em `product_filaments` pela Edge Function — confirmado também no banco: `service_role` não tem grant de escrita direta na tabela, só a RPC `SECURITY DEFINER`, dona `postgres`, escreve), duplicidade/peso zero-negativo/tipo inativo/UUID inválido todos rejeitados, conjunto vazio permitido, nenhum campo de rolo (`filament_spool_id`) aceito em nenhum ponto, autenticação exigida antes de qualquer acesso ao banco. Auditoria somente leitura do RPC remoto confirmou assinatura única `set_product_filaments(uuid, jsonb, uuid)`, `SECURITY DEFINER`/`search_path=""` seguro, `EXECUTE` restrito a `service_role`, RLS ativa em `product_filaments` com política única de `SELECT` para `authenticated` (sem grant de escrita), constraints (`UNIQUE(product_id, filament_type_id)`, `CHECK(theoretical_weight_grams > 0)`, FKs `RESTRICT`) idênticas à migration local — tudo já aplicado desde o Incremento 4, nenhuma migration pendente. Como a cobertura existente (`filament_inventory_test.sql` Seção 8) não cobria múltiplos tipos simultâneos, duplicidade no mesmo array, peso negativo (só testava zero) nem tipo inativo (só testava tipo inexistente), foi criado `supabase/tests/product_filaments_composition_test.sql` (script novo, dentro de `BEGIN...ROLLBACK`, só produto/tipos `TESTE%`) cobrindo exatamente essas lacunas — executado contra o remoto: **8 PASS, 0 FAIL**; confirmado que a falha de duplicidade não corrompe a composição válida anterior (atomicidade); consultas pós-teste confirmaram **zero resíduo** (nenhum produto `TESTE%` remanescente; os 4 tipos `TESTE%` pré-existentes no banco são de sessões manuais anteriores, com `created_at` de 2026-08-28, nenhum com os nomes usados por este script). Testes locais: os 5 arquivos específicos pedidos (135/135), suíte completa **1449/1449** (sem mudança em relação à rodada anterior — nenhum código de produção alterado nesta auditoria, só o comentário de CORS e o script SQL novo), lint 0 erros, `tsc -b`/`vite build` sem erros, `git diff --check` limpo. Testes Deno de `products` não executados (Deno não instalado neste ambiente, mesma limitação de todas as rodadas anteriores — nenhuma ferramenta nova instalada). **Publicada exclusivamente a Edge Function `products`** (`ACTIVE`, v7 → v8) — as outras 12 funções preservaram versão e `updated_at` exatos (comparação antes/depois confirmada). Smoke tests HTTP reais pós-publicação: `OPTIONS` → 204 com CORS correto; `POST /products` sem autenticação → 401 (`Header Authorization ausente.`); `PATCH /products/:id/filaments` sem autenticação → 401, mesma mensagem — nenhum produto criado ou alterado (rejeitado antes de tocar o banco). **Nenhuma migration aplicada, nenhuma outra Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink/dado oficial.** `PATCH /products/:id/filaments` está agora tecnicamente disponível em produção, mas **nenhuma validação manual do usuário pela interface ainda ocorreu** — Módulo 3 continua **NÃO concluído**; reserva de estoque e consumo automático continuam sem nenhuma linha de código; `change_order_status` não foi tocado. Percentual macro **não alterado**. Checkpoint local: commit `docs: record filament composition function deployment`, sem push. |
| 2026-08-29 | **Validação manual do usuário aprovada** para a composição de Filamentos no Produto (`PATCH /products/:id/filaments`, publicada na rodada anterior), passos 1–8 do checklist. Em seguida, **ajustes de Produtos/Clientes/Pedidos implementados localmente** — ver detalhamento completo abaixo. Resumo: **4 migrations novas** (`20260829140000_add_customer_deletion_function.sql` — `delete_customer`, exclusão física protegida por vínculo com Pedido/Empresa; `20260829141000_add_order_deletion_function.sql` — `delete_order`, exclusão física protegida por status/pagamento/aprovação/versão; `20260829142000_add_order_payment_condition_and_atomic_creation.sql` — `orders.payment_condition` + `create_order_with_payment`, que reaproveita `create_order`/`register_payment` já existentes numa única transação; `20260829143000_add_product_edit_function.sql` — `update_product`, whitelist PATCH nunca incluindo `default_price`). **Edge Function `customers` criada** (nova, só `DELETE /customers/:id`) e **`orders` dividida** em `index.ts`/`handler.ts` (mesmo padrão já usado nas demais Edge Functions) com 2 rotas novas (`POST /orders/with-payment`, `DELETE /orders/:id`); `products/handler.ts` ganhou `PATCH /products/:id`. `_shared/errors.ts` ganhou os marcadores estáveis novos (`CUSTOMER_HAS_ORDERS:`/`CUSTOMER_HAS_COMPANY:`/`ORDER_DELETE_INVALID_STATUS:`/`ORDER_DELETE_HAS_PAYMENTS:`/`ORDER_DELETE_HAS_APPROVALS:`/`ORDER_DELETE_HAS_VERSIONS:` + validações de `create_order_with_payment`/`update_product`). Frontend: diálogo **"Editar produto"** substitui "Alterar preço" (3 seções independentes — Dados do produto/`update_product`, Preço/`update_product_price` inalterado, Histórico de preços somente leitura via `product_price_history`, mais recente primeiro); **exclusão protegida de Cliente** (ícone de lixeira, confirmação, mensagem de bloqueio do backend exibida tal qual); **exclusão protegida de Pedido** (terceiro botão na coluna Ações, confirmação com o número do pedido); **cores nos status operacionais** (azul: Orçamento/Aprovado/Em produção; amarelo: Aguardando aprovação/Fila de produção/Aguardando entrega; verde: Entregue; vermelho: Cancelado, reaproveitando o token `destructive`) e **financeiro** (verde: Pago; amarelo: Ag. Pagamento); **"Ag. Pagamento"** unifica a apresentação de `WAITING_PAYMENT`/`DEPOSIT_RECEIVED` em todo lugar que mostra o status ATUAL (badge da listagem, "Situação financeira" do painel de gerenciamento, resumo do modo edição) — o histórico de transições (`OrderManagementPanel`) continua com os rótulos distintos originais, para nunca perder informação real da transição; **"Atrasado"** é um indicador visual derivado (prazo vencido, pedido não `DELIVERED`/`CANCELLED`), nunca um novo status persistido, exibido ao lado do status operacional real, nunca no lugar dele; **"Forma de pagamento"** (Adiantado/Sinal/Na entrega) em "Novo Pedido", só em `mode="create"` — nunca renderizada nem incluída no objeto emitido em modo edição, garantia estrutural (não só de regra de negócio) de que editar um pedido nunca recria o pagamento inicial; Adiantado/Sinal chamam `createOrderWithPayment` (nova rota, nunca duas chamadas separadas), "Na entrega" pré-selecionada por padrão (opção que nunca cria pagamento); campo **"Tipo de pagamento" removido** de Registrar Pagamento — `payment_type` agora sempre inferido no cliente (`inferPaymentType`, `lib/orders/paymentTypeInference.ts`) a partir do valor/saldo/pagamento anterior, enviado ao backend no mesmo contrato de sempre. Testes: suíte completa do frontend 1449 → **1504 testes, todos passando** (testes antigos que dependiam do campo "Tipo de pagamento" removido foram **atualizados**, nunca apagados, para refletir a inferência automática); `products/handler.test.ts` ampliado, `orders/handler.test.ts` e `customers/handler.test.ts` **novos** (Deno, estruturais — 401/404/roteamento, sem rede/banco); `supabase/tests/product_customer_order_ops_test.sql` **novo** (transacional, `BEGIN...ROLLBACK`, cobrindo as 4 RPCs novas: `update_product` whitelist, `delete_customer` permitida/bloqueada, `delete_order` permitida/bloqueada por status/pagamento, `create_order_with_payment` ADVANCE/DEPOSIT/ON_DELIVERY + rollback comprovado quando o pagamento falha). Lint 0 erros (7 avisos pré-existentes de `react-refresh`, não relacionados); `tsc -b`/`vite build` sem erros; `git diff --check` limpo; busca por segredos no diff sem ocorrências reais; `.env.local` confirmado ignorado. **Testes Deno e o teste SQL das 4 RPCs novas NÃO foram executados nesta rodada** — mesma limitação de ambiente já registrada em todas as rodadas anteriores para os Deno (runtime não instalado), e o SQL depende das 4 migrations estarem aplicadas (não aplicadas nesta rodada, por restrição explícita) — ambos precisam de uma primeira execução real numa rodada futura autorizada, antes de serem tratados como comprovadamente corretos. **Exclusão permanente e expressamente autorizada de FS-26-002/003/004/005**: auditoria somente leitura confirmou os quatro como registros de teste (todo item de todos os quatro usa o mesmo produto "teste - Produtos"; numeração sequencial logo após o pedido de homologação FS-26-001; criados numa janela de 2 dias), executada em transação única com `COMMIT` (não `ROLLBACK` — única exceção explicitamente autorizada) apagando só `payments`/`order_items`/`order_status_history`/`payment_status_history`/`orders` dos 4 ids exatos; confirmado individualmente, após o commit, que nenhum dos quatro `order_number` existe mais, que o quinto pedido do mesmo cliente (FS-26-006, fora da lista autorizada) permanece intacto, e que o Cliente e o Produto "teste - Produtos" continuam existindo — contagem total de pedidos confirmada em 6 antes e 2 depois (−4 exatos, nenhum outro afetado). **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink/dado oficial** (a exclusão dos 4 pedidos de teste foi a única escrita remota desta rodada, expressamente autorizada à parte). Reserva de estoque e consumo automático continuam sem nenhuma linha de código; `change_order_status` não foi tocado. **Validação manual das novas alterações desta rodada (Editar produto/exclusões protegidas/cores/Forma de pagamento/inferência de Tipo de pagamento) ainda está pendente** — Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint local: commit `feat: refine product customer and order workflows`, sem push. |
| 2026-08-29 | **Auditoria final da rodada anterior, correção da proteção da Petlink, aplicação das 4 migrations pendentes e publicação controlada de 3 Edge Functions.** Reconciliação confirmou HEAD `e4fc0bc`, árvore limpa, 37/41 migrations sincronizadas (as 4 esperadas pendentes), `products` ACTIVE v8, `orders` ACTIVE v5, `customers` ainda não publicada, FS-26-002/003/004/005 confirmados ausentes, FS-26-006/Cliente `Eliana Guimarães`/Produto "teste - Produtos" confirmados preservados, **Petlink reconfirmada ausente** de `customers`/`companies` por busca somente leitura (`ilike '%petlink%'`) imediatamente antes de qualquer migration. **Achado de auditoria**: a proteção da Petlink descrita na entrada anterior ("protegida pelo mesmo mecanismo genérico de vínculos") foi considerada insuficiente — um vínculo pode ser removido no futuro, esvaziando a proteção. Como a Petlink não existe como registro hoje, foi apresentada ao usuário a escolha entre um UUID hardcoded (impossível, sem registro) e um mecanismo estrutural agora, sem UUID — **o usuário escolheu o mecanismo estrutural**. Implementado editando a migration local ainda pendente (`20260829140000`, nunca criando uma migration nova para algo ainda não aplicado): coluna `customers.is_protected boolean not null default false`, checada **primeiro** em `delete_customer` (antes de qualquer checagem de vínculo, com o marcador próprio `PROTECTED_CUSTOMER:`), e o grant de `UPDATE` de `customers` para `authenticated` restrito à lista de colunas já pretendida (`name, whatsapp, instagram, company_id, acquisition_source_id, notes, is_active`), **excluindo `is_protected`** — mesmo padrão já usado por `products.default_price` — para que a proteção não possa ser contornada por um `UPDATE` direto via PostgREST. Nenhuma linha marcada `is_protected=true` nesta rodada (Petlink continua sem cadastro oficial); o mecanismo fica pronto para protegê-la assim que o cadastro for autorizado. **Segunda lacuna encontrada durante a auditoria dos requisitos da própria tarefa** ("idempotência e retry", ausente da rodada anterior): `create_order_with_payment` não tinha proteção de retry — corrigido retrofitando o mesmo padrão já usado em `register_inventory_purchase`/`stock_movements`/`filament_movements` (`orders.idempotency_key` nullable + índice único parcial; parâmetro `p_idempotency_key` checado antes do trabalho pesado; repetição com a mesma chave e payload equivalente devolve o pedido já criado, payload divergente rejeitado com `IDEMPOTENCY_KEY_CONFLICT:`; corrida real de concorrência deliberadamente deixada para o `unique_violation` genérico, mesma justificativa já documentada no precedente). Frontend: `OrderForm.tsx` ganhou a mesma geração de chave por `useRef`+"fingerprint" já usada em `StockMovementForm.tsx` (só em `mode === 'create'`, nunca em edição). Auditoria completa das 4 migrations e das 3 Edge Functions (`products`/`orders`/`customers`) sem outras inconsistências encontradas — grants/`SECURITY DEFINER`/`search_path`/UUID/whitelist/CORS/mapeamento de erro conferidos linha a linha. Testes: 6 novos em `OrderForm.test.tsx` cobrindo a idempotência (chave estável no retry, nova chave após mudança real, nunca presente em modo edição), 2 novos em `CustomersPage.test.tsx` provando a proteção incondicional (cliente protegido COM e SEM vínculo, ambos rejeitados pela mesma mensagem específica), fixtures de `Customer` corrigidas em 6 arquivos (`tsc -b` usado para localizar todos). Suíte completa do frontend **1510/1510 passando**, lint 0 erros, `tsc -b`/`vite build` sem erros, `git diff --check` limpo. Checkpoint local: commit `fix: protect permanent customer records and prevent duplicate order creation on retry`. **4 novos cenários adicionados a `product_customer_order_ops_test.sql`** (Seção 2: protegido com/sem vínculo, cliente comum ainda excluível, checagem estrutural do grant) e **8 novos** (Seção 4: `DEPOSIT` zero/negativo/igual-ao-total; Seção 5 nova: retry com mesma chave devolve o mesmo pedido, mesma chave com payload diferente rejeitada sem afetar o primeiro pedido, chamadas sem chave nunca tratadas como retry uma da outra, `EXECUTE` das 4 RPCs confirmado exclusivo de `service_role`). **Migrations aplicadas ao Supabase remoto `tjhacqreupfqefntjevf`** via `npx supabase db push --linked` (dry-run prévio confirmou exclusivamente as 4 esperadas) — **41/41 migrations agora sincronizadas**; verificação somente leitura confirmou as colunas novas (`customers.is_protected boolean not null default false`; `orders.payment_condition`/`orders.idempotency_key text`), as 4 funções com `SECURITY DEFINER`/`search_path=""`, `EXECUTE` restrito a `service_role`, e `is_protected` de fato inacessível a `authenticated` via `UPDATE` (`has_column_privilege` confirmando `false`); contagens de `customers`/`companies`/`orders`/`products` idênticas antes/depois, Petlink e FS-26-006 reconfirmados intactos. **Teste de integração SQL executado contra o remoto dentro de `BEGIN...ROLLBACK`**: primeira execução **29 PASS/1 FAIL** — o FAIL era um bug da própria asserção do teste novo (Seção 5.2 agrupava as duas chamadas de `create_order_with_payment` num único bloco `begin/exception`, então o `rollback to savepoint` da segunda exceção desfazia também o primeiro pedido, já bem-sucedido, de dentro do mesmo bloco); corrigido separando as duas chamadas em blocos `begin/end` distintos, reexecutado: **30 PASS, 0 FAIL**, `ROLLBACK` confirmado, zero resíduo, Petlink intacta. Checkpoint local: commit `test: fix savepoint scope bug in idempotency-conflict SQL test`. **Publicadas exclusivamente `products` (v8→v9), `orders` (v5→v6) e `customers` (nova, v1)** — as 11 demais Edge Functions preservaram versão/`updated_at` exatos (comparação antes/depois confirmada). Smoke tests HTTP reais nas 3 funções: `OPTIONS` → 204 com CORS correto; chamada sem autenticação → 401 (`Header Authorization ausente.`, sem detalhe interno) nas rotas de escrita; rota desconhecida → 404 — todos sem tocar o banco. **Smoke tests autenticados (registros `TESTE`, validação de UUID, fluxos completos de criação/edição/exclusão) não foram realizados nesta rodada** — sem sessão de usuário disponível neste ambiente para autenticar chamadas HTTP reais (e a obtenção da chave de API do projeto foi bloqueada pelo classificador de permissões da sessão); a cobertura equivalente de regra de negócio já foi validada de forma exaustiva pelo teste SQL transacional direto nas mesmas 4 RPCs. Testes Deno permanecem não executados (Deno não instalado, mesma limitação de todas as rodadas anteriores). **Nenhuma migration adicional aplicada, nenhuma outra Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink/dado oficial** — FS-26-002/003/004/005 seguem ausentes, FS-26-006/Cliente/Produto "teste - Produtos" seguem preservados. **Validação manual do usuário pela interface (Editar produto/exclusões protegidas/Forma de pagamento/proteção da Petlink) continua pendente** — reserva e consumo automático de estoque continuam sem nenhuma linha de código; `change_order_status` não foi tocado; Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint documental desta entrada: commit `docs: record product customer and order deployment`, sem push. |
| 2026-08-29 | **Status inicial automático de Pedidos NOVOS conforme composição de itens (regra aprovada pelo usuário), implementado localmente.** Reconciliação confirmou HEAD `cac7ea9`, árvore limpa, 19 commits à frente de `feature/customers-orders`, 41/41 migrations sincronizadas, `products` ACTIVE v9, `orders` ACTIVE v6, `customers` ACTIVE v1 — tudo exatamente como esperado, nenhuma divergência. **Diagnóstico explícito pedido nesta rodada**: nenhum commit posterior a `cac7ea9` registrando a validação manual dos passos 1–8 (checklist da entrada anterior — Editar produto/exclusões protegidas/Forma de pagamento/proteção da Petlink) foi encontrado — essa validação **continua pendente**, sem alteração desde a entrada anterior; nada foi descartado, só confirmado ausente. **Regra**: um Pedido NOVO com só itens CATALOG e/ou SPOT (nenhum CUSTOM) nasce direto em `IN_PRODUCTION_QUEUE` — pula QUOTE/WAITING_APPROVAL/APPROVED, sem histórico ou aprovação fictícios; um Pedido com ao menos um item CUSTOM mantém o fluxo normal (nasce em QUOTE, Orçamento/Aprovação inalterados). Cada item CATALOG de um Pedido que nasceria em IN_PRODUCTION_QUEUE precisa ter `product_id` válido apontando para um Produto de Catálogo com ao menos uma linha em `product_filaments` — bloqueia toda a criação e lista os nomes reais dos produtos incompletos numa única mensagem (`ORDER_CATALOG_MISSING_COMPOSITION:`) quando não tiver. **Decisão de escopo documentada explicitamente** (interpretação necessária, não literal no pedido): um Pedido CATALOG+CUSTOM continua indo para QUOTE e **não** exige composição de filamento agora — só um Pedido que de fato nasceria em IN_PRODUCTION_QUEUE passa por essa checagem; um item CATALOG dentro de um Pedido CUSTOM segue o fluxo normal, sem essa exigência nova. **Contrato centralizado no banco**: duas funções internas novas, sem nenhum grant (nem para `service_role` — mesmo padrão de `assert_active_user`/`jsonb_whitelist`) — `determine_order_initial_status(p_items)` (pura, só classifica) e `validate_catalog_composition_for_creation(p_items)` (consulta `products`/`product_filaments`) — chamadas de dentro das **duas sobrecargas** de `create_order()` (10 e 11 parâmetros; a de 11 é a efetivamente usada hoje pela Edge Function `orders` e por `create_order_with_payment()`, que a chama internamente e por isso herda o mesmo resultado sem nenhuma lógica duplicada). Migration nova (ainda **não aplicada**): `20260829150000_add_order_initial_status_classification.sql` — `CREATE OR REPLACE FUNCTION` nas duas sobrecargas reais de `create_order` (assinaturas conferidas integralmente antes da substituição), preservando owners/grants/`SECURITY DEFINER`/`search_path=""`; nenhuma tabela alterada; nenhum Pedido existente tocado (só `CREATE OR REPLACE FUNCTION`, nenhum `UPDATE` em dado real). **Dois achados de auditoria registrados explicitamente no cabeçalho da migration, não corrigidos por não terem sido pedidos**: (1) o gate de SPOT em `change_order_status()` (`search_time_status = RECORDED`) só age na transição, não na criação direta — um Pedido SPOT-only criado por esta regra nasce sem essa checagem (hoje dormant: SPOT ainda não é criável pelo frontend); (2) `update_quote_order()` só edita Pedidos em QUOTE com só itens CATALOG — a partir desta migration, nenhum Pedido novo só-CATALOG/SPOT chega a estar em QUOTE, então "Alterar pedido" (edição completa) se torna estruturalmente inalcançável para Pedidos novos (continua funcionando para Pedidos antigos como FS-26-006, que seguem em QUOTE) — o modo somente leitura já existente de `OrderEditForm.tsx` já cobre esse caso graciosamente, sem precisar de nenhuma mudança de código. Interação com reserva/congelamento do Incremento 6B (ainda não implementado) documentada: quando existir, `create_order()` também precisará participar do mecanismo atômico para os Pedidos que esta regra já cria direto em IN_PRODUCTION_QUEUE — nenhuma reserva/consumo implementado agora. Frontend: `OrderForm.tsx` ganha uma mensagem informativa (não editável, nunca um controle) só em `mode === 'create'` — "Pedidos formados somente por itens de Catálogo e/ou Spot seguem diretamente para a Fila de produção. Produtos de Catálogo precisam ter composição de filamentos cadastrada." — nunca aparece em "Alterar pedido"; o status inicial nunca é lido do formulário nem enviado no payload (já era assim antes — a Edge Function `orders` nunca aceitava esse campo). `_shared/errors.ts` ganha o marcador `ORDER_CATALOG_MISSING_COMPOSITION:` (business_rule/409). Testes: 3 novos em `OrderForm.test.tsx` (mensagem aparece em create, nunca em edit), 1 novo em `OrdersPage.test.tsx` (erro real de composição incompleta via toast, campos preservados, diálogo continua aberto) — suíte completa do frontend **1513/1513 passando**, lint 0 erros, `tsc -b`/`vite build` sem erros, `git diff --check` limpo, busca por segredos sem ocorrências, `.env.local` confirmado ignorado. Novo teste SQL transacional `supabase/tests/order_initial_status_test.sql` (17 cenários: as 3 combinações que viram fila, CUSTOM sozinho e as 2 combinações mistas que continuam QUOTE, product_id ausente, composição ausente, dois produtos incompletos na mesma mensagem, as 3 formas de pagamento sobre o novo status, rollback financeiro completo, idempotência, edição de pedido existente sem alterar status, permissões/grants) — **não executado nesta rodada** (a migration correspondente ainda não foi aplicada, mesma restrição de todas as rodadas anteriores nesse mesmo padrão); revisado estruturalmente (balanceamento de `$$`, contagem de blocos) por não haver Postgres local disponível. Testes Deno permanecem não executados (Deno não instalado). **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhum Pedido existente alterado, nenhuma alteração em Petlink/dado oficial, nenhuma reserva ou consumo implementados, `change_order_status` não tocado.** Módulo 3 continua **NÃO concluído**; validação manual da nova regra ainda pendente (depende da migration ser aplicada primeiro). Percentual macro **não alterado**. Checkpoint local: commit `feat: queue catalog and spot orders automatically`, sem push. |
| 2026-08-29 | **Correção documental obrigatória + remoção do gate de tempo de pesquisa/modelagem do SPOT como condição de entrada na Fila de produção**, ambas por decisão explícita do usuário. **Correção documental**: as duas entradas anteriores deste histórico registravam a validação manual dos passos 1–8 (Editar produto e Histórico de preços; exclusão protegida de Cliente; cores nos status operacional/financeiro e indicador "Atrasado"; Forma de pagamento Na entrega/Sinal/Adiantado; Registrar pagamento; exclusão protegida de Pedido) como **pendente** — o usuário confirmou nesta rodada que os 8 passos foram **integralmente aprovados**; a afirmação de pendência no resumo vigente do Módulo 1 (§3) foi corrigida para refletir a aprovação, com o checklist completo listado ali. As duas entradas antigas do histórico abaixo NÃO foram reescritas (convenção já estabelecida neste documento: histórico nunca é editado retroativamente, só corrigido por uma entrada nova, como esta). **Regra de SPOT**: o gate que bloqueava a entrada de um item SPOT em `IN_PRODUCTION_QUEUE` sem `search_time_status = RECORDED` foi **removido por completo** de `change_order_status()` — decisão do usuário: "o tempo de pesquisa/modelagem de itens SPOT não deverá ser considerado para entrada na Fila de produção", nem na criação (já não bloqueava, entrada anterior) nem em nenhuma transição de um pedido existente (agora corrigido). Auditoria integral confirmou que esse gate era o **único** ponto de bloqueio em todo o sistema — nenhum outro lugar (Edge Functions, frontend, `try_auto_approve_order`) usa `search_time_status`/`search_minutes` para autorizar status; as demais ocorrências são só leitura/escrita do campo como dado (payload de criação/edição do item) e o CHECK de integridade da própria tabela (`RECORDED` exige `search_minutes` preenchido, mantido — regra do dado, não gate de produção). **Nenhuma coluna, tabela ou tempo já registrado foi apagado**; os campos continuam existindo, consultáveis e preenchíveis exatamente como antes, agora só informativos/históricos. Correção incluída na própria migration `20260829150000` (ainda não aplicada) — `CREATE OR REPLACE FUNCTION change_order_status()` com o corpo real hoje em vigor (Migration 15, nunca modificado por nenhuma migration desde então, conferido integralmente antes da substituição), removendo exclusivamente o bloco do gate; máquina de transições, autenticação, `changed_by`, histórico, cancelamento, aprovações, mensagens não relacionadas, grants, `SECURITY DEFINER` e `search_path=""` preservados byte a byte — nenhuma regra de pagamento ou idempotência existe nesta função (nunca existiu). Verificação estrutural adicionada à própria migration: confirma por `pg_get_functiondef` que nenhum `JOIN`/referência qualificada a `spot_item_details.search_time_status` sobra no código real da função (não uma busca de texto genérica, que geraria falso positivo contra o próprio comentário explicativo deixado no corpo da função), e que as colunas `search_time_status`/`search_minutes` continuam existindo em `spot_item_details`. Classificação inicial de Pedidos novos (entrada anterior) **preservada integralmente** — nenhuma mudança de comportamento para CATALOG/CUSTOM/CATALOG+CUSTOM. Testes: `supabase/tests/order_initial_status_test.sql` ganhou a Seção 6 (6 cenários novos: SPOT sem/com `search_time_status` explícito no payload, CATALOG+SPOT sem tempo, transição real de um pedido SPOT+CUSTOM existente — QUOTE→WAITING_APPROVAL→APPROVED→IN_PRODUCTION_QUEUE — sem `RECORDED` em nenhum momento, tempo já registrado sobrevivendo a uma transição real, cancelamento continuando bloqueado após IN_PRODUCTION — prova que remover o gate não afetou nenhuma outra transição) e a Seção 5 (grants) ganhou `change_order_status`; `supabase/tests/bloco1_integration_test.sql` (arquivo de integração mais antigo do Bloco 1) teve sua Seção 5 **substituída** (não apagada) — os 3 testes antigos que esperavam bloqueio por tempo agora testam a nova decisão (transição permitida sem `RECORDED`, tempo previamente registrado permanece armazenado), mais uma asserção nova (8.4a-bis) confirmando que o tempo registrado sobrevive a uma transição real subsequente (`IN_PRODUCTION_QUEUE` → `IN_PRODUCTION`) já existente naquele arquivo. **Nenhum teste SQL executado nesta rodada** — a migration continua pendente, mesma restrição de sempre; ambos os arquivos revisados estruturalmente (balanceamento de `$$`) por não haver Postgres local. Frontend: nenhuma mudança de comportamento (SPOT continua "Em breve", não criável pela interface) — só um comentário desatualizado corrigido em `orderStatusMachine.ts` (não citava mais um motivo de rejeição do backend que deixou de existir); suíte completa 1513/1513 (sem alteração — nenhum código de produção do frontend tocado), lint 0 erros, `tsc -b`/`vite build` sem erros, `git diff --check` limpo, busca por segredos sem ocorrências, `.env.local` confirmado ignorado. Testes Deno permanecem não executados (Deno não instalado). **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhum Pedido existente alterado, nenhuma alteração em Petlink/dado oficial, nenhum campo/dado de tempo excluído, nenhuma reserva ou consumo implementados.** Módulo 3 continua **NÃO concluído**; validação manual da regra de classificação inicial (incluindo agora a remoção do gate de SPOT) continua pendente de aplicação da migration. Percentual macro **não alterado**. Checkpoint local: commit `fix: remove spot search time production gate`, sem push. |
| 2026-08-29 | **Aplicação da migration `20260829150000_add_order_initial_status_classification.sql` e republicação exclusiva da Edge Function `orders`.** Reconciliação confirmou HEAD `b02af41`, árvore limpa, 21 commits à frente de `feature/customers-orders`, 41/42 migrations sincronizadas (só a pendente esperada), `orders` ACTIVE v6, FS-26-002/003/004/005 confirmados ausentes, FS-26-006 preservado (avançou naturalmente para `DELIVERED` desde o último checkpoint — uso legítimo da interface pelo usuário, não uma ação desta sessão), Petlink reconfirmada ausente; um terceiro pedido real (FS-26-008, `QUOTE`) também apareceu desde o último checkpoint, mesma origem (uso real do usuário) — nenhum dado oficial foi criado, alterado ou apagado por esta sessão em nenhum momento. Suíte específica (`OrderForm`/`OrdersPage`/`OrderStatusControl`/`orderStatusMachine`) e completa **1513/1513**, lint 0 erros, `tsc -b`/`vite build` sem erros, `git diff --check` limpo — todos aprovados antes de tocar o remoto. Dry-run confirmou exclusivamente a migration pendente; **aplicada** via `npx supabase db push --linked` — **42/42 migrations agora sincronizadas**. Verificação somente leitura confirmou `determine_order_initial_status`/`validate_catalog_composition_for_creation`/as 2 sobrecargas de `create_order`/`change_order_status` todas com `SECURITY DEFINER`/`search_path=""` corretos, os 2 helpers internos com **zero** grant (nem para `service_role`, nem `authenticated`/`anon`), as 4 RPCs de escrita exclusivas de `service_role` — nenhum Pedido existente alterado (só `CREATE OR REPLACE FUNCTION`, confirmado pelas contagens de `customers`/`companies`/`orders`/`products` idênticas antes/depois). **Teste `order_initial_status_test.sql` executado contra o remoto dentro de `BEGIN...ROLLBACK`**: primeira execução 23 PASS/1 FAIL — o FAIL era um bug da própria asserção do teste 2.2 (comparava a contagem de `order_items` do produto contra um valor absoluto zero, mas o teste 1.6 já cria legitimamente um item real para esse mesmo produto — CATALOG+CUSTOM não exige composição — gerando um falso positivo); corrigido para comparar contagem antes/depois em vez de um valor absoluto, reexecutado: **24 PASS, 0 FAIL**, `ROLLBACK` confirmado, zero resíduo (contagens idênticas antes/depois). Checkpoint local: commit `test: fix false-positive orphan check in composition-missing SQL test`. **`bloco1_integration_test.sql` (arquivo de integração mais antigo do Bloco 1, nunca executado contra o remoto em nenhuma rodada anterior) abortou** ao criar sua própria fixture de pedido CATALOG — o produto de teste usado por essa fixture nunca recebeu composição de filamento, e a regra introduzida 2 rodadas atrás (não a remoção do gate de SPOT desta rodada) passou a exigi-la para qualquer pedido só-CATALOG. Como a correção dessa fixture teria efeito em cascata sobre 10+ seções deste arquivo de 4500+ linhas que dependem dela começar em `QUOTE`, a decisão foi apresentada ao usuário, que optou por **pular este arquivo nesta rodada** e publicar com base só em `order_initial_status_test.sql` (que já cobre exaustivamente a mudança real desta rodada) — a correção de `bloco1_integration_test.sql` fica registrada como pendência para uma rodada futura dedicada, não relacionada ao SPOT. **Publicada exclusivamente `orders` (v6→v7)** — as 13 demais Edge Functions preservaram versão/`updated_at` exatos (comparação antes/depois confirmada). Smoke tests HTTP reais: `OPTIONS /orders` → 204 com CORS correto; `POST /orders` e `POST /orders/with-payment` sem autenticação → 401 (`Header Authorization ausente.`, sem detalhe interno); rota desconhecida → 404; contagem de `orders` confirmada idêntica antes/depois (nenhum dado alterado pelos smoke tests). **A regra Catálogo/Spot e a remoção do gate de tempo do SPOT estão agora ATIVAS em produção** — `search_time_status`/`search_minutes` permanecem só informativos/históricos, nenhuma coluna ou dado apagado. **Nenhuma outra Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhum Pedido existente/oficial alterado, nenhuma alteração em Petlink, FS-26-002/003/004/005 seguem ausentes.** Frontend ainda não permite criar itens SPOT (continua "Em breve") — a regra fica pronta, não exercitável pela interface ainda. **Validação manual da regra (Catálogo/Spot direto para a Fila + remoção do gate de tempo) continua pendente** — reserva e consumo automático de estoque continuam sem nenhuma linha de código; Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint documental desta entrada: commit `docs: record automatic order queue deployment`, sem push. |
| 2026-08-29 | **Validação manual PARCIAL do usuário aprovada** para a regra de status inicial automático (`orders` v7, migration `20260829150000` já aplicada na entrada anterior) — restrita aos dois cenários hoje possíveis pela interface, já que Personalizado e Spot continuam "Em breve" em `OrderForm.tsx`: (1) **Pedido só Catálogo com composição de filamentos: aprovado** — confirmado nascendo diretamente em Fila de produção (`IN_PRODUCTION_QUEUE`), sem passar por Orçamento/Aprovação; (2) **Pedido Catálogo sem composição: aprovado** — criação bloqueada, mensagem exibida corretamente (`ORDER_CATALOG_MISSING_COMPOSITION:`, nomes reais dos produtos), nenhum Pedido residual/órfão criado. **Catálogo + Personalizado NÃO pôde ser testado manualmente nesta rodada** — item Personalizado segue indisponível na interface; esse cenário misto permanece validado exclusivamente pelo teste SQL transacional (`order_initial_status_test.sql`, testes 1.6 e 1.7, já executados com sucesso contra o remoto na entrada anterior). **Spot (sozinho ou combinado com Catálogo) também não tem validação manual** — item Spot segue indisponível na interface; permanece validado exclusivamente pelo teste SQL (testes 1.2, 1.3 e a Seção 6 completa — 6.1 a 6.6 — cobrindo especificamente a remoção do gate de tempo de pesquisa/modelagem). **Nenhuma validação manual integral desta regra é declarada nesta entrada** — só os dois cenários de Catálogo puro (com e sem composição) foram de fato confirmados na prática pelo usuário; os cenários mistos e Spot continuam dependendo só da cobertura SQL até que Personalizado/Spot sejam habilitados na interface. **Limpeza dos registros `TESTE` usados na validação manual: aprovada** — removidos pelo próprio usuário, sem resíduo. A pendência de `bloco1_integration_test.sql` (abortando por uma fixture de pedido Catálogo sem composição, achado da entrada anterior, não relacionado ao SPOT) **permanece em aberto**, sem nenhuma alteração nesta entrada. **Reserva e consumo automático de estoque continuam sem nenhuma linha de código** — a entrada de um Pedido em Fila de produção (`IN_PRODUCTION_QUEUE`), automática ou manual, **ainda não reserva nem baixa nenhum estoque** (filamento, acessório ou embalagem); esse mecanismo continua planejado só para o Incremento 6B, ainda não implementado. Módulo 3 continua **NÃO concluído**. **Nenhum código (frontend ou backend) alterado nesta entrada, nenhum dado modificado, nenhuma migration criada ou aplicada, nenhuma Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink, nenhum Pedido excluído restaurado** — só documentação, registrando fatos já confirmados pelo usuário. Percentual macro **não alterado**. Checkpoint local: commit `docs: record automatic order queue validation`, sem push. |
| 2026-08-29 | **Retorno temporário ao módulo Produtos: estrutura produtiva por plates implementada localmente** (banco/contratos e frontend), por pedido explícito do usuário — Pedidos ficam parados nesta rodada. Reconciliação confirmou HEAD `cc3199e`, árvore limpa, 24 commits à frente de `feature/customers-orders`, 42/42 migrations sincronizadas, `orders` ACTIVE v7, suíte do frontend 1513/1513 — tudo exatamente como esperado. **Modelo adotado**: cada Produto pode ter N "plates" (cada um representando um job de impressão distinto, com composição/peso/tempo próprios) — conceito **não relacionado** à coluna legada `products.units_per_plate` (não tocada, permanece morta). Duas tabelas novas (`product_plates`, `product_plate_filaments`), FKs `on delete restrict`, únicas por `(product_id, plate_number)`/`(plate_id, filament_type_id)`, RLS só-leitura para `authenticated`, sem grant de escrita direta a ninguém — toda escrita passa pelas RPCs atômicas. **Totais efetivos continuam vivendo nas colunas já existentes** `products.default_weight_grams`/`default_print_time_seconds` (nenhuma coluna "efetiva" nova, nenhum leitor existente precisa mudar); dois campos novos, **independentes entre si**, `production_weight_manual_override_grams`/`production_time_manual_override_seconds` (nullable — `null` = usa o automático somado dos plates só para aquele campo específico) — desenho motivado diretamente pelo exemplo de aceite do usuário (Suporte PS5: Plate 1 preto 37,16 g/01:17 + Plate 2 97,34 g/02:54 = automático 134,50 g/04:11; "Ajustar totais" permite alterar só o peso para 134,49 g mantendo o tempo automático) — testado literalmente em SQL e no frontend. Nova função interna `set_product_production()` (mesmo padrão zero-grant de `assert_active_user`/`jsonb_whitelist` — nem `service_role` pode chamá-la diretamente), chamada de dentro de duas novas RPCs atômicas exclusivas de `service_role`: `create_product_with_plates()` e `update_product_full()` — ambas reaproveitam `create_product`/`update_product`/`set_product_composition` já existentes (nenhuma lógica duplicada), salvando Produto+plates+filamentos+totais+ajuste+acessórios+embalagens numa única transação, com rollback comprovado em falha parcial (teste 1.6). **Duplicidade de filamento bloqueada só dentro do mesmo plate** — o mesmo filamento em plates diferentes do mesmo Produto é permitido, conforme pedido. **Compatibilidade com Produtos existentes**: migration nova (ainda **não aplicada**) inclui backfill que cria "Plate 1" para todo Produto com composição/peso/tempo prévios (copiando `product_filaments` verbatim, quando existir) e grava os dois novos campos de ajuste com o valor **exato** já existente em `default_weight_grams`/`default_print_time_seconds` — garante efetivo pós-backfill idêntico byte a byte ao pré-backfill em 100% dos casos, sem tocar preço/status/Pedidos. `product_filaments` (legado) **não foi removida nem descontinuada** — continua funcionando; `validate_catalog_composition_for_creation()` (usada na criação de Pedido) foi atualizada via `CREATE OR REPLACE` (mesma assinatura) para aceitar composição vinda de `product_plates` OU de `product_filaments`, preservando integralmente as regras/mensagens/snapshots/idempotência/formas de pagamento já existentes em Pedidos. Edge Function `products/handler.ts` ganhou 2 rotas novas (`POST /products/with-plates`, `PATCH /products/:id/full`) — as 5 rotas existentes preservadas sem alteração de contrato. Frontend: `ProductForm.tsx` **reescrito por completo** — 3 seções (Dados Gerais; Composição — stepper de plates com mínimo 1 e confirmação antes de remover um plate com dados preenchidos, linhas de filamento por plate em gramas, tempo em `hh:mm` via novo `formatSecondsToHHMM()`, totais automáticos com "Ajustar totais"/"Usar cálculo automático"; Acessórios e Embalagem — reaproveita o `ProductCompositionForm.tsx` já existente, inalterado, dentro de um `Dialog` aninhado). **Decisão de escopo explícita e deliberada, não pedida literalmente**: por causa do tamanho da tarefa e do risco de regressão na extensa suíte já existente dos diálogos de edição de `ProductsPage.tsx`, **só o fluxo de CRIAÇÃO foi religado nesta rodada** — `ProductForm.tsx` já suporta `mode='edit'` (componente completo, com hidratação de plates via `plateRowsFrom()`), mas os diálogos antigos de edição (`ProductEditDetailsForm`/`ProductPriceForm`/`ProductCompositionForm`/`FilamentCompositionForm`) permanecem exatamente como estavam, sem nenhuma integração com plates — **editar um Produto existente ainda não usa a nova estrutura**; isso fica como pendência explícita para uma rodada futura dedicada. Testes: `ProductForm.test.tsx` **reescrito** (24 testes, incluindo o exemplo de aceite literal do Suporte PS5 — 134,50 g/04:11 automático e 134,49 g/04:11 após ajuste manual só do peso), `ProductsPage.test.tsx` ajustado (mock `createWithPlates`, payload `plates[0].production_time_seconds` em vez do campo antigo) — suíte completa do frontend **1475/1475 passando**, lint 0 erros, `tsc -b`/`vite build` sem erros, `git diff --check` limpo, busca por segredos no diff sem ocorrências reais, `.env.local` confirmado ignorado nos 3 `.gitignore` do repositório. Novo teste SQL transacional `supabase/tests/product_plates_test.sql` (criação com 1 e com múltiplos plates — incl. o exemplo de aceite do Suporte PS5 —, múltiplos filamentos por plate, duplicidade rejeitada só dentro do mesmo plate, peso inválido rejeitado, rollback de falha parcial, ajuste manual independente por campo, edição substituindo plates por completo, `p_patch` vazio não chama `update_product()`, vínculo de acessórios/embalagens, quantidade inválida rejeitada, compatibilidade da validação CATALOG com `product_plates`, ausência total de reserva/consumo, grants/segurança) — **não executado nesta rodada**, mesma limitação de todas as rodadas anteriores (a migration correspondente ainda não foi aplicada; Deno/Postgres local seguem indisponíveis neste ambiente); revisado estruturalmente por não haver runtime disponível. **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhum Produto ou Pedido existente/remoto alterado, nenhuma alteração em Petlink/dado oficial, nenhuma reserva ou consumo implementados, FS-26-002/005 não restaurados.** Validação manual da nova estrutura **continua pendente** (depende da migration ser aplicada primeiro). Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint local: commits `feat: add plate-based product production structure` (banco e contratos), `feat: rewire product creation with plates in the frontend` (frontend), `test: add product plates SQL coverage and document temporary return to products` (testes e documentação), sem push. |
| 2026-08-29 | **Rodada corretiva — auditoria da entrada anterior reprovada, três bloqueios corrigidos**: (1) edição de Produto agora usa a estrutura por plates; (2) cobertura de testes restaurada acima do total anterior; (3) eliminado o caminho de escrita conflitante da composição. Reconciliação confirmou HEAD `b324e87`, árvore limpa, 27 commits à frente de `feature/customers-orders`, 42/43 migrations sincronizadas (só `20260829160000` pendente, como esperado), `products` ACTIVE v9, `orders` ACTIVE v7 — nenhuma divergência. **1. Edição integrada**: `ProductForm.tsx` (mode="edit") agora é religado de fato em `ProductsPage.tsx` — abrir "Editar produto" carrega, em paralelo, plates+filamentos (`useProductPlates`, novo hook), acessórios/embalagens (`useProductComposition`) e tipos de filamento (`useFilamentTypes`); mostra skeleton enquanto qualquer uma carrega, erro real com "Tentar novamente" se qualquer uma falhar, e só então monta o formulário populado — nunca abre vazio. Salva por `updateProductFull`/`PATCH /products/:id/full`, atomicamente, incluindo Dados Gerais+Composição por plates+ajuste manual+Acessórios/Embalagens; `default_price`/`product_type` são deliberadamente omitidos do payload de edição (nunca fizeram parte do contrato de `update_product_full`) — Tipo do produto vira texto fixo e Preço vira só uma nota apontando para a seção "Preço" (`ProductPriceForm`/`update_product_price`, inalterada, com Motivo/histórico preservados) quando `mode="edit"`; essa decisão foi tomada por segurança explícita, não por omissão (task pedia preservar o contrato de histórico ou manter o preço fora da edição — optou-se pela segunda, mais simples e sem risco). **Produtos legados** (ainda sem nenhum plate, backfill pendente): `plateRowsFromLegacyFilaments()` (novo, `lib/forms/productPlates.ts`) sintetiza "Plate 1" a partir de `product_filaments`+`default_print_time_seconds` — mesma regra do backfill da migration, replicada no cliente — nunca perde peso/tempo/composição existente, nunca inventa um plate vazio quando havia dado real; Produto sem plates E sem nenhuma composição legada nasce com 1 plate vazio (mesmo comportamento já usado na criação). Itens de filamento/acessório/embalagem inativos já vinculados continuam visíveis (nunca somem), bloqueiam salvar até remoção consciente, nunca podem ser escolhidos como seleção nova — comportamento já existente, agora também exercitado no fluxo de edição real. **2. Fonte autoritativa definida e documentada** (cabeçalho da migration `20260829160000` ampliado): `product_plates`/`product_plate_filaments` é a ÚNICA fonte autoritativa da composição de produção; `product_filaments` (legado) é só projeção de leitura de compatibilidade, nunca mais escrita de forma independente — o antigo diálogo "Composição de filamentos" (`FilamentCompositionForm`, escrita direta e isolada em `product_filaments` via `set_product_filaments`) foi **removido de `ProductsPage.tsx`** (o componente e seu teste dedicado permanecem no repositório, órfãos/não religados a nenhuma tela — não apagados, para não reduzir cobertura de testes sem necessidade; candidatos a remoção física numa rodada futura dedicada). `ProductDetailPage.tsx` (Ficha Técnica) também foi corrigida no mesmo espírito — o card "Filamentos" agora lê `product_plates` (um card "Filamentos — Plate N" por plate, nunca uma tabela única que colidiria ao repetir o mesmo tipo em plates diferentes) com o mesmo fallback legado de "Plate 1" só quando não há nenhum plate — antes desta correção, essa Ficha continuaria mostrando `product_filaments` desatualizado para qualquer Produto já editado pela nova estrutura, exatamente a divergência entre telas que a auditoria apontou. Nenhuma sincronização automática entre as duas tabelas foi necessária: como não sobra nenhum caminho de escrita independente em `product_filaments`, não há como as duas fontes divergirem depois de um Produto passar a ter plates. **3. Cobertura de testes restaurada e ampliada**: `ProductForm.test.tsx` — todos os testes de `cc3199e` que haviam desaparecido na rodada anterior foram individualmente restaurados (preço bancário completo, tempo de produção, personalização, categoria/Beauty-Office, Tipo do produto, edição completa) ou adaptados com justificativa explícita quando o mecanismo mudou de fato (peso solto do Produto → peso por linha de filamento/ajuste manual; tempo com blur-normalize → tempo validado no salvar, mesmo padrão já usado por peso/filamento nesta versão do formulário; preço/Tipo do produto editáveis na edição → não editáveis mais, por decisão desta própria rodada) — nenhum teste foi apagado sem um substituto equivalente registrado; ganhou também testes novos de item inativo vinculado (filamento/acessório) e de Acessórios/Embalagens com testes separados por acessório e por embalagem (o pedido explícito de não testar só acessório sob um título genérico) — **24 → 89 testes**. `ProductsPage.test.tsx` — o antigo bloco "Filamentos" (10 testes do diálogo removido) foi substituído por 2 testes confirmando que a seção não existe mais ali e que salvar Acessórios/Embalagens no diálogo rápido nunca aciona nenhuma escrita legada; a seção "Editar produto" ganhou 9 testes (pré-carregamento completo, ausência do campo Preço, salvamento atômico com payload exato, skeleton de carregamento, erro com retry, fallback legado, Produto sem nenhuma composição, múltiplos plates, ajuste manual pré-existente, item inativo bloqueando salvar). `ProductDetailPage.test.tsx` ganhou 2 testes novos (plates como fonte autoritativa mesmo com `product_filaments` divergente presente; erro ao carregar plates isolado com retry próprio). **Suíte completa do frontend: 1475 → 1543/1543 passando** (acima dos 1513 do checkpoint anterior à rodada reprovada), lint 0 erros (7 avisos pré-existentes), `tsc -b`/`vite build` sem erros, `git diff --check` limpo, busca por segredos sem ocorrências reais, `.env.local` confirmado ignorado. `supabase/tests/product_plates_test.sql` ganhou 2 cenários (2.3 — falha parcial na edição desfaz tudo, nome/composição revertem ao estado anterior; 2.4 — criar/editar plates nunca grava em `product_filaments`, prova direta da ausência de divergência) — **ainda não executado nesta rodada**, mesma limitação de sempre (migration pendente, sem runtime local); revisado estruturalmente (17 blocos `do $$`/`end $$;` balanceados). **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhum Produto ou Pedido existente/remoto alterado, nenhuma alteração em Petlink/dado oficial, nenhuma reserva ou consumo implementados, FS-26-002/005 não restaurados, nenhum comando destrutivo de Git usado, nenhum commit anterior reescrito.** Validação manual da estrutura por plates (criação e agora também edição) **continua pendente** (depende da migration ser aplicada primeiro). Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint local: commits `feat: complete plate-based product editing` (frontend) e `test: restore product regression coverage` (banco/testes/documentação), sem push. |
| 2026-08-29 | **Rodada corretiva 2 — definição CONCLUÍDA da fonte autoritativa da composição, antes de aplicar a migration.** A rodada anterior só tinha corrigido o lado FRONTEND (edição integrada, Ficha Técnica lendo plates); uma auditoria nova encontrou que `validate_catalog_composition_for_creation()` (a validação usada na criação de Pedidos) continuava aceitando `product_plate_filaments` OU `product_filaments` (legado) como alternativas — permitindo o cenário: Produto legado com linha em `product_filaments` → migration copia para Plate 1 → usuário remove toda a composição dos plates pela edição → `product_filaments` permanece intocado (nenhuma escrita a apaga) → a validação encontra a linha antiga e aprova o Produto incorretamente. A rota `PATCH /products/:id/filaments` também continuava chamando a RPC `set_product_filaments` (migration `20260827113000`, **já aplicada e ativa no remoto**), uma segunda fonte de escrita operacional real, não só teórica. Reconciliação confirmou HEAD `5f7c8f1`, árvore limpa, 29 commits à frente de `feature/customers-orders`, 42/43 migrations sincronizadas, `products` v9/`orders` v7 inalterados — nenhuma divergência. **Correção 1 — validação de Pedidos**: `validate_catalog_composition_for_creation()` (dentro da própria migration `20260829160000`, ainda não aplicada) foi **movida para depois do backfill e da verificação do backfill no arquivo** (antes vinha antes) e seu corpo passou a checar EXCLUSIVAMENTE `product_plate_filaments` via `product_plates` do Produto — `product_filaments` deixou de ser aceita como alternativa. Assinatura, `SECURITY DEFINER`, `search_path=''`, mensagem `ORDER_CATALOG_MISSING_COMPOSITION:` com os nomes reais, classificação CATALOG/SPOT/CUSTOM, formas de pagamento, idempotência, rollback, histórico, autenticação e todos os demais grants preservados byte a byte. **Correção 2 — segurança do backfill**: 4 verificações novas adicionadas ao bloco de verificação do backfill (que já existia) — 8.5 (toda linha de `product_filaments` tem a correspondente copiada, nenhum Produto legado esquecido), 8.6 (peso copiado bate exatamente com o original, nenhuma divergência), 8.7 ("nenhum plate órfão": contagem de Plate 1 criados bate exatamente com a contagem de Produtos elegíveis pelo mesmo critério do próprio backfill) e 8.8 (ajuste manual copiado preserva byte a byte o peso/tempo efetivo anterior) — qualquer uma aborta a migration inteira (`raise exception`, mesma transação única do arquivo) antes que a nova validação, mais estrita, chegue a ficar operacional. **Correção 3 — rota e RPC legadas**: `PATCH /products/:id/filaments` (`supabase/functions/products/handler.ts`, arquivo local ainda não publicado) **nunca mais chama `set_product_filaments`** — responde sempre com um erro de negócio claro (`PRODUCT_FILAMENTS_ROUTE_RETIRED:`, 409) depois de autenticar (rota continua existindo e exigindo auth — nunca 404, roteamento preservado); a RPC `set_product_filaments` perde o `EXECUTE` de `service_role` na própria migration (defesa em profundidade, nível banco — mesmo se algo além da Edge Function tentasse chamá-la diretamente). Nem a função nem a tabela `product_filaments` foram removidas ou tiveram dado apagado — só a capacidade de escrita operacional independente. `PATCH /products/:id/full` (`update_product_full`) continua sendo o único caminho autorizado, inclusive para Produtos com múltiplos plates (que a RPC legada nunca soube representar). **Leituras legadas**: o fallback do frontend para `product_filaments` (introduzido na rodada anterior, `plateRowsFromLegacyFilaments`/`ProductDetailPage.tsx`) teve seus comentários reforçados explicitando que é uma proteção TRANSITÓRIA, não o fluxo normal — deixa de ser alcançado, na prática, para qualquer Produto que já tinha composição/peso/tempo antes da migration, assim que o backfill rodar; nenhuma mudança de lógica foi necessária no frontend (já lia plates corretamente desde a rodada anterior). **Limpeza de código**: `FilamentCompositionForm.tsx`/`.test.tsx` (diálogo antigo, já sem nenhum import/JSX consumidor desde a rodada anterior, confirmado por busca no repositório) foi **listado como código morto mas PRESERVADO nesta rodada** — removê-lo reduziria a suíte abaixo do piso de 1543 testes exigido nesta própria rodada; fica registrado como candidato explícito para uma rodada de limpeza futura dedicada. `useProductFilaments`/`lib/api/productFilaments.ts` continuam em uso real (leitura do fallback legado, 2 chamadores) — só o método `.save`/`updateProductFilaments` ficou sem nenhum chamador de UI (a RPC que ele chamava também está desativada no banco a partir desta migration); preservado pela mesma razão. `validateFilamentCompositionItems` (`handler.ts`) segue exportada e testada por `handler.test.ts`, mesmo sem nenhuma rota HTTP chamando-a mais — preservada. Testes: `supabase/tests/product_plates_test.sql` ganhou a Seção 3.3 (composição só em `product_filaments`, sem nenhum plate, agora REJEITADA), 3.4 (backfill simulado manualmente para um Produto de teste — replica a fórmula do bloco real da migration — prova peso/filamento preservados E validação passando a usar plates), **3.5 — o CENÁRIO CRÍTICO pedido explicitamente**: Produto com composição real nos plates → usuário esvazia os plates (`update_product_full` com `filaments: []`) → linha antiga isolada em `product_filaments` (nunca tocada) → criação de Pedido **corretamente BLOQUEADA** (`ORDER_CATALOG_MISSING_COMPOSITION:`), e 3.6 (a mesma operação de esvaziar os plates nunca alterou nem apagou a linha legada — continua intacta); Seção 4 ganhou 4.3 (RPC legada sem nenhum grant) e 4.4 (corpo real da nova validação, via `pg_get_functiondef`, comprovadamente sem referência a `product_filaments`, com referência a `product_plate_filaments`, tabela legada confirmada preservada). **"Falha no backfill desfaz toda a migration" registrada como limitação estrutural, não testável isoladamente deste arquivo** — o backfill é um bloco `DO` embutido na própria migration (não uma função reutilizável), então só é exercitável de fato aplicando a migration inteira; a atomicidade vem de todo o arquivo rodar numa única transação (convenção do Supabase CLI), já comprovada pelas verificações internas 8.1-8.8/11.1-11.4 da própria migration (cada uma aborta com `raise exception` se algo divergir) — os testes 3.4/3.5 replicam a FÓRMULA do backfill manualmente para provar que ela está correta, não a atomicidade da migration inteira. **Nenhum teste SQL executado nesta rodada** (migration ainda pendente, sem runtime local) — revisado estruturalmente (22 blocos `do $$`/`end $$;` balanceados, 44 marcadores `$$`). `supabase/functions/products/handler.test.ts` ganhou 3 testes novos (POST /products/with-plates e PATCH /products/:id/full exigem auth 401, antes nunca testados neste arquivo; PATCH /products/:id/filaments confirmado continuar respondendo com 401 sem auth — nunca 404 — provando que o roteamento foi preservado mesmo com a rota descontinuada para escrita) — **não executado nesta rodada** (Deno não instalado, mesma limitação de todas as rodadas anteriores). Frontend: só comentários atualizados (nenhuma lógica nova necessária — já lia plates corretamente) em `ProductsPage.tsx`, `ProductDetailPage.tsx` e `lib/forms/productPlates.ts`; suíte completa **1543/1543 passando** (inalterada, como esperado de uma rodada sem mudança de comportamento no frontend), lint 0 erros, `tsc -b`/`vite build` sem erros, `git diff --check` limpo, busca por segredos sem ocorrências reais, `.env.local` confirmado ignorado. **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhum Produto ou Pedido existente/remoto alterado, nenhuma alteração em Petlink/dado oficial, nenhum dado legado apagado (`product_filaments` preservada intacta, com todas as linhas, em todos os cenários testados), nenhuma reserva ou consumo implementados, FS-26-002/005 não restaurados, nenhum comando destrutivo de Git usado, nenhum commit anterior reescrito.** A definição da fonte autoritativa da composição está **CONCLUÍDA** em todos os três níveis (escrita, validação de Pedidos, RPC/rota legada) dentro da migration pendente — falta só aplicá-la ao remoto (fora do escopo desta rodada) e então rodar a validação manual completa. Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint local: commit `fix: make product plates the authoritative composition`, sem push. |
| 2026-08-29 | **Rodada de limpeza — remoção do fluxo frontend legado de escrita de filamentos, comprovadamente órfão.** **Correção documental obrigatória desta entrada**: a entrada anterior declarava "`FilamentCompositionForm.tsx`/`.test.tsx`... listado como código morto mas PRESERVADO nesta rodada — removê-lo reduziria a suíte abaixo do piso de 1543 testes exigido nesta própria rodada" — uma auditoria posterior apontou, corretamente, que código e testes comprovadamente órfãos não devem ser mantidos só para preservar uma contagem numérica; essa justificativa foi **retirada** (a entrada anterior não foi reescrita — convenção já estabelecida neste histórico — só corrigida por esta entrada nova). Reconciliação confirmou HEAD `9987516`, árvore limpa, 30 commits à frente de `feature/customers-orders`, 42/43 migrations sincronizadas, `products` v9/`orders` v7 inalterados — nenhuma divergência. **Auditoria completa de exportações**: `FilamentCompositionForm.tsx` (zero import/JSX consumidor, só comentários históricos) — código totalmente órfão, **removido** junto com `FilamentCompositionForm.test.tsx` (11 testes de comportamento de um componente sem nenhuma tela que o monte — teste de código morto, removido). `lib/forms/productFilamentComposition.ts` — 4 dos seus 9 exports (`filterSelectableFilamentTypes`, `filamentTypeLabel`, `findFilamentTypeById`, `isFilamentRowInactive`) continuavam reutilizados por `productPlates.ts` (helper ainda reutilizado pelos plates); os outros 5 (`FilamentCompositionRow`, `nextFilamentRowKey`, `filamentRowsFrom`, `hasInactiveFilamentSelection`, `validateFilamentRows`/`ValidatedFilamentRows`) só serviam ao formulário removido — escrita legada sem consumidor. Os 4 helpers ainda necessários foram **movidos para dentro de `productPlates.ts`** (seu único consumidor real, nome já coerente — nenhum módulo compartilhado novo foi necessário); o arquivo `productFilamentComposition.ts` foi **removido** por inteiro. `productFilamentComposition.test.ts` (20 testes: 10 cobriam os 4 helpers movidos, 10 cobriam os 5 exports órfãos) — os 10 testes de comportamento ainda operacional foram **movidos** (não recriados do zero) para o novo `productPlates.test.ts` (11 testes — 3 casos de `hasInactiveFilamentSelectionInPlates`, o wrapper público já existente, substituem os 3 casos que testavam `isFilamentRowInactive` diretamente, uma função interna não exportada — mesma cobertura, adaptada ao ponto de entrada público real); os 10 testes de código morto foram removidos junto com o código. `lib/api/productFilaments.ts` — `listProductFilaments` é leitura transitória necessária ao fallback (preservada); `updateProductFilaments`/`ProductFilamentItemInput`/`UpdateProductFilamentsInput` eram escrita legada sem consumidor (a RPC que chamavam, `set_product_filaments`, já não tem `EXECUTE` operacional desde a migration pendente) — **removidos**; `productFilaments.test.ts` perdeu os 2 testes que cobriam essa escrita, manteve o teste de leitura. `hooks/useProductFilaments.ts` — `status`/`filaments`/`isLoading`/`error`/`retry` são a leitura transitória necessária ao fallback de Produtos legados (preservados, ainda usados por `ProductsPage.tsx`/`ProductDetailPage.tsx`); `save` (chamava `updateProductFilaments`) só era usado pelo fluxo removido — **removido**; `useProductFilaments.test.ts` perdeu o teste do `save()`, manteve os 5 testes de leitura/erro/corrida/retry. `handler.ts`/`handler.test.ts` (Edge Function) e a RPC/tabela no banco **não foram tocados nesta rodada** — já haviam sido corretamente desativados/preservados na rodada anterior, fora do escopo desta limpeza (só frontend). **Contagem de testes**: total antes da limpeza **1543**; nenhum teste artificial criado para substituir a quantidade removida — os 11 testes movidos para `productPlates.test.ts` são a MESMA cobertura de comportamento reutilizado, relocada junto com o código, não uma reposição numérica. Removidos: 11 (`FilamentCompositionForm.test.tsx`, deletado) + 20 (`productFilamentComposition.test.ts`, deletado) + 2 (`productFilaments.test.ts`, escrita legada) + 1 (`useProductFilaments.test.ts`, `save()`) = 34; readicionados por relocação (não novos): 11 (`productPlates.test.ts`); líquido: **-23**. **Total final: 1520/1520 passando** — acima do checkpoint funcional de 1513 citado na auditoria (a estimativa de "~1532" da própria auditoria não previa os 20 testes órfãos de `productFilamentComposition.test.ts`, encontrados só nesta rodada mais profunda; 1520 ainda satisfaz com folga o critério real: "total final igual ou superior a 1513"). Confirmações funcionais pós-limpeza (por busca no repositório): nenhuma tela importa `FilamentCompositionForm` (só comentários históricos); nenhum código frontend chama `updateProductFilaments` nem a rota `PATCH /products/:id/filaments`; edição usa `updateProductFull`; criação usa `createProductWithPlates`; Ficha Técnica lê `product_plates`; fallback legado (`useProductFilaments`) permanece somente leitura; Produtos com múltiplos plates continuam representados por N cards/plates (nunca achatados); histórico de preços e Acessórios/Embalagens não tocados nesta rodada. Validação: suíte completa **1520/1520**, lint 0 erros (7 avisos pré-existentes), `tsc -b`/`vite build` sem erros, `git diff --check` limpo, busca por segredos sem ocorrências reais, `.env.local` confirmado ignorado. **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhum Produto ou Pedido existente/remoto alterado, nenhuma alteração em Petlink/dado oficial, nenhum dado ou objeto legado do banco removido (`product_filaments`/`set_product_filaments` preservados intactos — só o frontend foi limpo nesta rodada), nenhuma reserva ou consumo implementados, nenhum comando destrutivo de Git usado, nenhum commit anterior reescrito.** Validação manual da estrutura por plates **continua pendente** (depende da migration ser aplicada primeiro). Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint local: commit `refactor: remove legacy filament write frontend`, sem push. |
| 2026-08-29 | **Migration `20260829160000_add_product_plates_structure.sql` APLICADA ao Supabase remoto e Edge Function `products` republicada (v9→v10) — estrutura produtiva por plates agora ATIVA em produção.** Reconciliação confirmou HEAD `765bc9e`, árvore limpa, 31 commits à frente de `feature/customers-orders`, 42/43 migrations sincronizadas (só a pendente esperada), `products` v9, `orders` v7, FS-26-002/003/004/005 ausentes, FS-26-006 preservado, Petlink (cliente/empresa) ausente — nenhuma divergência. **Snapshot somente leitura antes da migration**: 7 Produtos (Cubo Infinito, Estrela Sensorial, Petlink [produto, nome coincidente — nunca o cliente/empresa oficial], Porta Anel Kettlebell [2 variantes], Suporte para Tablet, "teste - Produtos"); só "teste - Produtos" tinha linhas em `product_filaments` (3: 12.50g/8.75g/10.00g); `product_accessories`/`product_packaging`: Cubo Infinito e Porta Anel Kettlebell - Personalizado com 1 cada, demais zero; 3 Pedidos (FS-26-001 DELIVERED/PAID, FS-26-006 DELIVERED/WAITING_PAYMENT, FS-26-008 QUOTE/PAID), 3 `order_items`. **Auditoria integral da migration** confirmou a ordem exigida (colunas/tabelas → constraints/índices → funções necessárias ao backfill → backfill → verificações pós-backfill → validação CATALOG redefinida → revogação da escrita legada → verificações finais) sem nenhuma função redefinida antes de suas dependências. **Correção de 2 bugs reais encontrados só nesta execução** (nunca detectáveis antes por revisão estrutural, sem runtime): `supabase/tests/order_initial_status_test.sql` criava seus 2 fixtures "COM composição" via `set_product_filaments` (legado) — sob a nova validação plates-only isso os tornaria "sem composição"; substituído por Plate 1 + `product_plate_filaments` equivalente (mesmo peso/filamento), preservando `default_weight_grams`/`default_print_time_seconds` já fixados. `supabase/tests/bloco1_integration_test.sql` tinha a pendência conhecida (Produto CATALOG sem nenhuma composição, cuja fixture nunca tinha sido atualizada) — corrigida com um Plate 1 + composição real logo após a criação do Produto. **Dry-run confirmou exclusivamente a migration pendente; aplicada via `npx supabase db push --linked` — 43/43 migrations agora sincronizadas.** **Verificação do backfill real, Produto por Produto, comparada byte a byte ao snapshot**: os 6 Produtos com peso/tempo/composição prévios (todos exceto "Cubo Infinito", sem nenhum dos dois) ganharam exatamente 1 Plate 1 cada, com `production_time_seconds` = tempo anterior, e os 2 ajustes manuais (`production_weight_manual_override_grams`/`production_time_manual_override_seconds`) copiados verbatim do `default_weight_grams`/`default_print_time_seconds` anterior — todos os 6 valores efetivos IDÊNTICOS ao snapshot; "teste - Produtos" (weight=1000.00/time=3600000, muito maior que a soma real dos filamentos, 31.25g) confirmou o mecanismo de ajuste manual funcionando exatamente como projetado — o efetivo permanece 1000.00/3600000 mesmo com a soma automática divergente; "Cubo Infinito" corretamente NÃO recebeu nenhum plate (0 plates, inalterado). `product_filaments` (legado) confirmada com as MESMAS 3 linhas, mesmos pesos, mesmos `filament_type_id` — intocada. `product_accessories`/`product_packaging` idênticos ao snapshot. Contagem de Produtos (7) e Pedidos (3) idêntica antes/depois — nenhum Produto ou Pedido criado, alterado ou excluído; `order_items` (3) inalterado; FS-26-002/003/004/005 seguem ausentes, FS-26-006 preservado, Petlink (cliente/empresa) segue ausente. Grants confirmados: `set_product_filaments` sem `EXECUTE` para nenhum papel (nem `service_role`, que era como a Edge Function a chamava antes); `validate_catalog_composition_for_creation` continua zero grants; `create_product_with_plates` exclusiva de `service_role`; `product_filaments` (tabela) confirmada existente, nenhuma linha apagada. **`supabase/tests/product_plates_test.sql` executado contra o remoto (`BEGIN...ROLLBACK`)**: primeira execução 22 PASS/1 FAIL — 2 bugs de sintaxe/referência nunca detectados sem runtime (`select value::uuid from public.customers` em 4 lugares, deveria ser `select id`; `has_function_privilege` chamado com uma tabela em vez de `has_table_privilege`) — corrigidos, reexecutado: **23 PASS, 0 FAIL**, incluindo o cenário crítico (3.5: plates esvaziados + `product_filaments` com linha antiga → Pedido corretamente BLOQUEADO) e o backfill simulado (3.4: peso copiado preserva 22.75 exatamente). **`order_initial_status_test.sql` executado**: **24 PASS, 0 FAIL** já na primeira execução após a correção do fixture. Ambos com `ROLLBACK` confirmado e zero resíduo específico desta rodada (uma cliente "TESTE - Cliente" e 4 tipos de filamento "TESTE%" encontrados são resíduo de rodadas/sessões anteriores, com timestamps anteriores a esta execução — não criados por nenhum teste desta rodada, confirmado por nome/data). **`bloco1_integration_test.sql` executado até o fim** (não abortou mais — a correção da fixture de composição funcionou) **com 147 PASS / 35 FAIL** — as falhas não são mais sobre composição ausente; são causadas por uma incompatibilidade estrutural pré-existente e mais profunda: o Pedido CATALOG-only da fixture única deste arquivo agora nasce direto em `IN_PRODUCTION_QUEUE` (regra de classificação automática, migration `20260829150000`, aplicada 2 rodadas atrás), e dezenas de testes das Seções 3–13 assumem que ele começa em `QUOTE` (edição via `update_quote_order`, fluxo de aprovação) — não relacionado ao trabalho de plates desta rodada. Apresentada a decisão ao usuário (mesmo precedente já estabelecido 2 rodadas atrás para este mesmo arquivo): escolhido **pular `bloco1_integration_test.sql` e publicar com base nos outros dois testes** — a correção completa desse arquivo (reescrever as ~35 falhas) fica registrada como pendência para uma rodada futura dedicada, sem relação com plates. Zero resíduo confirmado mesmo na execução com falhas (todas capturadas por `exception when others`, `ROLLBACK` final intacto). **Publicada exclusivamente `products` (v9→v10)** — as 13 demais Edge Functions preservaram versão exata (`orders` continua v7). Smoke tests HTTP reais: `OPTIONS /products` → 204; `POST /products`, `POST /products/with-plates`, `PATCH /products/:id/full` sem autenticação → 401 (`Header Authorization ausente.`, sem detalhe interno); `PATCH /products/:id/filaments` sem autenticação → 401 (rota continua respondendo e exigindo auth — nunca 404 — antes mesmo de chegar ao erro de negócio de rota descontinuada); rota desconhecida → 404; contagem de Produtos confirmada idêntica antes/depois dos smoke tests (nenhum dado criado). **A estrutura produtiva por plates e a fonte autoritativa única estão agora ATIVAS em produção**: `product_plates`/`product_plate_filaments` validam Pedidos CATALOG; `product_filaments` (legado) preservada só como histórico, sem nenhuma escrita operacional possível. **Nenhum Produto ou Pedido oficial excluído, nenhum preço alterado, Acessórios/Embalagens preservados integralmente, nenhuma outra Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink, FS-26-002/005 não restaurados, nenhuma reserva ou consumo implementados** (Pedidos que entram em Fila de produção, automática ou manualmente, ainda não reservam nem baixam nenhum estoque — mecanismo continua planejado só para o Incremento 6B). **Validação manual da estrutura por plates (criação, edição, múltiplos plates, ajuste manual, Acessórios/Embalagens) continua pendente** — só validado por SQL/smoke tests até aqui. Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint documental desta entrada: commit `docs: record product plates deployment`, sem push. |
| 2026-08-29 | **Validação manual INTEGRAL do usuário aprovada para a estrutura produtiva por plates**, exercitada pela interface real contra o ambiente remoto (`products` v10, migration `20260829160000` já aplicada nas entradas anteriores). Diferente da validação parcial registrada durante a rodada de criação (só o fluxo de criação existia então), esta rodada cobre também **edição**, hoje já religada em `ProductsPage.tsx`. Checklist aprovado pelo usuário: (1) **criação de Produto com dois plates** — aprovado; (2) **composição própria de filamentos/cores por plate** — aprovado, cada plate mantendo sua própria composição independente; (3) **soma automática de peso e tempo** a partir dos plates — aprovado; (4) **ajuste manual dos totais** ("Ajustar totais") — aprovado, com o valor calculado automaticamente preservado e exibido separadamente do valor efetivo ajustado (mesmo comportamento do exemplo de aceite Suporte PS5 já coberto por SQL/frontend, agora confirmado manualmente); (5) **seleção e quantidades de Acessórios e Embalagens** — aprovado; (6) **Ficha Técnica do Produto exibindo os plates separadamente** (um card "Filamentos — Plate N" por plate, nunca uma composição achatada) — aprovado; (7) **edição de um Produto existente e persistência das alterações** — aprovado; (8) **confirmação antes de remover um plate preenchido** (nunca perde composição digitada silenciosamente) — aprovado; (9) **Produto `TESTE` usado na validação removido pelo próprio usuário, sem nenhum resíduo** — aprovado. Com isso, **a estrutura por plates está integralmente validada no ambiente remoto** — criação, edição, múltiplos plates, composição por plate, totais automáticos, ajuste manual, Acessórios/Embalagens e a Ficha Técnica, todos exercitados de ponta a ponta pelo usuário contra o backend real, não só por teste automatizado. `product_plates`/`product_plate_filaments` permanecem a fonte autoritativa única da composição de produção; `product_filaments` permanece só como dado legado (nenhuma escrita operacional possível, RPC sem `EXECUTE`, nenhuma linha apagada). Ressalvas explícitas que continuam valendo, sem nenhuma alteração nesta entrada: **reserva e consumo automático de estoque continuam sem nenhuma linha de código** — a entrada de um Pedido em Fila de produção (`IN_PRODUCTION_QUEUE`), automática ou manual, ainda não reserva nem baixa nenhum estoque (filamento, acessório ou embalagem); esse mecanismo continua planejado só para o Incremento 6B, ainda não implementado. A pendência de `supabase/tests/bloco1_integration_test.sql` (incompatibilidade estrutural pré-existente com a regra de classificação automática de Pedidos, não relacionada a plates) **permanece em aberto exatamente como registrada na entrada anterior — 147 PASS / 35 FAIL**, sem nenhuma tentativa de correção nesta rodada. Módulo 3 (Estoque e Inventário) continua **NÃO concluído** — esta validação cobre a estrutura de composição de Produtos, não o módulo de Estoque como um todo; **nenhuma conclusão do módulo Estoque é declarada por esta entrada**. **Nenhum código (frontend ou backend) alterado nesta rodada, nenhum teste modificado, nenhum dado modificado, nenhuma migration criada ou aplicada, nenhuma Edge Function publicada, nenhum push, nenhum deploy de frontend, nenhuma alteração em Petlink** — só documentação, registrando fatos já confirmados pelo usuário. Percentual macro **não alterado**. Checkpoint local: commit `docs: record product plates manual validation`, sem push. |
| 2026-08-30 | **Reorganização de navegação, novo layout de Produto, categorias múltiplas, peso direto por plate e escolha de cores/filamentos movida do Produto para o Pedido — tudo LOCAL/PENDENTE, migration `20260829180000_add_categories_plate_weight_and_order_colors.sql` ainda NÃO aplicada ao ambiente remoto.** **(1) Navegação:** ordem oficial dos módulos passa a ser Clientes, Empresas, Produtos, Pedidos, Estoque (só a lista de exibição em `AppLayout.tsx`; rotas/permissões inalteradas). **(2) Layout de Novo/Editar Produto:** diálogo alargado (~95vw, até 1400px), seções Dados Gerais/Composição/Acessórios e Embalagens em cartões visualmente distintos (borda + fundo na paleta da marca), plates em grid de 2 colunas no desktop. **(3) Múltiplas categorias:** nova tabela `product_categories` (N por Produto, com posição) é a fonte autoritativa a partir desta migration; `products.category` vira espelho DERIVADO da categoria de posição 1 (escrito só por `set_product_categories`, nunca uma segunda fonte); backfill migra a categoria única existente de cada Produto para `position=1`, sem perder nenhuma. **(4) Filamentos/cores retirados do Produto:** o cadastro e a edição de Produto não têm mais nenhuma seleção de filamento ou cor — `product_plates.weight_grams` passa a ser uma coluna DIRETA (informada pelo usuário), substituindo a soma de `product_plate_filaments` (que vira legada, preservada só como histórico, sem nenhuma escrita operacional a partir de agora); backfill de peso por plate aborta explicitamente (nunca inventa uma distribuição) se encontrar múltiplos plates sem dado legado suficiente para reconstituir o peso individual. **(5) Cores/filamentos migraram para o Pedido:** cada item CATALOG de um Pedido ganha, na criação, um snapshot IMUTÁVEL da estrutura de plates do Produto (`order_item_plates` — peso/tempo congelados no momento da criação; editar o Produto depois nunca altera um Pedido já criado); a escolha de cor passa a ser por UNIDADE e por PLATE (`order_item_unit_plate_filaments`), permitindo múltiplas cores no mesmo plate, a mesma cor em unidades diferentes e cores diferentes por unidade; uma seleção já existente sobrevive mesmo que o filamento escolhido fique inativo depois, mas uma seleção NOVA sempre exige filamento ativo. **(6) Cores sempre opcionais na criação:** um Pedido só de itens CATALOG/SPOT continua entrando direto na Fila de produção (`IN_PRODUCTION_QUEUE`) mesmo com cores pendentes ou parciais — a validação de criação (`validate_catalog_production_structure_for_creation`, substituindo a versão que checava composição de filamento) agora exige só que cada Produto CATALOG tenha ao menos 1 plate cadastrado, nunca filamento. **(7) Gate de início real de produção:** `change_order_status` ganha uma checagem nova (`validate_order_production_readiness`) que bloqueia SÓ a transição `IN_PRODUCTION_QUEUE` → `IN_PRODUCTION` enquanto qualquer item CATALOG tiver unidade/plate sem nenhum filamento ativo selecionado, identificando produto/item/unidade/plate pendente na mensagem (`ORDER_PRODUCTION_COLORS_PENDING:`); nenhuma outra transição, o cancelamento, as aprovações, o histórico ou o comportamento de SPOT/CUSTOM foram alterados. **(8) Completar cores depois da criação:** nova rota `PATCH /orders/:id/production-colors` (RPC `update_order_item_production_colors`, substituição completa do conjunto de cores dos itens CATALOG do Pedido, nunca altera `order_status`) — usada tanto pela edição de itens em Orçamento quanto por um cartão dedicado "Cores e filamentos" em `OrderManagementPanel.tsx`, disponível para qualquer Pedido não `DELIVERED`/`CANCELLED`. **Frontend:** `ProductForm.tsx` reescrito (categorias em chips + "Outra categoria", plates com peso direto, sem nenhum seletor de filamento); `ProductsPage.tsx`/`ProductDetailPage.tsx` atualizados (filtro/busca/ordenação por QUALQUER categoria vinculada; Ficha Técnica mostra todas as categorias e uma tabela de Plates com peso/tempo diretos, sem mais nenhuma tabela de Filamentos); `OrderForm.tsx` ganha a seção colapsável "Cores e filamentos" (componente novo `ProductionColorsPicker.tsx`, reaproveitado também em `OrderManagementPanel.tsx`) — organizada Unidade → Plate, indicador Completo/Pendente, "Aplicar cores da Unidade 1 a todas" com confirmação antes de sobrescrever, aviso não bloqueante ("As cores podem ser definidas agora ou antes de iniciar a produção."), aumentar quantidade nunca copia cor automaticamente, diminuir pede confirmação antes de descartar uma unidade já colorida. **Validação técnica desta rodada:** `npx tsc -b`, `npm run lint` (0 erros, só os mesmos avisos pré-existentes de fast-refresh) e a suíte `vitest` completa do frontend (1527 testes, incluindo os novos de `ProductionColorsPicker`/seção de cores em `OrderForm`/cartão em `OrderManagementPanel`) executados e verificados limpos contra o código local; novo arquivo de teste SQL transacional (`supabase/tests/product_categories_plate_weight_order_colors_test.sql`, cobertura representativa — não exaustiva, decisão explícita dada a extensão deste incremento) escrito seguindo a mesma disciplina `BEGIN`/`ROLLBACK`/dados só `TESTE%` já usada no projeto, mas **não pôde ser executado nesta rodada** (migration ainda não aplicada — mesma limitação já registrada em toda rodada anterior que cria migration nova); `supabase/tests/product_plates_test.sql` ganhou uma nota explícita marcando suas Seções 1–2 como estruturalmente defasadas por esta migration (assinaturas de `create_product_with_plates`/`update_product_full` mudaram), sem reescrita completa nesta rodada (custo desproporcional frente ao novo arquivo dedicado). **A pendência de `supabase/tests/bloco1_integration_test.sql` (35 falhas estruturais pré-existentes, incompatibilidade com a classificação automática de status) permanece EXATAMENTE como estava, sem nenhuma tentativa de correção — não relacionada a este incremento.** **Reserva e consumo automático de estoque continuam sem nenhuma linha de código** (fora de escopo explícito desta rodada). **Nenhum dado remoto foi tocado: a migration não foi aplicada, nenhuma Edge Function foi publicada, nenhum frontend foi deployado, nenhum push foi feito, nenhum Produto ou Pedido oficial foi alterado, Petlink não foi tocado.** **Validação manual do usuário contra a interface real continua pendente** — esta entrada registra só o trabalho local (banco/contratos/frontend/testes), nunca uma disponibilidade em produção. Módulo 1 permanece operacional com a ressalva de que este incremento ainda não foi aplicado ao remoto; Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** por esta entrada (critério já estabelecido: só conta quando aplicado, publicado e validado manualmente no ambiente real). Checkpoint local: commits `feat: categorias multiplas, peso direto no plate e cores no pedido (banco e contratos)`, `feat: reordena navegacao e adapta Produtos ao novo contrato (categorias/plates)` e `feat: adiciona secao "Cores e filamentos" no Pedido (criacao e pos-criacao)`, sem push. |
| 2026-08-30 | **Rodada corretiva sobre a entrada anterior — 4 bloqueios encontrados na revisão e corrigidos, tudo ainda LOCAL/PENDENTE (migration `20260829180000` continua sem aplicar ao remoto).** **(1) Regra definitiva de congelamento de cores:** a edição de cores por unidade/plate (`update_order_item_production_colors`) passa a usar uma allow-list explícita — permitida somente em `QUOTE`, `WAITING_APPROVAL`, `APPROVED` e `IN_PRODUCTION_QUEUE` (antes do início real da produção); bloqueada em `IN_PRODUCTION`, `WAITING_DELIVERY`, `DELIVERED` e `CANCELLED` (marcador estável `ORDER_PRODUCTION_COLORS_FROZEN:`, mapeado em `_shared/errors.ts`). Nenhuma alteração residual ocorre numa tentativa bloqueada; uma seleção já existente continua visível mesmo depois do congelamento. O Pedido continua podendo nascer e entrar na Fila de produção (`IN_PRODUCTION_QUEUE`) sem nenhuma cor ou com cores parciais — cores só se tornam obrigatórias no momento da transição `IN_PRODUCTION_QUEUE` → `IN_PRODUCTION` (gate inalterado desta rodada, `validate_order_production_readiness`). **Frontend:** novo `canEditProductionColors()` (`orderStatusMachine.ts`) espelha essa mesma allow-list; `ProductionColorsPicker.tsx` ganha `frozenMessage` (abre automaticamente mostrando a configuração congelada, sem exigir clique extra, e nunca permite interação quando `disabled`); `OrderManagementPanel.tsx` esconde o botão "Salvar cores" e desabilita todos os controles fora da janela editável, sempre preservando a visualização dos dados já salvos — nunca oferece um controle que o backend rejeitaria. **(2) `supabase/tests/product_plates_test.sql` reescrito integralmente** (estava estruturalmente defasado, chamando assinaturas antigas de `create_product_with_plates`/`update_product_full` e a função substituída `validate_catalog_composition_for_creation`) — agora cobre o contrato atual (peso direto por plate, sem filamento no Produto): 1/2/3 plates, peso/tempo inválidos, ajustes manuais, edição/persistência, Acessórios/Embalagens, "remoção protegida" (FK `ON DELETE CASCADE` de `product_plate_filaments` ao substituir um plate antigo com legado preso), rollback atômico na criação e na edição, estrutura incompleta bloqueando só o Pedido (nunca o cadastro do Produto), backfill simulado (peso a partir de filamento legado, peso a partir do efetivo anterior, aborto por ambiguidade em múltiplos plates), e grants/segurança com as assinaturas atuais. Volta a ser executável (sem nenhuma anotação de "estruturalmente obsoleto") assim que a migration for aplicada. **(3) `supabase/tests/order_initial_status_test.sql` auditado e adaptado** — os fixtures pararam de inserir linhas em `product_plate_filaments`/depender de filamento (`create_filament_type` removido do setup principal) e passam a criar `product_plates` com peso direto; o marcador `ORDER_CATALOG_MISSING_COMPOSITION:` foi trocado por `ORDER_CATALOG_MISSING_PRODUCTION_STRUCTURE:` nas asserções; nova Seção 7 cobre `production_colors` explicitamente (Pedido sem cores, cores parciais e cores completas nascem todos no MESMO status — cor nunca influencia a classificação inicial — e o snapshot de `order_item_plates` é conferido linha a linha). As regras já aprovadas de Catálogo/Spot/Personalizado permanecem exatamente como estavam — nenhuma delas foi alterada. **(4) Cobertura SQL ampliada especificamente para o congelamento** em `product_categories_plate_weight_order_colors_test.sql` (nova Seção 7): os 8 status reais da máquina de estados testados individualmente contra `update_order_item_production_colors`, `unit_number`/`plate_number`/`filament_type_id` inexistentes também no caminho de atualização pós-criação (não só na criação), e confirmação de que editar itens (`update_quote_order`, mudança de quantidade) limpa corretamente as cores antigas e re-cria o snapshot, sem nenhuma linha órfã. **(5) Cobertura frontend ampliada:** `ProductForm.test.tsx` ganha verificação estrutural das 3 seções com cartão visualmente distinto e do grid responsivo de plates (`grid-cols-1`/`md:grid-cols-2`); `ProductsPage.test.tsx` confirma as classes de largura ampliada (`w-[95vw]`, `max-w-[1400px]`) dos diálogos de Novo/Editar produto; novo `HomePage.test.tsx` confirma a ordem oficial dos módulos também na Página Inicial. Registrado explicitamente (nenhuma mudança de código tenta contornar isso): **jsdom não mede altura/largura real de viewport** — a ausência de rolagem vertical própria com 1-2 plates a 1920×1080/100% e o comportamento visual com 3+ plates continuam dependendo de **validação manual em navegador real**, ainda pendente. **Validação técnica desta rodada:** suíte `vitest` completa (1557 testes), `npm run lint` (0 erros), `npx tsc -b` e `npm run build` executados e verificados limpos; `git diff --check` sem problemas; busca por segredos no diff sem resultado; `.env.local` confirmado ignorado nos dois `.gitignore` (raiz e `frontend/`); confirmado por busca que nenhuma chamada ativa do frontend ainda grava em `product_filaments`/`product_plate_filaments` (as poucas referências restantes são só leitura de compatibilidade/comentários, já documentadas em rodadas anteriores). Deno **continua indisponível** neste ambiente — os testes das Edge Functions (`.test.ts` em `supabase/functions/`) permanecem só estruturalmente revisados, nenhuma tentativa de instalar o runtime. Os 3 arquivos SQL desta rodada **não puderam ser executados** (migration `20260829180000` ainda pendente) — permanecem estruturalmente revisados (blocos `do $$`/`end $$;` balanceados, `BEGIN`/`ROLLBACK` únicos), prontos para rodar assim que a migration for aplicada. **A pendência de `supabase/tests/bloco1_integration_test.sql` (35 falhas estruturais pré-existentes) permanece EXATAMENTE como estava — nenhuma tentativa de correção, nenhum aumento de incompatibilidade.** **Nenhuma migration foi aplicada, nenhuma Edge Function foi publicada, nenhum frontend foi deployado, nenhum push foi feito, nenhum dado remoto foi alterado, nenhum vínculo antigo de filamento foi apagado, nenhuma reserva ou consumo de estoque foi implementado.** **A implementação ainda NÃO está aprovada para aplicação** — esta entrada registra só correções e testes locais, nunca uma disponibilidade em produção; validação manual do usuário contra a interface real continua pendente. Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint local: commits `fix: freeze order colors when production starts` e `test: update product and order production regressions`, sem push. |
| 2026-08-30 | **Migration `20260829180000_add_categories_plate_weight_and_order_colors.sql` APLICADA ao banco remoto** (rodada de aplicação, após auditoria integral confirmando backfill seguro) — **44/44 migrations sincronizadas** (`supabase migration list --linked`), schema pós-aplicação reconferido linha a linha contra o que a migration declara (tabelas `product_categories`/`order_item_plates`/`order_item_unit_plate_filaments` com RLS ativo e grant `SELECT` único para `authenticated`; `product_plates.weight_grams` `numeric not null default 0`; FK `product_plate_filaments.plate_id` agora `ON DELETE CASCADE`; as 4 funções internas — `set_product_categories`/`set_product_production`/`validate_catalog_production_structure_for_creation`/`validate_order_production_readiness` — sem NENHUM grant, nem para `service_role`; as RPCs públicas exclusivas de `service_role`; assinaturas antigas de `create_product_with_plates`/`update_product_full` confirmadas ausentes). **Backfill auditado e confirmado sem perda**: peso EFETIVO (`default_weight_grams`) preservado byte a byte nos 8 Produtos existentes (plates com legado somam o peso legado; plates sem legado preservam o peso efetivo anterior via `production_weight_manual_override_grams`, ex. "teste - Produtos" com plate somando 31,25g mas override mantendo 1000g); `product_categories` populada com 7 linhas (1 por Produto com categoria legada não nula; "teste - Produtos" corretamente excluído por ter categoria nula); nenhuma linha retroativa criada em `order_item_plates`/`order_item_unit_plate_filaments` para Pedidos já existentes; `orders`/`order_status_history`/`payments`/FS-26-002..005 (ausentes)/FS-26-006 (presente) inalterados. **Os 3 arquivos de teste SQL transacionais executados contra o remoto já migrado, todos com ROLLBACK: `product_plates_test.sql` 29/29 PASS, `order_initial_status_test.sql` 29/29 PASS, `product_categories_plate_weight_order_colors_test.sql` 32/32 PASS — 90/90 no total, 0 FAIL, zero resíduo confirmado por contagem antes/depois idêntica.** Na primeira execução do terceiro arquivo foi descoberto e corrigido um bug de sintaxe SQL no próprio script de teste (Seções 1.2/1.4): `select public.update_product_full(...) into v_row;` disparava `invalid input syntax for type uuid` ao chamar uma função `SECURITY DEFINER` que retorna `public.products` dessa forma; trocado para `v_row := public.update_product_full(...);` (forma já usada em outros pontos do mesmo arquivo, confirmada correta por reprodução isolada) — bug do script de teste, não da migration nem das RPCs (commit próprio `test: fix update_product_full composite-into syntax in colors test`, separado do commit documental). **Publicadas exclusivamente as Edge Functions `products` (v10→v11) e `orders` (v7→v8)** — nenhuma outra função tocada (`order-items`/`order-status`/`payments`/`order-approvals`/`accessories`/`packaging`/`stock-movements`/`filament-types`/`filament-spools`/`filament-movements`/`inventory-purchases`/`customers` com `updated_at` inalterado). **Smoke tests não autenticados OK nas duas**: `OPTIONS`→204, escritas sem token→401 com corpo genérico ("Header Authorization ausente.", sem vazamento de detalhe interno), rota inexistente→404 (incluindo `GET /products`, que nunca teve rota — listagem é sempre client-side); nenhum dado criado, nenhuma credencial obtida ou contornada. **Contrato operacional agora ativo em produção**: múltiplas categorias por Produto (`product_categories`, espelho `products.category` = position 1); peso direto por plate (`weight_grams`, nunca mais somado de filamento); Produto sem NENHUMA composição de filamento/cor no cadastro (escolha movida para o Pedido); cores/filamentos por unidade+plate em `order_item_unit_plate_filaments`, sempre opcionais na criação; Pedido CATALOG entra em `IN_PRODUCTION_QUEUE` mesmo com cor pendente ou ausente; início efetivo de produção (`IN_PRODUCTION_QUEUE`→`IN_PRODUCTION`) bloqueado por `ORDER_PRODUCTION_COLORS_PENDING:` até cor completa; edição de cor congelada a partir de `IN_PRODUCTION` (`ORDER_PRODUCTION_COLORS_FROZEN:`), allow-list idêntica nos dois lados (backend e frontend, corrigida na rodada anterior). **Validação manual do usuário contra a interface real continua pendente** — esta entrada confirma apenas produção/backend, não substitui o teste humano. **Frontend NÃO publicado, nenhum push realizado.** Pendência de `supabase/tests/bloco1_integration_test.sql` (35 falhas estruturais pré-existentes) permanece EXATAMENTE como estava — não executado nesta rodada, nenhuma tentativa de correção. Nenhuma reserva ou consumo de estoque implementado. Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** (esta entrada é sobre disponibilizar em produção um contrato já implementado, não sobre uma fase nova). Checkpoint: migration aplicada e sincronizada, commit de correção de teste `test: fix update_product_full composite-into syntax in colors test`, sem push. |
| 2026-08-30 | **Validação manual do usuário no layout de Produtos** — resultado misto: **APROVADOS** ordem da Página Inicial, separação visual em 3 seções (Dados Gerais/Composição/Acessórios e Embalagem), múltiplas categorias, plates/pesos/tempos; **REPROVADOS** a janela "Novo Produto" continuava estreita e havia rolagem vertical mesmo com até 2 plates; **AJUSTE SOLICITADO** eliminar a segunda janela de Acessórios e Embalagens ("Selecionar acessórios e embalagens"), tornando os campos inline no próprio formulário, no mesmo padrão de "Adicionar" já usado pelos plates. **Rodada corretiva aplicada, só frontend, sem tocar banco/migrations/Edge Functions**: (1) **Causa raiz da largura estreita identificada e corrigida** — `DialogContent` (`components/ui/dialog.tsx`) já define `sm:max-w-sm` por padrão; a classe anterior `max-w-[1400px]` (sem breakpoint) nunca vencia esse padrão no CSS gerado pelo Tailwind (a camada `@media (min-width:640px)` é emitida depois da camada base, então empata em especificidade e ganha por ordem), e `twMerge` não resolvia o conflito porque as duas classes pertencem a grupos diferentes (base vs. variante `sm:`) — na prática a janela sempre ficava limitada a 24rem em qualquer desktop. Corrigido usando o MESMO breakpoint do padrão (`sm:max-w-[1600px]`, mais `w-[96vw]`), aí sim reconhecido como o mesmo grupo/variante e substituindo o padrão; Novo e Editar Produto usam a mesma constante `PRODUCT_FORM_DIALOG_CLASSNAME`, garantindo a mesma largura nos dois. (2) **Rolagem vertical condicionada ao número de plates** — `ProductForm.tsx` ganhou uma região própria (`data-testid="product-form-sections"`, envolvendo as 3 seções, nunca o rodapé Cancelar/Salvar nem o cabeçalho do Dialog) que só recebe `max-h-[65vh] overflow-y-auto` quando há 3 ou mais plates; com 1 ou 2 plates a classe fica ausente, dependendo do layout compactado (padding/gap reduzidos nos cartões; Nome+Tipo do produto e Descrição+Preço lado a lado no desktop em Dados Gerais) para caber sem rolagem própria — jsdom não mede altura real de viewport, então a confirmação visual definitiva (sem rolagem de fato a 1920×1080/100% com até 2 plates; cabeçalho/ações sempre acessíveis com 3+) continua dependendo de validação manual, ainda pendente. (3) **Acessórios e Embalagens convertidos para campos INLINE, nunca mais uma segunda janela** — removida inteiramente a seção "Selecionar acessórios e embalagens" (Dialog aninhado dentro do próprio Dialog de Novo/Editar Produto); "Adicionar acessório"/"Adicionar embalagem" agora inserem a linha (Select do item + Select de quantidade + Remover) diretamente na Seção 3, lado a lado em 2 blocos no desktop (`md:grid-cols-2`), no mesmo padrão de interação dos plates. Toda a lógica de duplicidade (item já escolhido some das opções de outra linha), item inativo vinculado preservado e bloqueando o salvamento até ser removido, quantidade fora do intervalo 1-20 preservada sem correção silenciosa, e persistência na edição foi reaproveitada sem reescrita — extraída para um componente compartilhado novo (`CompositionRowsField.tsx`) usado tanto aqui quanto pelo diálogo independente "Acessórios e Embalagem" da listagem (`ProductCompositionForm.tsx`, refatorado para a mesma base, comportamento 100% preservado — 26/26 testes próprios continuam passando sem alteração). Esse diálogo independente da listagem (ação rápida fora do formulário completo) foi mantido como está — fora do escopo pedido, que era especificamente dentro de Novo/Editar Produto. **Nenhum contrato de API alterado**: payload de criação/edição, persistência de acessórios/embalagens, múltiplas categorias, peso direto por plate, tempos, totais automáticos, ajuste manual, permite personalização, remoção protegida, Ficha Técnica, ausência de Filamentos/Cores no Produto, Pedidos, cores por unidade/plate, status, pagamentos e Estoque — todos preservados byte a byte; nenhuma migration criada ou aplicada, nenhuma Edge Function publicada. **Validação técnica**: suíte `vitest` completa 1568/1568 (1558 + 10 líquidos: testes de layout/rolagem condicional e de campos inline substituindo os do fluxo antigo de 2ª janela), `npm run lint` (0 erros, mesmos 8 avisos pré-existentes), `npx tsc -b` e `npm run build` limpos, `git diff --check` sem problemas, busca por segredos no diff sem resultado, `.env.local` confirmado ignorado. **Nova validação manual do layout (largura efetiva, ausência de rolagem com até 2 plates, inline de Acessórios/Embalagens em navegador real) continua pendente** — só será considerada aprovada quando o usuário confirmar visualmente. **Validação manual do fluxo de Pedido (cores por unidade/plate, congelamento, Fila) ainda não foi realizada nesta ou em rodada anterior.** Nenhuma mudança de backend, nenhuma reserva ou consumo de estoque implementado. Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** (correção de layout/interação, não uma fase nova). Checkpoint local: commit `fix: widen product form and inline components`, sem push, sem deploy de frontend. |
| 2026-08-30 | **Rodada corretiva sobre a rodada anterior (largura/inline)** — dois comportamentos incorretos identificados após revisão do relatório, ambos só frontend, sem tocar banco/migrations/Edge Functions. **(1) Item inativo histórico deixa de bloquear o salvamento.** A regra anterior ("item inativo bloqueia salvamento até remoção") estava errada: um acessório/embalagem já vinculado ao Produto ANTES de ser inativado agora permanece visível, identificado como inativo, com quantidade editável e removível só voluntariamente — o usuário nunca é obrigado a removê-lo para editar outro campo do Produto (nome, preço, plates etc.). Removido o bloqueio pré-envio (`hasInactiveSelection`) tanto em `ProductForm.tsx` quanto em `ProductCompositionForm.tsx` (diálogo independente da listagem, comportamento agora IDÊNTICO nos dois — nenhuma divergência por props foi necessária, já que a mesma regra relaxada se aplica aos dois contextos); mensagem da linha (`CompositionRowsField.tsx`) reescrita para nunca mais afirmar que a remoção é obrigatória. **Conflito real identificado e resolvido em conjunto com o usuário antes de implementar**: `set_product_composition` (RPC que processa toda escrita de composição, chamada tanto pelo diálogo independente quanto por `update_product_full`) rejeita qualquer `accessory_id`/`packaging_id` que não esteja `is_active` no momento do salvamento — inclusive um item que já estava vinculado antes de ser desativado, sem exceção para vínculo histórico. Como alterar essa RPC está fora do escopo autorizado nesta rodada, a correção manteve o payload do frontend enviando o item histórico normalmente quando ele permanece na linha (nunca omitido silenciosamente — decisão explícita para nunca perder o vínculo sem uma ação do usuário), mas isso significa que uma tentativa real de salvar com o item ainda presente **continuará sendo rejeitada pelo banco de produção como está hoje** (erro surfaced via `submitError`, padrão já existente e testado) até uma rodada futura autorizada atualizar `set_product_composition` para tolerar um vínculo histórico. Itens inativos SEM vínculo anterior continuam nunca aparecendo como opção nova (em nenhum dos dois fluxos, em nenhum modo — inclusive Novo Produto); depois de removido voluntariamente, um item inativo não pode ser selecionado de novo em nenhuma linha — ambas as garantias já existiam via `filterSelectableItems` e permaneceram inalteradas. **(2) Rolagem vertical responsiva revisada para considerar Acessórios/Embalagens, não só plates.** `needsScrollRegion = plates.length >= 3` virou `hasExtendedContent = plates.length >= 3 || accessoryRows.length > 1 || packagingRows.length > 1`, com 3 estados por breakpoint (nunca `overflow-hidden`): mobile sempre com `max-h-[85vh] overflow-y-auto` (rolagem sempre disponível, sem depender do conteúdo); desktop compacto (até 2 plates, até 1 acessório, até 1 embalagem) com `md:max-h-none md:overflow-visible` (sem rolagem própria); desktop com conteúdo estendido (3+ plates OU mais de 1 acessório OU mais de 1 embalagem) com `md:max-h-[65vh] md:overflow-y-auto`. Rodapé (Cancelar/Salvar) permanece fora da região rolável nos 3 estados. **Validação técnica**: suíte `vitest` completa 1577/1577 (1568 + 9 líquidos), `npm run lint` (0 erros, mesmos 8 avisos pré-existentes), `npx tsc -b` e `npm run build` limpos, `git diff --check` sem problemas, busca por segredos no diff sem resultado, `.env.local` confirmado ignorado, nenhum contrato de API alterado (payload de criação/edição, persistência de acessórios/embalagens, múltiplas categorias, peso direto por plate, totais, ajuste manual, remoção protegida, Ficha Técnica, ausência de Filamentos/Cores, Pedidos, cores por unidade/plate, status, pagamentos e Estoque — todos preservados; nenhuma migration criada/aplicada, nenhuma Edge Function publicada). **Nova validação manual do layout (largura, rolagem nos 3 estados, item inativo preservável em navegador real) continua pendente.** **Validação manual do fluxo de Pedido continua pausada, sem nenhuma tentativa nesta rodada.** Nenhuma mudança de backend, nenhuma reserva ou consumo de estoque implementado. Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint local: commit `fix: preserve inactive product components`, sem push, sem deploy de frontend. |
| 2026-08-30 | **Conflito real de preservação histórica confirmado e migration local criada para corrigi-lo — nada aplicado, nada publicado.** A rodada anterior corrigiu o frontend (item inativo histórico visível, preservável no payload, quantidade editável, remoção só voluntária), mas deixar o frontend enviar o payload para o backend rejeitar depois não resolve a preservação pedida: `set_product_composition` (única function que escreve `product_accessories`/`product_packaging`, chamada por `create_product_with_plates`/`update_product_full` e pelo diálogo independente "Acessórios e Embalagem" da listagem) exige `is_active` para TODO `accessory_id`/`packaging_id` recebido, sem nenhuma exceção para um vínculo que já existia antes do item ser desativado — confirmado por leitura integral da function original (`20260816150500_create_product_composition_function.sql`, nunca redefinida até agora) e das functions irmãs de exclusão protegida (`delete_accessory`/`delete_packaging`, que já orientam "desative o item" em vez de excluir — provando que o sistema foi desenhado para permitir item vinculado E inativo coexistirem, só a RPC de composição não tratava esse caso). **Migration local criada** (`supabase/migrations/20260830120000_preserve_inactive_product_composition_links.sql`, timestamp posterior a `20260829180000`) — regra nova: um `accessory_id`/`packaging_id` é aceito quando ATIVO, OU quando já estava vinculado a ESTE MESMO Produto imediatamente antes desta chamada (capturado via `array_agg` logo após o lock `for update` em `products`, já existente, e antes do delete — nunca um histórico global/permanente, nenhuma tabela nova de "já vinculado algum dia"; a fonte da exceção é só a linha atual de `product_accessories`/`product_packaging`, então um item removido numa chamada anterior deixa de ser aceito na próxima). Autenticação, lock de `products`, validação de tipo dos arrays, atomicidade (delete+reinsert dentro da mesma transação implícita, erro em qualquer ponto desfaz tudo), CHECK de quantidade e UNIQUE contra duplicidade, `security definer`/`search_path=''`/owner/grants (`service_role` apenas) — todos preservados byte a byte, só a validação de `is_active` ganhou a exceção histórica. **Teste SQL transacional criado** (`supabase/tests/product_composition_inactive_links_test.sql`, `BEGIN`/`ROLLBACK`, 26 blocos) cobrindo os 10 cenários pedidos para acessórios e para embalagens (ativo novo; inativo histórico do mesmo Produto; alteração de quantidade do histórico; edição de outro campo preservando o vínculo; ativo+histórico juntos; inativo nunca vinculado — rejeitado; inativo histórico de OUTRO Produto — rejeitado, nunca um histórico global; inativo em Produto novo — rejeitado; remoção voluntária; reintrodução depois de removido — rejeitado), mais atomicidade (duplicidade, quantidade inválida, falha no meio do array sem mistura entre conjunto antigo e novo) e segurança (owner/`SECURITY DEFINER`/`search_path`/grants). **Migration e teste SQL não puderam ser aplicados/executados nesta rodada** (restrição explícita: só implementação local) — permanecem prontos para uma rodada futura autorizada aplicar a migration e então rodar os três testes SQL relacionados (`product_plates_test.sql`/`order_initial_status_test.sql`/`product_categories_plate_weight_order_colors_test.sql`) mais este novo, seguidos da publicação de `products`/`orders`. **Frontend não foi revertido nem alterado** — já estava correto desde a rodada anterior (item histórico visível/preservável/editável/removível voluntariamente, item nunca-vinculado nunca ofertado, erro real do backend continua exibido via `submitError`); suíte `vitest` completa 1577/1577 (inalterada), `npm run lint` (0 erros, mesmos 8 avisos), `npx tsc -b` e `npm run build` limpos, `git diff --check` sem problemas, busca por segredos sem resultado, `.env.local` ignorado, somente a nova migration + teste SQL + este registro de roadmap foram adicionados (nenhuma função Edge alterada). Deno continua indisponível — sem tentativa de instalação. **Validação visual do layout (largura/rolagem/inline) em navegador real continua pendente.** **Testes do Pedido continuam pausados, sem nenhuma tentativa nesta rodada.** Nenhuma reserva ou consumo de estoque implementado. Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado**. Checkpoint local: commit `fix: preserve inactive product composition links`, sem push, sem deploy de frontend, migration pendente. |
| 2026-08-30 | **Migration `20260830120000_preserve_inactive_product_composition_links.sql` APLICADA ao banco remoto** — **45/45 migrations sincronizadas** (`supabase migration list --linked`, contagem explícita, 0 divergências). Auditoria final confirmou que a única mudança funcional em `set_product_composition` é a exceção histórica (`is_active OR id = any(vínculo anterior capturado)`); assinatura, retorno `void`, `assert_active_user`, lock `FOR UPDATE` em `products`, validação de arrays, CHECK de quantidade, UNIQUE contra duplicidade, substituição atômica completa, `SECURITY DEFINER`, `search_path=''`, owner (`postgres`) e grants (`service_role` apenas) — todos preservados byte a byte; nenhuma tabela/coluna nova criada; nenhum dado alterado pela migration em si (só redefine a function). **Preservação de vínculo inativo histórico confirmada ativa no remoto**: um item que já estava vinculado ao mesmo Produto antes de ser desativado pode permanecer, ter a quantidade alterada e coexistir com itens ativos, sem bloquear a edição de outros campos; **novos vínculos com item inativo continuam bloqueados** em todos os casos (nunca vinculado, vinculado só a outro Produto, usado em Produto novo); **a remoção de um vínculo histórico elimina a exceção** — uma tentativa posterior de reintroduzi-lo (ainda inativo) volta a ser rejeitada, provado na prática pelos testes 1.10/2.9. **Bug real encontrado e corrigido em `product_composition_inactive_links_test.sql` durante a auditoria pré-aplicação** (mesmo padrão `select fn(...) into v_row` já visto e corrigido em `product_categories_plate_weight_order_colors_test.sql` numa rodada anterior; commit próprio antes da aplicação) **e um segundo bug real encontrado só após a primeira execução contra o remoto já migrado**: `update_product_full` sempre substitui os DOIS arrays (acessórios E embalagens) na mesma chamada, e os testes da Seção 1 (só acessórios) enviavam `packaging='[]'`, apagando silenciosamente a embalagem histórica antes da Seção 2 sequer começar — e vice-versa nos testes da Seção 2 — contaminando a Seção 3 e a checagem de zero resíduo (10 FAIL na primeira execução real, todos rastreados a essa causa única); corrigido reenviando o array do "outro lado" com seu estado atual em toda chamada, commit próprio separado. **Resultado final, executado de fato contra o remoto já migrado**: `product_composition_inactive_links_test.sql` **26/26 PASS** (10 cenários de acessórios, 10 de embalagens, 3 de atomicidade, segurança, zero resíduo — todos registrados individualmente, nenhum FAIL confundido com PASS: cada rejeição confere a mensagem exata e o estado final, não só que uma exceção ocorreu); `product_plates_test.sql` **29/29 PASS** (confere a referência anterior); `product_categories_plate_weight_order_colors_test.sql` **32/32 PASS** (confere a referência anterior) — números observados de fato, não assumidos. Os 3 arquivos usam `BEGIN`/`ROLLBACK`; contagens de Produtos/Pedidos/vínculos/pagamentos/histórico conferidas byte a byte idênticas antes e depois de toda a rodada (products=8, orders=3, product_accessories=2, product_packaging=1, accessories_active=3, packaging_active=3, 0 vínculos inativos pré-existentes, order_status_history=15, payments=3, FS-26-002..005 ausentes, FS-26-006 presente, Petlink inalterado) — zero resíduo real confirmado, não só declarado pelos testes. `bloco1_integration_test.sql` **não executado nesta rodada** (35 falhas estruturais pré-existentes permanecem como pendência separada, sem nenhuma tentativa de correção). **Nenhuma Edge Function publicada** — as 14 funções (incluindo `products` v11 e `orders` v8) seguem com `version`/`updated_at` idênticos ao registrado antes da aplicação (diff vazio) — a mudança foi exclusivamente na RPC do banco, sem nenhum impacto no contrato HTTP. Smoke test mínimo (`OPTIONS`→204, `POST`/`PATCH` sem autenticação→401, corpo genérico sem detalhe interno) confirma `products` operacional sem necessidade de republicação. **Validação visual do layout (largura/rolagem/inline) em navegador real continua pendente — agora liberada para acontecer junto da validação do vínculo inativo histórico preservado (ambas dependem só do estado atual do remoto, já migrado).** Testes do Pedido continuam pausados, sem nenhuma tentativa nesta rodada. Nenhuma reserva ou consumo de estoque implementado. Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** (correção de contrato de RPC já implementado, não uma fase nova). Checkpoint: migration aplicada e sincronizada (45/45), commits de correção de teste `test: fix update_product_full composite-into syntax in composition test` e `test: fix cross-contamination between accessories/packaging in composition test`, sem push. |
| 2026-08-31 | **Validação manual do usuário — Página Inicial e novo formulário de Produto — APROVADAS INTEGRALMENTE, sem nenhuma alteração de código, banco ou função nesta entrada.** **Página Inicial**: ordem dos módulos aprovada — Clientes, Empresas, Produtos, Pedidos, Estoque. **Novo/Editar Produto — os 6 itens de layout pedidos foram aprovados integralmente, nenhum parcialmente**: (1) largura ampliada da janela; (2) um plate sem rolagem vertical; (3) dois plates sem rolagem vertical; (4) três plates com rolagem vertical; (5) acessórios inline, sem segunda janela; (6) embalagens inline, sem segunda janela — confirma na prática, em navegador real, o comportamento responsivo implementado (`hasExtendedContent`, mobile sempre com rolagem, desktop compacto sem rolagem, desktop estendido com rolagem) e a eliminação completa da segunda janela de "Selecionar acessórios e embalagens". **Também já aprovados nesta mesma entrega** (validações anteriores, reafirmadas aqui): separação visual entre Dados Gerais/Composição/Acessórios e Embalagens; múltiplas categorias; plates com peso e tempo próprios; totais automáticos; ajuste manual; acessórios e embalagens; Ficha Técnica; edição e persistência; remoção protegida; ausência de Filamentos/Cores no cadastro do Produto. Produto TESTE usado na validação removido pelo usuário após a conferência. **Backend comprovado no remoto** (rodada anterior, reafirmado aqui): preservação de acessórios/embalagens inativos históricos ativa (`set_product_composition`, migration `20260830120000_preserve_inactive_product_composition_links.sql`, **45/45 migrations sincronizadas**); novos vínculos com item inativo continuam bloqueados em todos os casos (nunca vinculado, vinculado só a outro Produto, usado em Produto novo); remover um vínculo histórico elimina a autorização — uma reintrodução posterior volta a ser rejeitada; teste específico `product_composition_inactive_links_test.sql` **26/26 PASS**; regressões `product_plates_test.sql` **29/29 PASS** e `product_categories_plate_weight_order_colors_test.sql` **32/32 PASS**; nenhuma reserva ou consumo de estoque implementado. **Pendências explícitas, nenhuma delas resolvida por esta entrada**: **validação manual do fluxo de cores no Pedido (seleção por unidade/plate, congelamento, gate de `IN_PRODUCTION`) ainda NÃO foi realizada** — não confundir com a validação do formulário de Produto, que é integral; frontend ainda não publicado; `bloco1_integration_test.sql` (35 falhas estruturais pré-existentes) continua como pendência separada, sem nenhuma tentativa de correção; Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** (esta entrada registra validação manual de um contrato já implementado e já aplicado em produção, não uma fase nova). Checkpoint: nenhum código/banco/função alterado nesta rodada, sem push, sem deploy. |
| 2026-08-31 | **Ajuste visual: coluna Ações da tabela de Pedidos reorganizada — os 3 controles ("Alterar pedido"/"Gerenciar pedido"/"Excluir") quebravam em 2 linhas.** Causa raiz identificada por auditoria (`OrdersPage.tsx`): o agrupamento dos botões usava `flex flex-wrap` (permitindo quebra) combinado com a coluna Ações reservando só `w-[14%]` (~204px de um total de 1460px) — bem menos que os ~320px que os 2 botões de texto + o botão de exclusão exigem lado a lado. Corrigido com duas mudanças complementares: (1) o agrupamento passou para `flex flex-nowrap items-center gap-1.5` (nunca mais permite quebra, controles centralizados verticalmente, `shrink-0` em cada botão como reforço); (2) a coluna Ações subiu de `w-[14%]` para `w-[22%]`, com a largura mínima da tabela ampliada de `min-w-[1460px]` para `min-w-[1600px]` — o espaço extra veio de colunas de conteúdo tipicamente curto (Nº pedido, Tipo(s), Total, Saldo devedor, Prazo), preservando ou ampliando o espaço de Cliente/Produto(s)/Status/Status financeiro. Padding horizontal dos botões "Alterar pedido"/"Gerenciar pedido" reduzido de `px-2.5` (padrão do `size="sm"`) para `px-2` antes de qualquer redução de fonte, conforme pedido — texto completo preservado nos dois, nunca substituído por ícone; botão "Excluir" trocado de `size="sm"` para `size="icon-sm"` (mesmo padrão já usado nos botões de remover linha de Produtos/Composição), mais compacto, com `aria-label`/`title` (nome do pedido) preservados. Ordem dos 3 controles inalterada. Nenhuma regra de Pedido, status, pagamento ou permissão alterada — ajuste exclusivamente visual/estrutural em `OrdersPage.tsx`. **Testes**: 9 testes novos cobrindo agrupamento sem quebra (`flex-nowrap`, nunca `flex-wrap`), ordem preservada dos 3 controles, largura da coluna Ações (`w-[22%]`) e da tabela (`min-w-[1600px]`), textos completos dos 2 botões, nome acessível/`title` do botão Excluir mantidos com `size="icon-sm"`, e regressão de abrir "Alterar pedido"/"Gerenciar pedido", renderização de todas as colunas/dados e ordenação — suíte específica de `OrdersPage.test.tsx` 157/157, suíte `vitest` completa 1586/1586 (1577 + 9), `npm run lint` (0 erros, mesmos 8 avisos pré-existentes), `npx tsc -b` e `npm run build` limpos, `git diff --check` sem problemas. **Validação visual manual em navegador real (1920×1080, 1366×768, largura menor com rolagem horizontal) ainda pendente** — nenhuma ferramenta de automação de navegador (Playwright/chromium-cli) está disponível neste ambiente, então a confirmação de que os 3 controles realmente ficam numa única linha, sem sobreposição, depende do usuário conferir visualmente; os testes acima comprovam só a estrutura/classes responsáveis pelo comportamento esperado, nunca o layout renderizado de fato. Validação manual do fluxo de cores no Pedido continua pendente, sem nenhuma tentativa nesta rodada. `bloco1_integration_test.sql` continua como pendência separada. Nenhuma reserva ou consumo de estoque implementado. Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** (ajuste visual pontual, não uma fase nova). Checkpoint: commit `fix: keep order actions on one line`, sem push, sem deploy. |
| 2026-08-31 | **Padronização visual das 5 listagens principais (Clientes, Empresas, Produtos, Pedidos, Estoque — incluindo Filamentos, Acessórios e Embalagens) — só frontend, sem tocar banco/migrations/Edge Functions/regras de negócio.** **(1) Tipografia compacta:** nova constante compartilhada `TABLE_COMPACT_TEXT_CLASSNAME` (`text-[13.6px]`, 16px×0.85) aplicada no elemento `<Table>` de cada listagem — cascateia para cabeçalhos/células (que não declaram `text-*` próprio) sem nenhuma redução acumulada em botões/badges aninhados (que já têm `text-*` absoluto próprio, imune à cascata); controles já pequenos (`size="sm"`, 12.8px) usam o piso explícito `TABLE_COMPACT_ACTION_TEXT_CLASSNAME` (`text-xs`, 12px) em vez do fator de 15%, nunca abaixo do mínimo de 12px exigido. **(2) Datas em dd/mm/aa:** novo formatador compartilhado `formatTableDate`/`tableDateSortValue` (`lib/dates/tableDateFormat.ts`), extraído/generalizado do `formatDateOnly` de Pedidos — nunca instancia `Date` (evita o bug conhecido de deslocamento de dia por fuso horário), aplicado na coluna Prazo de Pedidos (única coluna de data nas 5 listagens) com a data completa (dd/mm/aaaa) disponível via `title`; ordenação continua usando o valor numérico `AAAAMMDD`, nunca o texto formatado. **(3) Colunas redimensionáveis individualmente:** nova infraestrutura compartilhada — `columnWidths.ts` (lógica pura: clamp, normalização, total, chave de storage), `usePersistentColumnWidths` (hook React fino sobre a lógica pura), `ColumnResizeHandle` (alça com Pointer Events — captura via `setPointerCapture` no próprio elemento, nunca listeners em `window`, então nunca há listener residual após desmontar; `stopPropagation` isola do menu de ordenação; teclado ArrowLeft/Right ±10px, Shift ±30px, respeitando min/max de cada coluna), `ResizableTableHead` (cabeçalhos sem ordenação) e a extensão opcional `resize` de `SortableColumnHeader` (cabeçalhos ordenáveis — o menu de ordenação permanece navegável separadamente da alça). Cada tabela usa `colgroup` + `table-fixed`, com a largura mínima da tabela (`style={{minWidth}}`) calculada dinamicamente pela soma das larguras atuais das colunas (nunca mais um valor estático) — preserva o comportamento responsivo já aprovado (telas largas usam o espaço disponível normalmente; telas estreitas mantêm as larguras escolhidas com rolagem horizontal, nunca compactando uma coluna customizada a ponto de esconder conteúdo). A coluna Ações de cada listagem tem `minWidth` calibrado para nunca permitir quebra dos botões em 2 linhas mesmo no menor arraste possível (Pedidos 320px/3 botões; Produtos, Acessórios/Embalagens e Filamentos 300–340px/2–3 botões; Clientes e Empresas 110–130px/1–2 botões) — todas as colunas de Ações confirmadas com `flex-nowrap` + `shrink-0` (nunca mais `flex-wrap`). **(4) Persistência local por usuário e por tabela:** `usePersistentColumnWidths` grava no `localStorage` só ao final do arraste (`pointerup`/`pointercancel`, nunca a cada pixel) ou imediatamente a cada ajuste por teclado (cada tecla já é um ajuste completo), sob a chave `forma-sky:table-column-widths:v1:<user-id ou "anon">:<table-id>` — isolada por tabela (`orders`, `customers`, `companies`, `products`, `inventory-accessories`, `inventory-packaging`, `inventory-filaments`) e por usuário autenticado (`session.user.id`); coluna nova usa o default, coluna removida não quebra a leitura, JSON corrompido ou fora dos limites min/max cai nos defaults silenciosamente, ausência de `localStorage` nunca bloqueia a renderização (tudo dentro de `isBrowserStorageAvailable`, com try/catch). Botão discreto **"Restaurar larguras"** (`RestoreColumnWidthsButton`, `variant="ghost" size="sm"`) em cada listagem restaura só as larguras da tabela atual (apaga só aquela chave do `localStorage`), nunca afeta busca, filtro ou ordenação. Acessibilidade: cada alça é `role="separator"` `aria-orientation="vertical"` com nome acessível `"Redimensionar coluna {Label}"`, focável por teclado — nota: em cabeçalhos COM alça, o nome acessível computado do `<th>` passa a incluir esse texto (ex.: `"Cliente Redimensionar coluna Cliente"`); avaliado deliberadamente **sem** `aria-label` explícito no `<th>` para não colidir com `getByLabelText` nos testes (que casa `aria-label` em qualquer elemento, não só campos de formulário) sempre que o rótulo da coluna coincidir com o de um campo em outro lugar da tela (ex.: "Preço" em Produtos vs. o campo "Preço" do formulário) — mais verboso que o ideal, nunca ambíguo/quebrado. Infraestrutura compartilhada deliberadamente cega a regras de negócio (nunca importa nada específico de Clientes/Empresas/Produtos/Pedidos/Estoque); nenhuma dependência externa nova instalada; reformulação para "cards" explicitamente fora do escopo desta rodada. Estoque: identificadas e convertidas todas as listagens principais existentes — Filamentos (tabela própria, nunca teve ordenação — permanece sem ordenação nesta rodada, todo cabeçalho usa `ResizableTableHead` puro), Acessórios e Embalagens (mesmo componente `InventoryAreaPanel`, agora recebendo um `tableId` por área para isolar a largura persistida entre as duas). **Nenhuma regra de status, pagamento, cor, permissão, autenticação ou dado alterada; nenhuma migration criada/aplicada; nenhuma Edge Function publicada; nenhuma reserva ou consumo de estoque implementado.** **Validação técnica:** suítes específicas da infraestrutura compartilhada e de cada uma das 6 tabelas convertidas, mais `OrdersPage.test.tsx` (168/168) — suíte `vitest` completa **1714/1714**, `npm run lint` (0 erros, mesmos 8 avisos pré-existentes), `npx tsc -b` e `npm run build` limpos, `git diff --check` sem problemas. Corrigido durante a validação: `usePersistentColumnWidths` inicialmente usava um `ref` "mais recente" para `specs` (lido/escrito durante a renderização) e um `useEffect` chamando `setState` diretamente — ambos sinalizados pelas regras mais novas do ESLint (`react-hooks/refs`, `react-hooks/set-state-in-effect`); corrigido usando `specs` direto (já é uma constante de módulo estável em cada página, nunca recriada por render) e o padrão oficial do React de "ajustar estado durante a renderização" no lugar do efeito, eliminando os 2 erros sem alterar nenhum comportamento (mesmos testes, mesmo resultado). **Validação manual do usuário em navegador real (arraste com mouse, redimensionamento em diferentes resoluções, persistência entre sessões, comportamento da rolagem horizontal) ainda pendente** — nenhuma ferramenta de automação de navegador está disponível neste ambiente; os testes acima comprovam a estrutura/comportamento simulado via Pointer Events, nunca o layout renderizado de fato. **Aprovação manual prévia das cores por unidade/plate em Pedidos**: não encontrada no histórico deste roadmap — a entrada de 2026-08-31 (validação da Página Inicial/formulário de Produto) registra explicitamente que essa validação **ainda não foi realizada**; nada foi registrado aqui como aprovação porque nenhuma ocorreu. Validação manual do fluxo de cores no Pedido continua pendente, sem nenhuma tentativa nesta rodada. `bloco1_integration_test.sql` continua como pendência separada. Módulo 3 continua **NÃO concluído**; nenhuma reserva ou consumo de estoque implementado. Percentual macro **não alterado** (padronização visual pontual, não uma fase nova). Checkpoint: commit `feat: add persistent resizable table columns`, sem push, sem deploy. |
| 2026-08-31 | **CORREÇÃO DOCUMENTAL — validação manual das cores por unidade e por plate em Pedidos: na verdade JÁ FOI REALIZADA e APROVADA INTEGRALMENTE pelo usuário. Esta entrada supera/corrige a afirmação em contrário registrada na entrada anterior (a de "Padronização visual das 5 listagens..."), que dizia — incorretamente — que essa validação "ainda não foi realizada".** Nenhuma entrada anterior foi reescrita ou apagada — esta é uma correção por entrada nova, preservando o histórico exatamente como foi registrado (inclusive o próprio engano). **O usuário aprovou expressamente, em navegador real, os seguintes pontos do fluxo de cores por unidade/plate**: (1) criação do Pedido sem nenhuma cor selecionada (o Pedido nasce e entra normalmente na Fila de produção — `IN_PRODUCTION_QUEUE` — mesmo com cores ausentes ou parciais, comportamento inalterado desta rodada); (2) seleção de cores por unidade e por plate; (3) persistência de uma configuração PARCIAL de cores (algumas unidades/plates configurados, outros ainda pendentes); (4) bloqueio da transição para `IN_PRODUCTION` enquanto houver qualquer combinação unidade/plate sem cor definida — a produção não pode começar com configuração incompleta (gate `validate_order_production_readiness`, regra de backend inalterada, só validada manualmente aqui); (5) configuração COMPLETA de cores (todas as unidades/plates definidos) e sua persistência correta; (6) remoção, pelo próprio usuário após a conferência, dos registros de teste (pedidos/dados TESTE usados durante esta validação). **Nenhuma mudança de código, banco, Edge Function ou regra de negócio nesta entrada** — confirma na prática um contrato já implementado e já registrado tecnicamente em rodadas anteriores (congelamento de cores por status, `update_order_item_production_colors`, `ProductionColorsPicker.tsx`, `canEditProductionColors()`), nunca uma fase nova. **Nenhuma reserva ou consumo automático de estoque foi implementado nem foi objeto desta validação** — a aprovação cobre exclusivamente seleção/persistência/bloqueio de cores, não consumo de filamento. Módulo 3 continua **NÃO concluído** (reserva/consumo de estoque, entre outros itens, seguem pendentes). Percentual macro **não alterado**. Checkpoint: nenhum código/banco/função alterado nesta rodada, sem push, sem deploy. |
| 2026-08-31 | **Validação manual do usuário — as 7 melhorias da padronização das listagens (tipografia compacta, dd/mm/aa, colunas redimensionáveis, persistência por usuário/tabela, botão "Restaurar larguras", piso de 12px nos controles de ação e correção da coluna Ações de Pedidos) — APROVADAS INTEGRALMENTE.** **Ajuste final solicitado e aplicado nesta mesma rodada**: os botões da coluna Ações de Pedidos foram compactados de "Alterar pedido"/"Gerenciar pedido" (texto visível) para só **"Alterar"/"Gerenciar"** — o contexto "pedido" já é dado pela própria coluna/linha da tabela. Nome acessível completo e contextualizado preservado via `aria-label`/`title` explícitos ("Alterar pedido {nº}"/"Gerenciar pedido {nº}"), mesmo padrão já usado no botão "Excluir" ao lado; ordem dos 3 controles (Alterar → Gerenciar → Excluir), agrupamento `flex-nowrap` numa única linha e fonte compacta (`text-xs`, piso de 12px) preservados sem nenhuma alteração. **Os títulos completos dos diálogos abertos por esses botões permanecem inalterados**: "Alterar pedido" e "Gerenciar pedido" (DialogTitle), nunca abreviados. Nenhuma regra de negócio, status, pagamento, cor ou produção alterada; nenhuma alteração de banco, migration ou Edge Function; nenhuma reserva ou consumo de estoque implementado; infraestrutura de larguras persistentes (`usePersistentColumnWidths`, `columnWidths.ts`) inalterada. **Validação técnica**: `OrdersPage.test.tsx` **175/175**, suíte `vitest` completa **1728/1728**, `npm run lint` (0 erros, mesmos 8 avisos pré-existentes), `npx tsc -b` e `npm run build` limpos, `git diff --check` sem problemas. Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** (ajuste visual pontual, não uma fase nova). Checkpoint: commit `fix: shorten order action labels`, sem push, sem deploy. |
| 2026-08-31 | **Filtro de Produtos por Categoria e Tipo implementado localmente (só frontend), com correção de acessibilidade dos rótulos.** Novo painel "Filtros" na listagem de Produtos (`ProductsPage.tsx`), aberto por um `Popover` não-modal (novos primitivos `frontend/src/components/ui/popover.tsx` e `frontend/src/components/ui/checkbox.tsx`, clones fiéis do padrão Base UI já usado em `dropdown-menu.tsx`/`switch.tsx` — nenhuma dependência nova instalada). **Categorias**: multisseleção por checkbox, lista só as categorias de fato em uso pelos Produtos carregados (nunca um catálogo à parte — o modelo atual não tem tabela própria de categorias), ordenadas por `localeCompare('pt-BR')`; regra **OR** dentro do grupo — um Produto passa se tiver QUALQUER uma das categorias marcadas, inclusive **Produto com múltiplas categorias** (qualquer categoria marcada que ele tenha o inclui). **Tipo de produto**: multisseleção por checkbox sobre o enum inteiro do domínio (`CATALOG`/`CUSTOM`/`SPOT`, sempre os três, mesmo os ausentes na lista atual, com os rótulos em português `Catálogo`/`Personalizado`/`SPOT`); regra **OR** dentro do grupo. **Entre os dois grupos a regra é AND** — só restringe pelo grupo que tiver alguma seleção; grupo sem seleção nunca restringe. Opção **"Sem categoria"** aparece só quando existe pelo menos um Produto carregado sem nenhuma categoria vinculada, e traz exatamente esses Produtos (flag à parte, nunca uma string mágica dentro do `Set` de categorias). **Busca e ordenação preservadas**: a ordem conceitual é dados → busca → Categoria → Tipo → ordenação (o filtro roda depois da busca e antes do `sortByColumn`, sem mutar nenhum array). O botão mostra um **contador de filtros ativos** (`Filtros (N)`), cada seleção vira um **chip removível individualmente** (com o nome integral da categoria/tipo no rótulo visível e no `aria-label`), e **"Limpar filtros" remove só Categoria e Tipo, preservando a busca** (`searchTerm`) e a ordenação (`sort`). O `Popover` **fecha com Escape e com clique externo** (comportamento padrão do Base UI, agora coberto por teste); **as seleções permanecem enquanto a página estiver aberta** — fechar e reabrir o painel na mesma sessão mantém as caixas marcadas e o contador — e **os filtros NÃO persistem após recarregar a página** (estado `useState` puro; `localStorage` continua exclusivo das larguras de coluna). **Correção de acessibilidade (commit `c598e35`, sobre o commit de feature `887f7c6` da sessão anterior)**: os `id`/`htmlFor` dos checkboxes derivavam do texto cru da categoria e o Base UI monta o `aria-labelledby` do checkbox como `${id}-label`; uma categoria com espaço (comum em pt-BR, ex. `Peças Grandes / Cachepôs (2cm)`) transformava o `aria-labelledby` numa lista de IDREFs separada por espaços que não resolvia, deixando o checkbox **sem nome acessível** (além de `id` inválido em HTML). Agora **todos os ids do painel vêm de um único prefixo técnico (`useId()`) + índice posicional** (categorias, "Sem categoria" e tipos) — **o texto cru da Categoria nunca entra em `id`, `htmlFor`, `aria-labelledby` ou qualquer IDREF**, ficando só no conteúdo visível/nome acessível (`<span>`) e nos rótulos dos chips. Exemplo de id técnico final: `«r7»-category-0` (prefixo do `useId` + `-category-` + índice). Categorias com espaço, barra, acento, parênteses, hífen, números e nomes semelhantes passam a ter checkbox corretamente nomeado (`getByRole('checkbox', { name: 'Peças Grandes / Cachepôs (2cm)' })`) e selecionável, com o IDREF resolvendo para o rótulo certo. **Nenhuma API, Edge Function, migration ou banco alterado; nenhuma categoria/Produto é modificado pelo filtro (só leitura); nenhuma reserva ou consumo de estoque implementado.** **Validação técnica concluída**: `ProductsPage.test.tsx` **120/120** (cobrindo opções disponíveis, OR/AND, "Sem categoria", estado vazio, contador, chips, "Limpar filtros" preservando a busca, filtro antes da ordenação, múltiplas categorias, categoria com caracteres especiais e id técnico seguro, nomes semelhantes, Escape, clique externo, persistência na sessão); suíte `vitest` completa **1750/1750**; `npm run lint` (0 erros, mesmos 8 avisos `react-refresh` pré-existentes em arquivos não tocados); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas. **Validação manual do usuário pendente.** Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** (recurso de listagem só frontend, não uma fase nova). Checkpoint: commits `feat: add category and type filters to the products list` (`887f7c6`, sessão anterior) e `fix: preserve accessible product filter labels` (`c598e35`), sem push, sem deploy. |
| 2026-08-31 | **Validação manual do usuário — filtro de Produtos por Categoria e Tipo — APROVADA INTEGRALMENTE, sem nenhuma alteração de código, banco ou função nesta entrada.** Os 12 passos do checklist foram exercitados em navegador real contra o Supabase remoto e todos aprovados, sem nenhum problema encontrado: (1) abertura e fechamento do painel "Filtros"; (2) lista de Categorias só com as categorias em uso, em ordem alfabética, e opção "Sem categoria" quando aplicável; (3) múltiplas categorias marcadas combinam por **OU** (união); (4) múltiplos tipos marcados combinam por **OU**; (5) Categoria e Tipo combinados aplicam **E** entre os grupos; (6) filtro **"Sem categoria"** traz só os Produtos sem categoria vinculada; (7) **contador de filtros ativos** no botão (`Filtros (N)`); (8) **chips removíveis individualmente**, com o nome integral da categoria/tipo; (9) **"Limpar filtros" preserva a busca** (`searchTerm`) e a ordenação; (10) **integração busca + filtros + ordenação** funcionando em conjunto (ordem dados → busca → Categoria → Tipo → ordenação); (11) **as seleções permanecem ao fechar e reabrir o painel** na mesma sessão (caixas marcadas e contador mantidos); (12) painel **fecha por Escape e por clique externo**, e **recarregar a página (F5) zera todos os filtros** (estado não persistido). Categorias com espaço, acento, barra e parênteses confirmadas com rótulo lido corretamente e checkbox clicável, comprovando na prática a correção de acessibilidade dos IDs técnicos (`useId()` + índice, texto cru da Categoria nunca em `id`/`htmlFor`/`aria-labelledby`). **Implementação aprovada manualmente.** **Nenhuma alteração de banco, migration, Edge Function ou regra de negócio nesta rodada; nenhuma reserva ou consumo de estoque implementado.** Base técnica reafirmada (rodada anterior): `ProductsPage.test.tsx` 120/120, suíte `vitest` completa 1750/1750, `npm run lint` (0 erros, 8 avisos pré-existentes), `npx tsc -b` e `npm run build` limpos. **Frontend ainda não publicado nesta rodada** — a validação foi feita contra o app rodando localmente sobre o Supabase remoto real. **Sincronização no GitHub da branch `feature/inventory-operations` realizada somente após este commit documental** (`git push origin feature/inventory-operations`, sem force, nenhuma outra branch, nenhum deploy). Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** (validação manual de um recurso de listagem só frontend, não uma fase nova). Checkpoint: commit `docs: record product filter validation`. |
| 2026-08-31 | **Janela "Novo produto"/"Editar produto" — ordem dos campos de cada plate invertida e novos formatos de digitação de tempo aceitos; tudo LOCAL, só frontend.** **(1) Ordem dos campos.** Em cada card de Plate, na criação e na edição, os campos passam a ser **Peso (g) à esquerda | Tempo de produção à direita** (antes era o contrário). Só a posição visual mudou: o contrato enviado por `onSubmit`/`create_product_with_plates`/`update_product_full` continua `{ production_time_seconds, weight_grams }`, e todo plate adicionado dinamicamente herda a mesma ordem. **(2) Formatos de tempo.** `parseDurationToSeconds` (`frontend/src/lib/forms/durationField.ts`) ganhou três formas compostas além das que já aceitava (`HH:MM`, `HH:MM:SS`, `1h30min`, `1,5h`, `90m`, `30m45s`…): `18h32` = 18 h 32 min (o número depois de `h` é sempre minuto, 0–59); `19m44` = 19 min 44 s e `32min20` = 32 min 20 s (o número depois de `m`/`min` é sempre segundo, 0–59). Aceita maiúsculas/minúsculas e ignora espaços externos. **Rejeitados, com mensagem clara, nunca normalizados em silêncio**: minutos/segundos acima de 59 nessas formas (`18h75`, `19m70`, `32min99`), negativos (`-1h20`), texto residual/parcial (`18h32abc`, `h32`, `min20`, `1h2m3`), `NaN`, `Infinity` e notação exponencial. As formas antigas seguem idênticas — `90m` e `01:90:00` continuam **carregando** para a unidade superior (só as novas formas compostas têm o teto de 0–59). Placeholder/texto auxiliar dos campos de tempo (por plate e do "Tempo efetivo" do ajuste manual) atualizado para `Ex.: 18:32, 18h32, 19m44 ou 32min20`, associado por `aria-describedby`. **(3) Precisão de segundos — auditada e preservada.** O contrato de armazenamento já é **segundos inteiros** (`products.default_print_time_seconds`, `product_plates.production_time_seconds`, `products.production_time_manual_override_seconds` — renomeados de `_minutes` numa migration anterior); o envio (criação e edição) já mandava exatamente o resultado de `parseDurationToSeconds`, sem arredondar. O único ponto com perda era o **pré-preenchimento da edição / exibição do total**, que usava `formatSecondsToHHMM` e truncava os segundos (um "Salvar" sem tocar no campo então os perdia). Novo `formatSecondsAdaptive`: mostra `hh:mm` quando o valor não tem segundos e `hh:mm:ss` quando tem — um plate salvo como `00:30:45` reabre como `00:30:45` e faz round-trip sem perda. **Nenhuma migration criada ou aplicada, nenhuma Edge Function publicada, nenhum dado remoto tocado** (o contrato já suportava segundos; a auditoria confirmou isso antes de qualquer edição). Nenhuma alteração em Pedidos, Produção ou Estoque além de continuarem lendo o mesmo contrato. **Validação técnica**: `durationField.test.ts` + `productPlates.test.ts` + `ProductForm.test.tsx` (específicos) **347/347**; suíte `vitest` completa **1807/1807** (1750 → 1807, +57 testes novos cobrindo ordem dos campos incl. plates adicionados, formatos legado e novos no submit, erros de faixa bloqueando o envio, round-trip de edição preservando segundos, ajuste manual, somas com carry, `formatSecondsAdaptive`); `npm run lint` (0 erros, mesmos 8 avisos `react-refresh` pré-existentes em arquivos não tocados); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas; varredura de segredos no diff sem resultado. `npx prettier --write` normalizou de passagem algumas linhas longas pré-existentes nos mesmos 7 arquivos já modificados (nenhum arquivo novo tocado). **Validação manual do usuário ainda pendente.** **Nenhum deploy; nenhuma reserva ou consumo de estoque implementado.** Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** (ajuste de formulário só frontend, não uma fase nova). Checkpoint: commit `feat: support flexible plate production times` (`5a3ffc3`), sem push, sem deploy. |
| 2026-09-01 | **Validação manual do usuário — nova ordem dos campos de plate e formatos flexíveis de tempo — APROVADA INTEGRALMENTE, sem nenhuma alteração de código, banco ou função nesta entrada.** Os 12 passos do checklist foram exercitados em navegador real contra o Supabase remoto e todos aprovados, sem nenhum problema: (1) Peso (g) à esquerda e Tempo de produção à direita em cada card de Plate; (2) a mesma ordem no segundo plate e em plates adicionados dinamicamente; (3) `18h32` interpretado como 18 h 32 min; (4) `19m44` interpretado como 19 min 44 s; (5) `32min20` interpretado como 32 min 20 s; (6) formato anterior `HH:MM` preservado; (7) os totais preservam e somam os segundos corretamente (carry de 60 s → 1 min e 60 min → 1 h); (8) formatos com minutos ou segundos fora da faixa 0–59 (`18h75`, `19m70`, `32min99`) são bloqueados com mensagem clara, sem normalização silenciosa; (9) o ajuste manual de totais aceita os novos formatos; (10) salvar → fechar → editar de novo preserva os segundos (um plate salvo como `00:30:45` reabre como `00:30:45`); (11) a Ficha Técnica apresenta o tempo correto; (12) o Produto TESTE usado na validação foi removido pelo usuário após a conferência. **Funcionalidade aprovada integralmente.** **Armazenamento exato em segundos confirmado** (`products.default_print_time_seconds`, `product_plates.production_time_seconds`, `products.production_time_manual_override_seconds` — inteiros em segundos); **nenhuma migration necessária** (a auditoria da rodada anterior confirmou que o contrato já suportava segundos; o único ponto de truncamento estava no pré-preenchimento da edição/exibição do total, corrigido no frontend com `formatSecondsAdaptive`). **Nenhuma alteração de banco ou Edge Function; nenhuma reserva ou consumo de estoque implementado.** Base técnica reafirmada (rodada anterior): `durationField.test.ts`/`productPlates.test.ts`/`ProductForm.test.tsx` 347/347, suíte `vitest` completa 1807/1807, `npm run lint` (0 erros, 8 avisos pré-existentes), `npx tsc -b` e `npm run build` limpos. **Frontend não publicado nesta rodada** — a validação foi feita contra o app rodando localmente sobre o Supabase remoto real. **Sincronização no GitHub da branch `feature/inventory-operations` realizada somente após este commit documental** (`git push origin feature/inventory-operations`, sem force, nenhuma outra branch, nenhum deploy). Módulo 3 continua **NÃO concluído**. Percentual macro **não alterado** (validação manual de um ajuste de formulário só frontend, não uma fase nova). Checkpoint: commit `docs: record flexible production time validation`. |
| 2026-09-01 | **Auditoria somente-leitura do Módulo 3 + correção documental + congelamento arquitetural do próximo incremento (motor de reserva/consumo/liberação de estoque por Pedido).** Nenhum código, migration, RPC, Edge Function, interface ou dado remoto alterado. **Correções de estado obsoleto** (histórico datado preservado; correção acrescentada, nunca sobrescrita): a tabela macro (§2) e a de módulos (§5) diziam "27 migrations", "`stock_movements` só local / não aplicado", "nenhuma implementação de inventário físico", "cadastro mestre em implementação", "migrations de Acessórios/Embalagens não aplicadas", "composição do Produto é o próximo incremento", "`product_filaments` estrutura ativa", "`spool_tares` pendente", "Módulo 3 ~21%". Estado real auditado: **45/45 migrations sincronizadas**; `stock_movements`+`register_stock_movement`, filamentos (tipos/rolos/movimentações/pesagem) e Compras **aplicados ao remoto**; **7 Edge Functions de Estoque publicadas** (v1); cadastro mestre + movimentações + filamentos + compras **validados manualmente** (2026-08-27/28/29); `spool_tares` **descartada por arquitetura** (tara virou `filament_spools.empty_spool_weight_grams` por rolo); `product_filaments`/`product_plate_filaments` **LEGADAS** desde `20260829180000` (filamento/cor saiu do Produto e foi para o Pedido); composição do Produto por plates **já concluída**. **Percentual do Módulo 3 recalculado pela metodologia das 7 fases** → F1 100% / F2 70% / F3 70% / F4 65% / F5 50% / F6 0% / F7 0% = **355/7 ≈ 51%** (ver §9c.2 para as evidências por fase). Módulo 3 **continua NÃO concluído**. **Congelado em §9c** (nova seção): as 8 regras aprovadas pelo usuário para o motor (reserva de Acessórios/Embalagens na entrada da Fila; reserva de filamento só após cores completas; saldo insuficiente alerta mas não impede a Fila; produção só começa com tudo reservado; reserva→consumo ao entrar em `IN_PRODUCTION`; cancelamento antes do consumo libera; sem devolução automática pós-consumo; gramas por filamento por unidade/plate somando o peso congelado do plate); **auditoria do snapshot de Acessórios/Embalagens** → **não existe** estrutura order-scoped (só `order_item_plates` e `order_item_unit_plate_filaments` são congelados) → criar `order_item_accessories`/`order_item_packaging` copiados atomicamente na criação do item é **requisito obrigatório**; **auditoria de gramas por filamento** → `order_item_unit_plate_filaments` guarda só `filament_type_id` (sem gramas, sem rolo), várias cores por unidade/plate permitidas, seleção editável até `IN_PRODUCTION` → campo de gramas é requisito, soma = peso do plate, sem divisão implícita; **alocação entre rolos** → Pedido escolhe tipo, estoque está em rolos, `register_filament_movement` exige `spool_id` → 3 opções (reserva por tipo com alocação de rolo no consumo / reserva já aloca rolo / híbrido), recomendação preliminar "reserva por tipo, rolo no consumo (R1)", política de escolha de rolo e escolha manual **pendentes do usuário**; **semântica da reserva** → `RESERVATION` no ledger físico causaria baixa dupla e `filament_movements` exige rolo → 3 arquiteturas comparadas (A eventos no ledger atual / B ledger separado / C coluna `reserved` materializada + ledger de reservas), **recomendação: C** (preserva o invariante dos ledgers físicos, disponibilidade O(1) = físico − reservado, resolve reserva de filamento por tipo); **Pedidos legados** → nunca modificar automaticamente, nunca reconstruir composição do Produto atual, nunca reserva retroativa, migration identifica Pedidos em Fila/Produção e os deixa em modo "sem rastreamento", decisões do usuário sobre reprocessar ou exigir Fila vazia; **fronteira Módulo 3 × Módulo 4** → Módulo 4 só inicia depois do motor aplicado+validado **e** do consumo valorado (política de custo — snapshot ou custo médio — a definir). **8 decisões do usuário ficam pendentes** (§9c.10). Nenhum push, nenhum deploy. Checkpoint: commit `docs: define order stock integration contract`. |
| 2026-09-01 | **Listagem de Filamentos consolidada por Material + Linha + Cor + filtros — só frontend, sem tocar banco/migrations/RPC/Edge Function/dados.** A tabela principal de `/estoque/filamentos` **perde a coluna Fabricante**: tipos de fabricantes diferentes com o mesmo Material + Linha + Cor (normalizados: minúsculas, sem acentos, espaços de borda e internos colapsados — grafias equivalentes nunca duplicam a linha) viram **uma linha consolidada**. O **fabricante continua íntegro** no banco, no cadastro de tipo/rolo, nas compras, no histórico e nos detalhes de "Ver rolos" (nada removido de `filament_types.manufacturer`). Identidade da Cor = `commercial_color` (NOT NULL, já é a identidade comercial do domínio — `color_code` é nullable e foi retirado da UI no Incremento 5, nunca serviria). Rótulo canônico exibido = grafia mais frequente do grupo (empate: ordem pt-BR). **Valores consolidados**: peso disponível = Σ `vw_filament_type_summary.total_available_grams` dos tipos do grupo; **"Rolos disponíveis"** = Σ `usable_spool_count` (a view já exclui `is_active=false` e status `ESGOTADO`/`DESCARTADO`; nenhum rolo é contado duas vezes — cada rolo pertence a um só `filament_type_id`, cada tipo aparece uma vez na view). **Estoque mínimo consolidado** = **maior** `minimum_stock_grams` entre os tipos **ativos** do grupo (regra determinística documentada — nunca soma limites de fabricantes diferentes, que multiplicaria o mínimo; `null` se nenhum tipo ativo tem limite). Ordenação da listagem: determinística por Material → Linha → Cor (rótulo consolidado), **nunca pelo primeiro fabricante**. Busca adaptada: encontra por Material/Linha/Cor e por **qualquer fabricante do grupo** (mesmo sem a coluna). **Filtros novos** (`frontend/src/lib/inventory/filamentGroups.ts` + `filamentGroupFilters.ts`, funções puras): três botões multisseleção **Material / Linha / Cor** ao lado da busca (OR dentro do grupo, AND entre grupos; opções derivadas dos dados, sem duplicatas por maiúsculas/acentos/espaços; contador no botão; "Limpar" por grupo e "Limpar filtros" geral; combinam com busca; estado vazio específico) — mesmo `Popover`/`Checkbox` já usados no filtro de Produtos, IDs técnicos só de `useId`+índice. **Filtro de Nº de rolos** (mín/máx) no **cabeçalho da própria coluna** "Rolos disponíveis": inteiros, mínimo 0, limites inclusivos, aceita só mín / só máx / ambos, avisa e não aplica quando mín > máx, usa a contagem **consolidada** (nunca por fabricante), não altera a ordenação, preserva a alça de redimensionamento. **Ações da linha**: só **"Ver rolos"** — Editar/Excluir tipo saíram da linha consolidada (nunca agiriam sobre um `filament_type_id` arbitrário) e foram para dentro de "Ver rolos", por tipo/fabricante individual, junto de Ativar/Desativar tipo e "Novo rolo" por tipo. **Painel "Ver rolos"** passa a receber o **grupo** (não um `filament_type_id`): carrega os rolos de **todos os tipos do grupo numa única consulta** (`listFilamentSpools` aceita `string[]` via `.in(...)` — SELECT já concedido a `authenticated`, **sem N+1, sem contrato novo**); cada rolo mostra o fabricante/tipo a que pertence; se "Editar tipo" mover o tipo para outro Material/Linha/Cor, o grupo encolhe (ou o painel fecha quando some o último tipo) e a listagem se reorganiza. **Preservados integralmente**: delete guards, arquivamento vs. exclusão decidido por `has_movement_history` antes da confirmação, histórico sempre acessível, rolo `DESCARTADO` terminal, "Mostrar arquivados" só muda visibilidade (nunca o resumo), mensagens reais do backend, resumo consolidado sempre vindo da view (nunca recalculado em JS). **Larguras persistidas**: `FILAMENTS_TABLE_ID` inalterado; `normalizeColumnWidths` descarta sozinho as larguras das colunas removidas (`manufacturer`, `is_active`) e preserva as demais — **nenhum `localStorage.clear()`, nenhum bump de `SCHEMA_VERSION`**. **Validação técnica**: 2 arquivos de teste puros novos (`filamentGroups.test.ts` 21, `filamentGroupFilters.test.ts` cobrindo consolidação, canônico, mínimo consolidado, OR/AND, faixa mín/máx incl. 0-0 e inválido); `FilamentsInventoryPage.test.tsx` reescrito (consolidação, filtros, faixa no cabeçalho, ações só "Ver rolos", drawer por grupo com fabricante por rolo, ações de tipo individuais, edição de tipo reorganizando o grupo, "Novo rolo" por tipo, regressões de movimentação/pesagem/descarte/excluir-arquivar/mostrar-arquivados); `filamentSpools`/`useFilamentSpools` cobrindo o modo `string[]`. Suíte `vitest` completa **1833/1833** (74 arquivos); `npm run lint` (0 erros, mesmos 8 avisos pré-existentes); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas; `.env.local` confirmado ignorado; scan de segredos sem resultado. **Nenhuma migration, nenhuma Edge Function, nenhum dado remoto alterado; nenhuma reserva/consumo/custo médio implementado. Implementação apenas local, validação manual do usuário ainda pendente.** Módulo 3 permanece em **~51%** (recurso de listagem só frontend, não uma fase nova). Checkpoint: commit `feat: consolidate filament inventory filters`, sem push, sem deploy. |
| 2026-09-01 | **Correção: "Rolos disponíveis" passa a exigir saldo positivo — só frontend, sem migration, sem alterar a view remota.** A entrada anterior descrevia "Rolos disponíveis" como Σ `vw_filament_type_summary.usable_spool_count`. Essa contagem exclui rolo inativo/`ESGOTADO`/`DESCARTADO`, mas **não exige `current_net_weight_grams > 0`**: um rolo `LACRADO` recém-criado com 0 g (sem `INITIAL_BALANCE` ainda) seria contado como disponível. A regra aprovada é: **rolo disponível = `is_active` E status ∉ {ESGOTADO, DESCARTADO} E `current_net_weight_grams > 0`** (saldo negativo, que não deveria existir, também não conta). **Implementação**: novo predicado puro `isFilamentSpoolAvailable` + `countAvailableSpoolsByType` (`frontend/src/lib/inventory/filamentGroups.ts`); nova função de leitura `listAvailableSpoolCountsByType(ids)` (`frontend/src/lib/api/filamentSpools.ts`) — **uma única consulta** `supabase.from('filament_spools').select('id, filament_type_id, current_net_weight_grams, status, is_active').in('filament_type_id', ids)` (SELECT já concedido a `authenticated` / RLS `is_active_user()`), **sem consultar `filament_movements`, sem N+1, cada `spool.id` contado no máximo uma vez, lista vazia não consulta**; novo hook `useFilamentSpoolCounts` (mesmo padrão de `useFilamentSpools`, chave de ids estável). `FilamentGroup.usableSpoolCount` vira `availableSpoolCount: number | null` — `groupFilamentTypes(types, availableCountByTypeId)` soma o mapa por grupo; `null` quando a contagem ainda não carregou ou **a consulta falhou**. **A mesma contagem correta é usada em**: coluna "Rolos disponíveis" da listagem, filtro mín/máx do cabeçalho, e o resumo "Rolos disponíveis" dentro de "Ver rolos" (o drawer exibe `group.availableSpoolCount`, nunca recalcula) — listagem e drawer sempre mostram o mesmo número. O **peso disponível** continua vindo da view (rolos zerados somam 0 g — correto). **Diante de erro**: a coluna e o drawer mostram **"—"**, o filtro de faixa fica desabilitado com aviso ("Contagem … indisponível"), aparece um banner "Tentar novamente" que chama `refetchCounts` — **nunca cai em `usable_spool_count` como fallback silencioso**; a faixa mín/máx nunca descarta um grupo cuja contagem é `null`. Enquanto a primeira contagem carrega, a tabela espera (skeleton). Criar/movimentar/pesar/arquivar/descartar/excluir rolo aciona `refetchAll` (tipos **e** contagem). `usable_spool_count` deixou de ser exibido em qualquer lugar. **Validação técnica**: `filamentGroups.test.ts` (predicado com os 8 casos: ABERTO 100g→conta, LACRADO 1000g→conta, ABERTO/LACRADO 0g→não, negativo→não, ESGOTADO→não, DESCARTADO+→não, inativo+→não; `countAvailableSpoolsByType` sem dobrar id repetido), `filamentGroupFilters.test.ts` (faixa usa a contagem correta; 0–0 encontra grupo só-zerado; `null` nunca descarta), `filamentSpools.test.ts` (uma consulta `.in`, sem `filament_movements`, select mínimo, vazio→sem consulta, erro→ApiError), `useFilamentSpoolCounts.test.ts` (lote, ids estáveis, erro→null, refetch), `FilamentsInventoryPage.test.tsx` (coluna mostra a contagem correta e não a da view quando divergem; soma consolidada sem duplicação; filtro usa a correta; 0–0; listagem == drawer; erro→"—" + filtro desabilitado + banner/refetch + drawer "—"; loading espera; movimentar aciona refetch da contagem). Suíte `vitest` completa **1859/1859** (75 arquivos; 1833 anteriores todos preservados); `npm run lint` (0 erros, mesmos 8 avisos pré-existentes); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas; `.env.local` ignorado; scan de segredos sem resultado. **Nenhuma migration, nenhuma alteração da view remota, nenhum dado remoto tocado; nenhuma reserva/consumo. Implementação apenas local, validação manual do usuário ainda pendente.** Módulo 3 permanece em **~51%**. Checkpoint: commit `fix: count only available filament spools`, sem push, sem deploy. |
| 2026-09-01 | **Campo Fabricante removido só da janela "Novo tipo de filamento" — decisão revisada do usuário; só frontend.** No modo **CREATE** o formulário (`frontend/src/components/inventory/FilamentTypeForm.tsx`) não exibe mais o campo Fabricante/Fornecedor, não o exige e nunca mostra "Informe o fabricante" — um tipo pode ser cadastrado só com Material, Linha, Cor, Peso mínimo de alerta (opcional) e Observações (opcionais). Como `filament_types.manufacturer` é **NOT NULL** no schema atual e alterar isso está fora de escopo, a criação **envia internamente `manufacturer: 'Não informado'`** (nova constante exportada `UNSPECIFIED_MANUFACTURER`). O modo **EDIT** preserva o campo Fabricante integralmente — para consultar e corrigir tipos históricos e os criados pelo fluxo de **Compras** (que podem trazer um fabricante real informado na compra). Fabricantes já gravados **continuam intactos** no banco e visíveis/editáveis. Comentário introdutório da listagem (`frontend/src/pages/FilamentsInventoryPage.tsx`) atualizado. **Nenhuma migration, nenhuma alteração de banco remoto, RPC, Edge Function, dados existentes, compras, movimentações ou regras de reserva/consumo.** **Validação técnica**: novos testes em `FilamentsInventoryPage.test.tsx` provando que "Novo tipo de filamento" não exibe Fabricante nem Fornecedor, salva só com Material/Linha/Cor sem erro de fabricante, `create` recebe `manufacturer: 'Não informado'` com os demais campos (Material/Linha/Cor/mínimo/observações) corretos, e que "Editar tipo de filamento" mantém o campo Fabricante para tipos históricos. Suíte `vitest` completa **1862/1862** (75 arquivos; 1859 anteriores preservados + 3 novos); `npm run lint` (0 erros, mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas. **Implementação apenas local, validação manual do usuário ainda pendente.** Módulo 3 permanece em **~51%**. Checkpoint: commit `fix: remove manufacturer from filament type creation`. |
| 2026-09-02 | **Cadastro de tipo sem fabricante aprovado integralmente pelo usuário (passos 1 a 9 OK) — e limpeza visual da janela "Ver rolos"; só frontend.** A validação manual do fluxo de cadastro sem fabricante foi concluída e aprovada em todos os passos. Nesta rodada, ajuste **apenas de apresentação** da janela aberta pelo botão "Ver rolos" (`frontend/src/pages/FilamentsInventoryPage.tsx` + `frontend/src/components/inventory/FilamentTypeDrawer.tsx`): (1) o **título da janela** deixa de ser o texto fixo "Ver rolos" e passa a ser a **identificação dinâmica do grupo** no formato `Material - Linha - Cor` (ex.: `PLA - TESTE SILK - TESTE Dourado`), na mesma posição e com a mesma configuração tipográfica do `DialogTitle` anterior — **o `DialogTitle` continua existindo** (nome acessível da janela passa a ser essa identificação); (2) removido o bloco redundante **"Grupo"** + `Material · Linha · Cor` que repetia a identificação no corpo; (3) removido o resumo **"Fabricantes: …"** do topo (o fabricante **continua íntegro no banco** e visível **por rolo** na tabela e **por tipo** na seção de tipos — nada apagado de `filament_types.manufacturer`); (4) a seção **"Tipos e fabricantes"** foi renomeada para **"Rolos em estoque"** (mesma configuração visual). O **botão da listagem continua "Ver rolos"** — a mudança de rótulo é só depois que a janela abre. **Preservados integralmente**: cards de resumo (Disponível, Rolos disponíveis, Abertos, Esgotados, Estoque mínimo, Situação), botão "Novo rolo", menu de ações do tipo, "Mostrar arquivados", listagem de rolos, movimentação/pesagem/histórico/arquivamento/exclusão, agrupamento por Material + Linha + Cor, filtros, e todas as regras de reserva/consumo. **Nenhuma alteração de banco, migration, RPC, Edge Function, dados existentes ou regras de estoque.** **Validação técnica**: novo teste em `FilamentsInventoryPage.test.tsx` provando que o botão da linha continua "Ver rolos", que a janela é localizável pelo nome acessível `PLA - Sólida - Preto`, que não há "Ver rolos"/"Grupo"/"Fabricantes:" dentro da janela nem a identificação duplicada como subtítulo, que aparece "Rolos em estoque", que os cards de resumo permanecem e que "Novo rolo"/menu de ações do tipo/"Mostrar arquivados" seguem ativos; asserções de outros testes da janela migradas para o novo título dinâmico. Suíte `vitest` completa **1863/1863** (75 arquivos; 1862 anteriores preservados + 1 novo); `npm run lint` (0 erros, mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `prettier --check` nos arquivos tocados OK; `git diff --check` sem problemas. **Implementação apenas local, nova mudança visual ainda pendente de validação manual do usuário.** Módulo 3 permanece em **~51%**. Checkpoint: commit `fix: simplify filament spool dialog labels`. |
| 2026-09-02 | **Reorganização da janela "Ver rolos" — a validação manual anterior (itens 1 a 10) foi aprovada; esta reorganização aguarda nova validação manual. Só frontend.** A limpeza visual anterior da janela "Ver rolos" (título dinâmico `Material - Linha - Cor`, remoção do bloco "Grupo" e do resumo "Fabricantes:", seção renomeada para "Rolos em estoque") foi **validada manualmente e aprovada pelo usuário nos itens 1 a 10**. Nesta rodada, reorganização do **conteúdo** da janela (`frontend/src/components/inventory/FilamentTypeDrawer.tsx`), **apenas de interface**: (1) **removida a linha redundante de tipo** que ficava logo abaixo de "Rolos em estoque" — o antigo bloco `data-testid="filament-type-row-${filament_type_id}"` com Fabricante ("Não informado" nos cadastros sem fabricante), Linha · Cor, botão "Novo rolo" e o **menu de três pontos de tipo** — sem deixar espaço vazio; (2) a **listagem física de rolos** (tabela no desktop, cards no mobile) passa a vir **logo abaixo do título "Rolos em estoque"**, com as mesmas colunas (Identificador, Fabricante / tipo, Peso, Status, Ações) e o mesmo comportamento — nada mudou nos dados nem nas ações da tabela; (3) o filtro **"Mostrar arquivados"** foi movido para **depois** da tabela/cards/estado vazio, num bloco de controles inferiores, e continua funcionando igual; (4) o **menu de três pontos de tipo** foi substituído por **action buttons visíveis** no rodapé: **Novo rolo** (outline da marca), **Editar tipo** (outline), **Ativar tipo / Desativar tipo** conforme o estado (outline, `disabled` durante o toggle pendente) e **Excluir tipo** (`destructive`), com `flex-wrap` para telas estreitas; (5) **um conjunto de botões por tipo** — quando o grupo reúne tipos históricos de fabricantes diferentes, cada conjunto é embrulhado em `role="group"` com `aria-label="Ações do tipo <fabricante> — <cor>"` e, nesse caso, recebe um rótulo visível (fabricante + linha · cor + selo "Inativo"); com um único tipo, aparece só um conjunto, sem rótulo visível (a identificação já está no título da janela) — **a antiga linha redundante do topo não foi recriada**; (6) cada botão continua ligado ao `filament_type_id` correto e reaproveita os mesmos callbacks (`onEditType` / `onToggleType` / `onDeleteType` / `openCreateDialog`) e seus diálogos, confirmações e mensagens — `TypeActionsMenu` foi trocado por `TypeActionButtons`, sem duplicar regra alguma. Ordem visual final da janela: título → resumo do estoque → "Rolos em estoque" → tabela/cards → estado vazio → "Mostrar arquivados" + botões de tipo → rodapé "Fechar". **Preservados integralmente**: o **menu de três pontos de cada rolo físico** na coluna "Ações" (Editar / Ativar-Desativar / Descartar / Arquivar-Excluir rolo) e o botão "Gerenciar"; o resumo consolidado vindo de `vw_filament_type_summary`; delete guards, arquivamento vs. exclusão por `has_movement_history`, `DESCARTADO` terminal, histórico acessível, mensagens reais do backend; no estado vazio a mensagem fica abaixo de "Rolos em estoque" e "Novo rolo" continua cadastrando o primeiro rolo. **Nenhuma alteração de banco, migration, view, RPC, Edge Function, dados existentes ou regras de negócio/estoque.** **Validação técnica**: novos testes em `FilamentsInventoryPage.test.tsx` — ausência do bloco `filament-type-row-*` e do menu de três pontos de tipo; listagem física antes do filtro e dos botões inferiores (ordem verificada por `compareDocumentPosition`, tabela desktop e card mobile); presença e funcionamento de Novo rolo / Editar tipo / Desativar tipo / Excluir tipo; preservação do menu de três pontos do rolo físico; ações ligadas ao `filament_type_id` certo; grupo com dois tipos históricos aciona `delete` só no tipo escolhido; "Novo rolo" disponível e funcional no estado vazio; testes anteriores da janela migrados do menu para os botões/`role="group"`. Suíte `vitest` completa **1867/1867** (75 arquivos; 1863 anteriores preservados + 4 novos); `npm run lint` (0 erros, mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `prettier --check` nos arquivos tocados OK; `git diff --check` sem problemas. **Implementação apenas local, sem deploy; reorganização ainda pendente de validação manual do usuário.** Módulo 3 permanece em **~51%**. Checkpoint: commit `fix: reorganize filament spool dialog actions`. |
| 2026-09-02 | **Enxugamento da janela "Ver rolos" e da janela "Gerenciar" — a validação manual anterior da reorganização da janela "Ver rolos" (itens 1 a 10, commit `fix: reorganize filament spool dialog actions`) foi aprovada; esta rodada aguarda nova validação manual. Só frontend, sem banco/migration/view/RPC/Edge Function.** Alterações de interface em `frontend/src/components/inventory/FilamentTypeDrawer.tsx`, `FilamentSpoolPanel.tsx` e `FilamentMovementForm.tsx`. **Janela "Ver rolos"**: (1) a coluna "Peso" da tabela desktop passa a se chamar **"Peso Líquido"** e mostra só o peso líquido atual formatado em gramas (`current_net_weight_grams`), sem a composição `200g / 1.000g · 20%`; o card mobile idem (rótulo "Peso Líquido"). **Peso Líquido = quantidade física real de filamento no rolo, sem o carretel** — usa o campo de saldo físico já persistido, sem mudar o significado do dado. (2) Nova coluna **"Ajustar peso"** entre "Status" e "Ações": botão com o ícone `SlidersHorizontalIcon` (o mesmo da operação "Ajuste"), `aria-label` "Ajustar peso do rolo &lt;código&gt;", que abre a mesma janela do botão "Gerenciar" já na aba "Movimentar" com a operação "Ajuste" pré-selecionada — sem exigir segundo clique, mantendo o rolo pelo id e sem gravar nada antes da confirmação. O botão também aparece nos cards mobile, antes de "Gerenciar". Não duplica a lógica de ajuste: reaproveita o mesmo formulário/estado/callback (`FilamentMovementForm` com `initialOperation='ADJUST'`). (3) **Menu de três pontos de cada rolo físico** reduzido a **"Editar"** e **"Excluir rolo"** — saíram Desativar, Descartar e Arquivar rolo. "Editar" preserva a edição atual do rolo. (4) **"Excluir rolo"** é sempre **exclusão real e definitiva** via `delete_filament_spool` (Edge Function `DELETE /filament-spools/:id`, RPC que já existe e **bloqueia por `FILAMENT_SPOOL_HAS_MOVEMENTS`, nunca cascateia**) — nunca arquivamento nem desativação; confirmação explícita cita o código do rolo, avisa que é permanente, só exclui após confirmar, atualiza listagem e resumo no sucesso e exibe a mensagem real do backend quando a exclusão não é permitida. As restrições de integridade do backend não foram enfraquecidas; nenhuma cascata foi improvisada. **Janela "Gerenciar"** (`FilamentSpoolPanel`): (5) cabeçalho passa a mostrar só **`Marca - Cor - Tipo`** (fabricante - cor comercial - linha) como `DialogTitle` e o **código do rolo** como subtítulo (`DialogDescription`), uma única vez — sem o texto fixo "Movimentar / Pesar / Histórico" e sem repetição do código. (6) O resumo do rolo passa de quatro indicadores (Peso nominal / Peso disponível / % restante / Status) para **três, nesta ordem: Peso Líquido, Status, Peso Disponível**. **Peso Disponível = max(0, Peso Líquido − peso reservado para pedidos)**. Auditoria: o modelo atual **não tem nenhum campo nem cálculo de reserva persistido** (o motor de reserva Pedidos → Estoque do Módulo 3 ainda não existe), então nesta rodada `peso_reservado = 0` e **Peso Disponível == Peso Líquido**; nenhuma coluna, migration ou regra de reserva foi criada — só o cálculo local (`reservedGrams = 0`) fica pronto para os dois valores divergirem quando a integração for desenvolvida. "% restante" foi removido. (7) **Área de movimentação** (`FilamentMovementForm`): o seletor de operações passa a ter só **"Registrar perda"** e **"Ajuste"**. "Registrar perda" substitui o antigo "Saída" e grava sempre `movement_type = LOSS` (baixa negativa, motivo obrigatório — comportamento de perda inalterado). "Ajuste" pede o **novo peso líquido em gramas**; o sinal do delta (novo − atual) escolhe `POSITIVE_ADJUSTMENT` ou `NEGATIVE_ADJUSTMENT` e a quantidade enviada é o valor absoluto — delta zero é rejeitado, motivo obrigatório (regra já existente dos dois ajustes), isento do teto de peso nominal. Foram removidos o botão "Entrada", as opções "Compra" e "Devolução", "Saldo inicial" e **toda a seção "Tipo"**. O formulário continua com Quantidade/novo peso em gramas, "Ocorrido em", Motivo/observação e botão de confirmação (rótulo adaptado: "Registrar perda" / "Confirmar ajuste"); idempotência por tentativa lógica preservada. As regras de `register_filament_movement` (migration 20260827110000) não mudaram. (8) **"Registrar pesagem"** (aba, cálculo com tara, histórico, validações) **preservada integralmente**; `WEIGHING_ADJUSTMENT` continua exclusivo dela. (9) O atalho "Ajustar peso" não vaza: o painel é remontado a cada abertura do diálogo (a `key` inclui o modo de abertura), então reabrir o mesmo rolo pelo botão "Gerenciar" começa sem operação selecionada — estado inicial previsível. **Validação técnica**: `FilamentMovementForm.test.tsx` reescrito (só "Registrar perda" e "Ajuste"; LOSS com motivo e teto de saldo; Ajuste para mais → POSITIVE_ADJUSTMENT, para menos → NEGATIVE_ADJUSTMENT, delta zero rejeitado, fracionário, sem teto nominal; `initialOperation` pré-seleciona; ausência de Entrada/Compra/Devolução/Saldo inicial/"Tipo"; erro do backend preserva os valores). Novos testes em `FilamentsInventoryPage.test.tsx` (cabeçalho "Peso Líquido" e célula sem nominal/percentual; card mobile "Peso Líquido:"; coluna "Ajustar peso" entre Status e Ações com `aria-label` pelo código; atalho abre o rolo certo com "Ajuste" marcado; menu de três pontos só "Editar"/"Excluir rolo"; "Editar" abre a edição; "Excluir rolo" com confirmação citando o código + "permanente" chama o handler de exclusão, nunca `update`, e atualiza a listagem; erro real do backend exibido quando bloqueada; cabeçalho "Gerenciar" só "Marca - Cor - Tipo" + código único; resumo só Peso Líquido/Status/Peso Disponível nessa ordem, sem nominal/percentual; Peso Disponível == Peso Líquido sem reserva; operações só "Registrar perda"/"Ajuste"; "Registrar pesagem" e histórico preservados; atalho de ajuste não vaza para uma abertura posterior por "Gerenciar"). Suíte `vitest` completa **1880/1880** (75 arquivos; 1867 anteriores + 13 novos/reescritos); `npm run lint` (0 erros, mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `prettier --check` nos arquivos tocados OK (o pré-existente `FilamentSpoolPanel.tsx` também foi normalizado); `git diff --check` sem problemas. **Nenhuma regra de reserva foi inventada. Nenhuma alteração de banco, migration, view, RPC ou Edge Function. Implementação apenas local, sem deploy; aguardando validação manual do usuário.** Módulo 3 permanece em **~51%**. Checkpoint: commit `fix: streamline filament spool management`. |
| 2026-09-03 | **Validação manual da interface de rolos aprovada nos itens 1 a 15.** O usuário confirmou o funcionamento da coluna **Peso Líquido**, do atalho **Ajustar peso**, do menu reduzido a **Editar/Excluir rolo**, do cabeçalho e resumo da janela **Gerenciar**, das operações **Registrar perda/Ajuste** e da preservação de **Registrar pesagem/Histórico**. Os dois cenários destrutivos permaneceram explicitamente **não testados**: exclusão definitiva de rolo vazio e bloqueio da exclusão de rolo com histórico. Portanto, a interface está aprovada, mas a validação manual da exclusão definitiva continua pendente e não deve ser tratada como concluída. Nenhum código, banco, migration, view, RPC, Edge Function ou dado remoto foi alterado nesta entrada; nenhum deploy foi realizado. O Módulo 3 permanece em **~51%**. Checkpoint documental: commit `docs: record spool management validation`. |
| 2026-09-03 | **"Excluir rolo": exclusão física só sem histórico; com movimentações, arquiva preservando o histórico — regra revisada do usuário, só frontend.** Contexto: a exclusão definitiva de rolo vazio (0 g, não contabilizado como disponível, menu só Editar/Excluir rolo, confirmação de exclusão permanente, exclusão física concluída, resumos atualizados) foi **validada manualmente e aprovada** (itens 1 a 7). Ao tentar excluir um rolo de TESTE com histórico, o backend retornou corretamente `FILAMENT_SPOOL_HAS_MOVEMENTS`: "Este rolo possui movimentações registradas e não pode ser excluído. Desative ou descarte o rolo." — comportamento de proteção esperado. O usuário revisou a regra: todos os rolos podem ser removidos exceto os vinculados a Pedido; havendo movimentações, o histórico é preservado e o rolo apenas some do estoque ativo. **Nova regra implementada em `frontend/src/components/inventory/FilamentTypeDrawer.tsx`** (nenhuma alteração de banco/migration/view/RPC/Edge Function): o menu de três pontos de cada rolo continua **só com "Editar" e "Excluir rolo"** (Desativar/Descartar/Arquivar **não** voltaram). Ao clicar em "Excluir rolo", o handler `handleConfirmRemoveSpool` decide por `has_movement_history`: (a) **rolo SEM movimentações** → `deleteSpool` (Edge Function `DELETE /filament-spools/:id` → `delete_filament_spool`, sem cascata, FKs de `filament_movements` intactas); confirmação mantém o título "Excluir rolo" e o texto de **exclusão permanente**; botão `destructive` "Excluir definitivamente"; toast "Rolo excluído."; (b) **rolo COM movimentações** → **não chama DELETE**; usa o **mecanismo de inativação segura já existente** (`update` → `PATCH /filament-spools/:id` → `update_filament_spool` com `{ is_active: false }`, o mesmo que o antigo "Arquivar rolo" fazia antes da simplificação do menu). Confirmação diferente, com o título "Remover rolo do estoque" e o texto "Este rolo possui histórico. Ele será removido do estoque ativo, mas suas movimentações serão preservadas." — **sem a palavra "permanente"** e **sem** a orientação "Desative ou descarte o rolo"; botão não destrutivo "Remover do estoque"; toast "Rolo removido do estoque. Histórico preservado.". Em ambos os casos, ao concluir, `onSummaryChanged` refaz tipos e contagem. O rolo arquivado sai da listagem padrão e das contagens/pesos disponíveis/resumos (filtros por `is_active` já existentes em `visibleSpools`, `countAvailableSpoolsByType` e `vw_filament_type_summary`), continua acessível com **"Mostrar arquivados"**, aparece com o selo **"Arquivado"**, e o histórico segue acessível. **"Excluir rolo" fica desabilitado (`data-disabled`) quando o rolo já está arquivado** (`is_active=false`) — não repete a remoção. "Editar" não reativa o rolo (o formulário de edição nunca envia `is_active`). **Vínculo rolo → Pedido (regra 3 do usuário): não implementado agora porque não existe nenhuma estrutura que ligue `filament_spools` a `orders`** — `filament_movements.reference_type`/`reference_id` são campos polimórficos reservados, nunca populados, e nenhuma tabela/coluna/RPC associa rolo a pedido. O bloqueio (tanto da exclusão física quanto do arquivamento, com mensagem de vínculo) **será implementado junto ao motor Pedidos → Estoque do Módulo 3**; nenhuma verificação falsa foi inventada e as FKs/RPCs existentes ficaram intactas. **Validação técnica**: testes atualizados/criados em `FilamentsInventoryPage.test.tsx` — rolo sem histórico usa DELETE e confirmação de exclusão permanente; rolo com histórico **não** chama DELETE, chama `update({is_active:false})`, mostra a confirmação de arquivamento (sem "permanente", sem "Desative ou descarte", sem o erro `FILAMENT_SPOOL_HAS_MOVEMENTS`), toast "Rolo removido do estoque. Histórico preservado.", e o rolo some da listagem padrão e reaparece com "Mostrar arquivados" identificado como "Arquivado"; "Excluir rolo" desabilitado em rolo já arquivado; menu segue só com Editar/Excluir rolo; "Editar" continua funcionando; mesmo comportamento no card mobile; teste `it.todo` registrado para o bloqueio por vínculo com Pedido, sem mock enganoso, pendente do motor Pedidos → Estoque. Suíte `vitest` completa **1882 passando + 1 todo** (75 arquivos; 1880 anteriores preservados); `npm run lint` (0 erros, mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `prettier --check` nos arquivos modificados OK; `git diff --check` sem problemas. **Nenhuma movimentação apagada. Nenhum `ON DELETE CASCADE` criado. Nenhuma alteração de banco/backend — o arquivamento reusa o RPC `update_filament_spool` já existente.** Execução local continua em `http://localhost` (o host `127.0.0.1` apresentou incompatibilidade de origem nas Edge Functions). **Correção (ver entrada seguinte, 2026-09-03):** a frase original desta entrada afirmando que as Edge Functions `filament-spools` (PATCH/DELETE) "ainda não estão publicadas em produção" estava **incorreta**. A função `filament-spools` já está **publicada e operacional para POST, PATCH e DELETE** — confirmado por evidência real de uso contra o ambiente remoto (criação de rolo, DELETE de rolo vazio, DELETE de rolo com histórico retornando `FILAMENT_SPOOL_HAS_MOVEMENTS`, PATCH `is_active=false` no novo fluxo) e coerente com as linhas de status do Módulo 3 (§ tabelas de status) que já a listavam entre as 7 Edge Functions de Estoque publicadas. O único item de backend pendente é a proteção de vínculo rolo → Pedido, que virá com o motor Pedidos → Estoque. **Implementação apenas local, sem deploy; nova regra aguardando validação manual do usuário.** Módulo 3 permanece em **~51%**. Checkpoint: commit `fix: preserve spool history on removal`. |
| 2026-09-03 | **Regra revisada de remoção de rolos — validação manual aprovada integralmente; correção documental sobre a publicação da Edge Function `filament-spools`. Só documentação.** O usuário validou a interface pelo `http://localhost` contra o ambiente remoto e **aprovou os itens 1 a 14** da nova regra de "Excluir rolo" para rolo COM histórico: (1) a janela foi atualizada pelo localhost; (2) "Excluir rolo" num rolo com histórico abre a confirmação **"Remover rolo do estoque"**; (3) a confirmação informa que o histórico será preservado; (4) **não** aparece a palavra "permanente"; (5) **não** aparece a orientação "Desative ou descarte o rolo"; (6) o botão **"Remover do estoque"** executa corretamente; (7) toast **"Rolo removido do estoque. Histórico preservado."**; (8) o rolo **desaparece da listagem padrão**; (9) **"Mostrar arquivados"** torna o rolo visível de novo; (10) o rolo aparece identificado como **"Arquivado"**; (11) o **histórico permanece acessível**; (12) as **movimentações foram preservadas** (nenhuma apagada); (13) **"Excluir rolo" fica desabilitado** no rolo já arquivado; (14) **resumos e disponibilidade não contabilizam** o rolo arquivado. A **exclusão física de rolo vazio** (sem movimentações) já havia sido aprovada integralmente nos itens 1 a 7 da validação anterior — continua válida. **Correção documental:** a entrada anterior (2026-09-03, `fix: preserve spool history on removal`) afirmava que as Edge Functions `filament-spools` (PATCH/DELETE) "ainda não estão publicadas em produção" — isso estava **errado**. Evidência real: criação de rolo pela interface contra o ambiente remoto funcionou; DELETE de rolo vazio funcionou; DELETE de rolo com histórico retornou `FILAMENT_SPOOL_HAS_MOVEMENTS`; PATCH `is_active=false` funcionou no novo fluxo. Portanto **`filament-spools` já está publicada e operacional para POST, PATCH e DELETE** (coerente com as linhas de status do Módulo 3, que já a listavam entre as 7 Edge Functions de Estoque publicadas). Nenhum número de versão da função é afirmado aqui por não ter sido confirmado por consulta real nesta rodada. **Esta rodada não exige deploy de backend.** **Pendência preservada:** o bloqueio de exclusão física **e** de arquivamento para rolo vinculado a Pedido continua pendente porque ainda não existe estrutura operacional que ligue `filament_spools` a `orders` — será implementado junto ao **motor Pedidos → Estoque** (teste já registrado como `it.todo`, sem mock enganoso). **Execução local deve usar `http://localhost`, nunca `127.0.0.1`** (o host `127.0.0.1` apresentou incompatibilidade de origem nas Edge Functions). **Nenhuma alteração de código, testes, banco, migrations, views, RPCs ou Edge Functions nesta entrada — só `docs/05_ROADMAP_MODULOS.md`. Nenhum deploy.** Módulo 3 permanece em **~51%**. Checkpoint documental: commit `docs: confirm spool removal validation`. |
| 2026-09-03 | **Remoção segura de TIPOS de filamento: exclusão física só sem nenhuma referência; caso contrário, arquivamento atômico do tipo e de TODOS os seus rolos; bloqueio por pedido ativo. Migration + RPC + Edge Function + frontend — implementação LOCAL, sem aplicar/publicar/deployar.** Contexto: a validação manual da regra revisada de **remoção de ROLOS** (itens 1 a 14: rolo com histórico abre "Remover rolo do estoque", preserva histórico/movimentações, some da listagem, reaparece em "Mostrar arquivados" como "Arquivado", fora dos resumos/disponibilidade, "Excluir rolo" desabilitado no já arquivado) foi **aprovada integralmente**; a **exclusão física de rolo vazio** também segue aprovada (itens 1 a 7). Ao tentar excluir um TIPO com rolos, o backend retornava `FILAMENT_TYPE_HAS_SPOOLS: "…não pode ser excluído. Desative o tipo."`. **Nova decisão do usuário:** (1) excluir um tipo que possui rolos = arquivar o tipo e arquivar todos os seus rolos, preservando históricos e movimentações; (2) quando o tipo estiver em pedido — bloquear se houver pedido ativo, permitir o arquivamento se os únicos pedidos vinculados estiverem DELIVERED ou CANCELLED, preservando vínculos e snapshots. **Auditoria de referências a `filament_types.id`** (todas `on delete restrict`, nenhuma cascata): `filament_spools`, `filament_movements` (denormalizado + via rolo), `product_filaments` (LEGADO), `product_plate_filaments` (LEGADO), `order_item_unit_plate_filaments` (seleção de cor do Pedido), e `inventory_purchases` (`category='FILAMENT' AND item_id=<tipo>` — **sem FK**, polimórfico, mas referência real). Caminho pedido confirmado: `order_item_unit_plate_filaments.order_item_id → order_items.id`, `order_items.order_id → orders.id`, `orders.order_status`. **Estados terminais reais da máquina de status** (`orders_order_status_check`): **`DELIVERED`, `CANCELLED`** — qualquer outro (`QUOTE`, `WAITING_APPROVAL`, `APPROVED`, `IN_PRODUCTION_QUEUE`, `IN_PRODUCTION`, `WAITING_DELIVERY`) é "pedido ativo". **Backend** (`supabase/migrations/20260903120000_add_remove_filament_type_function.sql`, append-only, nenhuma migration antiga alterada): nova RPC transacional `public.remove_filament_type(p_filament_type_id uuid, p_changed_by uuid) returns jsonb` — `security definer`, `search_path=''`, EXECUTE só a `service_role`. Passos: valida usuário ativo (`assert_active_user`) → `SELECT … FOR UPDATE` na linha do tipo → **bloqueia** com `FILAMENT_TYPE_IN_ACTIVE_ORDER:` (mensagem cita os `order_number`, sem UUID) se houver seleção do tipo em pedido não-terminal → detecta se há QUALQUER referência (as 6 acima) → **sem referência**: `DELETE FROM filament_types` (única linha apagada, e só a do próprio tipo) e retorna `{ result: 'PHYSICALLY_DELETED', archived_spool_count: 0 }` → **com referência**: `UPDATE filament_spools SET is_active=false WHERE filament_type_id=… AND is_active` (conta as linhas afetadas) e `UPDATE filament_types SET is_active=false`, retornando `{ result: 'ARCHIVED', archived_spool_count: N }`. Tudo numa transação: se qualquer etapa falhar, nada é alterado. **`filament_movements` / `inventory_purchases` / `product_filaments` / `product_plate_filaments` / `order_item_unit_plate_filaments` nunca são tocados — nenhuma linha dependente apagada, nenhum `ON DELETE CASCADE`, nenhum snapshot alterado.** A RPC antiga `delete_filament_type(uuid, uuid)` foi **redefinida via CREATE OR REPLACE para DELEGAR** a `remove_filament_type` (`perform …`), eliminando o caminho divergente — não levanta mais `FILAMENT_TYPE_HAS_SPOOLS:`/`FILAMENT_TYPE_HAS_COMPOSITION:`. **Edge Function** `filament-types`: `DELETE /filament-types/:id` passa a chamar `remove_filament_type` e devolve `{ success, result, archived_spool_count }` (helper `normalizeRemoveFilamentTypeResult`, defesa em profundidade); auth/CORS/validação de UUID/mensagens reais preservados; `_shared/errors.ts` mapeia `FILAMENT_TYPE_IN_ACTIVE_ORDER:` para `BusinessRuleError` (409) com o prefixo removido. **Frontend** (`FilamentsInventoryPage.tsx`, `useFilamentTypes.ts`, `filamentTypes.ts`, `filamentGroups.ts`, `FilamentTypeDrawer.tsx`): o botão continua **"Excluir tipo"** (Desativar tipo **não** volta como alternativa da ação). Confirmação **A** (tipo sem rolos — heurística `total_spool_count === 0`): título "Excluir tipo de filamento", texto de exclusão **permanente e irreversível**, botão `destructive` "Excluir definitivamente". Confirmação **B** (tipo com rolos): título "Remover tipo do estoque", texto "…serão retirados do estoque ativo, mas históricos e movimentações serão preservados", **sem "permanente"**, botão "Remover do estoque". O backend é autoritativo: se o tipo tiver outras referências que o front não carrega, ele arquiva e o **toast reflete o resultado real** ("Tipo removido do estoque. Histórico preservado." vs. "Tipo de filamento excluído."). Pedido ativo → o backend bloqueia e a **mensagem real** aparece no diálogo (não aparece "Desative o tipo"). Após `ARCHIVED`, `refetchAll()` reconcilia tipos e contagem; o grupo sai da listagem operacional. Novo controle **"Mostrar tipos arquivados"** (switch, desligado por padrão, ao lado dos filtros, com contador) — revela grupos cujos tipos estão todos inativos, com selo **"Arquivado"** na coluna Situação, "Ver rolos" ainda acessível para consultar o histórico; não altera resumos; **não se confunde com "Mostrar arquivados" dos rolos** (que fica dentro da janela "Ver rolos"). "Excluir tipo" fica **desabilitado** num tipo já arquivado (`!type.is_active`). Grupos totalmente arquivados não entram em pesos/contagens/disponibilidade (filtros por `is_active` já existentes na view e em `countAvailableSpoolsByType`; `minimumStockGrams` já filtra `is_active`). **Testes**: `supabase/tests/remove_filament_type_test.sql` novo, transacional BEGIN/ROLLBACK, 16 seções cobrindo os 15 casos exigidos (exclusão física sem dependência; arquivamento de tipo+rolo vazio; arquivamento com movimentações; movimentações preservadas; vários rolos arquivados atomicamente com `archived_spool_count` correto; operação abortada não deixa alteração parcial; bloqueio por pedido ativo + mensagem com número do pedido; DELIVERED e CANCELLED não bloqueiam; snapshots/seleções preservados; compra preservada; sem cascata; chamada repetida segura/idempotente; usuário inválido rejeitado; retorno estruturado). `supabase/tests/filament_inventory_test.sql`: 2 asserções obsoletas (5.2 e 8.5) atualizadas — `delete_filament_type` agora arquiva em vez de recusar. `supabase/functions/_shared/errors.test.ts`: `FILAMENT_TYPE_IN_ACTIVE_ORDER:` → 409 com prefixo removido; regressão de `FILAMENT_TYPE_HAS_SPOOLS:`/`HAS_COMPOSITION:`. `supabase/functions/filament-types/handler.test.ts`: `normalizeRemoveFilamentTypeResult` (PHYSICALLY_DELETED / ARCHIVED / shape inesperado); rota DELETE offline (401 antes do banco, não 405). `frontend`: `useFilamentTypes.test.ts` (PHYSICALLY_DELETED remove / ARCHIVED marca inativo + devolve resultado / bloqueio mantém a lista); `FilamentsInventoryPage.test.tsx` (novo describe "remoção segura de tipo de filamento" + 2 testes migrados) cobrindo confirmação permanente sem dependências, confirmação de arquivamento com rolos e texto de preservação, ausência de "Desative o tipo", bloqueio por pedido ativo com erro real, atualização da listagem (grupo some), toggle oculto por padrão / revela com selo "Arquivado" / "Ver rolos" acessível / fora da disponibilidade, "Excluir tipo" desabilitado em tipo já arquivado, e o controle distinto de "Mostrar arquivados" dos rolos. Suíte `vitest` completa **1889 passando + 1 todo** (75 arquivos); `npm run lint` (0 erros, mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `prettier --check` nos arquivos front tocados OK; `git diff --check` sem problemas; varredura confirma **nenhum `ON DELETE CASCADE` e nenhuma exclusão de `filament_movements`/linhas dependentes**. Os testes SQL e Deno foram **revisados estaticamente** (sem Docker/Postgres/Deno local) — precisam da primeira execução real. **`filament-spools` já está publicada e operacional** (POST/PATCH/DELETE), confirmado na rodada anterior. **Execução local usa `http://localhost`, nunca `127.0.0.1`.** **Nada foi aplicado no Supabase remoto, nenhuma Edge Function publicada, nenhum deploy de frontend, nenhum dado remoto alterado.** **Plano para a próxima rodada** (aplicação/publicação): (1) `supabase migration list --linked` para confirmar a defasagem só da `20260903120000`; (2) `npx supabase db query --linked --file supabase/tests/remove_filament_type_test.sql` dentro de BEGIN/ROLLBACK **antes** de aplicar, contra uma cópia da migration carregada na sessão, OU aplicar e então rodar o teste (também com ROLLBACK) — exigir 0 FAIL; (3) `npx supabase db push --linked` (dry-run antes, deve mostrar só a nova migration); (4) reexecutar `filament_inventory_test.sql` (ROLLBACK) para confirmar as 2 asserções atualizadas; (5) `deno test supabase/functions/filament-types/ supabase/functions/_shared/errors.test.ts`; (6) `npx supabase functions deploy filament-types` (só ela — nenhuma outra função tocada); smoke test HTTP não autenticado (OPTIONS→204, DELETE sem token→401); (7) validação manual do usuário pela interface (localhost) cobrindo os itens de exclusão física, arquivamento e bloqueio por pedido ativo; (8) **vínculo rolo/tipo → pedido ativo agora coberto pela RPC** para o caso da seleção de cor — o motor Pedidos → Estoque só precisará estender a definição de "referência de pedido" se surgirem novas tabelas (reserva/consumo). Módulo 3 permanece em **~51%**. Checkpoint: commit `feat: safely remove filament types with inventory`, sem push inicialmente nesta descrição — o push normal para `origin/feature/inventory-operations` é feito nesta mesma rodada, sem aplicar migration nem publicar função. |
| 2026-09-03 | **Revisão corretiva da remoção de TIPOS antes de aplicar a migration: planejamento autoritativo no backend, guard contra mudança de plano e serialização real das seleções de pedido. Nova migration + Edge Function + frontend — LOCAL, sem aplicar/publicar/deployar.** Checkpoint de entrada: `feat: safely remove filament types with inventory` (`5e50be1`), árvore limpa, divergência `0 0`. **Problema 1 — confirmação baseada só em rolos.** A interface escolhia entre "exclusão permanente" e "arquivamento" olhando apenas `total_spool_count`. Um tipo **sem rolos** ainda pode ter movimentações, compras, `product_filaments`/`product_plate_filaments` legados ou seleção em pedido DELIVERED/CANCELLED — nesses casos `remove_filament_type` **arquiva**, mas a interface prometia exclusão permanente. **Solução:** nova RPC somente-leitura `public.get_filament_type_removal_plan(p_filament_type_id uuid, p_changed_by uuid) returns jsonb` (migration `20260903130000_make_filament_type_removal_plan_authoritative.sql`, append-only), `security definer` / `search_path=''` / EXECUTE só `service_role`. Devolve `planned_result` (`PHYSICALLY_DELETED` | `ARCHIVED` | `BLOCKED_ACTIVE_ORDER`), `spool_count`, `active_spool_count`, `active_order_numbers[]`, `has_movements`, `has_purchases`, `has_product_filaments`, `has_product_plate_filaments`, `has_order_selection`. A lógica de plano vive numa função interna compartilhada `public._compute_filament_type_removal_plan(uuid)` (zero grants) reusada pela consulta e pela execução, garantindo regra única. **Edge Function** `filament-types`: nova rota `GET /filament-types/:id/removal-plan` → `get_filament_type_removal_plan` (helper `normalizeFilamentTypeRemovalPlan`, defesa em profundidade: `planned_result` desconhecido cai em `ARCHIVED`, nunca "permanente"). O **frontend** (`FilamentsInventoryPage.tsx`, `useFilamentTypes.ts` com `getRemovalPlan`, `filamentTypes.ts` com `getFilamentTypeRemovalPlan`/`isRemovalPlanChangedError`) **busca o plano ao abrir o diálogo** e deriva a variante SÓ de `planned_result`: `PHYSICALLY_DELETED` → título "Excluir tipo de filamento", texto de exclusão permanente citando a ausência de rolos/movimentações/compras/pedidos, botão `destructive` "Excluir definitivamente"; `ARCHIVED` → título "Remover tipo do estoque", texto "…retirados do estoque ativo. Históricos, movimentações, compras e vínculos de pedidos finalizados são preservados.", sem "permanente", botão "Remover do estoque"; `BLOCKED_ACTIVE_ORDER` → título "Não é possível remover o tipo", mensagem de bloqueio com os `active_order_numbers`, **sem nenhum botão destrutivo** (só "Fechar"). Enquanto o plano carrega, título neutro e nenhum botão de confirmação; erro ao carregar mostra a mensagem e "Tentar novamente". A heurística `total_spool_count` foi **removida** da decisão. **Problema 2 — o plano pode mudar entre a consulta e a confirmação.** `remove_filament_type` ganhou 3º parâmetro: assinatura agora `public.remove_filament_type(p_filament_type_id uuid, p_changed_by uuid, p_expected_result text default null)` (a de 2 args da `20260903120000` é **`drop function`**-ada para não deixar sobrecarga ambígua). Dentro da transação, **depois** do `SELECT … FOR UPDATE` na linha do tipo, o plano é recalculado por `_compute_filament_type_removal_plan`; se for `BLOCKED_ACTIVE_ORDER` → `FILAMENT_TYPE_IN_ACTIVE_ORDER:`; se `p_expected_result` não-nulo divergir do plano recalculado → **`FILAMENT_TYPE_REMOVAL_PLAN_CHANGED:`** e **nada é alterado**. `p_expected_result = null` é o caminho de compatibilidade de `delete_filament_type(uuid, uuid)` (adota o plano corrente, sem guard). A Edge Function `DELETE /filament-types/:id` passa a **exigir** `expected_result` no corpo (`'PHYSICALLY_DELETED'` | `'ARCHIVED'`; ausente/`BLOCKED_ACTIVE_ORDER`/lixo → 400) e o repassa como `p_expected_result`. `_shared/errors.ts` mapeia `FILAMENT_TYPE_REMOVAL_PLAN_CHANGED:` para `BusinessRuleError` (409) com o prefixo removido; o frontend reconhece a mensagem por `isRemovalPlanChangedError` (fragmento estável "plano de remoção mudou"), **mantém o diálogo aberto, recarrega o plano e pede nova confirmação** ("As condições deste tipo mudaram desde a conferência…"), nunca executando uma ação diferente da confirmada. **Problema 3 — concorrência com novas referências.** O ponto único de serialização é a **linha `public.filament_types` do tipo**: `remove_filament_type` a trava com `FOR UPDATE` por toda a transação; `create_filament_spool` e `register_inventory_purchase` (ramo FILAMENT) já a travavam com `FOR UPDATE` (e já recusam tipo inativo); `create_order` (as duas sobrecargas) e `update_order_item_production_colors` passam a validar o filamento ativo com **`SELECT … FROM public.filament_types WHERE id = v_filament_type_id FOR KEY SHARE`** — **única** mudança nesses três corpos, re-declarados na mesma migration com `CREATE OR REPLACE` e conferidos linha a linha contra a `20260829180000` (diff = só o `for key share`; a exceção da seleção "que já existia antes" em `update_order_item_production_colors` fica idêntica). Toda FK filho → `filament_types` (spools, movimentações, seleção de pedido, `product_filaments`/`product_plate_filaments`) já pega `FOR KEY SHARE` na linha pai em cada INSERT. `FOR KEY SHARE` conflita com o `FOR UPDATE` da remoção → ou a escrita concorrente termina primeiro (e a remoção enxerga a nova referência: bloqueia por pedido ativo ou recai em `ARCHIVED`) ou a remoção termina primeiro (e a escrita concorrente falha na checagem de `is_active` / na FK). **Não é lock unilateral** — as duas pontas usam a mesma linha; não há lock de tabela. **Impacto:** enquanto um tipo específico está sendo removido, uma criação/edição de pedido que selecione ESSE tipo (ou uma compra/rolo desse tipo) espera o commit da remoção (sub-milissegundo); pedidos que não referenciam o tipo não são afetados. Além disso, o recálculo do plano sob lock garante que, entre o cálculo e o commit, nenhuma referência nova aparece; um `PHYSICALLY_DELETED` que corresse com uma FK nova abortaria pelo `ON DELETE RESTRICT` (nunca órfão). **Preservado:** nenhuma linha dependente apagada; nenhum `ON DELETE CASCADE`; pedidos DELIVERED/CANCELLED continuam permitindo o arquivamento; pedidos ativos bloqueiam; tipo e rolos arquivados com `is_active=false`; exclusão física só sem qualquer referência; `delete_filament_type` continua delegando à regra única; "Mostrar tipos arquivados" e "Excluir tipo" desabilitado em tipo arquivado inalterados; histórico preservado; execução local em `http://localhost`, nunca `127.0.0.1`. **Testes.** SQL: `remove_filament_type_test.sql` ganhou as seções 17–26 (plano `PHYSICALLY_DELETED`; plano `ARCHIVED` por rolo; `ARCHIVED` só por compra sem rolo; `ARCHIVED` só por composição legada; `ARCHIVED` só por pedido terminal; `BLOCKED_ACTIVE_ORDER` com número do pedido; `p_expected_result` correto executa; `p_expected_result` divergente → `PLAN_CHANGED` sem alterar dados; plano muda entre consulta e execução → `PLAN_CHANGED`, nada arquivado em silêncio; tipo arquivado recusa nova seleção de pedido e novo rolo, movimentações intactas; verificação estática do mecanismo de serialização via `pg_get_functiondef` — `FOR UPDATE` em `remove_filament_type` e `FOR KEY SHARE` nas três funções de pedido; a concorrência real de duas sessões fica como **SKIP**, para a validação remota manual). Edge Function: `handler.test.ts` (`normalizeFilamentTypeRemovalPlan` válido/nulo/`planned_result` desconhecido→`ARCHIVED`/`active_order_numbers` não-array→`[]`; `requireExpectedRemovalResult` aceita só `PHYSICALLY_DELETED`/`ARCHIVED`; rota `GET /:id/removal-plan` 401 sem auth, 405 em POST, não cai em 404); `errors.test.ts` (`FILAMENT_TYPE_REMOVAL_PLAN_CHANGED:` → 409 com fragmento estável; regressão cruzada com `FILAMENT_TYPE_IN_ACTIVE_ORDER:`). Frontend: `filamentTypes.test.ts` (`getFilamentTypeRemovalPlan` chama `GET /:id/removal-plan`; `deleteFilamentType` envia `{ expected_result }`; `isRemovalPlanChangedError` distingue plano-alterado de bloqueio comum); `useFilamentTypes.test.ts` (`getRemovalPlan` só repassa; `delete` recebe `(id, expectedResult)` e propaga aos mocks); `FilamentsInventoryPage.test.tsx` (a confirmação vem do `planned_result` do backend; tipo sem rolos mas com compra → variante ARCHIVED, nunca "permanente"; `BLOCKED_ACTIVE_ORDER` já no plano → mensagem de bloqueio e nenhum botão destrutivo; `REMOVAL_PLAN_CHANGED` → diálogo permanece, recarrega o plano, pede nova confirmação, nenhum toast de sucesso; `deleteType` chamado com o `expected_result` exato; regressões da listagem e de "Mostrar tipos arquivados"). Suíte `vitest` completa **1894 passando + 1 todo** (75 arquivos); `npm run lint` (0 erros, mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `prettier --check`/`--write` nos arquivos front tocados OK; `git diff --check` sem problemas; varredura do novo migration confirma **nenhum `ON DELETE CASCADE`** e que o único `DELETE FROM` de tabela dependente é o `order_item_unit_plate_filaments` **pré-existente e inalterado** dentro de `update_order_item_production_colors` (a função reconstrói as próprias seleções do pedido), além do `DELETE FROM public.filament_types` da linha do tipo no ramo `PHYSICALLY_DELETED`. Os testes SQL e Deno foram **revisados estaticamente** (sem Docker/Postgres/Deno local) — precisam da primeira execução real. **Nada aplicado no Supabase remoto, nenhuma Edge Function publicada, nenhum deploy de frontend, nenhum dado remoto alterado.** **Plano para a próxima rodada:** (1) `supabase migration list --linked` — defasagem de `20260903120000` **e** `20260903130000`; (2) rodar `remove_filament_type_test.sql` (BEGIN/ROLLBACK) com as duas migrations carregadas — exigir 0 FAIL nas seções 1–26 (a 26.5 fica SKIP); (3) `npx supabase db push --linked` (dry-run antes; deve listar só as duas novas migrations); (4) reexecutar `filament_inventory_test.sql` (ROLLBACK); (5) `deno test supabase/functions/filament-types/ supabase/functions/_shared/errors.test.ts`; (6) `npx supabase functions deploy filament-types` (só ela); smoke test HTTP não autenticado (OPTIONS→204; `GET /:id/removal-plan` sem token→401; `DELETE` sem token→401); (7) **teste manual de concorrência** de duas sessões (`psql` A abre transação com `remove_filament_type`/`FOR UPDATE`; sessão B tenta `create_order`/`update_order_item_production_colors` selecionando o mesmo tipo e deve bloquear até o commit de A, então falhar por `is_active`); (8) validação manual do usuário pela interface (localhost) cobrindo plano `PHYSICALLY_DELETED`/`ARCHIVED`/`BLOCKED_ACTIVE_ORDER` e o fluxo de reconfirmação em `PLAN_CHANGED`. Módulo 3 permanece em **~51%**. Checkpoint: commit `fix: make filament type removal plan authoritative`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase, sem aplicar migration, sem publicar função, sem deploy. |
| 2026-09-04 | **Remoção segura de tipos de filamento: migrations aplicadas ao remoto, `filament-types` publicada na versão 2, teste real de concorrência de duas sessões aprovado e validação manual pela interface aprovada.** Sequência desta rodada (build-up de commits `51d453f` → publicação → validação): (1) **correção de fixture** (commit `51d453f`, `test: isolate filament inventory fixtures`) — `supabase/tests/filament_inventory_test.sql` reaproveitava a mesma fixture `type_id` depois dela ser arquivada pela Seção 5.2 (`delete_filament_type` → `remove_filament_type`), causando falhas em cascata nas Seções 5.3/7/8; corrigido com fixtures ativas independentes e nunca reaproveitadas (`active_type_id`, `composition_type_id`), sem alterar nenhuma função de produção nem afrouxar nenhuma asserção — resultado **53 PASS, 0 FAIL** (era 44 PASS/7 FAIL). (2) **Migrations aplicadas ao projeto remoto vinculado `tjhacqreupfqefntjevf`**: `20260903120000_add_remove_filament_type_function.sql` e `20260903130000_make_filament_type_removal_plan_authoritative.sql`, cada uma aplicada exatamente uma vez, confirmadas em `supabase_migrations.schema_migrations`. **Testes SQL remotos** (dentro de `BEGIN`/`ROLLBACK`, sem sobra): `remove_filament_type_test.sql` → **32 PASS, 1 SKIP (26.5, concorrência — coberta a seguir), 0 FAIL**; `filament_inventory_test.sql` → **53 PASS, 0 FAIL**. (3) **Teste real de concorrência com duas sessões PostgreSQL independentes** (não simulado, não só `pg_get_functiondef`) — Cenário A (remoção primeiro): `remove_filament_type` segura `FOR UPDATE` na linha do tipo; uma segunda sessão tentando `create_order`/`update_order_item_production_colors` (seleção de cor) OU `create_filament_spool` para o mesmo tipo **espera de verdade e estoura `lock_timeout`** (`55P03: canceling statement due to lock timeout`), confirmado por `pg_stat_activity`/`pg_locks`/`pg_blocking_pids` identificando PID bloqueador e bloqueado; depois do commit, a tentativa normal é recusada como inativa, sem pedido órfão. Cenário B (seleção primeiro): uma sessão cria um pedido com a cor selecionada e segura a transação aberta (`FOR KEY SHARE`, herdado da FK); `remove_filament_type` numa segunda sessão **espera e estoura o timeout** na mesma linha; depois do commit do pedido, `get_filament_type_removal_plan` devolve `BLOCKED_ACTIVE_ORDER` com o número real do pedido, `remove_filament_type` levanta `FILAMENT_TYPE_IN_ACTIVE_ORDER:`, e o tipo/pedido/seleção permanecem íntegros. Proteção **`FILAMENT_TYPE_REMOVAL_PLAN_CHANGED`** também validada com duas sessões reais: plano consultado como `PHYSICALLY_DELETED`, uma segunda sessão cria e comita um rolo novo (mudando legitimamente o plano para `ARCHIVED`), a remoção com o `expected_result` desatualizado é recusada (409, nada alterado, tipo e rolo íntegros). Todas as fixtures de teste (`TESTE CONCORRENCIA ...`) foram removidas por ID exato ao final; zero resíduo, zero FK órfã, contagens oficiais restauradas, `RL-26-016` (registro real criado pela interface durante os testes, PETG/"Não informado"/Sólida/Branco) preservado e inalterado durante toda a rodada. (4) **Publicação controlada**: `npx supabase functions deploy filament-types --project-ref tjhacqreupfqefntjevf` — **`filament-types` versão 1 → 2**; as outras 13 Edge Functions permaneceram com a mesma versão/hash, nenhuma outra tocada; nenhuma migration aplicada nesta etapa, nenhum secret/config de autenticação alterado, nenhum deploy de frontend. **Smoke tests remotos**: sem autenticação — `OPTIONS` → 204 com CORS correto; `GET`/`DELETE` sem token → 401 controlado; `POST` em `/filament-types/:id/removal-plan` → 405; nenhuma resposta expôs stack trace, SQL interno ou dado sensível. (5) **Validação manual do usuário pela interface, aprovada**: a remoção segura de tipos de filamento foi validada e está **aprovada** nos três comportamentos da regra — **tipo sem nenhuma dependência é excluído definitivamente** (`PHYSICALLY_DELETED`); **tipo com rolos ou histórico é arquivado** (`is_active=false` no tipo e em todos os seus rolos, `ARCHIVED`, nada apagado); **tipo usado por pedido ativo tem a remoção bloqueada** (`BLOCKED_ACTIVE_ORDER`/`FILAMENT_TYPE_IN_ACTIVE_ORDER:`, mensagem real, sem UUID). **Dados oficiais e históricos preservados** durante toda a rodada (migrations, testes remotos e teste de concorrência — nenhuma tabela oficial alterada além do esperado pela própria função sendo testada; `filament_movements`/`inventory_purchases`/`product_filaments`/`product_plate_filaments`/`order_item_unit_plate_filaments` nunca apagados). **Frontend não implantado nesta rodada** — a interface usada na validação manual já apontava para o Supabase remoto real (padrão do projeto: execução local sempre via `http://localhost`, nunca `127.0.0.1`), consumindo a Edge Function `filament-types` agora publicada em produção. Módulo 3 permanece em **~51%** (a remoção segura de tipos é parte do Incremento 4 já contabilizado nessa fase — nenhuma fase nova da metodologia das 7 fases concluída nesta rodada). Checkpoints: commits `test: isolate filament inventory fixtures` (`51d453f`) e este registro documental `docs: confirm safe filament type removal`. |
| 2026-09-04 | **Revisão do fluxo de Filamentos: novo caminho "Cadastrar tipo → Registrar compra → Rolos atualizados"; Linha limitada a botões (com "Matte"); Compra passa a escolher `filament_type_id`; rolos criados automaticamente na compra. Migration + Edge Function `inventory-purchases` publicadas.** **Cadastro do tipo** (`Estoque → Filamentos`): botão renomeado de "Novo tipo de filamento" para **"Cadastrar novo tipo"** (`FilamentsInventoryPage.tsx`) — continua abrindo o mesmo diálogo, cujo título permanece "Novo tipo de filamento". Campo **Linha** (`FilamentTypeForm.tsx`): texto livre removido; vira `role="radiogroup"` de botões de ação (mesmo padrão já usado em Material), com uma única seleção por vez e obrigatório como antes; opções oficiais `['Sólida', 'Silk', 'Matte', 'Velvet', 'Translúcido', 'DuoColor']` — **"Matte" é a única opção nova**, "Sólida"/"Silk" preservadas. **Preservação histórica**: se o `line` de um registro em edição não estiver entre as opções oficiais, ele nunca é alterado/convertido automaticamente — aparece como opção adicional já selecionada na lista de botões, e o usuário pode livremente trocá-la por uma oficial; nenhuma conversão silenciosa ocorre. Material/Cor/Estoque mínimo (peso, opcional)/Observações inalterados; Fabricante continua sem campo de criação, enviando "Não informado" internamente (schema `NOT NULL` inalterado). **Compra de Filamentos** (`PurchaseDialog.tsx`): os campos independentes de Cor e Acabamento foram **removidos**; novo componente `FilamentTypeItemPicker` (mesmo padrão do `AccessoryItemPicker`, busca própria via `normalizeForSearch` já que `SearchAutocomplete` não filtra sua própria lista) substitui-os por um campo único obrigatório **"Tipo de filamento"**, que lista só tipos **ativos** de `useFilamentTypes()`, exibidos como `"Material - Linha - Cor"` (ex.: "PLA - Matte - Preto"), buscável por Material/Linha/Cor, e mostra "Cadastre um tipo de filamento antes de registrar a compra." quando não há nenhum tipo ativo; a seleção usa `filament_type_id` como identificador real (nunca comparação textual de Material/Fabricante/Linha/Cor). Fornecedor da compra, quantidade de rolos, peso de cada rolo, valor, data, lote e observações preservados sem alteração; o fornecedor da compra **nunca** altera o fabricante do tipo — tipo e fornecedor continuam independentes. **Criação automática dos rolos**: a compra é vinculada diretamente ao `filament_type_id` escolhido e cria automaticamente a quantidade de rolos informada (ex.: quantidade 3 → 1 compra + 3 rolos), cada um com código único pelo gerador atual, vinculado ao tipo escolhido, com o peso informado por rolo e o status padrão de rolo novo, visível em "Ver rolos" do tipo; a operação é **atômica** (falha na compra não cria rolo nenhum; falha em qualquer rolo reverte compra e todos os rolos — mesmo padrão de idempotência/transação já existente); nunca cria um novo tipo nem localiza tipo por Material+Cor+Acabamento durante a compra; após salvar, histórico/listagem de compras, contagem de rolos do tipo, "Ver rolos" e cards/totais são atualizados pelo refetch já existente (`useFilamentTypes`). **Backend**: `register_inventory_purchase` já aceitava um contrato extensível — o parâmetro `p_filament_type_id` foi adicionado por `CREATE OR REPLACE` (a assinatura antiga de 15 argumentos foi `DROP FUNCTION`-ada primeiro para não deixar sobrecarga ambígua, mesmo padrão já usado em `remove_filament_type`), migration nova e append-only `supabase/migrations/20260904120000_add_filament_type_id_to_purchase.sql`: quando `p_filament_type_id` é informado, valida o tipo ativo com `FOR UPDATE` (mesmo padrão de serialização já usado por `create_filament_spool`) e rejeita se Material/Fabricante/Linha/Cor também vierem preenchidos; o caminho legado de find-or-create por nome foi **preservado byte a byte**, apenas reorganizado em um `if/else` — nenhum registro antigo ou teste das Seções 1–10 de `inventory_purchases_test.sql` precisou de alteração, mantendo compatibilidade total. Edge Function `inventory-purchases`: `PURCHASE_KEYS` ganhou `filament_type_id`; validação rejeita a combinação de `filament_type_id` com qualquer um dos 4 campos legados; `_shared/errors.ts` ganhou o mapeamento correspondente (400/`ValidationError`), sem colisão com o padrão pré-existente de `gross_weights_grams`. **Listagem de Filamentos**: nenhuma alteração de código foi necessária — confirmado por leitura que `hasActiveType`, o filtro `visibleGroups.filter((g) => g.hasActiveType || showArchivedTypes)`, o selo "Arquivado" e o switch "Mostrar tipos arquivados" já implementavam corretamente o comportamento exigido (tipo arquivado nunca aparece na listagem normal nem conta em totais; grupo some só quando todos os seus tipos ficam arquivados; toggle liga/desliga a visibilidade dos arquivados) desde a rodada de remoção segura de tipos; só novos testes foram adicionados para reconfirmar isso no contexto do novo fluxo de compra. **Testes**: SQL — `inventory_purchases_test.sql` ganhou a Seção 11 (6 sub-blocos: sucesso via `filament_type_id` sem criar tipo novo; rejeição de `filament_type_id` + campo legado; `filament_type_id` inexistente e inativo rejeitados sem criar nada; atomicidade com falha no meio da compra revertendo tudo) — rodado de verdade contra o remoto dentro de `BEGIN`/`ROLLBACK`: **32 PASS, 0 FAIL** (26 pré-existentes + 6 novas). Deno — `errors.test.ts` (+2 casos) e `handler.test.ts` (+4 casos, incluindo payload mínimo válido, rejeição de campo legado junto de `filament_type_id`, UUID inválido, obrigatoriedade de pesos) foram revisados estaticamente (Deno indisponível localmente, mesma limitação já registrada nas rodadas anteriores). Frontend: `PurchaseDialog.test.tsx` (24/24, novo picker — formato, só ativos, busca por Material/Linha/Cor, estado vazio, payload com `filament_type_id` e sem campos legados, fornecedor não enviado) e `FilamentsInventoryPage.test.tsx` (85/85, renomeação do botão, Linha só por botões incluindo Matte, seleção única, obrigatoriedade, preservação do valor histórico selecionado, payload de criação com `line: 'Matte'`). Suíte `vitest` completa **1903 passando + 1 todo** (75 arquivos); `npm run lint` (0 erros, os mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas. **Publicação controlada**: a migration `20260904120000` foi aplicada ao projeto remoto vinculado `tjhacqreupfqefntjevf` (confirmada exatamente uma vez em `supabase_migrations.schema_migrations`); a Seção 11 de `inventory_purchases_test.sql` rodou contra o remoto dentro de `BEGIN`/`ROLLBACK` (32 PASS, 0 FAIL, zero resíduo confirmado — a única linha `TESTE COMPRAS%` pré-existente encontrada é de 2026-08-29, não desta rodada); `npx supabase functions deploy inventory-purchases --project-ref tjhacqreupfqefntjevf` — **`inventory-purchases` versão 1 → 2**; `filament-types` permaneceu na versão 2 e as demais 12 funções permaneceram com a mesma versão, nenhuma outra tocada. **Smoke test básico** (escopo restrito pedido nesta rodada, sem repetir a bateria extensa de concorrência): `OPTIONS` → 204 com CORS correto; `POST` sem token → 401 controlado ("Header Authorization ausente."), rejeitado antes de qualquer escrita no banco. **Nenhum dado oficial alterado; nenhum deploy de frontend realizado.** `RL-26-016` não foi tocado por nenhuma etapa desta rodada. **Validação manual do usuário com um filamento real pela interface ainda está pendente** — não foi executada nesta rodada. Módulo 3 permanece em **~51%** (o fluxo de compra revisado é uma melhoria de UX/consistência de dado sobre um incremento já contabilizado — nenhuma fase nova da metodologia das 7 fases concluída). Checkpoint: commit `feat: streamline filament purchase flow`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase. |
| 2026-09-04 | **"Compra de filamentos" reestruturada para aceitar VÁRIOS itens/tipos/marcas na mesma compra, com um único cabeçalho e um único frete total. Nova tabela de itens + nova RPC dedicada + Edge Function republicada.** **Janela**: duas áreas, na ordem pedida — (1) **Dados gerais da compra**: só o campo "Frete total da compra" (data/observações não existiam na interface antes desta rodada — nada foi inventado); (2) **Itens da compra**: lista dinâmica, um card por item (Tipo de filamento, Peso líquido, Quantidade, Marca, Valor unitário, botão "Remover item"), botão **"Adicionar filamento"**, a janela abre com **1 item vazio**. Cada item: Tipo de filamento continua exigindo um `filament_type_id` já cadastrado e **ativo** (nunca cria/localiza por nome), rótulo "Material - Linha - Cor", busca por Material/Linha/Cor — reaproveita o mesmo picker da rodada anterior, agora instanciado uma vez por linha com rótulos/ids exclusivos ("Tipo de filamento — item N") para nunca colidir; **Peso líquido** continua limitado aos botões 250/500/1.000 g, agora exibidos no **padrão brasileiro** ("1.000 g", nunca "1000g" sem formatação — correção de exibição aplicada nesta rodada); **Quantidade** inteira positiva; **Marca** (nova, texto livre, obrigatória, ex.: "Bambu Lab"/"Voolt"/"3D Fila", pode ser diferente em cada item — nunca altera o `filament_types.manufacturer` interno, que continua sempre "Não informado"); **Valor unitário** monetário (preço de UM rolo). **Peso bruto individual não é mais pedido** nesta janela (requisito explícito) — cada rolo nasce sem `initial_gross_weight_grams`/`empty_spool_weight_grams`, exatamente como um rolo criado manualmente. **Totais** (só 3, sem distribuir o frete entre itens): Subtotal dos filamentos (Σ quantidade×valor unitário), Frete (único, do cabeçalho), Total da compra (subtotal+frete). Não permite salvar sem nenhum item válido ("Adicione ao menos um item de compra."). **Banco** (`supabase/migrations/20260904130000_support_multi_item_filament_purchases.sql`, append-only — nenhuma migration anterior editada; `register_inventory_purchase`, ACCESSORY/PACKAGING e o caminho legado de FILAMENT de item único ficam **byte a byte intocados**, preservando compras/rolos/históricos antigos): `inventory_purchases.item_id` passa a **NULLABLE** (só para o cabeçalho de uma compra multi-item — todas as linhas antigas continuam com `item_id` preenchido, leitura 100% compatível); nova tabela `inventory_purchase_filament_items` (`purchase_id`, `filament_type_id`, `manufacturer` da compra, `nominal_weight_grams`, `quantity`, `unit_value`, `item_value` gerado, ledger imutável, RLS espelhando `inventory_purchases`); `filament_spools` ganha `purchase_item_id` (nullable, FK para o item) — vínculo inequívoco rolo→item, de onde a marca/valor unitário do rolo são **derivados** (nunca duplicados numa coluna própria em `filament_spools`, conforme a folga explícita do pedido "derivar... pelo item da compra"). Nova RPC dedicada `register_filament_purchase(p_freight_value, p_changed_by, p_items jsonb, p_occurred_at, p_notes, p_idempotency_key)`: valida e acumula totais de TODOS os itens antes de qualquer escrita (payload inválido em qualquer item nunca cria nada); cria o cabeçalho (`category='FILAMENT'`, `item_id=null`, `quantity`/`item_value`=totais agregados); para cada item, trava e confirma o tipo **ativo** (`FOR UPDATE`, mesma serialização de `create_filament_spool`), grava o item e cria a quantidade de rolos correspondente (gerador `next_filament_spool_code()` + `register_filament_movement('PURCHASE', ...)` reaproveitados sem duplicar lógica); tudo numa única transação — **atômica por construção**: falha no cabeçalho, em qualquer item ou em qualquer rolo desfaz TUDO. Idempotente (mesma chave+payload devolve a compra já registrada; payload diferente → `IDEMPOTENCY_KEY_CONFLICT:`). Helper interno `_build_filament_purchase_summary` (zero grants) monta o jsonb de retorno (cabeçalho+itens+`spool_ids` por item), reaproveitado nos dois caminhos (normal e idempotente). **Edge Function** `inventory-purchases`: nova rota `POST /inventory-purchases/filament` → `register_filament_purchase` (a rota base, `POST /inventory-purchases`, continua servindo só ACCESSORY/PACKAGING a partir desta rodada — o caminho FILAMENT de item único nela permanece intacto no banco só para compatibilidade, sem exposição no cliente); validação estrutural nova (`validateRegisterFilamentPurchasePayload`/`validateFilamentPurchaseItem`); `_shared/errors.ts` ganhou 3 padrões novos (`register_filament_purchase: p_freight_value...`, `informe ao menos um item`, e um único padrão `register_filament_purchase: item ` cobrindo as 5 mensagens por-item — a mensagem de tipo inexistente/inativo reaproveita o padrão genérico "não encontrado" já existente, sem entrada própria). **Frontend**: `PurchaseDialog.tsx` reestruturado — Acessório/Embalagem **inalterados**; Filamento ganhou estado de lista (`filamentItems`), `useFilamentTypes()` chamado **uma única vez** no componente pai (nunca um por linha, evitando N buscas redundantes numa compra com vários itens) e repassado por prop a cada linha; `handleFilamentSubmit` valida cada item, monta `items[]` e chama a nova função `registerFilamentPurchase` (`lib/api/inventoryPurchases.ts`, rota `/filament`); título do diálogo passa a "Compra de filamentos" quando Filamento está selecionado. **Testes**: SQL — novo `supabase/tests/filament_purchase_test.sql` (35 casos: cabeçalho único com 2 itens/3 rolos batendo o exemplo do pedido — subtotal 300/frete 30/total 330 —, cada rolo no tipo/marca/peso corretos via `purchase_item_id`, nenhum peso bruto solicitado, nenhum tipo novo criado, 7 validações estruturais rejeitando ANTES de criar qualquer coisa, tipo inexistente/inativo rejeitado, idempotência com/sem conflito, atomicidade — falha no rolo do 2º item desfaz cabeçalho+1º item+seu rolo —, regressão do caminho antigo de item único, `item_id` nullable, RLS da tabela nova) — rodado de verdade contra o remoto dentro de `BEGIN`/`ROLLBACK`: **35 PASS, 0 FAIL**; `inventory_purchases_test.sql` (32 PASS) e `filament_inventory_test.sql` (53 PASS) reexecutados sem regressão. Deno — `handler.test.ts` (+18 casos: `validateFilamentPurchaseItem`/`validateRegisterFilamentPurchasePayload`, rotas `/filament`) e `errors.test.ts` (+5 casos, incluindo regressão cruzada com as mensagens de `register_inventory_purchase`) revisados estaticamente (Deno indisponível localmente, mesma limitação de sempre). Frontend: `PurchaseDialog.test.tsx` reescrito (28/28 — abre com 1 item, Adicionar/Remover item, bloqueio sem item, tipo/marca diferentes por item, "1.000 g" formatado, quantidade inválida, subtotal/frete/total corretos, payload com 2 itens batendo o exemplo do pedido, ausência de campos legados). Suíte `vitest` completa **1907 passando + 1 todo** (75 arquivos, +4 sobre a rodada anterior); `npm run lint` (0 erros, os mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `prettier --check`/`--write` nos arquivos front tocados OK; `git diff --check` sem problemas. **Publicação controlada**: migration `20260904130000` verificada primeiro dentro de `BEGIN`/`ROLLBACK` (sem erro de sintaxe) e só então aplicada ao remoto `tjhacqreupfqefntjevf` (confirmada exatamente uma vez); `npx supabase functions deploy inventory-purchases --project-ref tjhacqreupfqefntjevf` — **`inventory-purchases` versão 2 → 3**; `filament-types` permaneceu na versão 2 e as demais 12 funções permaneceram com a mesma versão/`updated_at`, nenhuma outra tocada. **Smoke test básico**: `OPTIONS /inventory-purchases/filament` → 204 com CORS correto; `POST` sem token → 401 ("Header Authorization ausente."), rejeitado antes de qualquer escrita; `GET /inventory-purchases/filament` → 405; rota base (`OPTIONS`/`POST` sem token) reconferida sem regressão. **Nenhum dado oficial alterado; nenhum deploy de frontend realizado.** **Observação registrada, não causada por esta rodada**: a checagem de preservação de `RL-26-016` (e seu `filament_type_id`) não encontrou mais o registro no banco remoto — nem o rolo nem o tipo existem mais; nenhuma consulta ou escrita desta rodada referenciou esse id/code (as fixtures usaram marcadores `TESTE COMPRA MULTI`/`TESTE MULTI ATOM`, distintos, e tudo rodou dentro de `BEGIN`/`ROLLBACK`), e a árvore estava limpa/sincronizada no checkpoint de entrada (`9d9b6a4`) — consistente com o próprio usuário tendo validado manualmente, nas rodadas de 2026-09-03, a exclusão física de rolo vazio e (depois de o rolo já não existir) a exclusão física de tipo sem dependências, ambas aprovadas na interface real; não foi possível confirmar a causa exata sem uma auditoria de log fora do escopo desta rodada. **Validação manual do usuário com filamentos reais pela interface (incluindo o cenário de 2 itens/marcas na mesma compra) continua pendente** — não foi executada nesta rodada. Módulo 3 permanece em **~51%** (melhoria de UX/modelo de dados sobre um incremento já contabilizado — nenhuma fase nova da metodologia das 7 fases concluída). Checkpoint: commit `feat: support multi-item filament purchases`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase. |
| 2026-09-04 | **Reorganização compacta da janela "Compra de filamentos" em 5 seções (Dados Gerais → Itens → Frete → Resumo → botões); Data da compra (dd/mm/aa) e Local da compra (4 opções) novos no cabeçalho; itens em uma linha no desktop; "Adicionar filamento" vira ícone; Resumo recalculado para Custo por filamento. Nova coluna + Edge Function republicada.** **Layout**: a janela passa a ter 5 áreas visuais nesta ordem — (1) **Dados Gerais**: Data da compra + Local da compra; (2) **Itens**: lista dinâmica, cabeçalho de coluna próprio (oculto em telas pequenas) + uma linha por item; (3) **Frete**: seção própria, só "Valor do frete"; (4) **Resumo**: Total da compra / Frete / Custo por filamento; (5) Cancelar/Registrar compra. A janela de Filamento fica mais larga no desktop (`sm:max-w-4xl`, antes `sm:max-w-2xl`) só para acomodar os itens em linha — Acessório/Embalagem mantêm a largura original, sem nenhuma mudança de layout. **Data da compra**: campo obrigatório, texto controlado no formato **dd/mm/aa** (ex.: `04/09/26`), pré-preenchido com a data atual ao entrar em Filamento; validação real de calendário (`new Date` + comparação dia/mês/ano, rejeita `31/02/26` e datas inexistentes, não só o formato); convertida internamente para `YYYY-MM-DD` (`occurred_at`) só no envio. **Local da compra**: novo `role="radiogroup"` de 4 botões — Mercado Livre/AliExpress/Shopee/Presencial — obrigatório, seleção única, destaque visual igual aos demais radiogroups de botão da janela, lado a lado no desktop e com quebra de linha automática em telas pequenas (`flex flex-wrap`, sem CSS extra — o comportamento nativo já atende). **Auditoria prévia** (antes de criar qualquer coluna): confirmado, por busca textual em migrations/Edge Functions/frontend, que **nenhum campo de local/canal de compra existia** em nenhuma tabela do projeto — não era um campo mal aproveitado, é uma coluna nova. **Banco** (`supabase/migrations/20260904140000_add_purchase_channel_to_filament_purchases.sql`, append-only — nenhuma migration anterior editada): `inventory_purchases.purchase_channel` (text, nullable, `CHECK` fechado aos 4 valores oficiais — `MERCADO_LIVRE`/`ALIEXPRESS`/`SHOPEE`/`PRESENCIAL`) — **nunca gravado em `notes`**; nullable na tabela preserva compras antigas e as de ACCESSORY/PACKAGING/FILAMENT de item único (todas continuam com `purchase_channel=null`, nunca inferido/reescrito). `register_filament_purchase` ganhou o parâmetro final `p_purchase_channel` (assinatura antiga de 6 args **`drop function`**-ada antes do `create or replace` com 7 args, mesmo padrão já usado para `remove_filament_type`/`register_inventory_purchase`, evita sobrecarga ambígua) — **obrigatório dentro da RPC** (rejeita null/valor fora dos 4 oficiais, mesmo com a coluna aceitando null no schema); idempotência passou a comparar também o canal (`is not distinct from`) — mesma chave com canal diferente agora é tratada como payload divergente (`IDEMPOTENCY_KEY_CONFLICT:`). `_build_filament_purchase_summary` (mesma assinatura, só o corpo mudou) devolve `purchase_channel` no jsonb. **Edge Function** `inventory-purchases`: `FILAMENT_PURCHASE_KEYS` ganhou `purchase_channel`; novo validador `requirePurchaseChannel` (400 se ausente/fora do enum); `_shared/errors.ts` ganhou o padrão `register_filament_purchase: purchase_channel` (400), sem colidir com o padrão já existente `register_filament_purchase: item `. **Itens — grade compacta**: `Tipo | Peso | Quantidade | Marca | Valor unitário | Remover` numa única linha no desktop (CSS Grid com colunas de largura mista — Tipo maior, Peso/Remover por conteúdo, Quantidade/Valor unitário fixos compactos, Marca intermediária — `grid-cols-1` empilhado em telas pequenas, nunca corta conteúdo nem força rolagem horizontal); cada linha é um `role="group"` com nome acessível `"Item N"` (novo — facilita tanto a leitura por leitor de tela quanto a localização em teste); rótulos de campo por linha viram `sr-only` no desktop (um cabeçalho de coluna visível cobre a associação visual, mantendo a associação por `htmlFor`/`aria-label` para acessibilidade). **"Peso"**: renomeado de "Peso líquido" só nesta linha compacta (mesmo significado — peso líquido por rolo —, mesmos 3 botões 250/500/1.000 g já formatados no padrão brasileiro, herdado da rodada anterior). **Botão "Adicionar filamento"**: migrou do rodapé da lista para o título da seção "Itens", à direita, agora **somente ícone** (`+`, `size="icon-sm"`), nome acessível "Adicionar filamento" — ao clicar, adiciona uma linha vazia, preserva as já preenchidas, e **foca automaticamente o Tipo do novo item** (novo prop opcional `autoFocus` em `SearchAutocomplete.tsx`, repassado só para a linha recém-criada; nenhum outro chamador do componente passa esse prop, nenhum comportamento anterior muda). **Botão "Remover item"**: virou ícone de lixeira (`Trash2Icon`, `size="icon-sm"`), nome acessível `"Remover item N"`; **a última linha restante nunca pode ser removida** — botão fica desabilitado (mesmo padrão já usado em `ProductForm.tsx` para "Remover Plate") em vez de esvaziar campos, critério explicitamente oferecido como alternativa válida. **Frete**: vira seção própria "Frete", só com "Valor do frete" (renomeado de "Frete total da compra"), continua único por compra, aceita R$ 0,00, nunca dividido/rateado entre itens. **Resumo**: recalculado — mostra só **Total da compra** / **Frete** / **Custo por filamento** (Subtotal dos itens deixou de aparecer como linha própria, usado só internamente para compor o Total); Custo por filamento = Total da compra ÷ quantidade total de rolos, com guarda explícita contra `NaN`/`Infinity` quando a quantidade total é 0 (mostra R$ 0,00). **Preservado sem nenhuma alteração**: vínculo por `filament_type_id`, compra com vários itens, criação automática de um rolo por unidade, atomicidade da RPC, gerador de código de rolo, peso líquido inicial, marca por item (`purchase_item_id`), compatibilidade com compras antigas, regras de arquivamento de tipos/rolos, e todos os demais módulos — nenhum arquivo fora de `inventory-purchases`/`PurchaseDialog.tsx`/`SearchAutocomplete.tsx`/`inventoryPurchases.ts`/`errors.ts` foi tocado. **Testes**: SQL — `filament_purchase_test.sql` ganhou a Seção 8 (8 casos: `purchase_channel` ausente rejeitado, valor fora do enum rejeitado, os 4 valores aceitos e persistidos, idempotência rejeita quando só o canal muda) e todas as 15 chamadas pré-existentes de `register_filament_purchase` foram atualizadas para informar o novo parâmetro obrigatório — rodado de verdade contra o remoto dentro de `BEGIN`/`ROLLBACK`: **43 PASS, 0 FAIL** (35 pré-existentes + 8 novas); `inventory_purchases_test.sql` (32 PASS) e `filament_inventory_test.sql` (53 PASS) reexecutados sem regressão. Deno — `handler.test.ts` (+3 casos: `requirePurchaseChannel`, payload exigindo o campo) e `errors.test.ts` (+2 casos, incluindo regressão cruzada com o padrão `item `) revisados estaticamente (Deno indisponível localmente). Frontend: `PurchaseDialog.test.tsx` reescrito para a janela compacta (36/36 — 19 pontos numerados do pedido: ordem das seções, data pré-preenchida/validada/convertida, os 4 locais/seleção única/persistência, layout em linha por item, ícone de adicionar, foco automático no novo item, preservação ao remover, bloqueio da última linha, "Peso" formatado, Frete único, Total inclui frete, Custo por filamento com o exemplo exato do pedido — 2×90+1×100+20 frete = 300, ÷3 = R$ 100,00 —, guarda contra NaN/Infinity com quantidade 0, grade responsável em telas pequenas, e o fluxo completo de 2 itens preservado). Suíte `vitest` completa **1915 passando + 1 todo** (75 arquivos, +8 sobre a rodada anterior); `npm run lint` (0 erros, os mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `prettier --check`/`--write` nos arquivos front tocados OK; `git diff --check` sem problemas. **Publicação controlada**: migration `20260904140000` verificada primeiro dentro de `BEGIN`/`ROLLBACK` (sem erro de sintaxe) e só então aplicada ao remoto `tjhacqreupfqefntjevf` (confirmada exatamente uma vez, dry-run mostrou só ela pendente); `npx supabase functions deploy inventory-purchases --project-ref tjhacqreupfqefntjevf` — **`inventory-purchases` versão 3 → 4**; as demais 13 Edge Functions permaneceram com a mesma versão/`updated_at`, nenhuma outra tocada. **Smoke test básico**: `OPTIONS /inventory-purchases/filament` → 204 com CORS correto; `POST` sem token → 401 ("Header Authorization ausente."), rejeitado antes de qualquer escrita; rota base (`OPTIONS`/`POST` sem token) reconferida sem regressão. **Nenhum dado oficial alterado; nenhum deploy de frontend realizado.** Checagem de resíduo confirma zero linha nova de `TESTE COMPRA%`/`teste-compra-multi%` após o `ROLLBACK`; `RL-26-016` segue no mesmo estado já registrado na entrada anterior (ausente do banco, não tocado por nenhuma consulta ou escrita desta rodada). **Validação manual do usuário com filamentos reais pela interface (incluindo Data/Local da compra e o layout compacto) continua pendente** — não foi executada nesta rodada. Módulo 3 permanece em **~51%** (melhoria de UX/modelo de dados sobre um incremento já contabilizado — nenhuma fase nova da metodologia das 7 fases concluída). Checkpoint: commit `feat: compact filament purchase form`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase. |
| 2026-09-04 | **Ajustes finais da área de Filamentos: validação manual dos itens 1–15 do fluxo "Cadastrar tipo → Compra multi-item → Rolos atualizados" registrada; Situação Normal em verde; nova coluna Estoque mínimo na listagem; Linha "Tricolor"; sugestões de Cor a partir dos tipos já cadastrados; Locais de compra Site/Outro; coluna Marca em "Ver rolos". Nova migration + Edge Function republicada.** **1. Validação manual anterior aprovada**: o usuário confirmou manualmente os itens **1 a 15** do fluxo completo "Cadastrar tipo → Compra multi-item → Rolos atualizados" (cadastro do tipo, seleção de `filament_type_id` na compra, criação automática de N rolos por item, atomicidade, código/peso líquido inicial corretos) — registrado aqui **sem apagar nenhuma entrada anterior**; a validação específica da janela compacta (Data/Local da compra) e destes ajustes desta rodada **continua pendente**, não confundir uma com a outra. **2. Situação Normal em verde**: novo `FilamentStockLevelBadge` (só na listagem de Filamentos, `FilamentsInventoryPage.tsx`) — `getStockLevel` (cálculo) **inalterado**; "normal" ganha texto **"Normal"** (antes "Estoque normal") e cor `border-emerald-300 bg-emerald-50 text-emerald-800` (mesmo padrão de sucesso já usado em `OrderStatusControl`/`OrderPaymentStatusControl` para DELIVERED/PAID — nenhuma cor inventada); "low"/"empty" preservam rótulo e cor de sempre (âmbar "Estoque baixo", vermelho "Sem estoque"). O `StockLevelBadge` **compartilhado** (`StockMovementPanel.tsx`, usado por Acessórios/Embalagens) **não foi tocado** — continua cinza "Estoque normal", exatamente como estava. **3. Coluna "Estoque mínimo"**: nova coluna na listagem consolidada, entre "Rolos disponíveis" e "Situação" — usa **a mesma regra já existente** (`group.minimumStockGrams` de `filamentGroups.ts`: maior limite entre os tipos **ATIVOS** do grupo, `null` quando nenhum tipo ativo tem limite — já era exatamente a regra do resumo de "Ver rolos", nenhuma segunda regra criada), formatada com ponto de milhar e espaço ("500 g"/"1.000 g"/"2.000 g" — um `formatMinimumStockGrams` novo, distinto do `formatGrams` sem espaço já usado por "Disponível", que esta rodada não altera); "—" quando não há mínimo. Esta listagem consolidada **nunca teve card mobile dedicado** (só a `<table>` com `overflow-x-auto`) — a coluna aparece nela em qualquer largura de tela, responsividade preservada por rolagem horizontal, como as demais. **4. Linha "Tricolor"**: acrescentada a `LINE_OPTIONS` em `FilamentTypeForm.tsx` — mesmo tratamento das demais opções (botão único, seleção exclusiva, enviada/preservada com exatamente essa grafia); registros históricos preservados (mecanismo de `historicalLine` inalterado). **5. Sugestões de Cor**: novo `distinctFilamentColors()` em `filamentGroups.ts`, reaproveitando **exatamente** `normalizeFilamentToken`/`pickCanonicalLabel` (mesma dedupe/canonicalização já usada para consolidar a listagem — nunca uma segunda regra): "Preto"/"preto"/" Preto "/colapsam numa única sugestão, grafia mais frequente preservada. O campo Cor de `FilamentTypeForm.tsx` passou de `<Input>` para `SearchAutocomplete` (mesmo componente já usado em Acessório/Embalagem/Tipo de filamento da compra), com filtro local caixa/acento-insensível (`normalizeForSearch`, sem requisição nova — opera só sobre os tipos já carregados por `useFilamentTypes()`, repassados por `FilamentsInventoryPage.tsx` via prop `existingColors`); digitar uma cor sem sugestão compatível continua aceito normalmente (nunca restringe a um valor da lista); nenhuma tabela nova, nenhuma cor de registro existente alterada. **6. Locais de compra Site e Outro**: `PURCHASE_CHANNEL_OPTIONS` (`PurchaseDialog.tsx`) ganhou **Site** (`SITE`) e **Outro** (`OUTRO`), lado a lado com os 4 já existentes (mesmo `flex flex-wrap`, sem CSS novo); sem campo de texto livre adicional para "Outro" nesta rodada, como pedido. **Auditoria confirmou** (`pg_get_constraintdef`) que `inventory_purchases_purchase_channel_check` só aceitava os 4 valores anteriores — migration nova e append-only (`supabase/migrations/20260904150000_add_site_outro_purchase_channels.sql`, `20260904140000` **não editada**): `DROP CONSTRAINT` + `ADD CONSTRAINT` (nunca `ALTER COLUMN`) troca só a definição do CHECK — um superconjunto do anterior, nenhuma linha reescrita/apagada, toda compra já registrada com um dos 4 valores antigos continua válida. `register_filament_purchase` ganhou os 2 valores na mesma validação inline (`CREATE OR REPLACE`, **mesma assinatura de 7 argumentos** — sem parâmetro novo, sem `DROP FUNCTION`). Edge Function `inventory-purchases`: `PURCHASE_CHANNELS` ganhou os 2 valores (mesmo padrão de erro 400 já existente, nenhuma entrada nova em `errors.ts`). **7. Marca em "Ver rolos"**: nova coluna/campo **Marca** em `FilamentTypeDrawer.tsx` (desktop: coluna própria na tabela, entre "Fabricante / tipo" e "Peso Líquido"; mobile: linha própria no card) — **distinta** de "Fabricante / tipo" (que continua mostrando o fabricante do TIPO, igual para todo rolo do tipo): Marca é a marca REAL da compra que originou o rolo, podendo variar rolo a rolo. Fonte, nesta ordem: (1) `purchase_item_manufacturer` — rolo da compra multi-item, derivado de `inventory_purchase_filament_items.manufacturer` via `purchase_item_id`; (2) fabricante histórico do TIPO, para rolo antigo sem `purchase_item_id` (já carregado, sem consulta extra); (3) "—" quando nenhum dos dois é um valor real (vazio ou "Não informado"). **Sem duplicar a marca no rolo**: `filament_spools` não ganhou nenhuma coluna nova — `purchase_item_id` (coluna já existente desde `20260904130000`) foi só exposta no tipo `FilamentSpool` do frontend; a Marca é sempre **derivada**, nunca gravada. `listFilamentSpools()` (`lib/api/filamentSpools.ts`) passou a usar **PostgREST resource embedding** (`.select('*, inventory_purchase_filament_items(manufacturer)')`) na MESMA consulta de `filament_spools` — continua exatamente **2 consultas no total** (a segunda, inalterada, é `filament_movements`), **nenhuma consulta adicional por linha/rolo** (confirmado em teste dedicado com `fromMock.toHaveBeenCalledTimes(2)` independente do nº de rolos). `useFilamentSpools.ts`: `create()` sempre define `purchase_item_manufacturer: null` (rolo manual nunca nasce de uma compra); `update()` preserva o valor local anterior (mesmo padrão já usado para `has_movement_history`). **Testes**: SQL — `filament_purchase_test.sql`, loop de canais estendido de 4 para 6 valores (`SITE`/`OUTRO` incluídos) — rodado de verdade contra o remoto dentro de `BEGIN`/`ROLLBACK`: **45 PASS, 0 FAIL** (43 pré-existentes + 2 novas); `inventory_purchases_test.sql` (32 PASS), `filament_inventory_test.sql` (53 PASS) e `remove_filament_type_test.sql` (32 PASS, 1 SKIP — concorrência, já registrado) reexecutados sem regressão. Deno — `handler.test.ts` (`requirePurchaseChannel` estendido para os 6 valores) revisado estaticamente (Deno indisponível localmente). Frontend: `filamentGroups.test.ts` (+5 casos, `distinctFilamentColors`); `filamentSpools.test.ts` (+1 caso, JOIN embutido/N+1); `useFilamentSpools.test.ts` (+1 caso, preservação de `purchase_item_manufacturer`); `FilamentsInventoryPage.test.tsx` (+18 casos: Situação Normal verde/demais cores preservadas, coluna Estoque mínimo aparece/formata/mostra "—"/só tipos ativos, Tricolor selecionável e enviado, sugestões de Cor — aparecem/deduplicam/ignoram caixa e acento/permitem cor nova/preenchem ao selecionar —, coluna Marca na tabela/card mobile/rolo multi-item/rolo antigo/"Não informado"→"—"); `PurchaseDialog.test.tsx` (+2 casos, Site/Outro aparecem e são enviados como `SITE`/`OUTRO`). Suíte `vitest` completa **1938 passando + 1 todo** (75 arquivos, +23 sobre a rodada anterior); `npm run lint` (0 erros, os mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `prettier --check`/`--write` nos arquivos tocados OK; `git diff --check` sem problemas. **Publicação controlada**: migration `20260904150000` verificada primeiro dentro de `BEGIN`/`ROLLBACK` (sem erro de sintaxe) e só então aplicada ao remoto `tjhacqreupfqefntjevf` (confirmada exatamente uma vez, dry-run mostrou só ela pendente); `npx supabase functions deploy inventory-purchases --project-ref tjhacqreupfqefntjevf` — **`inventory-purchases` versão 4 → 5**; as demais 13 Edge Functions permaneceram com a mesma versão/`updated_at`, nenhuma outra tocada (`filament-types`/`filament-spools` não precisaram de republicação — a derivação de Marca é só leitura via supabase-js, sem nenhuma mudança de contrato de escrita). **Smoke test básico**: `OPTIONS /inventory-purchases/filament` → 204 com CORS correto; `POST` sem token → 401, rejeitado antes de qualquer escrita; rota base (`OPTIONS`/`POST` sem token) reconferida sem regressão. **Nenhum dado oficial alterado; nenhum deploy de frontend realizado.** Checagem de resíduo confirma zero linha nova após o `ROLLBACK`; `RL-26-016` segue no mesmo estado já registrado nas entradas anteriores (ausente do banco, não tocado por nenhuma consulta ou escrita desta rodada). **Validação manual do usuário destes ajustes (Situação Normal, Estoque mínimo, Tricolor, sugestões de Cor, Site/Outro, Marca em Ver rolos) continua pendente** — não foi executada nesta rodada; a aprovação registrada no item 1 cobre só o fluxo de compra multi-item já validado antes, não estes ajustes novos. Módulo 3 permanece em **~51%** (ajustes de UX/exibição sobre um incremento já contabilizado — nenhuma fase nova da metodologia das 7 fases concluída). Checkpoint: commit `feat: refine filament inventory details`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase. |
| 2026-09-04 | **Correção da atualização automática do fluxo de Filamentos: nenhuma tela mais exige F5 depois de cadastrar/editar/arquivar um tipo, registrar uma compra, ou adicionar/editar/pesar/remover um rolo.** **0. Validação manual anterior reconfirmada**: os itens **1 a 15** do fluxo "Cadastrar tipo → Compra multi-item → Rolos atualizados" seguem **aprovados** (registrado na entrada anterior) — esta rodada não repete essa validação, só corrige um problema **encontrado depois** dela: o usuário precisava pressionar F5 para ver cadastros/alterações recentes em certas telas. **Diagnóstico** (auditoria de query keys/cache, sem TanStack Query/React Query no projeto — confirmado por busca textual, cada hook de dados é `useState`+`useEffect` próprio, sem cache compartilhado entre instâncias): a MAIORIA do fluxo já se atualizava sozinha (criação/edição/exclusão de tipo e de rolo já faziam merge local reativo, recalculando listagem/filtros/Situação/Estoque mínimo via `useMemo`; `onSummaryChanged` já disparava `refetchAll` em toda mutação de rolo). Duas lacunas reais confirmadas: **(a)** `PurchaseDialog.tsx` mantém sua PRÓPRIA instância de `useFilamentTypes()`, independente da instância da página — como não há cache compartilhado, um tipo cadastrado/editado/arquivado na página nunca chegava a essa segunda instância sem recarregar a página inteira, deixando o seletor "Tipo de filamento" da Compra desatualizado; **(b)** arquivar um tipo pelo botão "Excluir tipo" **de dentro** da janela "Ver rolos" (diálogo aninhado, continua aberto) deixava a própria tabela de rolos do drawer com dados obsoletos (rolos já arquivados ainda visíveis) até fechar/reabrir a janela ou recarregar a página. **Descartado por não ser um bug real**: um terceiro cenário suposto (compra registrada enquanto "Ver rolos" está aberto) não é alcançável — "Compras" (nível de página, botão no cabeçalho) e "Ver rolos" (nível de página, corpo) são diálogos **irmãos, não aninhados**; o primitivo de Dialog usado no projeto torna um inacessível (`aria-hidden`) enquanto o outro está aberto, confirmado por um teste que falhou ao tentar abrir "Compras" com "Ver rolos" já aberto — os dois nunca coexistem, então fechar e reabrir "Ver rolos" (que já remonta e busca os rolos do zero, comportamento existente, sem código novo) é o único caminho real e já funciona. **Correção (a)**: `PurchaseDialog.tsx` passou a chamar `refetchFilamentTypes()` (já exposto por `useFilamentTypes`) toda vez que a janela de Compras é **aberta** (`handleOpenChange`) — nunca em polling, nunca a cada tecla digitada, nunca via `setTimeout`; usa a chave/consulta canônica já existente (`listFilamentTypeSummaries`), sem criar uma segunda fonte de dados. **Correção (b)**: `FilamentsInventoryPage.tsx` ganhou `dataVersion`/`refetchAllAndSignalDrawer` (bump de contador, **só** no caminho ARCHIVED de `handleConfirmDelete`), repassado como prop `refreshSignal` a `FilamentTypeDrawer.tsx`; o drawer reage com um `useEffect` guardado por `useRef` (ignora a primeira renderização de cada abertura, evitando uma busca duplicada com a busca inicial do próprio `useFilamentSpools`) chamando seu `refetch()` só quando o sinal muda depois de montado — nenhum polling, nenhum `setTimeout`, nenhum reload. **Nenhuma outra mutação precisou de código novo**: pesagem/ajuste de rolo, criação/edição/exclusão de rolo e criação/edição de tipo já propagavam corretamente (confirmado por leitura e por testes de regressão já existentes, sem alteração). **Arquivos alterados** (só frontend, `.tsx`/`.ts`): `PurchaseDialog.tsx`, `FilamentsInventoryPage.tsx`, `FilamentTypeDrawer.tsx`, `useFilamentTypes.test.ts`, `PurchaseDialog.test.tsx`, `FilamentsInventoryPage.test.tsx` — nenhuma migration, nenhuma Edge Function, nenhum arquivo de banco tocado. **Testes** provando os 10 pontos pedidos sem F5: `useFilamentTypes.test.ts` (+1, `refetch()` substitui a lista pelo resultado mais recente — base do mecanismo); `PurchaseDialog.test.tsx` (+1, abrir "Compras" sempre refaz a busca de tipos, em toda reabertura, nunca só na primeira); `FilamentsInventoryPage.test.tsx`, novo describe **"atualização automática sem F5"** (+5 testes novos, cobrindo os itens 1 a 10 do pedido — os demais itens já tinham cobertura de regressão pré-existente e permanecem passando): registrar uma compra e reabrir "Ver rolos" mostra o número de rolos e a Marca do item novo sem F5 (fluxo real: fechar → comprar → reabrir, nunca as duas janelas abertas ao mesmo tempo); arquivar um tipo pelos botões de dentro de "Ver rolos" refaz a busca de rolos do drawer (aninhado, mesma janela); ações de dentro de "Ver rolos" que já se resolviam localmente (editar um rolo) seguem **sem** disparar uma segunda busca redundante; e uma checagem estática (via `import.meta.glob` tipado por `vite/client`, sem depender de módulos Node fora do escopo do frontend) confirmando que nenhum dos 6 arquivos tocados usa `window.location.reload`/`location.href`. Suíte `vitest` completa **1944 passando + 1 todo** (75 arquivos, +6 sobre a rodada anterior); `npm run lint` (0 erros, os mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas (só avisos de LF/CRLF do próprio Git, sem erro de whitespace). **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum deploy de frontend realizado nesta rodada** — correção só de frontend, testada localmente. Módulo 3 permanece em **~51%** (correção de comportamento reativo sobre um incremento já contabilizado — nenhuma fase nova da metodologia das 7 fases concluída). **Nova validação manual do usuário confirmando que as telas realmente não exigem mais F5 depois destas correções continua pendente** — não foi executada nesta rodada. Checkpoint: commit `fix: refresh filament inventory after mutations`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase. |
| 2026-09-04 | **Aprovação manual dos itens 1 a 7 da atualização automática sem F5; coluna Cor da listagem de Filamentos passa a ser exibida como badge colorida.** **1. Validação manual anterior aprovada**: o usuário confirmou manualmente os itens **1 a 7** da correção de atualização automática registrada na entrada anterior (F5 deixou de ser necessário depois de cadastrar/editar/arquivar um tipo, registrar uma compra e adicionar/editar/pesar/remover um rolo) — registrado aqui **sem apagar nenhuma entrada anterior**; itens 8–10 (checagens estáticas de query keys/ausência de reload) não fazem parte desta validação manual em navegador, só de teste automatizado, e seguem cobertos pela suíte. **2. Coluna Cor como badge**: na listagem `Estoque → Filamentos`, a célula de Cor deixou de ser texto solto e passou a ser um badge — mesma dimensão/arredondamento/espaçamento do badge de Situação já existente nesta listagem (`rounded-md border px-2 py-0.5 text-xs font-medium`), preservando sempre o **nome completo cadastrado** como texto (nunca um círculo/swatch sem texto) e nunca alterando o valor salvo no banco (`filament_types.commercial_color` intocado — só uma leitura diferente na renderização). **Componente novo e reutilizável** `FilamentColorBadge` (`components/inventory/FilamentColorBadge.tsx`) consome um helper puro `resolveFilamentColorClassName` (`lib/inventory/filamentColorStyles.ts`, sem React, testável isoladamente) — separados em dois arquivos deliberadamente (um componente, uma função) para não disparar o aviso `react-refresh/only-export-components` e manter o lint em zero avisos novos. **Reconhecimento de cor**: reaproveita **exatamente** `normalizeFilamentToken` (minúsculas + sem acentos + espaços colapsados), a mesma normalização já usada para consolidar a listagem por Cor — nunca uma segunda regra; cobre as 17 cores pedidas (Preto, Branco, Cinza, Vermelho, Azul, Verde, Amarelo, Laranja, Roxo, Rosa, Marrom, Bege, Dourado, Prateado, Bronze, Transparente, Natural) e as 6 variações compostas pedidas (Azul claro/escuro, Verde claro/escuro, Rosa claro, Cinza escuro — combinações de duas palavras verificadas antes das palavras isoladas, para não perder o modificador); nome composto (ex.: "Azul Bambu Lab") usa a cor principal reconhecida no texto, preservando o texto completo. **Acessibilidade**: Amarelo e Dourado usam texto num tom mais escuro (`text-yellow-900`, não `-800` como os demais) para permanecer legível sobre fundo claro; Branco nunca usa fundo branco puro sobre o fundo branco da página (`border-slate-400 bg-slate-50 text-slate-700` — fundo levemente acinzentado + borda visível); cor não reconhecida cai no **mesmo estilo neutro** já usado pelos badges "Inativo"/"Arquivado" desta mesma área (`border-input text-muted-foreground`), nunca com texto invisível; nenhuma variante `dark:` foi adicionada — auditoria confirmou que esta aplicação não tem nenhum alternador de tema próprio (nenhum `ThemeProvider`/`prefers-color-scheme` fora do componente de toast) e nenhum outro badge desta área (Situação, Inativo, Arquivado) usa `dark:`, então este também não usa, por coerência visual. **Classes Tailwind estáticas**: cada entrada do mapa cor→estilo é uma string literal completa (nunca montada por interpolação de partes), confirmado após o build de produção — as classes usadas (`bg-neutral-800`, `border-dashed`, `text-yellow-900`, `bg-sky-50`, `border-stone-400`, entre outras) foram localizadas de fato no CSS final gerado, nenhuma removida pelo purge do Tailwind. **Sem mobile card duplicado**: a listagem consolidada de Filamentos **nunca teve um card mobile dedicado** (fato já documentado desde a coluna "Estoque mínimo") — só a `<table>` com `overflow-x-auto`, responsiva por rolagem horizontal — então a mesma marcação do `FilamentColorBadge` é exatamente o que aparece em qualquer largura de tela, sem um segundo caminho de renderização a manter em sincronia; o componente foi desenhado para ser diretamente reaproveitável caso um card mobile próprio seja criado no futuro, sem duplicar o mapeamento. **Nenhuma outra coluna foi alterada**: Situação, Estoque mínimo, Material, Linha, Disponível, Rolos disponíveis e Ações permanecem exatamente como estavam. **Testes**: `filamentColorStyles.test.ts` (novo, 10 casos — Preto/Branco/Cinza escuro/Azul claro/Azul escuro/Dourado/Amarelo, busca ignorando acentos/caixa/espaços, nome composto, cor desconhecida, as 17 cores iniciais nunca caem no neutro); `FilamentColorBadge.test.tsx` (novo, 4 casos — nome completo renderizado mesmo composto, espaços internos preservados exatamente, cor desconhecida nunca com texto invisível, atributo `title` para tooltip); `FilamentsInventoryPage.test.tsx`, novo describe **"coluna Cor como badge"** (+10 casos, cobrindo os 10 pontos pedidos: badge na tabela, mesma marcação em qualquer largura de tela — via contagem de um único elemento com o texto, confirmando ausência de duplicação —, Preto/Azul claro/Dourado/Branco com o estilo esperado, busca ignorando acentos/caixa, texto original preservado, cor desconhecida neutra, Situação e demais colunas inalteradas). Suíte `vitest` completa **1968 passando + 1 todo** (77 arquivos, +24 sobre a rodada anterior); `npm run lint` (0 erros, os mesmos 8 avisos `react-refresh` pré-existentes — nenhum aviso novo, verificado explicitamente após separar componente e helper em arquivos distintos); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas (só avisos de LF/CRLF do próprio Git). **Nenhuma migration, Edge Function ou deploy de frontend nesta rodada** — correção só de frontend (estilo/apresentação), testada localmente. Módulo 3 permanece em **~51%** (ajuste de apresentação sobre um incremento já contabilizado — nenhuma fase nova da metodologia das 7 fases concluída). **Validação visual da badge de Cor pelo usuário continua pendente** — não foi executada nesta rodada; a aprovação registrada no item 1 cobre só os itens 1–7 da correção de F5 já validada antes, não esta mudança visual nova. Checkpoint: commit `feat: display filament colors as badges`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase. |
| 2026-09-05 | **Simplificação da área "Rolos em estoque" da janela "Ver rolos": botão "Gerenciar" removido, Status vira dropdown interativo, "Ajustar peso" ganha janela própria (peso absoluto), e o menu de três pontos ganha "Abrir rolo" e "Histórico".** **1. Tabela**: `FilamentTypeDrawer.tsx` — colunas finais (desktop e card mobile) são só **Identificador, Marca, Peso Líquido, Status, Ações**; a coluna "Fabricante / tipo" (fabricante+linha+cor do TIPO) e a coluna própria "Ajustar peso" foram removidas — "Ajustar peso" virou um botão compacto (só ícone `SlidersHorizontalIcon`, mesmo de antes) dentro da própria coluna "Ações", ao lado do menu de três pontos. O botão "Gerenciar" (que abria a janela grande com Movimentar/Registrar pesagem/Histórico) foi **removido por completo** — `FilamentSpoolPanel`/`FilamentMovementForm`/`FilamentWeighingForm` continuam no repositório (com seus próprios testes) mas **não são mais referenciados** por nenhuma tela; "Mostrar arquivados" e as ações Editar/Excluir continuam exatamente como estavam, e a tabela some atualização automática (sem F5) preservada por inteiro (mesmo mecanismo `setLocalSpoolState`/`onSummaryChanged`/`refetchAll` já existente). **2. Status interativo**: novo `FilamentSpoolStatusControl` (`components/inventory/FilamentSpoolStatusControl.tsx` + `lib/inventory/filamentSpoolStatusStyles.ts`, mesmo padrão de separação componente/estilo de `FilamentColorBadge`/`filamentColorStyles`) substitui o texto solto de status por um botão/dropdown com **Lacrado** (neutro/azul), **Aberto** (verde) e **Descartado** (vermelho); ESGOTADO não é uma opção selecionável (é sempre automático quando o saldo chega a 0) mas mantém rótulo/cor própria (cinza) quando o rolo já está nesse status. A opção atual vem marcada e **desabilitada** no menu (clicar nela de novo não dispara nada); toda mudança passa pela **MESMA** rota já usada por "Editar rolo" (`update_filament_spool` via `useFilamentSpools.update`, RPC/regras de backend inalteradas — inclusive `FILAMENT_SPOOL_DISCARD_IS_FINAL`, que também desabilita o próprio gatilho do dropdown quando o rolo já está Descartado). Mudar para **Descartado exige confirmação explícita** num diálogo próprio (`discardCandidate`, mesmo padrão visual da confirmação de "Excluir rolo"); as demais transições são imediatas. Um rolo arquivado (`is_active=false`) tem o dropdown desabilitado (mesma cautela que já existia para Movimentar/Pesar num rolo arquivado dentro da antiga janela "Gerenciar"). **3. Ajustar peso**: nova janela simplificada `FilamentSpoolWeightAdjustDialog.tsx` — só **Peso líquido atual** (somente leitura), **Novo peso líquido (g)** (campo numérico), **Cancelar** e **Salvar ajuste**; o valor digitado é sempre o **NOVO PESO LÍQUIDO ABSOLUTO** (nunca uma quantidade a somar/subtrair) — a diferença (`novo − atual`) é calculada no componente e gravada pela **MESMA** rota de ajuste que a antiga aba "Movimentar → Ajuste" já usava (`register_filament_movement`/`POSITIVE_ADJUSTMENT`/`NEGATIVE_ADJUSTMENT` via `useFilamentMovements(spool.id).register`, nunca um `UPDATE` direto do peso — histórico 100% preservado). Validações: campo obrigatório, aceita zero, rejeita negativo, só número válido (`parseNumberField`, mesma função já usada em todo o projeto), e um `isSubmitting` PRÓPRIO do formulário (somado ao `isRegistering` do hook) impede envio duplicado mesmo sob um duplo clique muito rápido. Como o backend exige motivo não vazio para ajustes (`filament_movements_reason_required_by_type`, migration já existente) e esta janela deliberadamente não pede motivo ao usuário, um motivo fixo (`"Ajuste de peso líquido (janela Ver rolos)"`) é enviado automaticamente — nenhuma migration/RPC nova, só uso da regra já existente. Depois de salvar: fecha a janela, atualiza Peso Líquido e os resumos do grupo (`setLocalSpoolState` + `onSummaryChanged`, sem F5) e mostra toast de sucesso/erro; o botão fica desabilitado para um rolo Descartado ou arquivado (mesma cautela do item 2). **4. Menu de três pontos**: `SpoolActionsMenu` mantém **Editar** e **Excluir rolo** (regras/confirmações inalteradas) e ganha **Abrir rolo** (só aparece quando o rolo está ativo e ainda não é ABERTO nem DESCARTADO; usa a **MESMA** rota de backend do dropdown de Status, sem confirmação) e **Histórico** (abre `FilamentSpoolHistoryDialog.tsx`, uma janela dedicada — nunca mais a antiga "Gerenciar" — reaproveitando **exatamente** a consulta já existente `useFilamentMovements`/`listFilamentMovements` filtrada por `spool_id` e o componente de exibição já existente `FilamentMovementHistory`, que já cobre carregando/vazio/erro; mostra data/hora, tipo, variação de peso, saldo resultante e motivo/observação, sem nenhuma lógica de busca duplicada). **5. Decisão de escopo (sinalizada para validação do usuário)**: com a remoção do botão "Gerenciar", as duas outras operações que só existiam dentro dele — "Registrar perda" (`LOSS`) e "Registrar pesagem" (`WEIGHING_ADJUSTMENT`, fluxo por peso bruto/tara) — **ficaram sem nenhum ponto de entrada na interface** nesta rodada (não foram pedidas nem mencionadas na simplificação solicitada). A capacidade de **reduzir** o peso líquido continua coberta pela nova "Ajustar peso" (aceita diferença negativa, só classificada como "Ajuste negativo" em vez de "Perda/Avaria" no histórico); histórico e RPCs de `LOSS`/`WEIGHING_ADJUSTMENT` **não foram tocados nem removidos** — só perderam o botão que os disparava. **Nenhuma migration, RPC ou Edge Function foi criada, alterada ou publicada** — todas as operações usam rotas já existentes (`update_filament_spool`, `register_filament_movement`, `listFilamentMovements`). **Testes**: `FilamentsInventoryPage.test.tsx` reescrito no describe de gerenciamento de rolo (as 15 antigas cobrindo a janela "Gerenciar" foram substituídas por equivalentes da nova interface) cobrindo: as 5 colunas finais e ausência do botão "Gerenciar"; dropdown de Status com as 3 opções, opção atual identificada/sem nova operação ao clicar de novo, confirmação antes de Descartado (cancelar não chama o backend), rolo Descartado com dropdown bloqueado; janela de Ajustar peso mostrando peso atual e tratando o valor como absoluto (cálculo correto da diferença, inclusive zero), campo obrigatório/negativo rejeitado/zero aceito, envio duplicado bloqueado, sem vazamento de valor entre rolos diferentes, atualização automática do saldo/resumo sem F5; "Abrir rolo" mudando o status (mesma rota do dropdown) e ausente para rolo já Aberto/Descartado; "Histórico" abrindo a janela do rolo certo com estados de carregando/vazio/erro; preservação de Editar/Excluir rolo. Suíte `vitest` completa **1971 passando + 1 todo** (2 falhas de timeout em arquivos não tocados desta rodada — `PurchaseDialog.test.tsx`/`ProductForm.test.tsx` — confirmadas como contenção de recursos ao rodar os 77 arquivos juntos, não regressão: as duas passam 100% quando reexecutadas isoladas); `npm run lint` (0 erros, os mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas. **Nenhuma migration aplicada, nenhuma Edge Function publicada, nenhum deploy de frontend realizado, nenhum dado oficial alterado.** **Validação manual do usuário pendente** — não foi executada nesta rodada: confirmar visualmente o dropdown de Status (cores/confirmação de Descartado), a janela simplificada de Ajustar peso, "Abrir rolo"/"Histórico" no menu de três pontos, e **decidir se "Registrar perda"/"Registrar pesagem" precisam de um novo ponto de entrada** em algum lugar da interface (item 5 acima). Módulo 3 permanece em **~51%** (simplificação de UX sobre um incremento já contabilizado — nenhuma fase nova da metodologia das 7 fases concluída). Checkpoint: commit `feat: simplify filament spool management`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase. |
| 2026-09-05 | **Aprovação manual dos itens 1 a 15 do checkpoint `e2d54bd` (simplificação de "Rolos em estoque"); janela "Histórico do rolo" sem rolagem horizontal no desktop; "Compra de filamentos" passa a pedir o Valor total de cada item (não mais o valor unitário).** **1. Validação manual anterior aprovada**: o usuário confirmou manualmente os itens **1 a 15** da simplificação de "Rolos em estoque" registrada no checkpoint `e2d54bd` (remoção do botão "Gerenciar", dropdown de Status, janela simplificada de Ajustar peso, "Abrir rolo"/"Histórico" no menu de três pontos) — registrado aqui **sem apagar nenhuma entrada anterior**; a decisão de escopo sobre "Registrar perda"/"Registrar pesagem" (item 5 daquela entrada) segue **em aberto**, não fazia parte desta validação. **2. Janela "Histórico do rolo" responsiva**: `FilamentSpoolHistoryDialog.tsx` — largura ampliada de `sm:max-w-2xl` para `sm:max-w-4xl` (margem lateral mínima de 1rem preservada pelo `max-w-[calc(100%-2rem)]` já embutido em `DialogContent`, `ui/dialog.tsx`); rolagem VERTICAL preservada (`overflow-y-auto`) para histórico longo. A causa raiz da barra horizontal era um `min-w-[700px]` fixo em `FilamentMovementHistory.tsx` (tabela reaproveitada por esta janela), removido — a tabela agora ocupa 100% do contêiner (`table-fixed`, colunas em `%`, sem largura mínima), então o wrapper `overflow-x-auto` do componente base `Table` (`ui/table.tsx`, presente em toda tabela do projeto) nunca mais precisa ativar rolagem na prática. A célula "Motivo/Observação" trocou `truncate` por `whitespace-normal break-words` (mesmo tratamento já dado à coluna Tipo numa rodada anterior) — um motivo longo agora quebra linha em vez de cortar, texto completo sempre visível sem depender só do tooltip. Em telas estreitas (`sm:hidden`), cada movimentação agora também renderiza como um card (mesmo padrão responsivo `hidden sm:block`/`sm:hidden` já usado pela tabela de rolos) — todos os 5 campos preservados (Data, Tipo, Quantidade, Saldo, Motivo/Observação), nenhuma informação escondida só para eliminar a rolagem. **3. "Compra de filamentos" — Valor total por item**: em `PurchaseDialog.tsx`, o campo "Valor unitário" de cada linha da seção Itens foi renomeado para **"Valor total"** — o usuário informa quanto pagou por TODOS os rolos daquela linha (ex.: 4 rolos por R$ 320,00), não mais o preço de um rolo só. O CONTRATO do backend **não mudou** (`RegisterFilamentPurchaseItemInput.unit_value` continua sendo o único campo aceito por `register_filament_purchase`) — o valor por rolo é derivado só no envio, via novo `unitValueFromTotalCents(totalCents, quantity)`: `Math.round(totalCents / quantity)` centavos, convertido para reais pelo mesmo `centsToAmount` (string, nunca ponto flutuante acumulado) já usado no projeto. **Resumo sempre exato**: o Subtotal/Total exibidos nesta janela somam os **centavos do Valor total de cada item diretamente** (nunca `quantity × unit_value` recalculado, que sofreria o mesmo arredondamento do envio) — por isso quantidade 3 + Valor total R$ 100,00 continua mostrando **exatamente R$ 100,00** no Resumo, comprovado por teste dedicado, mesmo sabendo que R$ 100,00 não divide em 3 partes exatas de centavo. **Limitação conhecida do contrato atual (sinalizada, nenhuma migration criada)**: `item_value` em `filament_purchase_items` é uma coluna `GENERATED ALWAYS AS (quantity * unit_value)` (migration `20260904130000`) — quando o Valor total informado não é múltiplo exato da quantidade em centavos (ex.: R$ 100,00 ÷ 3), o `unit_value` arredondado ao centavo mais próximo (R$ 33,33) faz o backend **persistir** `item_value = R$ 99,99`, um centavo abaixo do valor realmente informado; `subtotal_value`/`total_value` do cabeçalho da compra (calculados na RPC a partir da soma de `quantity*unit_value` por item) herdam a mesma divergência de poucos centavos nesse caso. Isso é inerente a QUALQUER contrato que registre um preço por unidade (mesmo efeito em qualquer nota fiscal com preço unitário) e só seria eliminado mudando o contrato para armazenar o total diretamente (aceitar um campo novo/inverter qual coluna é gerada) — **não foi feito nesta rodada, conforme instrução explícita de não alterar banco/migration/RPC/Edge Function sem nova autorização**; o formulário e seu Resumo, por outro lado, nunca sofrem esse arredondamento (ver acima). Validações do campo: obrigatório e **maior que zero** (antes só exigia "ter sido tocado", agora `cents <= 0` sempre rejeita, cobrindo tanto o campo nunca preenchido quanto um valor zerado explicitamente); quantidade continua inteira e maior que zero (inalterado); cada linha mantém seu próprio Valor total (2+ itens podem ter totais diferentes); frete continua único por compra, somado uma única vez ao Total; múltiplos tipos de filamento na mesma compra preservados. **4. Atualização automática (sem mudança de código)**: a propagação pós-compra (`onPurchaseCompleted` → `refetchAll` na página de Filamentos, sem F5) não foi tocada nesta rodada — nenhuma alteração de contrato ou de wiring era necessária; a cobertura existente (`FilamentsInventoryPage.test.tsx`, describe "atualização automática sem F5") permanece intacta e passando, só com o rótulo do campo atualizado ("Valor total" em vez de "Valor unitário") no helper de preenchimento da compra real usado por esse teste. **Testes**: `FilamentMovementHistory.test.tsx` (+3 casos — tabela sem largura mínima fixa, célula de Motivo permite quebra de linha para um motivo longo, card mobile com os 5 campos por movimentação); `PurchaseDialog.test.tsx` (rótulo/semântica atualizados em todos os testes que preenchem itens de Filamento, valores de teste recalculados onde a quantidade não era 1, +7 casos novos: campo "Valor unitário" não aparece mais, "Valor total" aparece em cada item com dois itens tendo totais diferentes, validação obrigatório/maior-que-zero, subtotal soma os Valores totais com frete somado uma única vez, **quantidade 3 + Valor total R$ 100,00 mantém o Resumo em exatamente R$ 100,00**, payload envia o unit_value calculado/arredondado numa divisão não exata); `FilamentsInventoryPage.test.tsx` (rótulo atualizado no fluxo real de compra do describe "atualização automática sem F5", sem mudança de comportamento). Suíte `vitest` completa **1975 passando + 1 todo** (6 falhas de timeout em arquivos não tocados ou já tocados desta rodada ao rodar os 77 arquivos juntos — `PurchaseDialog.test.tsx`, `ProductForm.test.tsx`, `OrdersPage.test.tsx` — confirmadas como contenção de recursos do ambiente, não regressão: as 3 passam 100% quando reexecutadas isoladas, 328/328 testes); `npm run lint` (0 erros, os mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas. **Nenhuma migration criada/aplicada, nenhuma RPC ou Edge Function alterada/publicada, nenhum deploy de frontend realizado, nenhum dado oficial alterado.** **Validação manual do usuário pendente** — não foi executada nesta rodada: confirmar visualmente a janela "Histórico do rolo" sem rolagem horizontal (desktop) e o card responsivo (tela estreita); confirmar o campo "Valor total" na "Compra de filamentos" e o Resumo reagindo corretamente a totais diferentes por item; e **decidir se a divergência de poucos centavos entre o Valor total informado e o `item_value`/`subtotal_value` persistido (só quando a divisão por quantidade não é exata) é aceitável ou exige revisitar o contrato do backend** (limitação sinalizada no item 3 acima). Módulo 3 permanece em **~51%** (ajustes de UX/exibição sobre um incremento já contabilizado — nenhuma fase nova da metodologia das 7 fases concluída). Checkpoint: commit `fix: refine spool history and purchase totals`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase. |
| 2026-09-05 | **Correção definitiva do arredondamento na Compra de filamentos: total_value passa a ser a coluna autoritativa de `inventory_purchase_filament_items`, persistida EXATAMENTE como informada — quantidade 3 + R$ 100,00 grava 100.00, não mais 99.99. Migration criada, mas NÃO aplicada/publicada nesta rodada (sem credenciais na sessão) — ver bloqueio abaixo.** **1. Autorização e diagnóstico**: o usuário autorizou explicitamente alterar banco/RPC/Edge Function para resolver o problema sinalizado na entrada anterior — o formulário já exibia R$ 100,00 corretamente, mas o valor enviado (`unit_value` derivado no frontend) fazia o backend persistir `item_value = 99,99` para quantidade 3 (`item_value` é coluna `GENERATED ALWAYS AS (quantity*unit_value)` em `inventory_purchase_filament_items`, migration `20260904130000`). **2. Migration nova, append-only** (`supabase/migrations/20260905160000_store_exact_filament_purchase_item_totals.sql`): (a) `inventory_purchase_filament_items` ganha `total_value numeric(12,2)`, nullable a princípio; (b) backfill de toda linha existente com `total_value = item_value` (o total que o contrato antigo já considerava correto para aquele item — nenhuma compra antiga alterada em valor); (c) só depois do backfill, `CHECK (total_value > 0)` + `NOT NULL`; (d) `unit_value`/`item_value` **continuam existindo, sem nenhuma mudança de tipo/CHECK/coluna gerada** — viram **LEGADOS**, documentados via `comment on column` explicando que podem divergir de `total_value` em poucos centavos quando a divisão por `quantity` não é exata (inerente a qualquer contrato de preço por unidade, nunca eliminado por completo — só deixado de ser a fonte de verdade). **3. `register_filament_purchase`** (mesma assinatura de 7 argumentos, só o corpo muda): cada item de `p_items` agora exige `total_value` (não mais `unit_value`) — validado como **obrigatório e maior que zero**; `unit_value` é **calculado internamente** como `round(total_value/quantity, 2)` e gravado já pronto (nunca mais aceito do chamador); o subtotal do cabeçalho (`inventory_purchases.item_value`, coluna comum já existente, nunca gerada nessa tabela) passa a somar `total_value` de cada item **diretamente** — nunca `quantity*unit_value` — e como `numeric` em Postgres é aritmética decimal exata (nunca ponto flutuante), o subtotal fica sempre exato mesmo quando um item individual sofre arredondamento de centavo; o frete continua somado uma única vez (`freight_value`, inalterado) e `total_value` do CABEÇALHO (coluna gerada já existente = `item_value + freight_value`) reflete automaticamente subtotal exato + frete. Idempotência passou a comparar `total_value` (em vez de `unit_value`) no snapshot de itens. `_build_filament_purchase_summary` passou a incluir `total_value` de cada item na resposta (junto com `unit_value`/`item_value`, preservados). Criação dos rolos, saldo inicial, vínculo `purchase_item_id` e todas as demais regras permanecem inalteradas. **4. Edge Function `inventory-purchases`**: `FILAMENT_PURCHASE_ITEM_KEYS`/`FilamentPurchaseItemParams`/`validateFilamentPurchaseItem` trocaram `unit_value` (aceitava `>= 0`) por `total_value` (exige **> 0**, validação explícita além do `requireNumber`). **5. Frontend `PurchaseDialog.tsx`**: removida a função `unitValueFromTotalCents` (o cálculo/arredondamento no cliente deixou de existir) — `handleFilamentSubmit` agora envia `total_value: centsToAmount(item.totalValueField.cents)` **diretamente, em centavos convertidos com segurança, nunca dividido antes do envio**; o Resumo da janela continua somando os centavos do Valor total de cada item (inalterado desde a entrada anterior) — agora o valor exibido e o valor persistido no banco **são a mesma fonte**, sem nenhuma divergência de centavo entre os dois. Múltiplos itens/tipos na mesma compra, marca, peso líquido, frete único e a atualização automática pós-compra (`onPurchaseCompleted` → `refetchAll`, sem F5) permanecem exatamente como estavam — nenhuma mudança de wiring foi necessária. **6. Testes**: `supabase/tests/filament_purchase_test.sql` — todas as chamadas das Seções 1–8 (já existentes) atualizadas para enviar `total_value` (renomeação simples nos casos de quantidade 1; item de quantidade 2 na Seção 1 recalculado para `total_value=190.00`); Seção 1 ganhou asserções novas (`total_value` exato do item, `unit_value` derivado corretamente); Seção 2 ganhou o caso `total_value=0` rejeitado (regra nova: zero não é mais aceito, diferente do `unit_value` antigo); Seção 7 ganhou checagens estruturais (`total_value` existe/numeric(12,2)/NOT NULL; `CHECK` presente via `pg_get_constraintdef`); **Seção 9, nova**, cobre especificamente o pedido desta rodada: quantidade 3 + `total_value` 100.00 persistido **exatamente** 100.00 (9.1), `unit_value` derivado 33.33 (9.2), subtotal do cabeçalho **exatamente** 100.00 — nunca 99.99 (9.3), dois itens com totais diferentes (100.00 + 50.00) somando 150.00 no cabeçalho (9.4), frete aplicado uma única vez (150+25=175, coluna gerada) (9.5), evidência de que os dados existem dentro da transação antes do `ROLLBACK` final reverter tudo (9.6 — a prova real de "nenhum dado fica" exige comparar contagens reais antes/depois da execução, fora desta transação), confirmação estrutural de que `register_inventory_purchase` (Acessórios/Embalagens) mantém a mesma assinatura — não tocado nesta migration, cobertura funcional completa permanece em `inventory_purchases_test.sql` (arquivo irmão, inalterado) (9.7), e contagem correta de rolos criados nos dois cenários (9.8). `supabase/functions/inventory-purchases/handler.test.ts`: `VALID_ITEM` e os dois testes de item multi-linha atualizados para `total_value`; teste de rejeição atualizado para cobrir negativo **e zero**; novo teste confirmando que uma divisão não exata (quantidade 3, total 100) é aceita pelo validador (o arredondamento fica só na RPC). `PurchaseDialog.test.tsx`: payload das compras multi-item (teste "19") atualizado para `total_value` (190/110, mesmos resultados de antes); teste de arredondamento reescrito para confirmar que o payload enviado é `total_value: 100` (nunca `unit_value` calculado) mesmo com quantidade 3. Suíte `vitest` completa executada com `--maxWorkers=1` (evitando os timeouts de contenção de recursos já registrados em rodadas anteriores) — **77 arquivos, 1981 passando + 1 todo, 0 falhas** (+10 sobre a rodada anterior); `npm run lint` (0 erros, os mesmos 8 avisos `react-refresh` pré-existentes); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas (só avisos de LF/CRLF do próprio Git). **7. BLOQUEIO — aplicação remota NÃO executada nesta rodada**: esta sessão não tinha `SUPABASE_ACCESS_TOKEN`/CLI autenticada, nem `SUPABASE_SERVICE_ROLE_KEY`/string de conexão direta ao Postgres, nem Docker para um stack local (`supabase start`) — não há nenhum caminho para rodar o dry-run, aplicar `20260905160000` no projeto `tjhacqreupfqefntjevf`, publicar `inventory-purchases`, ou executar de fato `filament_purchase_test.sql`/o smoke test de autenticação e CORS contra o remoto. A migration e as mudanças de Edge Function/frontend foram escritas e revisadas com o mesmo rigor de sempre (mesmo padrão de todas as migrations anteriores do projeto), mas **continuam pendentes de execução real** — mesma disciplina já registrada para os testes Deno deste projeto ("revisão estática" até a primeira execução real). **Nenhuma migration foi aplicada, nenhuma Edge Function foi publicada, nenhum dado oficial foi alterado, nenhum deploy de frontend foi realizado.** **Próximos passos (fora desta sessão)**: com credenciais válidas, seguir exatamente os passos pedidos — confirmar o projeto vinculado `tjhacqreupfqefntjevf`, `supabase db push --dry-run` (prosseguir só se aparecer exatamente `20260905160000_store_exact_filament_purchase_item_totals.sql`), aplicar, `supabase functions deploy inventory-purchases`, rodar `filament_purchase_test.sql` dentro de `BEGIN`/`ROLLBACK` conferindo contagens antes/depois, e smoke test de autenticação/CORS. **Validação manual do usuário continua pendente** — inclui agora também confirmar, após a aplicação real, que uma compra com quantidade 3 e total R$ 100,00 realmente persiste `total_value=100.00` no banco (não só no formulário). Módulo 3 permanece em **~51%** (correção de precisão monetária sobre um incremento já contabilizado — nenhuma fase nova da metodologia das 7 fases concluída). Checkpoint: commit `fix: store exact filament purchase item totals`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase. |
| 2026-09-05 | **FECHAMENTO DA RODADA ANTERIOR: migration `20260905160000` aplicada no projeto remoto `tjhacqreupfqefntjevf`, `inventory-purchases` publicada (versão 6, ACTIVE), R$ 100,00 ÷ 3 confirmado exato no banco. NOVA CONFIGURAÇÃO: abas de Estoque reordenadas (Filamentos primeiro), "Mostrar tipos arquivados" removido, filtros Material/Linha viram action buttons de seleção única, variações de "Sólido" consolidadas na apresentação e no filtro.** **1. Fechamento da correção de arredondamento (reportado pelo usuário, executado fora desta sessão — a sessão anterior não tinha credenciais para aplicar)**: validação manual da janela "Histórico" e da "Compra de filamentos" **aprovada**; migration `20260905160000_store_exact_filament_purchase_item_totals.sql` **aplicada** no projeto Supabase `tjhacqreupfqefntjevf`, schema local/remoto sincronizado; Edge Function `inventory-purchases` republicada — **versão 6, ACTIVE**; teste SQL remoto (`filament_purchase_test.sql`, dentro de `BEGIN`/`ROLLBACK`) — **59 PASS, 0 FAIL**; confirmado no banco real: quantidade 3 + total informado R$ 100,00 grava `total_value` e reflete no subtotal do cabeçalho como **exatamente R$ 100,00** (nunca R$ 99,99), com `unit_value` legado derivado corretamente em **R$ 33,33**; consulta de resíduo (`test_records_remaining`) retornou **0** — nenhum dado temporário do teste permaneceu após o `ROLLBACK`; um aviso do Docker durante a operação afetou só cache local da CLI, sem bloquear a publicação. Registrado aqui **sem apagar nenhuma entrada anterior** — a entrada anterior já documentava a migration como "criada, não aplicada"; esta entrada fecha esse pendente. **2. Ordem das abas de Estoque**: `InventoryPageShell.tsx` — `INVENTORY_AREA_ITEMS` reordenado para **Filamentos, Acessórios, Embalagens** (antes Acessórios/Embalagens/Filamentos); `App.tsx` — o redirect de `/estoque` (sem sub-rota) passou de `/estoque/acessorios` para **`/estoque/filamentos`**. Só a ORDEM/o destino padrão mudam — rotas (`/estoque/acessorios`, `/estoque/embalagens`, `/estoque/filamentos` continuam existindo e acessíveis diretamente), permissões (`ProtectedRoute`, só autenticação — nenhuma checagem por área) e o conteúdo/funcionamento de cada aba permanecem exatamente os mesmos; nenhum componente duplicado (`InventoryAreaNav` continua o único lugar que desenha a navegação). **3. "Mostrar tipos arquivados" removido**: `FilamentsInventoryPage.tsx` — o `Switch`, o estado `showArchivedTypes` e a contagem `archivedTypeGroupCount` foram removidos; `visibleGroups` agora filtra `group.hasActiveType` incondicionalmente (nenhuma forma de revelar um grupo totalmente arquivado nesta tela). O selo "Arquivado" na coluna Situação também foi removido (tornou-se código morto: só podia aparecer quando `!group.hasActiveType`, e esse caso nunca mais chega à tabela). **Nenhum dado tocado**: `filament_types.is_active=false` continua exatamente como estava — nenhum UPDATE/DELETE, nenhuma reativação; um tipo arquivado dentro de um grupo com outro tipo ainda ativo continua visível e com "Excluir tipo" corretamente desabilitado (`disabled={!type.is_active}`, inalterado). O controle **"Mostrar arquivados" dos ROLOS, dentro da janela "Ver rolos" (`FilamentTypeDrawer.tsx`), é um conceito DIFERENTE e não foi tocado** — continua exatamente como estava. **4/5/6. Material e Linha como action buttons; consolidação de "Sólido"**: os dois `MultiSelectFilterButton` (Popover + checkboxes, multisseleção) de Material e Linha foram substituídos por um novo `SingleSelectActionFilter` (role="radiogroup" + botões role="radio", mesmo padrão visual/semântico já usado em "Novo tipo de filamento" e "Compra de filamentos") — seleção **única**, "Todos" sempre a primeira opção e o estado inicial. Material: **Todos, PLA, PETG, TPU** (fixo, mesmo enum `FilamentMaterial`). Linha: **Todos, Sólido, Silk, Mate, Velvet, Translúcido, Duocolor, Tricolor** (fixo — 2026-09-05 deixou de ser derivado dinamicamente dos dados; a Cor continua dinâmica/multisseleção, inalterada). Escolher uma opção **substitui** a seleção anterior (nunca soma); clicar na opção já ativa não dispara nenhuma operação nova (o próprio `DropdownMenuItem`-like botão não gera um Set diferente); a filtragem é **imediata** (mesmo `setFilters` síncrono de sempre — sem F5). Material e Linha continuam em relação **AND** entre si e com Cor/busca/faixa de rolos — nenhuma mudança em `matchesFilamentGroupFilters` para Material; só a checagem de Linha passou a comparar pelo **rótulo de exibição consolidado** (`resolveFilamentLineDisplayLabel`, novo helper em `lib/inventory/filamentLineAliases.ts`) em vez do token normalizado cru. Esse MESMO helper (reaproveitado, nunca duplicado) resolve **Sólida/Solida/Solido/Sólido → "Sólido"**, **Mate/Matte → "Mate"**, **Translucido/Translúcido → "Translúcido"**, **"Duo Color"/Duocolor/DuoColor → "Duocolor"** e **"Tri Color"/Tricolor/TriColor → "Tricolor"** — usado tanto pelo filtro quanto pela própria coluna "Linha" da tabela e pelo título da janela "Ver rolos" (`Material - Linha - Cor`), que agora também exibe o rótulo consolidado. **Nenhum UPDATE no banco**: `filament_types.line` preserva sempre a grafia original; a consolidação é só de apresentação/filtragem (uma grafia fora das 7 oficiais, ex. um valor histórico livre anterior a 2026-09-04, passa pela função sem alteração — nunca escondida). "Limpar filtros" (`clearAllFilters`, inalterado) já zera Material/Linha/Cor de uma vez, o que automaticamente volta os dois novos radiogroups para "Todos". **Testes**: `filamentLineAliases.test.ts` (novo, 8 casos — as 5 famílias de alias + Silk/Velvet inalterados + valor histórico sem alias preservado); `filamentGroupFilters.test.ts` (2 casos ajustados: comparação de Linha agora por rótulo de exibição, não token cru); `App.test.tsx` (+2 casos: `/estoque` redireciona para Filamentos e a aparece primeiro na navegação; `mockEmptySupabaseFrom` corrigido para suportar `.order().order()` encadeado, necessário para a consulta de Filamentos); `FilamentsInventoryPage.test.tsx` (describe "filtros Material / Linha / Cor" reescrito — 10 casos: Material/Linha como action buttons na ordem certa, "Todos" inicial, seleção única com troca/substituição, clique repetido sem nova operação, AND Material+Linha, combinação com Cor/busca, "Limpar filtros" volta os dois para "Todos", consolidação real de Sólida/Solida/Solido/Sólido via o botão "Sólido", e confirmação de que a Linha sempre tem exatamente 8 botões fixos mesmo com grafias históricas estranhas nos dados; describe de remoção de tipo com 3 casos reescritos para o controle de arquivados removido, incluindo um novo teste de "Excluir tipo" desabilitado num tipo arquivado dentro de um grupo ainda visível, sem depender de nenhum toggle). Suíte `vitest` completa `--maxWorkers=1` (evita os timeouts de contenção já registrados): **78 arquivos, 1995 passando + 1 todo, 0 falhas** (+20 sobre a rodada anterior); `npm run lint` (0 erros, os mesmos 8 avisos `react-refresh` pré-existentes, nenhum aviso novo); `npx tsc -b` e `npm run build` limpos; `git diff --check` sem problemas (só aviso de LF/CRLF do próprio Git). **Nenhuma migration, RPC ou Edge Function criada/alterada/publicada nesta rodada; nenhum deploy de frontend; nenhum dado oficial alterado** — só reordenação de UI, remoção de um controle visual e troca de dois filtros por action buttons. Módulo 3 permanece em **~51%** (ajustes de UX/apresentação sobre um incremento já contabilizado — nenhuma fase nova da metodologia das 7 fases concluída). **Validação manual desta configuração nova (ordem das abas, ausência do controle de arquivados, action buttons de Material/Linha, consolidação visual de "Sólido") continua pendente** — não foi executada nesta rodada. Checkpoint: commit `feat: refine filament inventory filters`; `git fetch` antes do push; push normal para `origin/feature/inventory-operations`, sem force/merge/rebase. |
